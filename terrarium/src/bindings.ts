// This file has been generated by Tauri Specta. Do not edit this file manually.

import { invoke as __TAURI_INVOKE } from "@tauri-apps/api/core";

/** Commands */
export const commands = {
	getParserName: (directory: string) => typedError<string, string>(__TAURI_INVOKE("get_parser_name", { directory })),
	loadGrammar: (directory: string) => typedError<[string, string], string>(__TAURI_INVOKE("load_grammar", { directory })),
	saveGrammar: (directory: string, filename: string, content: string) => typedError<null, string>(__TAURI_INVOKE("save_grammar", { directory, filename, content })),
	buildParser: (directory: string, instrument: boolean, debugTrace: boolean) => __TAURI_INVOKE<void>("build_parser", { directory, instrument, debugTrace }),
	generateParser: (directory: string, noLl1: boolean) => __TAURI_INVOKE<void>("generate_parser", { directory, noLl1 }),
	parse: (directory: string, input: string, startNonterminal: string) => typedError<ParseOutput, string>(__TAURI_INVOKE("parse", { directory, input, startNonterminal })),
	/**
	 *  Run the parser with --write-stats and return the parsed Stats JSON.
	 *  Requires the binary to have been built with the `instrument` feature.
	 */
	getStats: (directory: string, input: string, startNonterminal: string) => typedError<StatsData, string>(__TAURI_INVOKE("get_stats", { directory, input, startNonterminal })),
	/**
	 *  Returns the cargo features the current parser binary was built with,
	 *  or `None` if no successful build has happened yet.
	 */
	getBuildFeatures: () => __TAURI_INVOKE<{
	instrument: boolean,
	debug_trace: boolean,
} | null>("get_build_features"),
	/**
	 *  Profile the parser by building with --features profile (release mode),
	 *  running the parser in a loop under a sampling profiler, and opening the
	 *  resulting flamegraph SVG in the default browser.
	 */
	profile: (directory: string, input: string, startNonterminal: string, iterations: number) => __TAURI_INVOKE<void>("profile", { directory, input, startNonterminal, iterations }),
	getSppf: () => typedError<SPPF, string>(__TAURI_INVOKE("get_sppf")),
	getGss: () => typedError<GSS, string>(__TAURI_INVOKE("get_gss")),
	/**
	 *  Sets up VS Code debug configuration for the current parser.
	 *  Writes input to a file and creates/updates .vscode/launch.json.
	 */
	setupVscodeDebug: (directory: string, input: string, startNonterminal: string) => typedError<string, string>(__TAURI_INVOKE("setup_vscode_debug", { directory, input, startNonterminal })),
	getNonterminals: (directory: string) => typedError<string[], string>(__TAURI_INVOKE("get_nonterminals", { directory })),
	loadDebugTrace: (directory: string, input: string, startNonterminal: string) => typedError<DebugInfo, string>(__TAURI_INVOKE("load_debug_trace", { directory, input, startNonterminal })),
	debugStepForward: () => typedError<DebugInfo, string>(__TAURI_INVOKE("debug_step_forward")),
	debugStepTo: (target: number) => typedError<DebugInfo, string>(__TAURI_INVOKE("debug_step_to", { target })),
	getDebugInfo: () => typedError<DebugInfo, string>(__TAURI_INVOKE("get_debug_info")),
	getStackTrace: () => typedError<string[], string>(__TAURI_INVOKE("get_stack_trace")),
	getDebugSppf: () => typedError<DebugSPPFInfo, string>(__TAURI_INVOKE("get_debug_sppf")),
	getDebugGss: () => typedError<DebugGSSInfo, string>(__TAURI_INVOKE("get_debug_gss")),
	debugGoToFurthestError: () => typedError<DebugInfo, string>(__TAURI_INVOKE("debug_go_to_furthest_error")),
	debugNextError: () => typedError<DebugInfo, string>(__TAURI_INVOKE("debug_next_error")),
	debugPrevError: () => typedError<DebugInfo, string>(__TAURI_INVOKE("debug_prev_error")),
	getDebugErrors: () => typedError<ErrorInfo[], string>(__TAURI_INVOKE("get_debug_errors")),
	getEventLog: () => typedError<EventLogEntry[], string>(__TAURI_INVOKE("get_event_log")),
	/**
	 *  Parse the grammar source and cache the result. Re-parses only if the source
	 *  text has changed since the last parse.
	 */
	analyzeGrammar: (source: string) => __TAURI_INVOKE<AnalyzeResult>("analyze_grammar", { source }),
	/**
	 *  Format the grammar using the cached parse result.
	 *  Falls back to parsing the source if no cached result exists.
	 *  Returns None if parsing fails (grammar cannot be formatted).
	 */
	formatGrammar: (source: string) => __TAURI_INVOKE<string | null>("format_grammar", { source }),
	/**  Return semantic tokens from the cached parse result. */
	getSemanticTokens: () => __TAURI_INVOKE<SemanticTokenData[]>("get_semantic_tokens"),
	getSemanticTokensLegend: () => __TAURI_INVOKE<SemanticTokensLegendData>("get_semantic_tokens_legend"),
	/**
	 *  Return document symbols (rule heads + alternative labels).
	 *  Ensures the parse result is fresh for the given source.
	 */
	getDocumentSymbols: (source: string) => __TAURI_INVOKE<DocumentSymbolData[]>("get_document_symbols", { source }),
	/**  Return the definition location of the symbol at the given position. */
	getDefinition: (source: string, line: number, column: number) => __TAURI_INVOKE<{
	range: RangeData,
} | null>("get_definition", { source, line, column }),
	/**  Return all references to the symbol at the given position. */
	getReferences: (source: string, line: number, column: number, includeDeclaration: boolean) => __TAURI_INVOKE<LocationData[]>("get_references", { source, line, column, includeDeclaration }),
	/**  Return folding ranges for the grammar. */
	getFoldingRanges: (source: string) => __TAURI_INVOKE<FoldingRangeData[]>("get_folding_ranges", { source }),
	/**  Return diagnostics (e.g. unresolved references) for the grammar. */
	getDiagnostics: (source: string) => __TAURI_INVOKE<DiagnosticData[]>("get_diagnostics", { source }),
	getLogPath: () => __TAURI_INVOKE<string | null>("get_log_path"),
	/**
	 *  Copies the session log file to a user-chosen path. The frontend supplies the
	 *  destination from a native save dialog.
	 */
	saveLog: (destination: string) => typedError<null, string>(__TAURI_INVOKE("save_log", { destination })),
	/**
	 *  Probe whether the `iguana` binary is reachable on PATH. Returns `Ok(())`
	 *  on success and an actionable install hint when missing.
	 */
	checkIguana: () => typedError<null, string>(__TAURI_INVOKE("check_iguana")),
};

/* Types */
/**  Result of analyzing/parsing a grammar source. */
export type AnalyzeResult = {
	success: boolean,
	/**  Time spent in the GLL parsing algorithm (milliseconds). */
	parse_duration_ms: number,
	/**  Time spent constructing the typed parse tree from the SPPF (milliseconds). */
	tree_construction_duration_ms: number,
};

/**  Cargo features the current parser binary was built with. */
export type BuildFeatures = {
	instrument: boolean,
	debug_trace: boolean,
};

/**  GSS edge for debug visualization. */
export type DebugGSSEdge = {
	src: number,
	dest: number,
	/**  Return slot name */
	label: string,
};

/**  Debug GSS info returned to the frontend. */
export type DebugGSSInfo = {
	nodes: DebugGSSNode[],
	edges: DebugGSSEdge[],
	/**  The current GSS node ID from the action being processed */
	current_gss_node_id: number | null,
};

/**  GSS node for debug visualization. */
export type DebugGSSNode = {
	id: number,
	/**  Format: "(Nonterminal, InputIndex)" */
	label: string,
};

/**  Debug info returned to the frontend. */
export type DebugInfo = {
	current_step: number,
	total_steps: number,
	/**  The current action being displayed (formatted as string) */
	current_action: string | null,
	/**  Pending descriptors in the worklist (formatted as strings) */
	descriptor_set: string[],
	/**  Current position in the input (character index) */
	input_index: number | null,
	/**  Total number of error steps (ParseError events) */
	total_errors: number,
	/**  Current error index (1-indexed) if at an error step, None otherwise */
	current_error_index: number | null,
	/**  Only set on initial load */
	input_path: string | null,
	symbols_path: string | null,
	trace_path: string | null,
};

/**  Debug SPPF info returned to the frontend. */
export type DebugSPPFInfo = {
	nodes: DebugSPPFNode[],
	/**  The current SPPF node ID from the descriptor being processed */
	current_node_id: number | null,
};

/**
 *  SPPF node for debug visualization, reconstructed incrementally from trace events.
 * 
 *  This is separate from iguana's `SPPFDotNode` because debug mode replays trace events
 *  step-by-step and builds the SPPF incrementally. Each step may add new nodes, so we
 *  need the `children` field to track parent-child relationships as they're discovered.
 *  In contrast, `SPPFDotNode` is built by traversing the completed SPPF tree after parsing,
 *  where edges are stored separately.
 */
export type DebugSPPFNode = {
	id: number,
	kind: DebugSPPFNodeKind,
	label: string,
	left_extent: number,
	right_extent: number,
	children: number[],
};

export type DebugSPPFNodeKind = "Terminal" | "Nonterminal" | "Intermediate";

export type DiagnosticData = {
	range: RangeData,
	severity: number,
	message: string,
};

/**
 *  Specta-compatible wrapper for lsp_types::DocumentSymbol.
 *  `kind` is the numeric LSP SymbolKind (e.g. 5 = Class, 9 = Constructor, 10 = Enum).
 */
export type DocumentSymbolData = {
	name: string,
	kind: number,
	range: RangeData,
	selection_range: RangeData,
	children: DocumentSymbolData[],
};

/**  Error info for the dropdown list. */
export type ErrorInfo = {
	/**  Step index (0-indexed into step_indices) */
	step_index: number,
	/**  Position in the input where the error occurred */
	input_index: number,
	/**  Description of the error (e.g. "expected Identifier" or "excluded by Keyword") */
	description: string,
	/**  The slot where the error occurred */
	slot: string,
};

/**  Entry in the event log. */
export type EventLogEntry = {
	/**  Index of this event in the trace */
	event_index: number,
	/**  Step index if this event is steppable, None otherwise */
	step_index: number | null,
	/**  Formatted message for this event */
	message: string,
	/**  Event type for styling (e.g., "processing", "match_success", "match_failed", "gss", "sppf", "layout") */
	event_type: string,
};

export type FoldingRangeData = {
	start_line: number,
	end_line: number,
};

export type GSS = {
	nodes: GSSDotNode[],
	edges: GSSDotEdge[],
};

export type GSSDotEdge = {
	src: GssNodeId,
	dest: GssNodeId,
	label: string,
};

export type GSSDotNode = {
	id: GssNodeId,
	label: string,
};

/**
 *  A unique identifier for a GSS node.
 * 
 *  This is a type-safe wrapper around an index into the parser's GSS node list.
 *  Uses `u32` since GSS nodes are of the form (nonterminal_id, input_index), which
 *  is bounded by the input length (u32).
 */
export type GssNodeId = number;

export type LocationData = {
	range: RangeData,
};

export type NodeKind = ({ Nonterminal: {
	ambiguous: boolean,
} }) & { Intermediate?: never } | ({ Intermediate: {
	ambiguous: boolean,
} }) & { Nonterminal?: never } | "Terminal" | "Packed";

/**  Parse error location and message, from the parser's --write-result JSON. */
export type ParseErrorInfo = {
	line: number,
	column: number,
	message: string,
};

/**  Result of a parse operation, indicating which outputs are available. */
export type ParseOutput = {
	success: boolean,
	error: string | null,
	error_info: ParseErrorInfo | null,
	/**
	 *  True when the parser did not run to a result: it crashed (panic, signal,
	 *  non-zero exit) or wrote no result file. Distinct from a parse failure
	 *  (`error_info` set), which is the input legitimately not matching the
	 *  grammar.
	 */
	unexpected_error: boolean,
	duration_ms: number | null,
	tree_construction_ms: number | null,
	has_sppf: boolean,
	has_gss: boolean,
	/**
	 *  The parse-tree JSON, read inline so a parse is a single command. `None`
	 *  when the parser produced no tree.
	 */
	parse_tree: string | null,
};

export type RangeData = {
	start_line: number,
	start_char: number,
	end_line: number,
	end_char: number,
};

export type SPPF = {
	nodes: SPPFDotNode[],
	edges: SPPFDotEdge[],
};

export type SPPFDotEdge = {
	src: SPPFNodeId,
	dest: SPPFNodeId,
};

export type SPPFDotNode = {
	id: SPPFNodeId,
	kind: NodeKind,
	label: string,
	left_extent: number,
	right_extent: number,
};

/**
 *  A unique identifier for an SPPF node.
 * 
 *  This is a type-safe wrapper around an index into the parser's SPPF nodes list.
 *  Uses `u32` since real-world grammars rarely exceed 2^32 - 1 nodes.
 */
export type SPPFNodeId = number;

/**  Specta-compatible wrapper for lsp_types::SemanticToken */
export type SemanticTokenData = {
	delta_line: number,
	delta_start: number,
	length: number,
	token_type: number,
	token_modifiers_bitset: number,
};

/**  Semantic token legend (token type names). */
export type SemanticTokensLegendData = {
	token_types: string[],
};

/**  Parser stats from --write-stats output (instrument feature). */
export type StatsData = {
	descriptors_count: number,
	gss_nodes_count: number,
	gss_edges_count: number,
	nonterminal_nodes_count: number,
	intermediate_nodes_count: number,
	terminal_nodes_count: number,
	ambiguous_nodes_count: number,
	/**  Map from collection name (e.g. "Env::bindings: InlineVec") to recorded sizes. */
	histograms: { [key in string]: number[] },
};

/* Tauri Specta runtime */
async function typedError<T, E>(result: Promise<T>): Promise<{ status: "ok"; data: T } | { status: "error"; error: E }> {
    try {
        return { status: "ok", data: await result };
    } catch (e) {
        if (e instanceof Error) throw e;
        return { status: "error", error: e as any };
    }
}

