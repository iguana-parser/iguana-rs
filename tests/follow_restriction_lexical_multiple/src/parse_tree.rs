// @generated by iguana. Do not edit manually.

use crate::parser::FollowRestrictionLexicalMultipleParser;
use iguana_runtime::{
    ids::{NonterminalId, SlotId, TerminalId},
    input::Span,
    parse_tree::{
        Bump, NodeKind, OneOrMany, Origin, ParseContext, ParseTreeBuilder, ParseTreeNode,
        SexprOptions, visit_sppf,
    },
    sppf::{NonterminalNode, SPPFNodeId, TerminalNode},
};
use std::vec::IntoIter;
#[derive(Debug, Clone, Copy)]
pub enum TokenKind {
    // Num
    T0,
    // Alpha
    T1,
    // Dot
    T2,
    // Word
    T3,
    // WS
    T4,
}
impl TokenKind {
    pub fn name(&self) -> &'static str {
        match self {
            TokenKind::T0 => "Num",
            TokenKind::T1 => "Alpha",
            TokenKind::T2 => "Dot",
            TokenKind::T3 => "Word",
            TokenKind::T4 => "WS",
        }
    }
}
#[derive(Debug, Clone, Copy)]
pub enum ParseTree<'a> {
    S(&'a S<'a>),
    Element(&'a Element<'a>),
    // Element+
    Plus0(&'a Plus0<'a>),
    Token(Token),
}
impl<'a> ParseTree<'a> {
    pub fn children(&self) -> Vec<ParseTree<'a>> {
        match self {
            ParseTree::S(s) => (0..s.child_count()).filter_map(|i| s.child(i)).collect(),
            ParseTree::Element(element) => (0..element.child_count())
                .filter_map(|i| element.child(i))
                .collect(),
            ParseTree::Plus0(plus_0) => match plus_0 {
                Plus0::Amb(alts) => alts.iter().copied().map(ParseTree::Plus0).collect(),
                _ => plus_0.iter().collect(),
            },
            ParseTree::Token(_) => vec![],
        }
    }
    pub fn display_name(&self) -> &'static str {
        match self {
            ParseTree::S(s) => s.display_name(),
            ParseTree::Element(element) => element.display_name(),
            ParseTree::Plus0(plus_0) => plus_0.display_name(),
            ParseTree::Token(token) => token.kind.name(),
        }
    }
    pub fn child_count(&self) -> usize {
        match self {
            ParseTree::S(s) => s.child_count(),
            ParseTree::Element(element) => element.child_count(),
            ParseTree::Plus0(plus_0) => plus_0.child_count(),
            ParseTree::Token(_) => 0,
        }
    }
    pub fn span(&self) -> Span {
        match self {
            ParseTree::S(s) => s.span(),
            ParseTree::Element(element) => element.span(),
            ParseTree::Plus0(plus_0) => plus_0.span(),
            ParseTree::Token(token) => token.span(),
        }
    }
    #[doc = "True when this node is an ambiguity cluster (any `*::Amb` variant). The"]
    #[doc = "uniform way to detect ambiguity without matching each nonterminal's enum."]
    pub fn is_amb(&self) -> bool {
        match self {
            ParseTree::S(s) => matches!(s, S::Amb(_)),
            ParseTree::Element(element) => matches!(element, Element::Amb(_)),
            ParseTree::Plus0(plus_0) => matches!(plus_0, Plus0::Amb(_)),
            ParseTree::Token(_) => false,
        }
    }
    #[doc = "Pointer identity of the underlying node, or `None` for tokens (by-value"]
    #[doc = "leaves that are never shared). Two parse trees with the same `node_id` are"]
    #[doc = "the same allocation, i.e. a node shared between parents in the ambiguity DAG."]
    pub fn node_id(&self) -> Option<usize> {
        match self {
            ParseTree::S(s) => Some(*s as *const _ as usize),
            ParseTree::Element(element) => Some(*element as *const _ as usize),
            ParseTree::Plus0(plus_0) => Some(*plus_0 as *const _ as usize),
            ParseTree::Token(_) => None,
        }
    }
    pub fn origin(&self) -> Option<Origin> {
        match self {
            ParseTree::S(s) => s.origin(),
            ParseTree::Element(element) => element.origin(),
            ParseTree::Plus0(plus_0) => plus_0.origin(),
            ParseTree::Token(_) => None,
        }
    }
    fn unwrap_s(self) -> &'a S<'a> {
        match self {
            ParseTree::S(s) => s,
            _ => panic!(),
        }
    }
    fn unwrap_element(self) -> &'a Element<'a> {
        match self {
            ParseTree::Element(element) => element,
            _ => panic!(),
        }
    }
    fn unwrap_plus_0(self) -> &'a Plus0<'a> {
        match self {
            ParseTree::Plus0(plus_0) => plus_0,
            _ => panic!(),
        }
    }
    fn unwrap_token(self) -> Token {
        match self {
            ParseTree::Token(t) => t,
            _ => panic!(),
        }
    }
}
pub trait ListNode<'a> {
    fn iter(&'a self) -> IntoIter<ParseTree<'a>>;
}
pub trait OptNode {
    type Inner;
    fn value(&self) -> Option<&Self::Inner>;
}
// S = Element+
#[derive(Debug)]
pub enum S<'a> {
    Alt0 { elements: &'a Plus0<'a>, span: Span },
    Amb(&'a [&'a S<'a>]),
}
#[derive(Debug)]
pub enum Element<'a> {
    // Element = Num
    Alt0 { num: Token, span: Span },
    // Element = Word
    Alt1 { word: Token, span: Span },
    // Element = Dot
    Alt2 { dot: Token, span: Span },
    Amb(&'a [&'a Element<'a>]),
}
// Element+
#[derive(Debug)]
pub enum Plus0<'a> {
    // Plus_0 = Element+ WS Element
    Alt0 {
        elements_0: &'a Plus0<'a>,
        ws: Token,
        element_2: &'a Element<'a>,
        span: Span,
    },
    // Plus_0 = Element
    Alt1 {
        element: &'a Element<'a>,
        span: Span,
    },
    Amb(&'a [&'a Plus0<'a>]),
}
impl<'a> S<'a> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::S(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match self {
            S::Alt0 { elements, .. } => match index {
                0 => Some(ParseTree::Plus0(elements)),
                _ => None,
            },
            S::Amb(alts) => alts.get(index).copied().map(ParseTree::S),
        }
    }
    pub fn child_count(&self) -> usize {
        match self {
            S::Alt0 { .. } => 1usize,
            S::Amb(alts) => alts.len(),
        }
    }
    pub fn span(&self) -> Span {
        match self {
            S::Alt0 { span, .. } => *span,
            S::Amb(alts) => alts[0].span(),
        }
    }
    pub fn display_name(&self) -> &'static str {
        match self {
            S::Amb(_) => "Amb",
            _ => "S",
        }
    }
    pub fn origin(&self) -> Option<Origin> {
        None
    }
    pub fn elements(&self) -> &'a Plus0<'a> {
        match self {
            S::Alt0 { elements, .. } => elements,
            S::Amb(_) => panic!("S is ambiguous"),
        }
    }
}
impl<'a> Element<'a> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::Element(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match self {
            Element::Alt0 { num, .. } => match index {
                0 => Some(ParseTree::Token(*num)),
                _ => None,
            },
            Element::Alt1 { word, .. } => match index {
                0 => Some(ParseTree::Token(*word)),
                _ => None,
            },
            Element::Alt2 { dot, .. } => match index {
                0 => Some(ParseTree::Token(*dot)),
                _ => None,
            },
            Element::Amb(alts) => alts.get(index).copied().map(ParseTree::Element),
        }
    }
    pub fn child_count(&self) -> usize {
        match self {
            Element::Alt0 { .. } => 1usize,
            Element::Alt1 { .. } => 1usize,
            Element::Alt2 { .. } => 1usize,
            Element::Amb(alts) => alts.len(),
        }
    }
    pub fn span(&self) -> Span {
        match self {
            Element::Alt0 { span, .. } => *span,
            Element::Alt1 { span, .. } => *span,
            Element::Alt2 { span, .. } => *span,
            Element::Amb(alts) => alts[0].span(),
        }
    }
    pub fn display_name(&self) -> &'static str {
        match self {
            Element::Amb(_) => "Amb",
            _ => "Element",
        }
    }
    pub fn origin(&self) -> Option<Origin> {
        None
    }
}
impl<'a> Plus0<'a> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::Plus0(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match self {
            Plus0::Alt0 {
                elements_0,
                ws,
                element_2,
                ..
            } => match index {
                0 => Some(ParseTree::Plus0(elements_0)),
                1 => Some(ParseTree::Token(*ws)),
                2 => Some(ParseTree::Element(element_2)),
                _ => None,
            },
            Plus0::Alt1 { element, .. } => match index {
                0 => Some(ParseTree::Element(element)),
                _ => None,
            },
            Plus0::Amb(alts) => alts.get(index).copied().map(ParseTree::Plus0),
        }
    }
    pub fn child_count(&self) -> usize {
        match self {
            Plus0::Alt0 { .. } => 3usize,
            Plus0::Alt1 { .. } => 1usize,
            Plus0::Amb(alts) => alts.len(),
        }
    }
    pub fn span(&self) -> Span {
        match self {
            Plus0::Alt0 { span, .. } => *span,
            Plus0::Alt1 { span, .. } => *span,
            Plus0::Amb(alts) => alts[0].span(),
        }
    }
    pub fn display_name(&self) -> &'static str {
        match self {
            Plus0::Amb(_) => "Amb",
            _ => "Element+",
        }
    }
    pub fn origin(&self) -> Option<Origin> {
        match self {
            Plus0::Amb(_) => None,
            _ => Some(Origin::List),
        }
    }
    pub fn elements(&'a self) -> impl Iterator<Item = &'a Element<'a>> {
        self.iter().filter_map(|node| match node {
            ParseTree::Element(r) => Some(r),
            other if other.is_amb() => panic!("Plus_0 is ambiguous"),
            _ => None,
        })
    }
}
impl<'a> ListNode<'a> for Plus0<'a> {
    fn iter(&'a self) -> IntoIter<ParseTree<'a>> {
        let mut items = vec![];
        let mut current = self;
        loop {
            match current {
                Plus0::Alt0 {
                    elements_0: rest,
                    ws,
                    element_2: item,
                    ..
                } => {
                    items.push(item.as_parse_tree());
                    items.push(ws.as_parse_tree());
                    current = rest;
                }
                Plus0::Alt1 { element: item, .. } => {
                    items.push(item.as_parse_tree());
                    break;
                }
                Plus0::Amb(_) => {
                    items.push(ParseTree::Plus0(current));
                    break;
                }
            }
        }
        items.reverse();
        items.into_iter()
    }
}
impl<'a> Element<'a> {
    pub fn as_num(&self) -> Option<&Token> {
        match self {
            Element::Alt0 { num, .. } => Some(num),
            _ => None,
        }
    }
    pub fn as_word(&self) -> Option<&Token> {
        match self {
            Element::Alt1 { word, .. } => Some(word),
            _ => None,
        }
    }
    pub fn as_dot(&self) -> Option<&Token> {
        match self {
            Element::Alt2 { dot, .. } => Some(dot),
            _ => None,
        }
    }
}
#[derive(Debug, Clone, Copy)]
pub struct Token {
    pub kind: TokenKind,
    span: Span,
}
impl Token {
    pub fn as_parse_tree<'a>(&self) -> ParseTree<'a> {
        ParseTree::Token(*self)
    }
    pub fn span(&self) -> Span {
        self.span
    }
}
fn token_kind(terminal_id: TerminalId) -> TokenKind {
    match terminal_id {
        // Num
        TerminalId(0) => TokenKind::T0,
        // Alpha
        TerminalId(1) => TokenKind::T1,
        // Dot
        TerminalId(2) => TokenKind::T2,
        // Word
        TerminalId(3) => TokenKind::T3,
        // WS
        TerminalId(4) => TokenKind::T4,
        _ => unreachable!("Unknown TerminalId: {:?}", terminal_id),
    }
}
pub struct FollowRestrictionLexicalMultipleParseTreeBuilder<'a> {
    pub bump: &'a Bump,
}
impl<'a> FollowRestrictionLexicalMultipleParseTreeBuilder<'a> {
    pub fn new(ctx: &'a ParseContext) -> Self {
        Self { bump: ctx.bump() }
    }
}
impl<'a> ParseTreeBuilder<ParseTree<'a>> for FollowRestrictionLexicalMultipleParseTreeBuilder<'a> {
    fn new_nonterminal_node(
        &self,
        nonterminal_node: &NonterminalNode,
        children: OneOrMany<ParseTree<'a>>,
    ) -> ParseTree<'a> {
        match nonterminal_node.nonterminal_id {
            // S
            NonterminalId(0) => match nonterminal_node.return_slot {
                // S : Element+.
                SlotId(1) => {
                    let [elements] = children.into_array::<1usize>();
                    ParseTree::S(self.bump.alloc(S::Alt0 {
                        elements: elements.unwrap_plus_0(),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // Element
            NonterminalId(1) => match nonterminal_node.return_slot {
                // Element : Num.
                SlotId(3) => {
                    let [num] = children.into_array::<1usize>();
                    ParseTree::Element(self.bump.alloc(Element::Alt0 {
                        num: num.unwrap_token(),
                        span: nonterminal_node.span,
                    }))
                }
                // Element : Word.
                SlotId(5) => {
                    let [word] = children.into_array::<1usize>();
                    ParseTree::Element(self.bump.alloc(Element::Alt1 {
                        word: word.unwrap_token(),
                        span: nonterminal_node.span,
                    }))
                }
                // Element : Dot.
                SlotId(7) => {
                    let [dot] = children.into_array::<1usize>();
                    ParseTree::Element(self.bump.alloc(Element::Alt2 {
                        dot: dot.unwrap_token(),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // Plus_0
            NonterminalId(2) => match nonterminal_node.return_slot {
                // Element+ : Element+ WS Element.
                SlotId(11) => {
                    let [elements_0, ws, element_2] = children.into_array::<3usize>();
                    ParseTree::Plus0(self.bump.alloc(Plus0::Alt0 {
                        elements_0: elements_0.unwrap_plus_0(),
                        ws: ws.unwrap_token(),
                        element_2: element_2.unwrap_element(),
                        span: nonterminal_node.span,
                    }))
                }
                // Element+ : Element.
                SlotId(13) => {
                    let [element] = children.into_array::<1usize>();
                    ParseTree::Plus0(self.bump.alloc(Plus0::Alt1 {
                        element: element.unwrap_element(),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            _ => unreachable!(),
        }
    }
    fn new_token(&self, terminal_node: &TerminalNode) -> ParseTree<'a> {
        ParseTree::Token(Token {
            kind: token_kind(terminal_node.terminal_id),
            span: terminal_node.span,
        })
    }
    fn new_ambiguity_node(
        &self,
        parent: NonterminalId,
        alternatives: Vec<ParseTree<'a>>,
    ) -> ParseTree<'a> {
        match parent {
            crate::grammar_data::S => {
                let slice = self
                    .bump
                    .alloc_slice_fill_iter(alternatives.into_iter().map(|a| a.unwrap_s()));
                ParseTree::S(self.bump.alloc(S::Amb(slice)))
            }
            crate::grammar_data::ELEMENT => {
                let slice = self
                    .bump
                    .alloc_slice_fill_iter(alternatives.into_iter().map(|a| a.unwrap_element()));
                ParseTree::Element(self.bump.alloc(Element::Amb(slice)))
            }
            crate::grammar_data::PLUS_0 => {
                let slice = self
                    .bump
                    .alloc_slice_fill_iter(alternatives.into_iter().map(|a| a.unwrap_plus_0()));
                ParseTree::Plus0(self.bump.alloc(Plus0::Amb(slice)))
            }
            _ => unreachable!("nonterminal cannot be ambiguous"),
        }
    }
}
pub fn create_parse_tree<'a>(
    root_id: SPPFNodeId,
    nonterminal_id: NonterminalId,
    parser: &FollowRestrictionLexicalMultipleParser,
    builder: &FollowRestrictionLexicalMultipleParseTreeBuilder<'a>,
) -> ParseTree<'a> {
    match nonterminal_id {
        crate::grammar_data::S => ParseTree::S(create_parse_tree_s(root_id, parser, builder)),
        crate::grammar_data::ELEMENT => {
            ParseTree::Element(create_parse_tree_element(root_id, parser, builder))
        }
        crate::grammar_data::PLUS_0 => {
            ParseTree::Plus0(create_parse_tree_plus_0(root_id, parser, builder))
        }
        _ => panic!(),
    }
}
pub fn create_parse_tree_s<'a>(
    root_id: SPPFNodeId,
    parser: &FollowRestrictionLexicalMultipleParser,
    builder: &FollowRestrictionLexicalMultipleParseTreeBuilder<'a>,
) -> &'a S<'a> {
    visit_sppf(root_id, parser, builder).unwrap_one().unwrap_s()
}
pub fn create_parse_tree_element<'a>(
    root_id: SPPFNodeId,
    parser: &FollowRestrictionLexicalMultipleParser,
    builder: &FollowRestrictionLexicalMultipleParseTreeBuilder<'a>,
) -> &'a Element<'a> {
    visit_sppf(root_id, parser, builder)
        .unwrap_one()
        .unwrap_element()
}
pub fn create_parse_tree_plus_0<'a>(
    root_id: SPPFNodeId,
    parser: &FollowRestrictionLexicalMultipleParser,
    builder: &FollowRestrictionLexicalMultipleParseTreeBuilder<'a>,
) -> &'a Plus0<'a> {
    visit_sppf(root_id, parser, builder)
        .unwrap_one()
        .unwrap_plus_0()
}
impl<'a> ParseTreeNode for ParseTree<'a> {
    fn children(&self) -> Vec<Self> {
        ParseTree::children(self)
    }
    fn display_name(&self) -> &'static str {
        ParseTree::display_name(self)
    }
    fn span(&self) -> Span {
        ParseTree::span(self)
    }
    fn kind(&self) -> NodeKind {
        if self.is_amb() {
            NodeKind::Amb
        } else if matches!(self, ParseTree::Token(_)) {
            NodeKind::Token
        } else {
            NodeKind::Nonterminal
        }
    }
    fn node_id(&self) -> Option<usize> {
        ParseTree::node_id(self)
    }
    fn origin(&self) -> Option<Origin> {
        ParseTree::origin(self)
    }
}
const LAYOUT_NAME: Option<&str> = Some("WS");
pub fn to_sexpr(node: ParseTree<'_>) -> String {
    iguana_runtime::parse_tree::to_sexpr(node, LAYOUT_NAME)
}
pub fn to_sexpr_with(node: ParseTree<'_>, options: SexprOptions) -> String {
    iguana_runtime::parse_tree::to_sexpr_with(node, LAYOUT_NAME, options)
}
/// Converts a parse tree to a JSON string of nodes and edges, for visualization.
pub fn to_json(node: ParseTree<'_>) -> String {
    iguana_runtime::parse_tree::to_json(node, LAYOUT_NAME)
}
