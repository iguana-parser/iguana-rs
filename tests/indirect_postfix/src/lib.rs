// @generated by iguana. Do not edit manually.

pub mod grammar_data;
pub mod parse_tree;
pub mod parser;
pub mod scanner;
pub mod types;
use iguana_runtime::{
    input::Input,
    parse_tree::ParseContext,
    parser::{ParseResult, Parser},
};
use parse_tree::*;
use parser::IndirectPostfixParser;
use std::error::Error;
use std::fmt::{self, Display, Formatter};
use std::time::Duration;
#[derive(Debug)]
pub struct ParseError {
    pub line: u32,
    pub column: u32,
    pub message: String,
}
impl Display for ParseError {
    fn fmt(&self, f: &mut Formatter<'_>) -> fmt::Result {
        write!(
            f,
            "Parse error at line {}, column {}: {}",
            self.line, self.column, self.message
        )
    }
}
impl Error for ParseError {}
pub struct ParseSuccess<T> {
    pub tree: T,
    pub parse_duration: Duration,
    pub tree_construction_duration: Duration,
}
pub fn parse_s<'a>(
    input: &Input,
    ctx: &'a ParseContext,
) -> std::result::Result<ParseSuccess<&'a Start<&'a S<'a>, Token>>, ParseError> {
    let mut parser = IndirectPostfixParser::new(input, grammar_data::START_S);
    match parser.run() {
        ParseResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectPostfixParseTreeBuilder::new(ctx);
            let tree = parse_tree::create_parse_tree_start_s(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
            })
        }
        ParseResult::Failure(error) => {
            let (line, column, message) = parser.format_error(&error);
            Err(ParseError {
                line,
                column,
                message,
            })
        }
    }
}
pub fn parse_e<'a>(
    input: &Input,
    ctx: &'a ParseContext,
) -> std::result::Result<ParseSuccess<&'a E<'a>>, ParseError> {
    let mut parser = IndirectPostfixParser::new(input, grammar_data::E);
    match parser.run() {
        ParseResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectPostfixParseTreeBuilder::new(ctx);
            let tree =
                parse_tree::create_parse_tree_e(success.sppf_node_id, &parser, &parse_tree_builder);
            let tree_construction_duration = tree_start.elapsed();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
            })
        }
        ParseResult::Failure(error) => {
            let (line, column, message) = parser.format_error(&error);
            Err(ParseError {
                line,
                column,
                message,
            })
        }
    }
}
pub fn parse_postfix<'a>(
    input: &Input,
    ctx: &'a ParseContext,
) -> std::result::Result<ParseSuccess<&'a Postfix<'a>>, ParseError> {
    let mut parser = IndirectPostfixParser::new(input, grammar_data::POSTFIX);
    match parser.run() {
        ParseResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectPostfixParseTreeBuilder::new(ctx);
            let tree = parse_tree::create_parse_tree_postfix(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
            })
        }
        ParseResult::Failure(error) => {
            let (line, column, message) = parser.format_error(&error);
            Err(ParseError {
                line,
                column,
                message,
            })
        }
    }
}
pub fn parse_body<'a>(
    input: &Input,
    ctx: &'a ParseContext,
) -> std::result::Result<ParseSuccess<&'a Body<'a>>, ParseError> {
    let mut parser = IndirectPostfixParser::new(input, grammar_data::BODY);
    match parser.run() {
        ParseResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectPostfixParseTreeBuilder::new(ctx);
            let tree = parse_tree::create_parse_tree_body(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
            })
        }
        ParseResult::Failure(error) => {
            let (line, column, message) = parser.format_error(&error);
            Err(ParseError {
                line,
                column,
                message,
            })
        }
    }
}
