// @generated by iguana. Do not edit manually.
// You may distribute this file under any license of your choice. It embeds
// fragments of iguana's GPL-licensed source, and iguana grants an additional
// permission (GPL v3 section 7) that waives the GPL for generated parsers.
// Full text: https://github.com/iguana-parser/iguana-rs/blob/main/iguana-compiler/LICENSE-GPL

use crate::parser::PlusAltParser;
use iguana_runtime::{
    ids::{NonterminalId, SlotId, TerminalId},
    input::Span,
    parse_tree::{
        Bump, DisplayOptions, NodeKind, OneOrMany, Origin, ParseTreeBuilder, ParseTreeNode,
        visit_sppf,
    },
    sppf::{NonterminalNode, SPPFNodeId, TerminalNode},
};
use std::vec::IntoIter;
#[derive(Debug, Clone, Copy)]
pub enum TokenKind {
    // "a"
    T0,
    // "1"
    T1,
}
impl TokenKind {
    pub fn name(&self) -> &'static str {
        match self {
            TokenKind::T0 => "\"a\"",
            TokenKind::T1 => "\"1\"",
        }
    }
}
#[derive(Debug)]
pub struct Start<T, L> {
    pub before: L,
    pub node: T,
    pub after: L,
    pub span: Span,
}
#[derive(Debug, Clone, Copy)]
pub enum ParseTree<'a> {
    S(&'a S<'a>),
    A(&'a A<'a>),
    Num(&'a Num<'a>),
    // (A | Num)
    Alt0(&'a Alt0<'a>),
    // (A | Num)+
    Plus0(&'a Plus0<'a>),
    // S
    StartS(&'a Start<&'a S<'a>, ()>),
    // A
    StartA(&'a Start<&'a A<'a>, ()>),
    // Num
    StartNum(&'a Start<&'a Num<'a>, ()>),
    Token(Token),
}
impl<'a> ParseTree<'a> {
    pub fn children(&self) -> Vec<ParseTree<'a>> {
        match self {
            ParseTree::S(s) => (0..s.child_count()).filter_map(|i| s.child(i)).collect(),
            ParseTree::A(a) => (0..a.child_count()).filter_map(|i| a.child(i)).collect(),
            ParseTree::Num(num) => (0..num.child_count())
                .filter_map(|i| num.child(i))
                .collect(),
            ParseTree::Alt0(alt_0) => (0..alt_0.child_count())
                .filter_map(|i| alt_0.child(i))
                .collect(),
            ParseTree::Plus0(plus_0) => match plus_0 {
                Plus0::Amb(alts) => alts.iter().copied().map(ParseTree::Plus0).collect(),
                _ => plus_0.iter().collect(),
            },
            ParseTree::StartS(start_s) => (0..start_s.child_count())
                .filter_map(|i| start_s.child(i))
                .collect(),
            ParseTree::StartA(start_a) => (0..start_a.child_count())
                .filter_map(|i| start_a.child(i))
                .collect(),
            ParseTree::StartNum(start_num) => (0..start_num.child_count())
                .filter_map(|i| start_num.child(i))
                .collect(),
            ParseTree::Token(_) => vec![],
        }
    }
    pub fn display_name(&self) -> &'static str {
        match self {
            ParseTree::S(s) => s.display_name(),
            ParseTree::A(a) => a.display_name(),
            ParseTree::Num(num) => num.display_name(),
            ParseTree::Alt0(alt_0) => alt_0.display_name(),
            ParseTree::Plus0(plus_0) => plus_0.display_name(),
            ParseTree::StartS(start_s) => start_s.display_name(),
            ParseTree::StartA(start_a) => start_a.display_name(),
            ParseTree::StartNum(start_num) => start_num.display_name(),
            ParseTree::Token(token) => token.kind.name(),
        }
    }
    pub fn child_count(&self) -> usize {
        match self {
            ParseTree::S(s) => s.child_count(),
            ParseTree::A(a) => a.child_count(),
            ParseTree::Num(num) => num.child_count(),
            ParseTree::Alt0(alt_0) => alt_0.child_count(),
            ParseTree::Plus0(plus_0) => plus_0.child_count(),
            ParseTree::StartS(start_s) => start_s.child_count(),
            ParseTree::StartA(start_a) => start_a.child_count(),
            ParseTree::StartNum(start_num) => start_num.child_count(),
            ParseTree::Token(_) => 0,
        }
    }
    pub fn span(&self) -> Span {
        match self {
            ParseTree::S(s) => s.span(),
            ParseTree::A(a) => a.span(),
            ParseTree::Num(num) => num.span(),
            ParseTree::Alt0(alt_0) => alt_0.span(),
            ParseTree::Plus0(plus_0) => plus_0.span(),
            ParseTree::StartS(start_s) => start_s.span(),
            ParseTree::StartA(start_a) => start_a.span(),
            ParseTree::StartNum(start_num) => start_num.span(),
            ParseTree::Token(token) => token.span(),
        }
    }
    ///True when this node is an ambiguity cluster (any `*::Amb` variant). The
    ///uniform way to detect ambiguity without matching each nonterminal's enum.
    pub fn is_amb(&self) -> bool {
        match self {
            ParseTree::S(s) => matches!(s, S::Amb(_)),
            ParseTree::A(a) => matches!(a, A::Amb(_)),
            ParseTree::Num(num) => matches!(num, Num::Amb(_)),
            ParseTree::Alt0(alt_0) => matches!(alt_0, Alt0::Amb(_)),
            ParseTree::Plus0(plus_0) => matches!(plus_0, Plus0::Amb(_)),
            ParseTree::StartS(_) => false,
            ParseTree::StartA(_) => false,
            ParseTree::StartNum(_) => false,
            ParseTree::Token(_) => false,
        }
    }
    ///Pointer identity of the underlying node, or `None` for tokens (by-value
    ///leaves that are never shared). Two parse trees with the same `node_id` are
    ///the same allocation, i.e. a node shared between parents in the ambiguity DAG.
    pub fn node_id(&self) -> Option<usize> {
        match self {
            ParseTree::S(s) => Some(*s as *const _ as usize),
            ParseTree::A(a) => Some(*a as *const _ as usize),
            ParseTree::Num(num) => Some(*num as *const _ as usize),
            ParseTree::Alt0(alt_0) => Some(*alt_0 as *const _ as usize),
            ParseTree::Plus0(plus_0) => Some(*plus_0 as *const _ as usize),
            ParseTree::StartS(start_s) => Some(*start_s as *const _ as usize),
            ParseTree::StartA(start_a) => Some(*start_a as *const _ as usize),
            ParseTree::StartNum(start_num) => Some(*start_num as *const _ as usize),
            ParseTree::Token(_) => None,
        }
    }
    pub fn origin(&self) -> Option<Origin> {
        match self {
            ParseTree::S(s) => s.origin(),
            ParseTree::A(a) => a.origin(),
            ParseTree::Num(num) => num.origin(),
            ParseTree::Alt0(alt_0) => alt_0.origin(),
            ParseTree::Plus0(plus_0) => plus_0.origin(),
            ParseTree::StartS(start_s) => start_s.origin(),
            ParseTree::StartA(start_a) => start_a.origin(),
            ParseTree::StartNum(start_num) => start_num.origin(),
            ParseTree::Token(_) => None,
        }
    }
    fn unwrap_s(self) -> &'a S<'a> {
        match self {
            ParseTree::S(s) => s,
            _ => panic!(),
        }
    }
    fn unwrap_a(self) -> &'a A<'a> {
        match self {
            ParseTree::A(a) => a,
            _ => panic!(),
        }
    }
    fn unwrap_num(self) -> &'a Num<'a> {
        match self {
            ParseTree::Num(num) => num,
            _ => panic!(),
        }
    }
    fn unwrap_alt_0(self) -> &'a Alt0<'a> {
        match self {
            ParseTree::Alt0(alt_0) => alt_0,
            _ => panic!(),
        }
    }
    fn unwrap_plus_0(self) -> &'a Plus0<'a> {
        match self {
            ParseTree::Plus0(plus_0) => plus_0,
            _ => panic!(),
        }
    }
    fn unwrap_start_s(self) -> &'a Start<&'a S<'a>, ()> {
        match self {
            ParseTree::StartS(start_s) => start_s,
            _ => panic!(),
        }
    }
    fn unwrap_start_a(self) -> &'a Start<&'a A<'a>, ()> {
        match self {
            ParseTree::StartA(start_a) => start_a,
            _ => panic!(),
        }
    }
    fn unwrap_start_num(self) -> &'a Start<&'a Num<'a>, ()> {
        match self {
            ParseTree::StartNum(start_num) => start_num,
            _ => panic!(),
        }
    }
    fn unwrap_token(self) -> Token {
        match self {
            ParseTree::Token(t) => t,
            _ => panic!(),
        }
    }
}
pub trait ListNode<'a> {
    fn iter(&'a self) -> IntoIter<ParseTree<'a>>;
}
pub trait OptNode {
    type Inner;
    fn value(&self) -> Option<&Self::Inner>;
}
// S = (A | Num)+
#[derive(Debug)]
pub enum S<'a> {
    Alt0 { plus_0: &'a Plus0<'a>, span: Span },
    Amb(&'a [&'a S<'a>]),
}
// A = "a"
#[derive(Debug)]
pub enum A<'a> {
    Alt0 { lit_0: Token, span: Span },
    Amb(&'a [&'a A<'a>]),
}
// Num = "1"
#[derive(Debug)]
pub enum Num<'a> {
    Alt0 { lit_0: Token, span: Span },
    Amb(&'a [&'a Num<'a>]),
}
// (A | Num)
#[derive(Debug)]
pub enum Alt0<'a> {
    // Alt_0 = A
    Alt0 { a: &'a A<'a>, span: Span },
    // Alt_0 = Num
    Alt1 { num: &'a Num<'a>, span: Span },
    Amb(&'a [&'a Alt0<'a>]),
}
// (A | Num)+
#[derive(Debug)]
pub enum Plus0<'a> {
    // Plus_0 = (A | Num)+ (A | Num)
    Alt0 {
        plus_0: &'a Plus0<'a>,
        alt_0: &'a Alt0<'a>,
        span: Span,
    },
    // Plus_0 = (A | Num)
    Alt1 {
        alt_0: &'a Alt0<'a>,
        span: Span,
    },
    Amb(&'a [&'a Plus0<'a>]),
}
impl<'a> S<'a> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::S(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match self {
            S::Alt0 { plus_0, .. } => match index {
                0 => Some(ParseTree::Plus0(plus_0)),
                _ => None,
            },
            S::Amb(alts) => alts.get(index).copied().map(ParseTree::S),
        }
    }
    pub fn child_count(&self) -> usize {
        match self {
            S::Alt0 { .. } => 1usize,
            S::Amb(alts) => alts.len(),
        }
    }
    pub fn span(&self) -> Span {
        match self {
            S::Alt0 { span, .. } => *span,
            S::Amb(alts) => alts[0].span(),
        }
    }
    pub fn display_name(&self) -> &'static str {
        match self {
            S::Amb(_) => "Amb",
            _ => "S",
        }
    }
    pub fn origin(&self) -> Option<Origin> {
        None
    }
    pub fn r#as(&self) -> impl Iterator<Item = &'a A<'a>> {
        match self {
            S::Alt0 { plus_0, .. } => plus_0.r#as(),
            S::Amb(_) => panic!("S is ambiguous"),
        }
    }
    pub fn nums(&self) -> impl Iterator<Item = &'a Num<'a>> {
        match self {
            S::Alt0 { plus_0, .. } => plus_0.nums(),
            S::Amb(_) => panic!("S is ambiguous"),
        }
    }
    pub fn plus_0(&self) -> &'a Plus0<'a> {
        match self {
            S::Alt0 { plus_0, .. } => plus_0,
            S::Amb(_) => panic!("S is ambiguous"),
        }
    }
}
impl<'a> A<'a> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::A(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match self {
            A::Alt0 { lit_0, .. } => match index {
                0 => Some(ParseTree::Token(*lit_0)),
                _ => None,
            },
            A::Amb(alts) => alts.get(index).copied().map(ParseTree::A),
        }
    }
    pub fn child_count(&self) -> usize {
        match self {
            A::Alt0 { .. } => 1usize,
            A::Amb(alts) => alts.len(),
        }
    }
    pub fn span(&self) -> Span {
        match self {
            A::Alt0 { span, .. } => *span,
            A::Amb(alts) => alts[0].span(),
        }
    }
    pub fn display_name(&self) -> &'static str {
        match self {
            A::Amb(_) => "Amb",
            _ => "A",
        }
    }
    pub fn origin(&self) -> Option<Origin> {
        None
    }
    pub fn lit_0(&self) -> Token {
        match self {
            A::Alt0 { lit_0, .. } => *lit_0,
            A::Amb(_) => panic!("A is ambiguous"),
        }
    }
}
impl<'a> Num<'a> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::Num(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match self {
            Num::Alt0 { lit_0, .. } => match index {
                0 => Some(ParseTree::Token(*lit_0)),
                _ => None,
            },
            Num::Amb(alts) => alts.get(index).copied().map(ParseTree::Num),
        }
    }
    pub fn child_count(&self) -> usize {
        match self {
            Num::Alt0 { .. } => 1usize,
            Num::Amb(alts) => alts.len(),
        }
    }
    pub fn span(&self) -> Span {
        match self {
            Num::Alt0 { span, .. } => *span,
            Num::Amb(alts) => alts[0].span(),
        }
    }
    pub fn display_name(&self) -> &'static str {
        match self {
            Num::Amb(_) => "Amb",
            _ => "Num",
        }
    }
    pub fn origin(&self) -> Option<Origin> {
        None
    }
    pub fn lit_0(&self) -> Token {
        match self {
            Num::Alt0 { lit_0, .. } => *lit_0,
            Num::Amb(_) => panic!("Num is ambiguous"),
        }
    }
}
impl<'a> Alt0<'a> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::Alt0(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match self {
            Alt0::Alt0 { a, .. } => match index {
                0 => Some(ParseTree::A(a)),
                _ => None,
            },
            Alt0::Alt1 { num, .. } => match index {
                0 => Some(ParseTree::Num(num)),
                _ => None,
            },
            Alt0::Amb(alts) => alts.get(index).copied().map(ParseTree::Alt0),
        }
    }
    pub fn child_count(&self) -> usize {
        match self {
            Alt0::Alt0 { .. } => 1usize,
            Alt0::Alt1 { .. } => 1usize,
            Alt0::Amb(alts) => alts.len(),
        }
    }
    pub fn span(&self) -> Span {
        match self {
            Alt0::Alt0 { span, .. } => *span,
            Alt0::Alt1 { span, .. } => *span,
            Alt0::Amb(alts) => alts[0].span(),
        }
    }
    pub fn display_name(&self) -> &'static str {
        match self {
            Alt0::Amb(_) => "Amb",
            _ => "(A | Num)",
        }
    }
    pub fn origin(&self) -> Option<Origin> {
        match self {
            Alt0::Amb(_) => None,
            _ => Some(Origin::Alt),
        }
    }
}
impl<'a> Plus0<'a> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::Plus0(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match self {
            Plus0::Alt0 { plus_0, alt_0, .. } => match index {
                0 => Some(ParseTree::Plus0(plus_0)),
                1 => Some(ParseTree::Alt0(alt_0)),
                _ => None,
            },
            Plus0::Alt1 { alt_0, .. } => match index {
                0 => Some(ParseTree::Alt0(alt_0)),
                _ => None,
            },
            Plus0::Amb(alts) => alts.get(index).copied().map(ParseTree::Plus0),
        }
    }
    pub fn child_count(&self) -> usize {
        match self {
            Plus0::Alt0 { .. } => 2usize,
            Plus0::Alt1 { .. } => 1usize,
            Plus0::Amb(alts) => alts.len(),
        }
    }
    pub fn span(&self) -> Span {
        match self {
            Plus0::Alt0 { span, .. } => *span,
            Plus0::Alt1 { span, .. } => *span,
            Plus0::Amb(alts) => alts[0].span(),
        }
    }
    pub fn display_name(&self) -> &'static str {
        match self {
            Plus0::Amb(_) => "Amb",
            _ => "(A | Num)+",
        }
    }
    pub fn origin(&self) -> Option<Origin> {
        match self {
            Plus0::Amb(_) => None,
            _ => Some(Origin::List),
        }
    }
    pub fn r#as(&'a self) -> impl Iterator<Item = &'a A<'a>> {
        self.iter().filter_map(|node| match node {
            ParseTree::Alt0(Alt0::Alt0 { a, .. }) => Some(*a),
            other if other.is_amb() => panic!("Plus_0 is ambiguous"),
            _ => None,
        })
    }
    pub fn nums(&'a self) -> impl Iterator<Item = &'a Num<'a>> {
        self.iter().filter_map(|node| match node {
            ParseTree::Alt0(Alt0::Alt1 { num, .. }) => Some(*num),
            other if other.is_amb() => panic!("Plus_0 is ambiguous"),
            _ => None,
        })
    }
}
impl<'a> Start<&'a S<'a>, ()> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::StartS(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match index {
            0 => Some(ParseTree::S(self.node)),
            _ => None,
        }
    }
    pub fn child_count(&self) -> usize {
        1usize
    }
    pub fn span(&self) -> Span {
        self.span
    }
    pub fn display_name(&self) -> &'static str {
        "Start"
    }
    pub fn origin(&self) -> Option<Origin> {
        Some(Origin::Start)
    }
}
impl<'a> Start<&'a A<'a>, ()> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::StartA(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match index {
            0 => Some(ParseTree::A(self.node)),
            _ => None,
        }
    }
    pub fn child_count(&self) -> usize {
        1usize
    }
    pub fn span(&self) -> Span {
        self.span
    }
    pub fn display_name(&self) -> &'static str {
        "Start"
    }
    pub fn origin(&self) -> Option<Origin> {
        Some(Origin::Start)
    }
}
impl<'a> Start<&'a Num<'a>, ()> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::StartNum(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match index {
            0 => Some(ParseTree::Num(self.node)),
            _ => None,
        }
    }
    pub fn child_count(&self) -> usize {
        1usize
    }
    pub fn span(&self) -> Span {
        self.span
    }
    pub fn display_name(&self) -> &'static str {
        "Start"
    }
    pub fn origin(&self) -> Option<Origin> {
        Some(Origin::Start)
    }
}
impl<'a> ListNode<'a> for Plus0<'a> {
    fn iter(&'a self) -> IntoIter<ParseTree<'a>> {
        let mut items = vec![];
        let mut current = self;
        loop {
            match current {
                Plus0::Alt0 {
                    plus_0: rest,
                    alt_0: item,
                    ..
                } => {
                    items.push(item.as_parse_tree());
                    current = rest;
                }
                Plus0::Alt1 { alt_0: item, .. } => {
                    items.push(item.as_parse_tree());
                    break;
                }
                Plus0::Amb(_) => {
                    items.push(ParseTree::Plus0(current));
                    break;
                }
            }
        }
        items.reverse();
        items.into_iter()
    }
}
impl<'a> Alt0<'a> {
    pub fn as_a(&self) -> Option<&A<'_>> {
        match self {
            Alt0::Alt0 { a, .. } => Some(a),
            _ => None,
        }
    }
    pub fn as_num(&self) -> Option<&Num<'_>> {
        match self {
            Alt0::Alt1 { num, .. } => Some(num),
            _ => None,
        }
    }
}
#[derive(Debug, Clone, Copy)]
pub struct Token {
    pub kind: TokenKind,
    span: Span,
}
impl Token {
    pub fn as_parse_tree<'a>(&self) -> ParseTree<'a> {
        ParseTree::Token(*self)
    }
    pub fn span(&self) -> Span {
        self.span
    }
}
fn token_kind(terminal_id: TerminalId) -> TokenKind {
    match terminal_id {
        // "a"
        TerminalId(0) => TokenKind::T0,
        // "1"
        TerminalId(1) => TokenKind::T1,
        _ => unreachable!("Unknown TerminalId: {:?}", terminal_id),
    }
}
pub struct PlusAltParseTreeBuilder<'a> {
    pub arena: &'a Bump,
}
impl<'a> PlusAltParseTreeBuilder<'a> {
    pub fn new(tree_arena: &'a Bump) -> Self {
        Self { arena: tree_arena }
    }
}
impl<'a> ParseTreeBuilder<ParseTree<'a>> for PlusAltParseTreeBuilder<'a> {
    fn new_nonterminal_node(
        &self,
        nonterminal_node: &NonterminalNode,
        children: OneOrMany<ParseTree<'a>>,
    ) -> ParseTree<'a> {
        match nonterminal_node.nonterminal_id {
            // S
            NonterminalId(0) => match nonterminal_node.return_slot {
                // S : (A | Num)+.
                SlotId(1) => {
                    let [plus_0] = children.into_array::<1usize>();
                    ParseTree::S(self.arena.alloc(S::Alt0 {
                        plus_0: plus_0.unwrap_plus_0(),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // A
            NonterminalId(1) => match nonterminal_node.return_slot {
                // A : "a".
                SlotId(3) => {
                    let [lit_0] = children.into_array::<1usize>();
                    ParseTree::A(self.arena.alloc(A::Alt0 {
                        lit_0: lit_0.unwrap_token(),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // Num
            NonterminalId(2) => match nonterminal_node.return_slot {
                // Num : "1".
                SlotId(5) => {
                    let [lit_0] = children.into_array::<1usize>();
                    ParseTree::Num(self.arena.alloc(Num::Alt0 {
                        lit_0: lit_0.unwrap_token(),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // Alt_0
            NonterminalId(3) => match nonterminal_node.return_slot {
                // (A | Num) : A.
                SlotId(7) => {
                    let [a] = children.into_array::<1usize>();
                    ParseTree::Alt0(self.arena.alloc(Alt0::Alt0 {
                        a: a.unwrap_a(),
                        span: nonterminal_node.span,
                    }))
                }
                // (A | Num) : Num.
                SlotId(9) => {
                    let [num] = children.into_array::<1usize>();
                    ParseTree::Alt0(self.arena.alloc(Alt0::Alt1 {
                        num: num.unwrap_num(),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // Plus_0
            NonterminalId(4) => match nonterminal_node.return_slot {
                // (A | Num)+ : (A | Num)+ (A | Num).
                SlotId(12) => {
                    let [plus_0, alt_0] = children.into_array::<2usize>();
                    ParseTree::Plus0(self.arena.alloc(Plus0::Alt0 {
                        plus_0: plus_0.unwrap_plus_0(),
                        alt_0: alt_0.unwrap_alt_0(),
                        span: nonterminal_node.span,
                    }))
                }
                // (A | Num)+ : (A | Num).
                SlotId(14) => {
                    let [alt_0] = children.into_array::<1usize>();
                    ParseTree::Plus0(self.arena.alloc(Plus0::Alt1 {
                        alt_0: alt_0.unwrap_alt_0(),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // StartS
            NonterminalId(5) => match nonterminal_node.return_slot {
                // S : start:S.
                SlotId(16) => {
                    let [start] = children.into_array::<1usize>();
                    ParseTree::StartS(self.arena.alloc(Start {
                        before: (),
                        node: start.unwrap_s(),
                        after: (),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // StartA
            NonterminalId(6) => match nonterminal_node.return_slot {
                // A : start:A.
                SlotId(18) => {
                    let [start] = children.into_array::<1usize>();
                    ParseTree::StartA(self.arena.alloc(Start {
                        before: (),
                        node: start.unwrap_a(),
                        after: (),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // StartNum
            NonterminalId(7) => match nonterminal_node.return_slot {
                // Num : start:Num.
                SlotId(20) => {
                    let [start] = children.into_array::<1usize>();
                    ParseTree::StartNum(self.arena.alloc(Start {
                        before: (),
                        node: start.unwrap_num(),
                        after: (),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            _ => unreachable!(),
        }
    }
    fn new_token(&self, terminal_node: &TerminalNode) -> ParseTree<'a> {
        ParseTree::Token(Token {
            kind: token_kind(terminal_node.terminal_id),
            span: terminal_node.span,
        })
    }
    fn new_ambiguity_node(
        &self,
        parent: NonterminalId,
        alternatives: Vec<ParseTree<'a>>,
    ) -> ParseTree<'a> {
        match parent {
            crate::grammar_data::S => {
                let slice = self
                    .arena
                    .alloc_slice_fill_iter(alternatives.into_iter().map(|a| a.unwrap_s()));
                ParseTree::S(self.arena.alloc(S::Amb(slice)))
            }
            crate::grammar_data::A => {
                let slice = self
                    .arena
                    .alloc_slice_fill_iter(alternatives.into_iter().map(|a| a.unwrap_a()));
                ParseTree::A(self.arena.alloc(A::Amb(slice)))
            }
            crate::grammar_data::NUM => {
                let slice = self
                    .arena
                    .alloc_slice_fill_iter(alternatives.into_iter().map(|a| a.unwrap_num()));
                ParseTree::Num(self.arena.alloc(Num::Amb(slice)))
            }
            crate::grammar_data::ALT_0 => {
                let slice = self
                    .arena
                    .alloc_slice_fill_iter(alternatives.into_iter().map(|a| a.unwrap_alt_0()));
                ParseTree::Alt0(self.arena.alloc(Alt0::Amb(slice)))
            }
            crate::grammar_data::PLUS_0 => {
                let slice = self
                    .arena
                    .alloc_slice_fill_iter(alternatives.into_iter().map(|a| a.unwrap_plus_0()));
                ParseTree::Plus0(self.arena.alloc(Plus0::Amb(slice)))
            }
            crate::grammar_data::START_S => {
                let first = alternatives[0].unwrap_start_s();
                let inner = self.arena.alloc_slice_fill_iter(
                    alternatives.into_iter().map(|a| a.unwrap_start_s().node),
                );
                let node = &*self.arena.alloc(S::Amb(inner));
                ParseTree::StartS(self.arena.alloc(Start {
                    before: first.before,
                    node,
                    after: first.after,
                    span: first.span,
                }))
            }
            crate::grammar_data::START_A => {
                let first = alternatives[0].unwrap_start_a();
                let inner = self.arena.alloc_slice_fill_iter(
                    alternatives.into_iter().map(|a| a.unwrap_start_a().node),
                );
                let node = &*self.arena.alloc(A::Amb(inner));
                ParseTree::StartA(self.arena.alloc(Start {
                    before: first.before,
                    node,
                    after: first.after,
                    span: first.span,
                }))
            }
            crate::grammar_data::START_NUM => {
                let first = alternatives[0].unwrap_start_num();
                let inner = self.arena.alloc_slice_fill_iter(
                    alternatives.into_iter().map(|a| a.unwrap_start_num().node),
                );
                let node = &*self.arena.alloc(Num::Amb(inner));
                ParseTree::StartNum(self.arena.alloc(Start {
                    before: first.before,
                    node,
                    after: first.after,
                    span: first.span,
                }))
            }
            _ => unreachable!("nonterminal cannot be ambiguous"),
        }
    }
}
pub fn create_parse_tree<'a>(
    root_id: SPPFNodeId,
    nonterminal_id: NonterminalId,
    parser: &PlusAltParser,
    builder: &PlusAltParseTreeBuilder<'a>,
) -> ParseTree<'a> {
    match nonterminal_id {
        crate::grammar_data::S => ParseTree::S(create_parse_tree_s(root_id, parser, builder)),
        crate::grammar_data::A => ParseTree::A(create_parse_tree_a(root_id, parser, builder)),
        crate::grammar_data::NUM => ParseTree::Num(create_parse_tree_num(root_id, parser, builder)),
        crate::grammar_data::ALT_0 => {
            ParseTree::Alt0(create_parse_tree_alt_0(root_id, parser, builder))
        }
        crate::grammar_data::PLUS_0 => {
            ParseTree::Plus0(create_parse_tree_plus_0(root_id, parser, builder))
        }
        crate::grammar_data::START_S => {
            ParseTree::StartS(create_parse_tree_start_s(root_id, parser, builder))
        }
        crate::grammar_data::START_A => {
            ParseTree::StartA(create_parse_tree_start_a(root_id, parser, builder))
        }
        crate::grammar_data::START_NUM => {
            ParseTree::StartNum(create_parse_tree_start_num(root_id, parser, builder))
        }
        _ => panic!(),
    }
}
pub fn create_parse_tree_s<'a>(
    root_id: SPPFNodeId,
    parser: &PlusAltParser,
    builder: &PlusAltParseTreeBuilder<'a>,
) -> &'a S<'a> {
    visit_sppf(root_id, parser, builder).unwrap_one().unwrap_s()
}
pub fn create_parse_tree_a<'a>(
    root_id: SPPFNodeId,
    parser: &PlusAltParser,
    builder: &PlusAltParseTreeBuilder<'a>,
) -> &'a A<'a> {
    visit_sppf(root_id, parser, builder).unwrap_one().unwrap_a()
}
pub fn create_parse_tree_num<'a>(
    root_id: SPPFNodeId,
    parser: &PlusAltParser,
    builder: &PlusAltParseTreeBuilder<'a>,
) -> &'a Num<'a> {
    visit_sppf(root_id, parser, builder)
        .unwrap_one()
        .unwrap_num()
}
pub fn create_parse_tree_alt_0<'a>(
    root_id: SPPFNodeId,
    parser: &PlusAltParser,
    builder: &PlusAltParseTreeBuilder<'a>,
) -> &'a Alt0<'a> {
    visit_sppf(root_id, parser, builder)
        .unwrap_one()
        .unwrap_alt_0()
}
pub fn create_parse_tree_plus_0<'a>(
    root_id: SPPFNodeId,
    parser: &PlusAltParser,
    builder: &PlusAltParseTreeBuilder<'a>,
) -> &'a Plus0<'a> {
    visit_sppf(root_id, parser, builder)
        .unwrap_one()
        .unwrap_plus_0()
}
pub fn create_parse_tree_start_s<'a>(
    root_id: SPPFNodeId,
    parser: &PlusAltParser,
    builder: &PlusAltParseTreeBuilder<'a>,
) -> &'a Start<&'a S<'a>, ()> {
    visit_sppf(root_id, parser, builder)
        .unwrap_one()
        .unwrap_start_s()
}
pub fn create_parse_tree_start_a<'a>(
    root_id: SPPFNodeId,
    parser: &PlusAltParser,
    builder: &PlusAltParseTreeBuilder<'a>,
) -> &'a Start<&'a A<'a>, ()> {
    visit_sppf(root_id, parser, builder)
        .unwrap_one()
        .unwrap_start_a()
}
pub fn create_parse_tree_start_num<'a>(
    root_id: SPPFNodeId,
    parser: &PlusAltParser,
    builder: &PlusAltParseTreeBuilder<'a>,
) -> &'a Start<&'a Num<'a>, ()> {
    visit_sppf(root_id, parser, builder)
        .unwrap_one()
        .unwrap_start_num()
}
impl<'a> ParseTreeNode for ParseTree<'a> {
    fn children(&self) -> Vec<Self> {
        ParseTree::children(self)
    }
    fn display_name(&self) -> &'static str {
        ParseTree::display_name(self)
    }
    fn span(&self) -> Span {
        ParseTree::span(self)
    }
    fn kind(&self) -> NodeKind {
        if self.is_amb() {
            NodeKind::Amb
        } else if matches!(self, ParseTree::Token(_)) {
            NodeKind::Token
        } else {
            NodeKind::Nonterminal
        }
    }
    fn node_id(&self) -> Option<usize> {
        ParseTree::node_id(self)
    }
    fn origin(&self) -> Option<Origin> {
        ParseTree::origin(self)
    }
}
const LAYOUT_NAME: Option<&str> = None;
pub fn to_sexpr(node: ParseTree<'_>) -> String {
    iguana_runtime::parse_tree::to_sexpr(node, LAYOUT_NAME)
}
pub fn to_sexpr_with(node: ParseTree<'_>, options: DisplayOptions) -> String {
    iguana_runtime::parse_tree::to_sexpr_with(node, LAYOUT_NAME, options)
}
/// Converts a parse tree to a JSON string of nodes and edges, for visualization.
pub fn to_json(node: ParseTree<'_>) -> String {
    iguana_runtime::parse_tree::to_json(node, LAYOUT_NAME)
}
