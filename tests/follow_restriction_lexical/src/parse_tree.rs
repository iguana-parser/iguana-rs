// @generated by iguana. Do not edit manually.

use crate::parser::FollowRestrictionLexicalParser;
use core::fmt;
use iguana_runtime::{
    ids::{NonterminalId, SlotId, TerminalId},
    parse_tree::{Bump, OneOrMany, ParseContext, ParseTreeBuilder, visit_sppf},
    parser::Parser,
    sppf::{NonterminalNode, SPPFNodeId, Span, TerminalNode},
};
use std::{fmt::Write, vec::IntoIter};
#[derive(Debug, Clone, Copy)]
pub enum TokenKind {
    // Num
    T0,
    // Alpha
    T1,
    // Id
    T2,
    // WS
    T3,
}
impl TokenKind {
    pub fn name(&self) -> &'static str {
        match self {
            TokenKind::T0 => "Num",
            TokenKind::T1 => "Alpha",
            TokenKind::T2 => "Id",
            TokenKind::T3 => "WS",
            _ => unreachable!(),
        }
    }
}
#[derive(Debug, Clone, Copy)]
pub enum ParseTree<'a> {
    S(&'a S<'a>),
    Element(&'a Element<'a>),
    // Element+
    SPlus0(&'a SPlus0<'a>),
    Token(Token),
}
impl<'a> ParseTree<'a> {
    pub fn children(&self) -> Vec<ParseTree<'a>> {
        match self {
            ParseTree::S(s) => (0..s.child_count()).filter_map(|i| s.child(i)).collect(),
            ParseTree::Element(element) => (0..element.child_count())
                .filter_map(|i| element.child(i))
                .collect(),
            ParseTree::SPlus0(s_plus_0) => s_plus_0.iter().collect(),
            ParseTree::Token(_) => vec![],
        }
    }
    pub fn display_name(&self) -> &'static str {
        match self {
            ParseTree::S(_) => "S",
            ParseTree::Element(_) => "Element",
            ParseTree::SPlus0(_) => "Element+",
            ParseTree::Token(token) => token.kind.name(),
        }
    }
    pub fn child_count(&self) -> usize {
        match self {
            ParseTree::S(s) => s.child_count(),
            ParseTree::Element(element) => element.child_count(),
            ParseTree::SPlus0(s_plus_0) => s_plus_0.child_count(),
            ParseTree::Token(_) => 0,
        }
    }
    pub fn span(&self) -> Span {
        match self {
            ParseTree::S(s) => s.span(),
            ParseTree::Element(element) => element.span(),
            ParseTree::SPlus0(s_plus_0) => s_plus_0.span(),
            ParseTree::Token(token) => token.span(),
        }
    }
    fn unwrap_s(self) -> &'a S<'a> {
        match self {
            ParseTree::S(s) => s,
            _ => panic!(),
        }
    }
    fn unwrap_element(self) -> &'a Element<'a> {
        match self {
            ParseTree::Element(element) => element,
            _ => panic!(),
        }
    }
    fn unwrap_s_plus_0(self) -> &'a SPlus0<'a> {
        match self {
            ParseTree::SPlus0(s_plus_0) => s_plus_0,
            _ => panic!(),
        }
    }
    fn unwrap_token(self) -> Token {
        match self {
            ParseTree::Token(t) => t,
            _ => panic!(),
        }
    }
}
pub trait ListNode<'a> {
    fn iter(&'a self) -> IntoIter<ParseTree<'a>>;
}
pub trait OptNode {
    type Inner;
    fn value(&self) -> Option<&Self::Inner>;
}
// S = Element+
#[derive(Debug)]
pub struct S<'a> {
    pub elements: &'a SPlus0<'a>,
    pub span: Span,
}
#[derive(Debug)]
pub enum Element<'a> {
    // Num
    Alt0 { num: Token, span: Span },
    // Id
    Alt1 { id: Token, span: Span },
    Amb(&'a [&'a Element<'a>]),
}
// Element+
#[derive(Debug)]
pub enum SPlus0<'a> {
    // Element+ WS Element
    Alt0 {
        elements: &'a SPlus0<'a>,
        ws: Token,
        element_2: &'a Element<'a>,
        span: Span,
    },
    // Element
    Alt1 {
        element: &'a Element<'a>,
        span: Span,
    },
    Amb(&'a [&'a SPlus0<'a>]),
}
impl<'a> S<'a> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::S(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match index {
            0 => Some({
                let elements = &self.elements;
                ParseTree::SPlus0(elements)
            }),
            _ => None,
        }
    }
    pub fn child_count(&self) -> usize {
        1usize
    }
    pub fn span(&self) -> Span {
        self.span
    }
    pub fn elements(&self) -> impl Iterator<Item = &'a Element<'a>> {
        self.elements.elements()
    }
}
impl<'a> Element<'a> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::Element(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match self {
            Element::Alt0 { num, .. } => match index {
                0 => Some(ParseTree::Token(*num)),
                _ => None,
            },
            Element::Alt1 { id, .. } => match index {
                0 => Some(ParseTree::Token(*id)),
                _ => None,
            },
            Element::Amb(_) => None,
        }
    }
    pub fn child_count(&self) -> usize {
        match self {
            Element::Alt0 { .. } => 1usize,
            Element::Alt1 { .. } => 1usize,
            Element::Amb(alts) => alts.len(),
        }
    }
    pub fn span(&self) -> Span {
        match self {
            Element::Alt0 { span, .. } => *span,
            Element::Alt1 { span, .. } => *span,
            Element::Amb(alts) => alts[0].span(),
        }
    }
}
impl<'a> SPlus0<'a> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::SPlus0(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match self {
            SPlus0::Alt0 {
                elements,
                ws,
                element_2,
                ..
            } => match index {
                0 => Some(ParseTree::SPlus0(elements)),
                1 => Some(ParseTree::Token(*ws)),
                2 => Some(ParseTree::Element(element_2)),
                _ => None,
            },
            SPlus0::Alt1 { element, .. } => match index {
                0 => Some(ParseTree::Element(element)),
                _ => None,
            },
            SPlus0::Amb(_) => None,
        }
    }
    pub fn child_count(&self) -> usize {
        match self {
            SPlus0::Alt0 { .. } => 3usize,
            SPlus0::Alt1 { .. } => 1usize,
            SPlus0::Amb(alts) => alts.len(),
        }
    }
    pub fn span(&self) -> Span {
        match self {
            SPlus0::Alt0 { span, .. } => *span,
            SPlus0::Alt1 { span, .. } => *span,
            SPlus0::Amb(alts) => alts[0].span(),
        }
    }
    pub fn elements(&'a self) -> impl Iterator<Item = &'a Element<'a>> {
        self.iter().filter_map(|node| match node {
            ParseTree::Element(r) => Some(r),
            _ => None,
        })
    }
}
impl<'a> ListNode<'a> for SPlus0<'a> {
    fn iter(&'a self) -> IntoIter<ParseTree<'a>> {
        let mut items = vec![];
        let mut current = self;
        loop {
            match current {
                SPlus0::Alt0 {
                    elements: rest,
                    ws: layout,
                    element_2: item,
                    ..
                } => {
                    items.push(item.as_parse_tree());
                    items.push(layout.as_parse_tree());
                    current = rest;
                }
                SPlus0::Alt1 { element: item, .. } => {
                    items.push(item.as_parse_tree());
                    break;
                }
                SPlus0::Amb(_) => panic!("unexpected ambiguity in list node"),
            }
        }
        items.reverse();
        items.into_iter()
    }
}
impl<'a> Element<'a> {
    pub fn as_num(&self) -> Option<&Token> {
        match self {
            Element::Alt0 { num, .. } => Some(num),
            _ => None,
        }
    }
    pub fn as_id(&self) -> Option<&Token> {
        match self {
            Element::Alt1 { id, .. } => Some(id),
            _ => None,
        }
    }
}
#[derive(Debug, Clone, Copy)]
pub struct Token {
    pub kind: TokenKind,
    span: Span,
}
impl Token {
    pub fn as_parse_tree<'a>(&self) -> ParseTree<'a> {
        ParseTree::Token(*self)
    }
    pub fn span(&self) -> Span {
        self.span
    }
}
fn token_kind(terminal_id: TerminalId) -> TokenKind {
    match terminal_id {
        // Num
        TerminalId(0) => TokenKind::T0,
        // Alpha
        TerminalId(1) => TokenKind::T1,
        // Id
        TerminalId(2) => TokenKind::T2,
        // WS
        TerminalId(3) => TokenKind::T3,
        _ => unreachable!("Unknown TerminalId: {:?}", terminal_id),
    }
}
pub struct FollowRestrictionLexicalParseTreeBuilder<'a> {
    pub bump: &'a Bump,
}
impl<'a> FollowRestrictionLexicalParseTreeBuilder<'a> {
    pub fn new(ctx: &'a ParseContext) -> Self {
        Self { bump: ctx.bump() }
    }
}
impl<'a> ParseTreeBuilder<ParseTree<'a>> for FollowRestrictionLexicalParseTreeBuilder<'a> {
    fn new_nonterminal_node(
        &self,
        nonterminal_node: &NonterminalNode,
        children: OneOrMany<ParseTree<'a>>,
    ) -> ParseTree<'a> {
        match nonterminal_node.nonterminal_id {
            // S
            NonterminalId(0) => match nonterminal_node.return_slot {
                // S : Element+.
                SlotId(1) => {
                    let [elements] = children.into_array::<1usize>();
                    ParseTree::S(self.bump.alloc(S {
                        elements: elements.unwrap_s_plus_0(),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // Element
            NonterminalId(1) => match nonterminal_node.return_slot {
                // Element : Num.
                SlotId(3) => {
                    let [num] = children.into_array::<1usize>();
                    ParseTree::Element(self.bump.alloc(Element::Alt0 {
                        num: num.unwrap_token(),
                        span: nonterminal_node.span,
                    }))
                }
                // Element : Id.
                SlotId(5) => {
                    let [id] = children.into_array::<1usize>();
                    ParseTree::Element(self.bump.alloc(Element::Alt1 {
                        id: id.unwrap_token(),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // S_Plus_0
            NonterminalId(2) => match nonterminal_node.return_slot {
                // Element+ : Element+ WS Element.
                SlotId(9) => {
                    let [elements, ws, element_2] = children.into_array::<3usize>();
                    ParseTree::SPlus0(self.bump.alloc(SPlus0::Alt0 {
                        elements: elements.unwrap_s_plus_0(),
                        ws: ws.unwrap_token(),
                        element_2: element_2.unwrap_element(),
                        span: nonterminal_node.span,
                    }))
                }
                // Element+ : Element.
                SlotId(11) => {
                    let [element] = children.into_array::<1usize>();
                    ParseTree::SPlus0(self.bump.alloc(SPlus0::Alt1 {
                        element: element.unwrap_element(),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            _ => unreachable!(),
        }
    }
    fn new_token(&self, terminal_node: &TerminalNode) -> ParseTree<'a> {
        ParseTree::Token(Token {
            kind: token_kind(terminal_node.terminal_id),
            span: terminal_node.span,
        })
    }
}
pub fn create_parse_tree<'a>(
    root_id: SPPFNodeId,
    nonterminal_id: NonterminalId,
    parser: &FollowRestrictionLexicalParser,
    builder: &FollowRestrictionLexicalParseTreeBuilder<'a>,
) -> ParseTree<'a> {
    match nonterminal_id {
        crate::grammar_data::S => ParseTree::S(create_parse_tree_s(root_id, parser, builder)),
        crate::grammar_data::ELEMENT => {
            ParseTree::Element(create_parse_tree_element(root_id, parser, builder))
        }
        crate::grammar_data::S_PLUS_0 => {
            ParseTree::SPlus0(create_parse_tree_s_plus_0(root_id, parser, builder))
        }
        _ => panic!(),
    }
}
pub fn create_parse_tree_s<'a>(
    root_id: SPPFNodeId,
    parser: &FollowRestrictionLexicalParser,
    builder: &FollowRestrictionLexicalParseTreeBuilder<'a>,
) -> &'a S<'a> {
    let node = parser.sppf_node(root_id);
    visit_sppf(node, parser, builder).unwrap_one().unwrap_s()
}
pub fn create_parse_tree_element<'a>(
    root_id: SPPFNodeId,
    parser: &FollowRestrictionLexicalParser,
    builder: &FollowRestrictionLexicalParseTreeBuilder<'a>,
) -> &'a Element<'a> {
    let node = parser.sppf_node(root_id);
    visit_sppf(node, parser, builder)
        .unwrap_one()
        .unwrap_element()
}
pub fn create_parse_tree_s_plus_0<'a>(
    root_id: SPPFNodeId,
    parser: &FollowRestrictionLexicalParser,
    builder: &FollowRestrictionLexicalParseTreeBuilder<'a>,
) -> &'a SPlus0<'a> {
    let node = parser.sppf_node(root_id);
    visit_sppf(node, parser, builder)
        .unwrap_one()
        .unwrap_s_plus_0()
}
pub fn to_sexpr(node: ParseTree<'_>) -> String {
    let mut s = String::new();
    node_to_sexpr(node, 0, &mut s).expect("error");
    s
}
fn node_to_sexpr(node: ParseTree<'_>, indent: usize, w: &mut impl Write) -> fmt::Result {
    let children = node.children();
    if children.is_empty() {
        writeln!(w, "{:indent$}{}", "", node.display_name())
    } else {
        writeln!(w, "{:indent$}({}", "", node.display_name())?;
        for child in children {
            node_to_sexpr(child, indent + 2, w)?;
        }
        writeln!(w, "{:indent$})", "")
    }
}
#[doc = r" Converts a parse tree to JSON format for visualization."]
#[doc = r" Returns a JSON string with nodes and edges arrays."]
pub fn to_json(node: ParseTree<'_>) -> String {
    let mut nodes = Vec::new();
    let mut edges = Vec::new();
    let mut next_id = 0u32;
    build_json_graph(node, &mut nodes, &mut edges, &mut next_id);
    let result = serde_json :: json ! ({ "nodes" : nodes , "edges" : edges });
    result.to_string()
}
fn build_json_graph(
    node: ParseTree<'_>,
    nodes: &mut Vec<serde_json::Value>,
    edges: &mut Vec<serde_json::Value>,
    next_id: &mut u32,
) -> u32 {
    let my_id = *next_id;
    *next_id += 1;
    let span = node.span();
    let kind = match node {
        ParseTree::Token(_) => "Token",
        _ => "Nonterminal",
    };
    nodes . push (serde_json :: json ! ({ "id" : my_id , "kind" : kind , "label" : node . display_name () , "start" : span . left_extent , "end" : span . right_extent })) ;
    for child in node.children() {
        let child_id = build_json_graph(child, nodes, edges, next_id);
        edges.push(serde_json :: json ! ({ "src" : my_id , "dest" : child_id }));
    }
    my_id
}
