// @generated by iguana. Do not edit manually.

pub mod grammar_data;
pub mod parse_tree;
pub mod parser;
pub mod scanner;
pub mod types;
pub use iguana_runtime::result::{ParseError, ParseSuccess};
use iguana_runtime::{
    arena::Arena,
    input::Input,
    parser::{GLLResult, Parser},
};
use parse_tree::*;
use parser::LeftRecursiveListParser;
/// Parses `input` starting from `A`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_a<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a A<'a>, ()>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser = LeftRecursiveListParser::new(input, grammar_data::START_A, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = LeftRecursiveListParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_a(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
