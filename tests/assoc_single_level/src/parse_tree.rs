// @generated by iguana. Do not edit manually.
// You may distribute this file under any license of your choice. It embeds
// fragments of iguana's GPL-licensed source, and iguana grants an additional
// permission (GPL v3 section 7) that waives the GPL for generated parsers.
// Full text: https://github.com/iguana-parser/iguana-rs/blob/main/iguana-compiler/LICENSE-GPL

use crate::parser::AssocSingleLevelParser;
use iguana_runtime::{
    ids::{NonterminalId, SlotId, TerminalId},
    input::Span,
    parse_tree::{
        Bump, DisplayOptions, NodeKind, OneOrMany, Origin, ParseTreeBuilder, ParseTreeNode,
        visit_sppf,
    },
    sppf::{NonterminalNode, SPPFNodeId, TerminalNode},
};
use std::vec::IntoIter;
#[derive(Debug, Clone, Copy)]
pub enum TokenKind {
    // "<"
    T0,
    // "a"
    T1,
}
impl TokenKind {
    pub fn name(&self) -> &'static str {
        match self {
            TokenKind::T0 => "\"<\"",
            TokenKind::T1 => "\"a\"",
        }
    }
}
#[derive(Debug)]
pub struct Start<T, L> {
    pub before: L,
    pub node: T,
    pub after: L,
    pub span: Span,
}
#[derive(Debug, Clone, Copy)]
pub enum ParseTree<'a> {
    E(&'a E<'a>),
    F(&'a F<'a>),
    G(&'a G<'a>),
    // E(0)
    StartE(&'a Start<&'a E<'a>, ()>),
    // F(0)
    StartF(&'a Start<&'a F<'a>, ()>),
    // G(0)
    StartG(&'a Start<&'a G<'a>, ()>),
    Token(Token),
}
impl<'a> ParseTree<'a> {
    pub fn children(&self) -> Vec<ParseTree<'a>> {
        match self {
            ParseTree::E(e) => (0..e.child_count()).filter_map(|i| e.child(i)).collect(),
            ParseTree::F(f) => (0..f.child_count()).filter_map(|i| f.child(i)).collect(),
            ParseTree::G(g) => (0..g.child_count()).filter_map(|i| g.child(i)).collect(),
            ParseTree::StartE(start_e) => (0..start_e.child_count())
                .filter_map(|i| start_e.child(i))
                .collect(),
            ParseTree::StartF(start_f) => (0..start_f.child_count())
                .filter_map(|i| start_f.child(i))
                .collect(),
            ParseTree::StartG(start_g) => (0..start_g.child_count())
                .filter_map(|i| start_g.child(i))
                .collect(),
            ParseTree::Token(_) => vec![],
        }
    }
    pub fn display_name(&self) -> &'static str {
        match self {
            ParseTree::E(e) => e.display_name(),
            ParseTree::F(f) => f.display_name(),
            ParseTree::G(g) => g.display_name(),
            ParseTree::StartE(start_e) => start_e.display_name(),
            ParseTree::StartF(start_f) => start_f.display_name(),
            ParseTree::StartG(start_g) => start_g.display_name(),
            ParseTree::Token(token) => token.kind.name(),
        }
    }
    pub fn child_count(&self) -> usize {
        match self {
            ParseTree::E(e) => e.child_count(),
            ParseTree::F(f) => f.child_count(),
            ParseTree::G(g) => g.child_count(),
            ParseTree::StartE(start_e) => start_e.child_count(),
            ParseTree::StartF(start_f) => start_f.child_count(),
            ParseTree::StartG(start_g) => start_g.child_count(),
            ParseTree::Token(_) => 0,
        }
    }
    pub fn span(&self) -> Span {
        match self {
            ParseTree::E(e) => e.span(),
            ParseTree::F(f) => f.span(),
            ParseTree::G(g) => g.span(),
            ParseTree::StartE(start_e) => start_e.span(),
            ParseTree::StartF(start_f) => start_f.span(),
            ParseTree::StartG(start_g) => start_g.span(),
            ParseTree::Token(token) => token.span(),
        }
    }
    ///True when this node is an ambiguity cluster (any `*::Amb` variant). The
    ///uniform way to detect ambiguity without matching each nonterminal's enum.
    pub fn is_amb(&self) -> bool {
        match self {
            ParseTree::E(e) => matches!(e, E::Amb(_)),
            ParseTree::F(f) => matches!(f, F::Amb(_)),
            ParseTree::G(g) => matches!(g, G::Amb(_)),
            ParseTree::StartE(_) => false,
            ParseTree::StartF(_) => false,
            ParseTree::StartG(_) => false,
            ParseTree::Token(_) => false,
        }
    }
    ///Pointer identity of the underlying node, or `None` for tokens (by-value
    ///leaves that are never shared). Two parse trees with the same `node_id` are
    ///the same allocation, i.e. a node shared between parents in the ambiguity DAG.
    pub fn node_id(&self) -> Option<usize> {
        match self {
            ParseTree::E(e) => Some(*e as *const _ as usize),
            ParseTree::F(f) => Some(*f as *const _ as usize),
            ParseTree::G(g) => Some(*g as *const _ as usize),
            ParseTree::StartE(start_e) => Some(*start_e as *const _ as usize),
            ParseTree::StartF(start_f) => Some(*start_f as *const _ as usize),
            ParseTree::StartG(start_g) => Some(*start_g as *const _ as usize),
            ParseTree::Token(_) => None,
        }
    }
    pub fn origin(&self) -> Option<Origin> {
        match self {
            ParseTree::E(e) => e.origin(),
            ParseTree::F(f) => f.origin(),
            ParseTree::G(g) => g.origin(),
            ParseTree::StartE(start_e) => start_e.origin(),
            ParseTree::StartF(start_f) => start_f.origin(),
            ParseTree::StartG(start_g) => start_g.origin(),
            ParseTree::Token(_) => None,
        }
    }
    fn unwrap_e(self) -> &'a E<'a> {
        match self {
            ParseTree::E(e) => e,
            _ => panic!(),
        }
    }
    fn unwrap_f(self) -> &'a F<'a> {
        match self {
            ParseTree::F(f) => f,
            _ => panic!(),
        }
    }
    fn unwrap_g(self) -> &'a G<'a> {
        match self {
            ParseTree::G(g) => g,
            _ => panic!(),
        }
    }
    fn unwrap_start_e(self) -> &'a Start<&'a E<'a>, ()> {
        match self {
            ParseTree::StartE(start_e) => start_e,
            _ => panic!(),
        }
    }
    fn unwrap_start_f(self) -> &'a Start<&'a F<'a>, ()> {
        match self {
            ParseTree::StartF(start_f) => start_f,
            _ => panic!(),
        }
    }
    fn unwrap_start_g(self) -> &'a Start<&'a G<'a>, ()> {
        match self {
            ParseTree::StartG(start_g) => start_g,
            _ => panic!(),
        }
    }
    fn unwrap_token(self) -> Token {
        match self {
            ParseTree::Token(t) => t,
            _ => panic!(),
        }
    }
}
pub trait ListNode<'a> {
    fn iter(&'a self) -> IntoIter<ParseTree<'a>>;
}
pub trait OptNode {
    type Inner;
    fn value(&self) -> Option<&Self::Inner>;
}
#[derive(Debug)]
pub enum E<'a> {
    // E(p) = [1 >= p] l=E(p) [(l == 0) || (l >= 1)] "<" E(2) return 1
    Alt0 {
        e_0: &'a E<'a>,
        lit_1: Token,
        e_2: &'a E<'a>,
        span: Span,
    },
    // E(p) = "a" return 0
    Alt1 {
        lit_0: Token,
        span: Span,
    },
    Amb(&'a [&'a E<'a>]),
}
#[derive(Debug)]
pub enum F<'a> {
    // F(p) = [1 >= p] l=F(p) [(l == 0) || (l >= 2)] "<" F(1) return 1
    Alt0 {
        f_0: &'a F<'a>,
        lit_1: Token,
        f_2: &'a F<'a>,
        span: Span,
    },
    // F(p) = "a" return 0
    Alt1 {
        lit_0: Token,
        span: Span,
    },
    Amb(&'a [&'a F<'a>]),
}
#[derive(Debug)]
pub enum G<'a> {
    // G(p) = [1 >= p] l=G(p) [(l == 0) || (l >= 2)] "<" G(2) return 1
    Alt0 {
        g_0: &'a G<'a>,
        lit_1: Token,
        g_2: &'a G<'a>,
        span: Span,
    },
    // G(p) = "a" return 0
    Alt1 {
        lit_0: Token,
        span: Span,
    },
    Amb(&'a [&'a G<'a>]),
}
impl<'a> E<'a> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::E(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match self {
            E::Alt0 {
                e_0, lit_1, e_2, ..
            } => match index {
                0 => Some(ParseTree::E(e_0)),
                1 => Some(ParseTree::Token(*lit_1)),
                2 => Some(ParseTree::E(e_2)),
                _ => None,
            },
            E::Alt1 { lit_0, .. } => match index {
                0 => Some(ParseTree::Token(*lit_0)),
                _ => None,
            },
            E::Amb(alts) => alts.get(index).copied().map(ParseTree::E),
        }
    }
    pub fn child_count(&self) -> usize {
        match self {
            E::Alt0 { .. } => 3usize,
            E::Alt1 { .. } => 1usize,
            E::Amb(alts) => alts.len(),
        }
    }
    pub fn span(&self) -> Span {
        match self {
            E::Alt0 { span, .. } => *span,
            E::Alt1 { span, .. } => *span,
            E::Amb(alts) => alts[0].span(),
        }
    }
    pub fn display_name(&self) -> &'static str {
        match self {
            E::Amb(_) => "Amb",
            _ => "E",
        }
    }
    pub fn origin(&self) -> Option<Origin> {
        None
    }
}
impl<'a> F<'a> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::F(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match self {
            F::Alt0 {
                f_0, lit_1, f_2, ..
            } => match index {
                0 => Some(ParseTree::F(f_0)),
                1 => Some(ParseTree::Token(*lit_1)),
                2 => Some(ParseTree::F(f_2)),
                _ => None,
            },
            F::Alt1 { lit_0, .. } => match index {
                0 => Some(ParseTree::Token(*lit_0)),
                _ => None,
            },
            F::Amb(alts) => alts.get(index).copied().map(ParseTree::F),
        }
    }
    pub fn child_count(&self) -> usize {
        match self {
            F::Alt0 { .. } => 3usize,
            F::Alt1 { .. } => 1usize,
            F::Amb(alts) => alts.len(),
        }
    }
    pub fn span(&self) -> Span {
        match self {
            F::Alt0 { span, .. } => *span,
            F::Alt1 { span, .. } => *span,
            F::Amb(alts) => alts[0].span(),
        }
    }
    pub fn display_name(&self) -> &'static str {
        match self {
            F::Amb(_) => "Amb",
            _ => "F",
        }
    }
    pub fn origin(&self) -> Option<Origin> {
        None
    }
}
impl<'a> G<'a> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::G(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match self {
            G::Alt0 {
                g_0, lit_1, g_2, ..
            } => match index {
                0 => Some(ParseTree::G(g_0)),
                1 => Some(ParseTree::Token(*lit_1)),
                2 => Some(ParseTree::G(g_2)),
                _ => None,
            },
            G::Alt1 { lit_0, .. } => match index {
                0 => Some(ParseTree::Token(*lit_0)),
                _ => None,
            },
            G::Amb(alts) => alts.get(index).copied().map(ParseTree::G),
        }
    }
    pub fn child_count(&self) -> usize {
        match self {
            G::Alt0 { .. } => 3usize,
            G::Alt1 { .. } => 1usize,
            G::Amb(alts) => alts.len(),
        }
    }
    pub fn span(&self) -> Span {
        match self {
            G::Alt0 { span, .. } => *span,
            G::Alt1 { span, .. } => *span,
            G::Amb(alts) => alts[0].span(),
        }
    }
    pub fn display_name(&self) -> &'static str {
        match self {
            G::Amb(_) => "Amb",
            _ => "G",
        }
    }
    pub fn origin(&self) -> Option<Origin> {
        None
    }
}
impl<'a> Start<&'a E<'a>, ()> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::StartE(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match index {
            0 => Some(ParseTree::E(self.node)),
            _ => None,
        }
    }
    pub fn child_count(&self) -> usize {
        1usize
    }
    pub fn span(&self) -> Span {
        self.span
    }
    pub fn display_name(&self) -> &'static str {
        "Start"
    }
    pub fn origin(&self) -> Option<Origin> {
        Some(Origin::Start)
    }
}
impl<'a> Start<&'a F<'a>, ()> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::StartF(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match index {
            0 => Some(ParseTree::F(self.node)),
            _ => None,
        }
    }
    pub fn child_count(&self) -> usize {
        1usize
    }
    pub fn span(&self) -> Span {
        self.span
    }
    pub fn display_name(&self) -> &'static str {
        "Start"
    }
    pub fn origin(&self) -> Option<Origin> {
        Some(Origin::Start)
    }
}
impl<'a> Start<&'a G<'a>, ()> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::StartG(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match index {
            0 => Some(ParseTree::G(self.node)),
            _ => None,
        }
    }
    pub fn child_count(&self) -> usize {
        1usize
    }
    pub fn span(&self) -> Span {
        self.span
    }
    pub fn display_name(&self) -> &'static str {
        "Start"
    }
    pub fn origin(&self) -> Option<Origin> {
        Some(Origin::Start)
    }
}
#[derive(Debug, Clone, Copy)]
pub struct Token {
    pub kind: TokenKind,
    span: Span,
}
impl Token {
    pub fn as_parse_tree<'a>(&self) -> ParseTree<'a> {
        ParseTree::Token(*self)
    }
    pub fn span(&self) -> Span {
        self.span
    }
}
fn token_kind(terminal_id: TerminalId) -> TokenKind {
    match terminal_id {
        // "<"
        TerminalId(0) => TokenKind::T0,
        // "a"
        TerminalId(1) => TokenKind::T1,
        _ => unreachable!("Unknown TerminalId: {:?}", terminal_id),
    }
}
pub struct AssocSingleLevelParseTreeBuilder<'a> {
    pub arena: &'a Bump,
}
impl<'a> AssocSingleLevelParseTreeBuilder<'a> {
    pub fn new(tree_arena: &'a Bump) -> Self {
        Self { arena: tree_arena }
    }
}
impl<'a> ParseTreeBuilder<ParseTree<'a>> for AssocSingleLevelParseTreeBuilder<'a> {
    fn new_nonterminal_node(
        &self,
        nonterminal_node: &NonterminalNode,
        children: OneOrMany<ParseTree<'a>>,
    ) -> ParseTree<'a> {
        match nonterminal_node.nonterminal_id {
            // StartE
            NonterminalId(0) => match nonterminal_node.return_slot {
                // E(0) : start:E(0).
                SlotId(1) => {
                    let [start] = children.into_array::<1usize>();
                    ParseTree::StartE(self.arena.alloc(Start {
                        before: (),
                        node: start.unwrap_e(),
                        after: (),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // StartF
            NonterminalId(1) => match nonterminal_node.return_slot {
                // F(0) : start:F(0).
                SlotId(3) => {
                    let [start] = children.into_array::<1usize>();
                    ParseTree::StartF(self.arena.alloc(Start {
                        before: (),
                        node: start.unwrap_f(),
                        after: (),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // StartG
            NonterminalId(2) => match nonterminal_node.return_slot {
                // G(0) : start:G(0).
                SlotId(5) => {
                    let [start] = children.into_array::<1usize>();
                    ParseTree::StartG(self.arena.alloc(Start {
                        before: (),
                        node: start.unwrap_g(),
                        after: (),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // E
            NonterminalId(3) => match nonterminal_node.return_slot {
                // E : [1 >= p] l=E(p) [(l == 0) || (l >= 1)] "<" E(2) return 1.
                SlotId(12) => {
                    let [e_0, lit_1, e_2] = children.into_array::<3usize>();
                    ParseTree::E(self.arena.alloc(E::Alt0 {
                        e_0: e_0.unwrap_e(),
                        lit_1: lit_1.unwrap_token(),
                        e_2: e_2.unwrap_e(),
                        span: nonterminal_node.span,
                    }))
                }
                // E : "a" return 0.
                SlotId(15) => {
                    let [lit_0] = children.into_array::<1usize>();
                    ParseTree::E(self.arena.alloc(E::Alt1 {
                        lit_0: lit_0.unwrap_token(),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // F
            NonterminalId(4) => match nonterminal_node.return_slot {
                // F : [1 >= p] l=F(p) [(l == 0) || (l >= 2)] "<" F(1) return 1.
                SlotId(22) => {
                    let [f_0, lit_1, f_2] = children.into_array::<3usize>();
                    ParseTree::F(self.arena.alloc(F::Alt0 {
                        f_0: f_0.unwrap_f(),
                        lit_1: lit_1.unwrap_token(),
                        f_2: f_2.unwrap_f(),
                        span: nonterminal_node.span,
                    }))
                }
                // F : "a" return 0.
                SlotId(25) => {
                    let [lit_0] = children.into_array::<1usize>();
                    ParseTree::F(self.arena.alloc(F::Alt1 {
                        lit_0: lit_0.unwrap_token(),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // G
            NonterminalId(5) => match nonterminal_node.return_slot {
                // G : [1 >= p] l=G(p) [(l == 0) || (l >= 2)] "<" G(2) return 1.
                SlotId(32) => {
                    let [g_0, lit_1, g_2] = children.into_array::<3usize>();
                    ParseTree::G(self.arena.alloc(G::Alt0 {
                        g_0: g_0.unwrap_g(),
                        lit_1: lit_1.unwrap_token(),
                        g_2: g_2.unwrap_g(),
                        span: nonterminal_node.span,
                    }))
                }
                // G : "a" return 0.
                SlotId(35) => {
                    let [lit_0] = children.into_array::<1usize>();
                    ParseTree::G(self.arena.alloc(G::Alt1 {
                        lit_0: lit_0.unwrap_token(),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            _ => unreachable!(),
        }
    }
    fn new_token(&self, terminal_node: &TerminalNode) -> ParseTree<'a> {
        ParseTree::Token(Token {
            kind: token_kind(terminal_node.terminal_id),
            span: terminal_node.span,
        })
    }
    fn new_ambiguity_node(
        &self,
        parent: NonterminalId,
        alternatives: Vec<ParseTree<'a>>,
    ) -> ParseTree<'a> {
        match parent {
            crate::grammar_data::E => {
                let slice = self
                    .arena
                    .alloc_slice_fill_iter(alternatives.into_iter().map(|a| a.unwrap_e()));
                ParseTree::E(self.arena.alloc(E::Amb(slice)))
            }
            crate::grammar_data::F => {
                let slice = self
                    .arena
                    .alloc_slice_fill_iter(alternatives.into_iter().map(|a| a.unwrap_f()));
                ParseTree::F(self.arena.alloc(F::Amb(slice)))
            }
            crate::grammar_data::G => {
                let slice = self
                    .arena
                    .alloc_slice_fill_iter(alternatives.into_iter().map(|a| a.unwrap_g()));
                ParseTree::G(self.arena.alloc(G::Amb(slice)))
            }
            crate::grammar_data::START_E => {
                let first = alternatives[0].unwrap_start_e();
                let inner = self.arena.alloc_slice_fill_iter(
                    alternatives.into_iter().map(|a| a.unwrap_start_e().node),
                );
                let node = &*self.arena.alloc(E::Amb(inner));
                ParseTree::StartE(self.arena.alloc(Start {
                    before: first.before,
                    node,
                    after: first.after,
                    span: first.span,
                }))
            }
            crate::grammar_data::START_F => {
                let first = alternatives[0].unwrap_start_f();
                let inner = self.arena.alloc_slice_fill_iter(
                    alternatives.into_iter().map(|a| a.unwrap_start_f().node),
                );
                let node = &*self.arena.alloc(F::Amb(inner));
                ParseTree::StartF(self.arena.alloc(Start {
                    before: first.before,
                    node,
                    after: first.after,
                    span: first.span,
                }))
            }
            crate::grammar_data::START_G => {
                let first = alternatives[0].unwrap_start_g();
                let inner = self.arena.alloc_slice_fill_iter(
                    alternatives.into_iter().map(|a| a.unwrap_start_g().node),
                );
                let node = &*self.arena.alloc(G::Amb(inner));
                ParseTree::StartG(self.arena.alloc(Start {
                    before: first.before,
                    node,
                    after: first.after,
                    span: first.span,
                }))
            }
            _ => unreachable!("nonterminal cannot be ambiguous"),
        }
    }
}
pub fn create_parse_tree<'a>(
    root_id: SPPFNodeId,
    nonterminal_id: NonterminalId,
    parser: &AssocSingleLevelParser,
    builder: &AssocSingleLevelParseTreeBuilder<'a>,
) -> ParseTree<'a> {
    match nonterminal_id {
        crate::grammar_data::E => ParseTree::E(create_parse_tree_e(root_id, parser, builder)),
        crate::grammar_data::F => ParseTree::F(create_parse_tree_f(root_id, parser, builder)),
        crate::grammar_data::G => ParseTree::G(create_parse_tree_g(root_id, parser, builder)),
        crate::grammar_data::START_E => {
            ParseTree::StartE(create_parse_tree_start_e(root_id, parser, builder))
        }
        crate::grammar_data::START_F => {
            ParseTree::StartF(create_parse_tree_start_f(root_id, parser, builder))
        }
        crate::grammar_data::START_G => {
            ParseTree::StartG(create_parse_tree_start_g(root_id, parser, builder))
        }
        _ => panic!(),
    }
}
pub fn create_parse_tree_e<'a>(
    root_id: SPPFNodeId,
    parser: &AssocSingleLevelParser,
    builder: &AssocSingleLevelParseTreeBuilder<'a>,
) -> &'a E<'a> {
    visit_sppf(root_id, parser, builder).unwrap_one().unwrap_e()
}
pub fn create_parse_tree_f<'a>(
    root_id: SPPFNodeId,
    parser: &AssocSingleLevelParser,
    builder: &AssocSingleLevelParseTreeBuilder<'a>,
) -> &'a F<'a> {
    visit_sppf(root_id, parser, builder).unwrap_one().unwrap_f()
}
pub fn create_parse_tree_g<'a>(
    root_id: SPPFNodeId,
    parser: &AssocSingleLevelParser,
    builder: &AssocSingleLevelParseTreeBuilder<'a>,
) -> &'a G<'a> {
    visit_sppf(root_id, parser, builder).unwrap_one().unwrap_g()
}
pub fn create_parse_tree_start_e<'a>(
    root_id: SPPFNodeId,
    parser: &AssocSingleLevelParser,
    builder: &AssocSingleLevelParseTreeBuilder<'a>,
) -> &'a Start<&'a E<'a>, ()> {
    visit_sppf(root_id, parser, builder)
        .unwrap_one()
        .unwrap_start_e()
}
pub fn create_parse_tree_start_f<'a>(
    root_id: SPPFNodeId,
    parser: &AssocSingleLevelParser,
    builder: &AssocSingleLevelParseTreeBuilder<'a>,
) -> &'a Start<&'a F<'a>, ()> {
    visit_sppf(root_id, parser, builder)
        .unwrap_one()
        .unwrap_start_f()
}
pub fn create_parse_tree_start_g<'a>(
    root_id: SPPFNodeId,
    parser: &AssocSingleLevelParser,
    builder: &AssocSingleLevelParseTreeBuilder<'a>,
) -> &'a Start<&'a G<'a>, ()> {
    visit_sppf(root_id, parser, builder)
        .unwrap_one()
        .unwrap_start_g()
}
impl<'a> ParseTreeNode for ParseTree<'a> {
    fn children(&self) -> Vec<Self> {
        ParseTree::children(self)
    }
    fn display_name(&self) -> &'static str {
        ParseTree::display_name(self)
    }
    fn span(&self) -> Span {
        ParseTree::span(self)
    }
    fn kind(&self) -> NodeKind {
        if self.is_amb() {
            NodeKind::Amb
        } else if matches!(self, ParseTree::Token(_)) {
            NodeKind::Token
        } else {
            NodeKind::Nonterminal
        }
    }
    fn node_id(&self) -> Option<usize> {
        ParseTree::node_id(self)
    }
    fn origin(&self) -> Option<Origin> {
        ParseTree::origin(self)
    }
}
const LAYOUT_NAME: Option<&str> = None;
pub fn to_sexpr(node: ParseTree<'_>) -> String {
    iguana_runtime::parse_tree::to_sexpr(node, LAYOUT_NAME)
}
pub fn to_sexpr_with(node: ParseTree<'_>, options: DisplayOptions) -> String {
    iguana_runtime::parse_tree::to_sexpr_with(node, LAYOUT_NAME, options)
}
/// Converts a parse tree to a JSON string of nodes and edges, for visualization.
pub fn to_json(node: ParseTree<'_>) -> String {
    iguana_runtime::parse_tree::to_json(node, LAYOUT_NAME)
}
