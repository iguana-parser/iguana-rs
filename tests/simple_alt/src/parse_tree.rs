// @generated by iguana. Do not edit manually.

use crate::parser::SimpleAltParser;
use core::fmt;
use iguana_runtime::{
    ids::{NonterminalId, SlotId, TerminalId},
    parse_tree::{Bump, OneOrMany, ParseContext, ParseTreeBuilder, visit_sppf},
    parser::Parser,
    sppf::{NonterminalNode, SPPFNodeId, Span, TerminalNode},
};
use std::{fmt::Write, vec::IntoIter};
#[derive(Debug, Clone, Copy)]
pub enum TokenKind {
    // "b"
    T0,
    // "c"
    T1,
    // "d"
    T2,
}
impl TokenKind {
    pub fn name(&self) -> &'static str {
        match self {
            TokenKind::T0 => "\"b\"",
            TokenKind::T1 => "\"c\"",
            TokenKind::T2 => "\"d\"",
            _ => unreachable!(),
        }
    }
}
#[derive(Debug, Clone, Copy)]
pub enum ParseTree<'a> {
    A(&'a A<'a>),
    B(&'a B<'a>),
    C(&'a C<'a>),
    D(&'a D<'a>),
    // (C | D)
    Alt0(&'a Alt0<'a>),
    Token(Token),
}
impl<'a> ParseTree<'a> {
    pub fn children(&self) -> Vec<ParseTree<'a>> {
        match self {
            ParseTree::A(a) => (0..a.child_count()).filter_map(|i| a.child(i)).collect(),
            ParseTree::B(b) => (0..b.child_count()).filter_map(|i| b.child(i)).collect(),
            ParseTree::C(c) => (0..c.child_count()).filter_map(|i| c.child(i)).collect(),
            ParseTree::D(d) => (0..d.child_count()).filter_map(|i| d.child(i)).collect(),
            ParseTree::Alt0(alt_0) => (0..alt_0.child_count())
                .filter_map(|i| alt_0.child(i))
                .collect(),
            ParseTree::Token(_) => vec![],
        }
    }
    pub fn display_name(&self) -> &'static str {
        match self {
            ParseTree::A(a) => a.display_name(),
            ParseTree::B(b) => b.display_name(),
            ParseTree::C(c) => c.display_name(),
            ParseTree::D(d) => d.display_name(),
            ParseTree::Alt0(alt_0) => alt_0.display_name(),
            ParseTree::Token(token) => token.kind.name(),
        }
    }
    pub fn child_count(&self) -> usize {
        match self {
            ParseTree::A(a) => a.child_count(),
            ParseTree::B(b) => b.child_count(),
            ParseTree::C(c) => c.child_count(),
            ParseTree::D(d) => d.child_count(),
            ParseTree::Alt0(alt_0) => alt_0.child_count(),
            ParseTree::Token(_) => 0,
        }
    }
    pub fn span(&self) -> Span {
        match self {
            ParseTree::A(a) => a.span(),
            ParseTree::B(b) => b.span(),
            ParseTree::C(c) => c.span(),
            ParseTree::D(d) => d.span(),
            ParseTree::Alt0(alt_0) => alt_0.span(),
            ParseTree::Token(token) => token.span(),
        }
    }
    fn unwrap_a(self) -> &'a A<'a> {
        match self {
            ParseTree::A(a) => a,
            _ => panic!(),
        }
    }
    fn unwrap_b(self) -> &'a B<'a> {
        match self {
            ParseTree::B(b) => b,
            _ => panic!(),
        }
    }
    fn unwrap_c(self) -> &'a C<'a> {
        match self {
            ParseTree::C(c) => c,
            _ => panic!(),
        }
    }
    fn unwrap_d(self) -> &'a D<'a> {
        match self {
            ParseTree::D(d) => d,
            _ => panic!(),
        }
    }
    fn unwrap_alt_0(self) -> &'a Alt0<'a> {
        match self {
            ParseTree::Alt0(alt_0) => alt_0,
            _ => panic!(),
        }
    }
    fn unwrap_token(self) -> Token {
        match self {
            ParseTree::Token(t) => t,
            _ => panic!(),
        }
    }
}
pub trait ListNode<'a> {
    fn iter(&'a self) -> IntoIter<ParseTree<'a>>;
}
pub trait OptNode {
    type Inner;
    fn value(&self) -> Option<&Self::Inner>;
}
// A = B (C | D)
#[derive(Debug)]
pub enum A<'a> {
    Alt0 {
        b: &'a B<'a>,
        alt_0: &'a Alt0<'a>,
        span: Span,
    },
    Amb(&'a [&'a A<'a>]),
}
// B = "b"
#[derive(Debug)]
pub enum B<'a> {
    Alt0 { lit_0: Token, span: Span },
    Amb(&'a [&'a B<'a>]),
}
// C = "c"
#[derive(Debug)]
pub enum C<'a> {
    Alt0 { lit_0: Token, span: Span },
    Amb(&'a [&'a C<'a>]),
}
// D = "d"
#[derive(Debug)]
pub enum D<'a> {
    Alt0 { lit_0: Token, span: Span },
    Amb(&'a [&'a D<'a>]),
}
// (C | D)
#[derive(Debug)]
pub enum Alt0<'a> {
    // Alt_0 = C
    Alt0 { c: &'a C<'a>, span: Span },
    // Alt_0 = D
    Alt1 { d: &'a D<'a>, span: Span },
    Amb(&'a [&'a Alt0<'a>]),
}
impl<'a> A<'a> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::A(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match self {
            A::Alt0 { b, alt_0, .. } => match index {
                0 => Some(ParseTree::B(b)),
                1 => Some(ParseTree::Alt0(alt_0)),
                _ => None,
            },
            A::Amb(alts) => alts.get(index).copied().map(ParseTree::A),
        }
    }
    pub fn child_count(&self) -> usize {
        match self {
            A::Alt0 { .. } => 2usize,
            A::Amb(alts) => alts.len(),
        }
    }
    pub fn span(&self) -> Span {
        match self {
            A::Alt0 { span, .. } => *span,
            A::Amb(alts) => alts[0].span(),
        }
    }
    pub fn display_name(&self) -> &'static str {
        match self {
            A::Amb(_) => "Amb",
            _ => "A",
        }
    }
    pub fn b(&self) -> &'a B<'a> {
        match self {
            A::Alt0 { b, .. } => b,
            A::Amb(_) => panic!("A is ambiguous"),
        }
    }
    pub fn alt_0(&self) -> &'a Alt0<'a> {
        match self {
            A::Alt0 { alt_0, .. } => alt_0,
            A::Amb(_) => panic!("A is ambiguous"),
        }
    }
}
impl<'a> B<'a> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::B(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match self {
            B::Alt0 { lit_0, .. } => match index {
                0 => Some(ParseTree::Token(*lit_0)),
                _ => None,
            },
            B::Amb(alts) => alts.get(index).copied().map(ParseTree::B),
        }
    }
    pub fn child_count(&self) -> usize {
        match self {
            B::Alt0 { .. } => 1usize,
            B::Amb(alts) => alts.len(),
        }
    }
    pub fn span(&self) -> Span {
        match self {
            B::Alt0 { span, .. } => *span,
            B::Amb(alts) => alts[0].span(),
        }
    }
    pub fn display_name(&self) -> &'static str {
        match self {
            B::Amb(_) => "Amb",
            _ => "B",
        }
    }
    pub fn lit_0(&self) -> Token {
        match self {
            B::Alt0 { lit_0, .. } => *lit_0,
            B::Amb(_) => panic!("B is ambiguous"),
        }
    }
}
impl<'a> C<'a> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::C(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match self {
            C::Alt0 { lit_0, .. } => match index {
                0 => Some(ParseTree::Token(*lit_0)),
                _ => None,
            },
            C::Amb(alts) => alts.get(index).copied().map(ParseTree::C),
        }
    }
    pub fn child_count(&self) -> usize {
        match self {
            C::Alt0 { .. } => 1usize,
            C::Amb(alts) => alts.len(),
        }
    }
    pub fn span(&self) -> Span {
        match self {
            C::Alt0 { span, .. } => *span,
            C::Amb(alts) => alts[0].span(),
        }
    }
    pub fn display_name(&self) -> &'static str {
        match self {
            C::Amb(_) => "Amb",
            _ => "C",
        }
    }
    pub fn lit_0(&self) -> Token {
        match self {
            C::Alt0 { lit_0, .. } => *lit_0,
            C::Amb(_) => panic!("C is ambiguous"),
        }
    }
}
impl<'a> D<'a> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::D(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match self {
            D::Alt0 { lit_0, .. } => match index {
                0 => Some(ParseTree::Token(*lit_0)),
                _ => None,
            },
            D::Amb(alts) => alts.get(index).copied().map(ParseTree::D),
        }
    }
    pub fn child_count(&self) -> usize {
        match self {
            D::Alt0 { .. } => 1usize,
            D::Amb(alts) => alts.len(),
        }
    }
    pub fn span(&self) -> Span {
        match self {
            D::Alt0 { span, .. } => *span,
            D::Amb(alts) => alts[0].span(),
        }
    }
    pub fn display_name(&self) -> &'static str {
        match self {
            D::Amb(_) => "Amb",
            _ => "D",
        }
    }
    pub fn lit_0(&self) -> Token {
        match self {
            D::Alt0 { lit_0, .. } => *lit_0,
            D::Amb(_) => panic!("D is ambiguous"),
        }
    }
}
impl<'a> Alt0<'a> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::Alt0(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match self {
            Alt0::Alt0 { c, .. } => match index {
                0 => Some(ParseTree::C(c)),
                _ => None,
            },
            Alt0::Alt1 { d, .. } => match index {
                0 => Some(ParseTree::D(d)),
                _ => None,
            },
            Alt0::Amb(alts) => alts.get(index).copied().map(ParseTree::Alt0),
        }
    }
    pub fn child_count(&self) -> usize {
        match self {
            Alt0::Alt0 { .. } => 1usize,
            Alt0::Alt1 { .. } => 1usize,
            Alt0::Amb(alts) => alts.len(),
        }
    }
    pub fn span(&self) -> Span {
        match self {
            Alt0::Alt0 { span, .. } => *span,
            Alt0::Alt1 { span, .. } => *span,
            Alt0::Amb(alts) => alts[0].span(),
        }
    }
    pub fn display_name(&self) -> &'static str {
        match self {
            Alt0::Amb(_) => "Amb",
            _ => "(C | D)",
        }
    }
}
impl<'a> Alt0<'a> {
    pub fn as_c(&self) -> Option<&C> {
        match self {
            Alt0::Alt0 { c, .. } => Some(c),
            _ => None,
        }
    }
    pub fn as_d(&self) -> Option<&D> {
        match self {
            Alt0::Alt1 { d, .. } => Some(d),
            _ => None,
        }
    }
}
#[derive(Debug, Clone, Copy)]
pub struct Token {
    pub kind: TokenKind,
    span: Span,
}
impl Token {
    pub fn as_parse_tree<'a>(&self) -> ParseTree<'a> {
        ParseTree::Token(*self)
    }
    pub fn span(&self) -> Span {
        self.span
    }
}
fn token_kind(terminal_id: TerminalId) -> TokenKind {
    match terminal_id {
        // "b"
        TerminalId(0) => TokenKind::T0,
        // "c"
        TerminalId(1) => TokenKind::T1,
        // "d"
        TerminalId(2) => TokenKind::T2,
        _ => unreachable!("Unknown TerminalId: {:?}", terminal_id),
    }
}
pub struct SimpleAltParseTreeBuilder<'a> {
    pub bump: &'a Bump,
}
impl<'a> SimpleAltParseTreeBuilder<'a> {
    pub fn new(ctx: &'a ParseContext) -> Self {
        Self { bump: ctx.bump() }
    }
}
impl<'a> ParseTreeBuilder<ParseTree<'a>> for SimpleAltParseTreeBuilder<'a> {
    fn new_nonterminal_node(
        &self,
        nonterminal_node: &NonterminalNode,
        children: OneOrMany<ParseTree<'a>>,
    ) -> ParseTree<'a> {
        match nonterminal_node.nonterminal_id {
            // A
            NonterminalId(0) => match nonterminal_node.return_slot {
                // A : B (C | D).
                SlotId(2) => {
                    let [b, alt_0] = children.into_array::<2usize>();
                    ParseTree::A(self.bump.alloc(A::Alt0 {
                        b: b.unwrap_b(),
                        alt_0: alt_0.unwrap_alt_0(),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // B
            NonterminalId(1) => match nonterminal_node.return_slot {
                // B : "b".
                SlotId(4) => {
                    let [lit_0] = children.into_array::<1usize>();
                    ParseTree::B(self.bump.alloc(B::Alt0 {
                        lit_0: lit_0.unwrap_token(),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // C
            NonterminalId(2) => match nonterminal_node.return_slot {
                // C : "c".
                SlotId(6) => {
                    let [lit_0] = children.into_array::<1usize>();
                    ParseTree::C(self.bump.alloc(C::Alt0 {
                        lit_0: lit_0.unwrap_token(),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // D
            NonterminalId(3) => match nonterminal_node.return_slot {
                // D : "d".
                SlotId(8) => {
                    let [lit_0] = children.into_array::<1usize>();
                    ParseTree::D(self.bump.alloc(D::Alt0 {
                        lit_0: lit_0.unwrap_token(),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // Alt_0
            NonterminalId(4) => match nonterminal_node.return_slot {
                // (C | D) : C.
                SlotId(10) => {
                    let [c] = children.into_array::<1usize>();
                    ParseTree::Alt0(self.bump.alloc(Alt0::Alt0 {
                        c: c.unwrap_c(),
                        span: nonterminal_node.span,
                    }))
                }
                // (C | D) : D.
                SlotId(12) => {
                    let [d] = children.into_array::<1usize>();
                    ParseTree::Alt0(self.bump.alloc(Alt0::Alt1 {
                        d: d.unwrap_d(),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            _ => unreachable!(),
        }
    }
    fn new_token(&self, terminal_node: &TerminalNode) -> ParseTree<'a> {
        ParseTree::Token(Token {
            kind: token_kind(terminal_node.terminal_id),
            span: terminal_node.span,
        })
    }
    fn new_ambiguity_node(
        &self,
        parent: NonterminalId,
        alternatives: Vec<ParseTree<'a>>,
    ) -> ParseTree<'a> {
        match parent {
            crate::grammar_data::A => {
                let slice = self
                    .bump
                    .alloc_slice_fill_iter(alternatives.into_iter().map(|a| a.unwrap_a()));
                ParseTree::A(self.bump.alloc(A::Amb(slice)))
            }
            crate::grammar_data::B => {
                let slice = self
                    .bump
                    .alloc_slice_fill_iter(alternatives.into_iter().map(|a| a.unwrap_b()));
                ParseTree::B(self.bump.alloc(B::Amb(slice)))
            }
            crate::grammar_data::C => {
                let slice = self
                    .bump
                    .alloc_slice_fill_iter(alternatives.into_iter().map(|a| a.unwrap_c()));
                ParseTree::C(self.bump.alloc(C::Amb(slice)))
            }
            crate::grammar_data::D => {
                let slice = self
                    .bump
                    .alloc_slice_fill_iter(alternatives.into_iter().map(|a| a.unwrap_d()));
                ParseTree::D(self.bump.alloc(D::Amb(slice)))
            }
            crate::grammar_data::ALT_0 => {
                let slice = self
                    .bump
                    .alloc_slice_fill_iter(alternatives.into_iter().map(|a| a.unwrap_alt_0()));
                ParseTree::Alt0(self.bump.alloc(Alt0::Amb(slice)))
            }
            _ => unreachable!("nonterminal cannot be ambiguous"),
        }
    }
}
pub fn create_parse_tree<'a>(
    root_id: SPPFNodeId,
    nonterminal_id: NonterminalId,
    parser: &SimpleAltParser,
    builder: &SimpleAltParseTreeBuilder<'a>,
) -> ParseTree<'a> {
    match nonterminal_id {
        crate::grammar_data::A => ParseTree::A(create_parse_tree_a(root_id, parser, builder)),
        crate::grammar_data::B => ParseTree::B(create_parse_tree_b(root_id, parser, builder)),
        crate::grammar_data::C => ParseTree::C(create_parse_tree_c(root_id, parser, builder)),
        crate::grammar_data::D => ParseTree::D(create_parse_tree_d(root_id, parser, builder)),
        crate::grammar_data::ALT_0 => {
            ParseTree::Alt0(create_parse_tree_alt_0(root_id, parser, builder))
        }
        _ => panic!(),
    }
}
pub fn create_parse_tree_a<'a>(
    root_id: SPPFNodeId,
    parser: &SimpleAltParser,
    builder: &SimpleAltParseTreeBuilder<'a>,
) -> &'a A<'a> {
    visit_sppf(root_id, parser, builder).unwrap_one().unwrap_a()
}
pub fn create_parse_tree_b<'a>(
    root_id: SPPFNodeId,
    parser: &SimpleAltParser,
    builder: &SimpleAltParseTreeBuilder<'a>,
) -> &'a B<'a> {
    visit_sppf(root_id, parser, builder).unwrap_one().unwrap_b()
}
pub fn create_parse_tree_c<'a>(
    root_id: SPPFNodeId,
    parser: &SimpleAltParser,
    builder: &SimpleAltParseTreeBuilder<'a>,
) -> &'a C<'a> {
    visit_sppf(root_id, parser, builder).unwrap_one().unwrap_c()
}
pub fn create_parse_tree_d<'a>(
    root_id: SPPFNodeId,
    parser: &SimpleAltParser,
    builder: &SimpleAltParseTreeBuilder<'a>,
) -> &'a D<'a> {
    visit_sppf(root_id, parser, builder).unwrap_one().unwrap_d()
}
pub fn create_parse_tree_alt_0<'a>(
    root_id: SPPFNodeId,
    parser: &SimpleAltParser,
    builder: &SimpleAltParseTreeBuilder<'a>,
) -> &'a Alt0<'a> {
    visit_sppf(root_id, parser, builder)
        .unwrap_one()
        .unwrap_alt_0()
}
pub fn to_sexpr(node: ParseTree<'_>) -> String {
    let mut s = String::new();
    node_to_sexpr(node, 0, &mut s).expect("error");
    s
}
fn node_to_sexpr(node: ParseTree<'_>, indent: usize, w: &mut impl Write) -> fmt::Result {
    let children = node.children();
    if children.is_empty() {
        writeln!(w, "{:indent$}{}", "", node.display_name())
    } else {
        writeln!(w, "{:indent$}({}", "", node.display_name())?;
        for child in children {
            node_to_sexpr(child, indent + 2, w)?;
        }
        writeln!(w, "{:indent$})", "")
    }
}
/// Converts a parse tree to JSON format for visualization.

/// Returns a JSON string with nodes and edges arrays.
pub fn to_json(node: ParseTree<'_>) -> String {
    let mut nodes = Vec::new();
    let mut edges = Vec::new();
    let mut next_id = 0u32;
    build_json_graph(node, &mut nodes, &mut edges, &mut next_id);
    let result = serde_json :: json ! ({ "layout_name" : None :: < & str > , "nodes" : nodes , "edges" : edges });
    result.to_string()
}
fn build_json_graph(
    node: ParseTree<'_>,
    nodes: &mut Vec<serde_json::Value>,
    edges: &mut Vec<serde_json::Value>,
    next_id: &mut u32,
) -> u32 {
    let my_id = *next_id;
    *next_id += 1;
    let span = node.span();
    let kind = match node {
        ParseTree::Token(_) => "Token",
        ParseTree::Alt0(e) if matches!(e, Alt0::Amb(_)) => "Amb",
        _ => "Nonterminal",
    };
    nodes . push (serde_json :: json ! ({ "id" : my_id , "kind" : kind , "label" : node . display_name () , "start" : span . left_extent , "end" : span . right_extent })) ;
    for child in node.children() {
        let child_id = build_json_graph(child, nodes, edges, next_id);
        edges.push(serde_json :: json ! ({ "src" : my_id , "dest" : child_id }));
    }
    my_id
}
