// @generated by iguana. Do not edit manually.

use crate::{grammar_data::*, scanner::ErrorUnionScanner};
#[allow(unused_imports)]
use iguana_runtime::input::Span;
#[cfg(feature = "debug-trace")]
use iguana_runtime::trace::TraceEvent;
use iguana_runtime::{
    arena::{Arena, ArenaVec},
    descriptor::Descriptor,
    env::{Env, EnvId},
    gss::GSSNode,
    ids::{BindingId, GssNodeId, NonterminalId, SlotId, TerminalId},
    input::Input,
    parser::{
        DESCRIPTORS_CAPACITY_DIVISOR, DESCRIPTORS_CAPACITY_FLOOR, GLLFailure, GLLFailureKind,
        GSS_CAPACITY_MULTIPLIER, Parser, SPPF_CAPACITY_MULTIPLIER, init_logger,
    },
    record,
    scanner::Scanner,
    sppf::{IntermediateNode, NonterminalNode, SPPFNode, SPPFNodeId, TerminalNode},
    utils::{inline_map::InlineMap, inline_vec::InlineVec},
};
use rustc_hash::FxHashMap;
use std::cell::OnceCell;
impl<'i, 'arena> Parser<'i, 'arena> for ErrorUnionParser<'i, 'arena> {
    fn nonterminal_display_name(nonterminal_id: NonterminalId) -> &'static str {
        NONTERMINALS[nonterminal_id.index()].display
    }
    fn terminal_name(terminal_id: TerminalId) -> &'static str {
        TERMINALS[terminal_id.index()].name
    }
    fn slot_name(slot_id: SlotId) -> &'static str {
        SLOTS[slot_id.index()].display_name
    }
    fn epsilon() -> TerminalId {
        TerminalId((TERMINALS.len() - 2) as u16)
    }
    fn terminal_count() -> u16 {
        (TERMINALS.len() - 2) as u16
    }
    // env is threaded only through recursive execute calls in grammars without data-dependent
    // constructs, so clippy sees it as recursion-only there.
    #[allow(clippy::only_used_in_recursion)]
    fn execute(
        &mut self,
        input_index: u32,
        slot_id: SlotId,
        result: Option<SPPFNodeId>,
        gss_node_id: GssNodeId,
        env: Option<EnvId>,
    ) {
        record!(
            self,
            ProcessingDescriptor,
            input_index,
            slot_id,
            result,
            gss_node_id
        );
        match slot_id {
            // T : . "[" BeforeP Layout P Layout "]"
            SlotId(0) => {
                if let Some((j, right_child)) =
                    self.match_terminal(TerminalId(5), input_index, SlotId(0), Some(gss_node_id))
                {
                    // T : "[" . BeforeP Layout P Layout "]"
                    self.execute(j, SlotId(1), Some(right_child), gss_node_id, env);
                }
            }
            // T : "[" . BeforeP Layout P Layout "]"
            SlotId(1) => {
                self.create(NonterminalId(4), result, gss_node_id, SlotId(2), env);
            }
            // T : "[" BeforeP . Layout P Layout "]"
            SlotId(2) => {
                if let Some(right_child) = self.parse_layout_ll1(input_index) {
                    if let Some((j, new_node)) =
                        self.create_intermediate_node(result, right_child, SlotId(3), env)
                    {
                        // T : "[" BeforeP Layout . P Layout "]"
                        self.execute(j, SlotId(3), Some(new_node), gss_node_id, env);
                    }
                }
            }
            // T : "[" BeforeP Layout . P Layout "]"
            SlotId(3) => {
                self.create(NonterminalId(2), result, gss_node_id, SlotId(4), env);
            }
            // T : "[" BeforeP Layout P . Layout "]"
            SlotId(4) => {
                if let Some(right_child) = self.parse_layout_ll1(input_index) {
                    if let Some((j, new_node)) =
                        self.create_intermediate_node(result, right_child, SlotId(5), env)
                    {
                        // T : "[" BeforeP Layout P Layout . "]"
                        self.execute(j, SlotId(5), Some(new_node), gss_node_id, env);
                    }
                }
            }
            // T : "[" BeforeP Layout P Layout . "]"
            SlotId(5) => {
                if let Some((_, right_child)) =
                    self.match_terminal(TerminalId(6), input_index, SlotId(5), Some(gss_node_id))
                {
                    if let Some((j, new_node)) =
                        self.create_intermediate_node(result, right_child, SlotId(6), env)
                    {
                        // T : "[" BeforeP Layout P Layout "]".
                        self.execute(j, SlotId(6), Some(new_node), gss_node_id, env);
                    }
                }
            }
            // T : "[" BeforeP Layout P Layout "]".
            SlotId(6) => {
                let nonterminal_node_id =
                    self.create_nonterminal_node(result, NonterminalId(0), SlotId(6), gss_node_id);
                self.pop(gss_node_id, SlotId(6), nonterminal_node_id, None);
            }
            // T : . "[" BeforeQ Layout Q Layout "]"
            SlotId(7) => {
                if let Some((j, right_child)) =
                    self.match_terminal(TerminalId(5), input_index, SlotId(7), Some(gss_node_id))
                {
                    // T : "[" . BeforeQ Layout Q Layout "]"
                    self.execute(j, SlotId(8), Some(right_child), gss_node_id, env);
                }
            }
            // T : "[" . BeforeQ Layout Q Layout "]"
            SlotId(8) => {
                self.create(NonterminalId(5), result, gss_node_id, SlotId(9), env);
            }
            // T : "[" BeforeQ . Layout Q Layout "]"
            SlotId(9) => {
                if let Some(right_child) = self.parse_layout_ll1(input_index) {
                    if let Some((j, new_node)) =
                        self.create_intermediate_node(result, right_child, SlotId(10), env)
                    {
                        // T : "[" BeforeQ Layout . Q Layout "]"
                        self.execute(j, SlotId(10), Some(new_node), gss_node_id, env);
                    }
                }
            }
            // T : "[" BeforeQ Layout . Q Layout "]"
            SlotId(10) => {
                if let Some(right_child) = self.parse_q_ll1(input_index) {
                    if let Some((j, new_node)) =
                        self.create_intermediate_node(result, right_child, SlotId(11), env)
                    {
                        // T : "[" BeforeQ Layout Q . Layout "]"
                        self.execute(j, SlotId(11), Some(new_node), gss_node_id, env);
                    }
                } else {
                    self.add_failure(input_index, SlotId(10), Some(gss_node_id), || {
                        GLLFailureKind::UnexpectedToken {
                            expected: FIRST_SET_Q.terminals.to_vec(),
                        }
                    });
                }
            }
            // T : "[" BeforeQ Layout Q . Layout "]"
            SlotId(11) => {
                if let Some(right_child) = self.parse_layout_ll1(input_index) {
                    if let Some((j, new_node)) =
                        self.create_intermediate_node(result, right_child, SlotId(12), env)
                    {
                        // T : "[" BeforeQ Layout Q Layout . "]"
                        self.execute(j, SlotId(12), Some(new_node), gss_node_id, env);
                    }
                }
            }
            // T : "[" BeforeQ Layout Q Layout . "]"
            SlotId(12) => {
                if let Some((_, right_child)) =
                    self.match_terminal(TerminalId(6), input_index, SlotId(12), Some(gss_node_id))
                {
                    if let Some((j, new_node)) =
                        self.create_intermediate_node(result, right_child, SlotId(13), env)
                    {
                        // T : "[" BeforeQ Layout Q Layout "]".
                        self.execute(j, SlotId(13), Some(new_node), gss_node_id, env);
                    }
                }
            }
            // T : "[" BeforeQ Layout Q Layout "]".
            SlotId(13) => {
                let nonterminal_node_id =
                    self.create_nonterminal_node(result, NonterminalId(0), SlotId(13), gss_node_id);
                self.pop(gss_node_id, SlotId(13), nonterminal_node_id, None);
            }
            // U : . "[" BeforeQ Layout Q Layout "]"
            SlotId(14) => {
                if let Some((j, right_child)) =
                    self.match_terminal(TerminalId(5), input_index, SlotId(14), Some(gss_node_id))
                {
                    // U : "[" . BeforeQ Layout Q Layout "]"
                    self.execute(j, SlotId(15), Some(right_child), gss_node_id, env);
                }
            }
            // U : "[" . BeforeQ Layout Q Layout "]"
            SlotId(15) => {
                self.create(NonterminalId(5), result, gss_node_id, SlotId(16), env);
            }
            // U : "[" BeforeQ . Layout Q Layout "]"
            SlotId(16) => {
                if let Some(right_child) = self.parse_layout_ll1(input_index) {
                    if let Some((j, new_node)) =
                        self.create_intermediate_node(result, right_child, SlotId(17), env)
                    {
                        // U : "[" BeforeQ Layout . Q Layout "]"
                        self.execute(j, SlotId(17), Some(new_node), gss_node_id, env);
                    }
                }
            }
            // U : "[" BeforeQ Layout . Q Layout "]"
            SlotId(17) => {
                if let Some(right_child) = self.parse_q_ll1(input_index) {
                    if let Some((j, new_node)) =
                        self.create_intermediate_node(result, right_child, SlotId(18), env)
                    {
                        // U : "[" BeforeQ Layout Q . Layout "]"
                        self.execute(j, SlotId(18), Some(new_node), gss_node_id, env);
                    }
                } else {
                    self.add_failure(input_index, SlotId(17), Some(gss_node_id), || {
                        GLLFailureKind::UnexpectedToken {
                            expected: FIRST_SET_Q.terminals.to_vec(),
                        }
                    });
                }
            }
            // U : "[" BeforeQ Layout Q . Layout "]"
            SlotId(18) => {
                if let Some(right_child) = self.parse_layout_ll1(input_index) {
                    if let Some((j, new_node)) =
                        self.create_intermediate_node(result, right_child, SlotId(19), env)
                    {
                        // U : "[" BeforeQ Layout Q Layout . "]"
                        self.execute(j, SlotId(19), Some(new_node), gss_node_id, env);
                    }
                }
            }
            // U : "[" BeforeQ Layout Q Layout . "]"
            SlotId(19) => {
                if let Some((_, right_child)) =
                    self.match_terminal(TerminalId(6), input_index, SlotId(19), Some(gss_node_id))
                {
                    if let Some((j, new_node)) =
                        self.create_intermediate_node(result, right_child, SlotId(20), env)
                    {
                        // U : "[" BeforeQ Layout Q Layout "]".
                        self.execute(j, SlotId(20), Some(new_node), gss_node_id, env);
                    }
                }
            }
            // U : "[" BeforeQ Layout Q Layout "]".
            SlotId(20) => {
                let nonterminal_node_id =
                    self.create_nonterminal_node(result, NonterminalId(1), SlotId(20), gss_node_id);
                self.pop(gss_node_id, SlotId(20), nonterminal_node_id, None);
            }
            // U : . "[" BeforeP Layout P Layout "]"
            SlotId(21) => {
                if let Some((j, right_child)) =
                    self.match_terminal(TerminalId(5), input_index, SlotId(21), Some(gss_node_id))
                {
                    // U : "[" . BeforeP Layout P Layout "]"
                    self.execute(j, SlotId(22), Some(right_child), gss_node_id, env);
                }
            }
            // U : "[" . BeforeP Layout P Layout "]"
            SlotId(22) => {
                self.create(NonterminalId(4), result, gss_node_id, SlotId(23), env);
            }
            // U : "[" BeforeP . Layout P Layout "]"
            SlotId(23) => {
                if let Some(right_child) = self.parse_layout_ll1(input_index) {
                    if let Some((j, new_node)) =
                        self.create_intermediate_node(result, right_child, SlotId(24), env)
                    {
                        // U : "[" BeforeP Layout . P Layout "]"
                        self.execute(j, SlotId(24), Some(new_node), gss_node_id, env);
                    }
                }
            }
            // U : "[" BeforeP Layout . P Layout "]"
            SlotId(24) => {
                self.create(NonterminalId(2), result, gss_node_id, SlotId(25), env);
            }
            // U : "[" BeforeP Layout P . Layout "]"
            SlotId(25) => {
                if let Some(right_child) = self.parse_layout_ll1(input_index) {
                    if let Some((j, new_node)) =
                        self.create_intermediate_node(result, right_child, SlotId(26), env)
                    {
                        // U : "[" BeforeP Layout P Layout . "]"
                        self.execute(j, SlotId(26), Some(new_node), gss_node_id, env);
                    }
                }
            }
            // U : "[" BeforeP Layout P Layout . "]"
            SlotId(26) => {
                if let Some((_, right_child)) =
                    self.match_terminal(TerminalId(6), input_index, SlotId(26), Some(gss_node_id))
                {
                    if let Some((j, new_node)) =
                        self.create_intermediate_node(result, right_child, SlotId(27), env)
                    {
                        // U : "[" BeforeP Layout P Layout "]".
                        self.execute(j, SlotId(27), Some(new_node), gss_node_id, env);
                    }
                }
            }
            // U : "[" BeforeP Layout P Layout "]".
            SlotId(27) => {
                let nonterminal_node_id =
                    self.create_nonterminal_node(result, NonterminalId(1), SlotId(27), gss_node_id);
                self.pop(gss_node_id, SlotId(27), nonterminal_node_id, None);
            }
            // P : . "p"
            SlotId(28) => {
                if let Some((j, right_child)) =
                    self.match_terminal(TerminalId(7), input_index, SlotId(28), Some(gss_node_id))
                {
                    // P : "p".
                    self.execute(j, SlotId(29), Some(right_child), gss_node_id, env);
                }
            }
            // P : "p".
            SlotId(29) => {
                let nonterminal_node_id =
                    self.create_nonterminal_node(result, NonterminalId(2), SlotId(29), gss_node_id);
                self.pop(gss_node_id, SlotId(29), nonterminal_node_id, None);
            }
            // P : . Word
            SlotId(30) => {
                if let Some((j, right_child)) =
                    self.match_terminal(TerminalId(0), input_index, SlotId(30), Some(gss_node_id))
                {
                    // P : Word.
                    self.execute(j, SlotId(31), Some(right_child), gss_node_id, env);
                }
            }
            // P : Word.
            SlotId(31) => {
                let nonterminal_node_id =
                    self.create_nonterminal_node(result, NonterminalId(2), SlotId(31), gss_node_id);
                self.pop(gss_node_id, SlotId(31), nonterminal_node_id, None);
            }
            // Q : . "q"
            SlotId(32) => {
                if let Some((j, right_child)) =
                    self.match_terminal(TerminalId(8), input_index, SlotId(32), Some(gss_node_id))
                {
                    // Q : "q".
                    self.execute(j, SlotId(33), Some(right_child), gss_node_id, env);
                }
            }
            // Q : "q".
            SlotId(33) => {
                let nonterminal_node_id =
                    self.create_nonterminal_node(result, NonterminalId(3), SlotId(33), gss_node_id);
                self.pop(gss_node_id, SlotId(33), nonterminal_node_id, None);
            }
            // BeforeP : . "x"
            SlotId(34) => {
                if let Some((j, right_child)) =
                    self.match_terminal(TerminalId(9), input_index, SlotId(34), Some(gss_node_id))
                {
                    // BeforeP : "x".
                    self.execute(j, SlotId(35), Some(right_child), gss_node_id, env);
                }
            }
            // BeforeP : "x".
            SlotId(35) => {
                let nonterminal_node_id =
                    self.create_nonterminal_node(result, NonterminalId(4), SlotId(35), gss_node_id);
                self.pop(gss_node_id, SlotId(35), nonterminal_node_id, None);
            }
            // BeforeP : . "x" Layout "y"
            SlotId(36) => {
                if let Some((j, right_child)) =
                    self.match_terminal(TerminalId(9), input_index, SlotId(36), Some(gss_node_id))
                {
                    // BeforeP : "x" . Layout "y"
                    self.execute(j, SlotId(37), Some(right_child), gss_node_id, env);
                }
            }
            // BeforeP : "x" . Layout "y"
            SlotId(37) => {
                if let Some(right_child) = self.parse_layout_ll1(input_index) {
                    if let Some((j, new_node)) =
                        self.create_intermediate_node(result, right_child, SlotId(38), env)
                    {
                        // BeforeP : "x" Layout . "y"
                        self.execute(j, SlotId(38), Some(new_node), gss_node_id, env);
                    }
                }
            }
            // BeforeP : "x" Layout . "y"
            SlotId(38) => {
                if let Some((_, right_child)) =
                    self.match_terminal(TerminalId(10), input_index, SlotId(38), Some(gss_node_id))
                {
                    if let Some((j, new_node)) =
                        self.create_intermediate_node(result, right_child, SlotId(39), env)
                    {
                        // BeforeP : "x" Layout "y".
                        self.execute(j, SlotId(39), Some(new_node), gss_node_id, env);
                    }
                }
            }
            // BeforeP : "x" Layout "y".
            SlotId(39) => {
                let nonterminal_node_id =
                    self.create_nonterminal_node(result, NonterminalId(4), SlotId(39), gss_node_id);
                self.pop(gss_node_id, SlotId(39), nonterminal_node_id, None);
            }
            // BeforeQ : . "x"
            SlotId(40) => {
                if let Some((j, right_child)) =
                    self.match_terminal(TerminalId(9), input_index, SlotId(40), Some(gss_node_id))
                {
                    // BeforeQ : "x".
                    self.execute(j, SlotId(41), Some(right_child), gss_node_id, env);
                }
            }
            // BeforeQ : "x".
            SlotId(41) => {
                let nonterminal_node_id =
                    self.create_nonterminal_node(result, NonterminalId(5), SlotId(41), gss_node_id);
                self.pop(gss_node_id, SlotId(41), nonterminal_node_id, None);
            }
            // BeforeQ : . "x" Layout "z"
            SlotId(42) => {
                if let Some((j, right_child)) =
                    self.match_terminal(TerminalId(9), input_index, SlotId(42), Some(gss_node_id))
                {
                    // BeforeQ : "x" . Layout "z"
                    self.execute(j, SlotId(43), Some(right_child), gss_node_id, env);
                }
            }
            // BeforeQ : "x" . Layout "z"
            SlotId(43) => {
                if let Some(right_child) = self.parse_layout_ll1(input_index) {
                    if let Some((j, new_node)) =
                        self.create_intermediate_node(result, right_child, SlotId(44), env)
                    {
                        // BeforeQ : "x" Layout . "z"
                        self.execute(j, SlotId(44), Some(new_node), gss_node_id, env);
                    }
                }
            }
            // BeforeQ : "x" Layout . "z"
            SlotId(44) => {
                if let Some((_, right_child)) =
                    self.match_terminal(TerminalId(11), input_index, SlotId(44), Some(gss_node_id))
                {
                    if let Some((j, new_node)) =
                        self.create_intermediate_node(result, right_child, SlotId(45), env)
                    {
                        // BeforeQ : "x" Layout "z".
                        self.execute(j, SlotId(45), Some(new_node), gss_node_id, env);
                    }
                }
            }
            // BeforeQ : "x" Layout "z".
            SlotId(45) => {
                let nonterminal_node_id =
                    self.create_nonterminal_node(result, NonterminalId(5), SlotId(45), gss_node_id);
                self.pop(gss_node_id, SlotId(45), nonterminal_node_id, None);
            }
            // UnexpectedFirst : . Identifier \ Keyword
            SlotId(46) => {
                if let Some((j, right_child)) =
                    self.match_terminal(TerminalId(1), input_index, SlotId(46), Some(gss_node_id))
                {
                    if let Some(error_kind) = self.post_conditions(SlotId(47), input_index, j) {
                        self.add_failure(j, SlotId(47), Some(gss_node_id), || error_kind);
                    } else {
                        // UnexpectedFirst : Identifier \ Keyword.
                        self.execute(j, SlotId(47), Some(right_child), gss_node_id, env);
                    }
                }
            }
            // UnexpectedFirst : Identifier \ Keyword.
            SlotId(47) => {
                let nonterminal_node_id =
                    self.create_nonterminal_node(result, NonterminalId(6), SlotId(47), gss_node_id);
                self.pop(gss_node_id, SlotId(47), nonterminal_node_id, None);
            }
            // UnexpectedFirst : . Identifier "!"
            SlotId(48) => {
                if let Some((j, right_child)) =
                    self.match_terminal(TerminalId(1), input_index, SlotId(48), Some(gss_node_id))
                {
                    // UnexpectedFirst : Identifier . "!"
                    self.execute(j, SlotId(49), Some(right_child), gss_node_id, env);
                }
            }
            // UnexpectedFirst : Identifier . "!"
            SlotId(49) => {
                if let Some((_, right_child)) =
                    self.match_terminal(TerminalId(12), input_index, SlotId(49), Some(gss_node_id))
                {
                    if let Some((j, new_node)) =
                        self.create_intermediate_node(result, right_child, SlotId(50), env)
                    {
                        // UnexpectedFirst : Identifier "!".
                        self.execute(j, SlotId(50), Some(new_node), gss_node_id, env);
                    }
                }
            }
            // UnexpectedFirst : Identifier "!".
            SlotId(50) => {
                let nonterminal_node_id =
                    self.create_nonterminal_node(result, NonterminalId(6), SlotId(50), gss_node_id);
                self.pop(gss_node_id, SlotId(50), nonterminal_node_id, None);
            }
            // ExcludedFirst : . Identifier "!"
            SlotId(51) => {
                if let Some((j, right_child)) =
                    self.match_terminal(TerminalId(1), input_index, SlotId(51), Some(gss_node_id))
                {
                    // ExcludedFirst : Identifier . "!"
                    self.execute(j, SlotId(52), Some(right_child), gss_node_id, env);
                }
            }
            // ExcludedFirst : Identifier . "!"
            SlotId(52) => {
                if let Some((_, right_child)) =
                    self.match_terminal(TerminalId(12), input_index, SlotId(52), Some(gss_node_id))
                {
                    if let Some((j, new_node)) =
                        self.create_intermediate_node(result, right_child, SlotId(53), env)
                    {
                        // ExcludedFirst : Identifier "!".
                        self.execute(j, SlotId(53), Some(new_node), gss_node_id, env);
                    }
                }
            }
            // ExcludedFirst : Identifier "!".
            SlotId(53) => {
                let nonterminal_node_id =
                    self.create_nonterminal_node(result, NonterminalId(7), SlotId(53), gss_node_id);
                self.pop(gss_node_id, SlotId(53), nonterminal_node_id, None);
            }
            // ExcludedFirst : . Identifier \ Keyword
            SlotId(54) => {
                if let Some((j, right_child)) =
                    self.match_terminal(TerminalId(1), input_index, SlotId(54), Some(gss_node_id))
                {
                    if let Some(error_kind) = self.post_conditions(SlotId(55), input_index, j) {
                        self.add_failure(j, SlotId(55), Some(gss_node_id), || error_kind);
                    } else {
                        // ExcludedFirst : Identifier \ Keyword.
                        self.execute(j, SlotId(55), Some(right_child), gss_node_id, env);
                    }
                }
            }
            // ExcludedFirst : Identifier \ Keyword.
            SlotId(55) => {
                let nonterminal_node_id =
                    self.create_nonterminal_node(result, NonterminalId(7), SlotId(55), gss_node_id);
                self.pop(gss_node_id, SlotId(55), nonterminal_node_id, None);
            }
            // Layout : . Star_0
            SlotId(56) => {
                if let Some(right_child) = self.parse_star_0_ll1(input_index) {
                    let j = self.sppf_node(right_child).right_extent();
                    // Layout : Star_0.
                    self.execute(j, SlotId(57), Some(right_child), gss_node_id, env);
                }
            }
            // Layout : Star_0.
            SlotId(57) => {
                let nonterminal_node_id =
                    self.create_nonterminal_node(result, NonterminalId(8), SlotId(57), gss_node_id);
                self.pop(gss_node_id, SlotId(57), nonterminal_node_id, None);
            }
            // Alt_0 : . WS
            SlotId(58) => {
                if let Some((j, right_child)) =
                    self.match_terminal(TerminalId(3), input_index, SlotId(58), Some(gss_node_id))
                {
                    // Alt_0 : WS.
                    self.execute(j, SlotId(59), Some(right_child), gss_node_id, env);
                }
            }
            // Alt_0 : WS.
            SlotId(59) => {
                let nonterminal_node_id =
                    self.create_nonterminal_node(result, NonterminalId(9), SlotId(59), gss_node_id);
                self.pop(gss_node_id, SlotId(59), nonterminal_node_id, None);
            }
            // Alt_0 : . Newline
            SlotId(60) => {
                if let Some((j, right_child)) =
                    self.match_terminal(TerminalId(4), input_index, SlotId(60), Some(gss_node_id))
                {
                    // Alt_0 : Newline.
                    self.execute(j, SlotId(61), Some(right_child), gss_node_id, env);
                }
            }
            // Alt_0 : Newline.
            SlotId(61) => {
                let nonterminal_node_id =
                    self.create_nonterminal_node(result, NonterminalId(9), SlotId(61), gss_node_id);
                self.pop(gss_node_id, SlotId(61), nonterminal_node_id, None);
            }
            // Plus_0 : . Plus_0 Alt_0
            SlotId(62) => {
                if let Some(right_child) = self.parse_plus_0_ll1(input_index) {
                    let j = self.sppf_node(right_child).right_extent();
                    // Plus_0 : Plus_0 . Alt_0
                    self.execute(j, SlotId(63), Some(right_child), gss_node_id, env);
                } else {
                    self.add_failure(input_index, SlotId(62), Some(gss_node_id), || {
                        GLLFailureKind::UnexpectedToken {
                            expected: FIRST_SET_PLUS_0.terminals.to_vec(),
                        }
                    });
                }
            }
            // Plus_0 : Plus_0 . Alt_0
            SlotId(63) => {
                if let Some(right_child) = self.parse_alt_0_ll1(input_index) {
                    if let Some((j, new_node)) =
                        self.create_intermediate_node(result, right_child, SlotId(64), env)
                    {
                        // Plus_0 : Plus_0 Alt_0.
                        self.execute(j, SlotId(64), Some(new_node), gss_node_id, env);
                    }
                } else {
                    self.add_failure(input_index, SlotId(63), Some(gss_node_id), || {
                        GLLFailureKind::UnexpectedToken {
                            expected: FIRST_SET_ALT_0.terminals.to_vec(),
                        }
                    });
                }
            }
            // Plus_0 : Plus_0 Alt_0.
            SlotId(64) => {
                let nonterminal_node_id = self.create_nonterminal_node(
                    result,
                    NonterminalId(10),
                    SlotId(64),
                    gss_node_id,
                );
                self.pop(gss_node_id, SlotId(64), nonterminal_node_id, None);
            }
            // Plus_0 : . Alt_0
            SlotId(65) => {
                if let Some(right_child) = self.parse_alt_0_ll1(input_index) {
                    let j = self.sppf_node(right_child).right_extent();
                    // Plus_0 : Alt_0.
                    self.execute(j, SlotId(66), Some(right_child), gss_node_id, env);
                } else {
                    self.add_failure(input_index, SlotId(65), Some(gss_node_id), || {
                        GLLFailureKind::UnexpectedToken {
                            expected: FIRST_SET_ALT_0.terminals.to_vec(),
                        }
                    });
                }
            }
            // Plus_0 : Alt_0.
            SlotId(66) => {
                let nonterminal_node_id = self.create_nonterminal_node(
                    result,
                    NonterminalId(10),
                    SlotId(66),
                    gss_node_id,
                );
                self.pop(gss_node_id, SlotId(66), nonterminal_node_id, None);
            }
            // Opt_0 : . Plus_0
            SlotId(67) => {
                if let Some(right_child) = self.parse_plus_0_ll1(input_index) {
                    let j = self.sppf_node(right_child).right_extent();
                    // Opt_0 : Plus_0.
                    self.execute(j, SlotId(68), Some(right_child), gss_node_id, env);
                } else {
                    self.add_failure(input_index, SlotId(67), Some(gss_node_id), || {
                        GLLFailureKind::UnexpectedToken {
                            expected: FIRST_SET_PLUS_0.terminals.to_vec(),
                        }
                    });
                }
            }
            // Opt_0 : Plus_0.
            SlotId(68) => {
                let nonterminal_node_id = self.create_nonterminal_node(
                    result,
                    NonterminalId(11),
                    SlotId(68),
                    gss_node_id,
                );
                self.pop(gss_node_id, SlotId(68), nonterminal_node_id, None);
            }
            // Opt_0 : .
            SlotId(69) => {
                let epsilon_node_id = self.get_or_create_epsilon_node(input_index);
                let nonterminal_node_id = self.get_or_create_nonterminal_node(
                    NonterminalId(11),
                    SlotId(69),
                    input_index,
                    input_index,
                    epsilon_node_id,
                    gss_node_id,
                    None,
                );
                self.pop(gss_node_id, SlotId(69), nonterminal_node_id, None);
            }
            // Star_0 : . Opt_0
            SlotId(70) => {
                if let Some(right_child) = self.parse_opt_0_ll1(input_index) {
                    let j = self.sppf_node(right_child).right_extent();
                    // Star_0 : Opt_0.
                    self.execute(j, SlotId(71), Some(right_child), gss_node_id, env);
                }
            }
            // Star_0 : Opt_0.
            SlotId(71) => {
                let nonterminal_node_id = self.create_nonterminal_node(
                    result,
                    NonterminalId(12),
                    SlotId(71),
                    gss_node_id,
                );
                self.pop(gss_node_id, SlotId(71), nonterminal_node_id, None);
            }
            // StartT : . Layout start:T Layout
            SlotId(72) => {
                if let Some(right_child) = self.parse_layout_ll1(input_index) {
                    let j = self.sppf_node(right_child).right_extent();
                    // StartT : Layout . start:T Layout
                    self.execute(j, SlotId(73), Some(right_child), gss_node_id, env);
                }
            }
            // StartT : Layout . start:T Layout
            SlotId(73) => {
                self.create(NonterminalId(0), result, gss_node_id, SlotId(74), env);
            }
            // StartT : Layout start:T . Layout
            SlotId(74) => {
                if let Some(right_child) = self.parse_layout_ll1(input_index) {
                    if let Some((j, new_node)) =
                        self.create_intermediate_node(result, right_child, SlotId(75), env)
                    {
                        // StartT : Layout start:T Layout.
                        self.execute(j, SlotId(75), Some(new_node), gss_node_id, env);
                    }
                }
            }
            // StartT : Layout start:T Layout.
            SlotId(75) => {
                let nonterminal_node_id = self.create_nonterminal_node(
                    result,
                    NonterminalId(13),
                    SlotId(75),
                    gss_node_id,
                );
                self.pop(gss_node_id, SlotId(75), nonterminal_node_id, None);
            }
            // StartU : . Layout start:U Layout
            SlotId(76) => {
                if let Some(right_child) = self.parse_layout_ll1(input_index) {
                    let j = self.sppf_node(right_child).right_extent();
                    // StartU : Layout . start:U Layout
                    self.execute(j, SlotId(77), Some(right_child), gss_node_id, env);
                }
            }
            // StartU : Layout . start:U Layout
            SlotId(77) => {
                self.create(NonterminalId(1), result, gss_node_id, SlotId(78), env);
            }
            // StartU : Layout start:U . Layout
            SlotId(78) => {
                if let Some(right_child) = self.parse_layout_ll1(input_index) {
                    if let Some((j, new_node)) =
                        self.create_intermediate_node(result, right_child, SlotId(79), env)
                    {
                        // StartU : Layout start:U Layout.
                        self.execute(j, SlotId(79), Some(new_node), gss_node_id, env);
                    }
                }
            }
            // StartU : Layout start:U Layout.
            SlotId(79) => {
                let nonterminal_node_id = self.create_nonterminal_node(
                    result,
                    NonterminalId(14),
                    SlotId(79),
                    gss_node_id,
                );
                self.pop(gss_node_id, SlotId(79), nonterminal_node_id, None);
            }
            // StartP : . Layout start:P Layout
            SlotId(80) => {
                if let Some(right_child) = self.parse_layout_ll1(input_index) {
                    let j = self.sppf_node(right_child).right_extent();
                    // StartP : Layout . start:P Layout
                    self.execute(j, SlotId(81), Some(right_child), gss_node_id, env);
                }
            }
            // StartP : Layout . start:P Layout
            SlotId(81) => {
                self.create(NonterminalId(2), result, gss_node_id, SlotId(82), env);
            }
            // StartP : Layout start:P . Layout
            SlotId(82) => {
                if let Some(right_child) = self.parse_layout_ll1(input_index) {
                    if let Some((j, new_node)) =
                        self.create_intermediate_node(result, right_child, SlotId(83), env)
                    {
                        // StartP : Layout start:P Layout.
                        self.execute(j, SlotId(83), Some(new_node), gss_node_id, env);
                    }
                }
            }
            // StartP : Layout start:P Layout.
            SlotId(83) => {
                let nonterminal_node_id = self.create_nonterminal_node(
                    result,
                    NonterminalId(15),
                    SlotId(83),
                    gss_node_id,
                );
                self.pop(gss_node_id, SlotId(83), nonterminal_node_id, None);
            }
            // StartQ : . Layout start:Q Layout
            SlotId(84) => {
                if let Some(right_child) = self.parse_layout_ll1(input_index) {
                    let j = self.sppf_node(right_child).right_extent();
                    // StartQ : Layout . start:Q Layout
                    self.execute(j, SlotId(85), Some(right_child), gss_node_id, env);
                }
            }
            // StartQ : Layout . start:Q Layout
            SlotId(85) => {
                if let Some(right_child) = self.parse_q_ll1(input_index) {
                    if let Some((j, new_node)) =
                        self.create_intermediate_node(result, right_child, SlotId(86), env)
                    {
                        // StartQ : Layout start:Q . Layout
                        self.execute(j, SlotId(86), Some(new_node), gss_node_id, env);
                    }
                } else {
                    self.add_failure(input_index, SlotId(85), Some(gss_node_id), || {
                        GLLFailureKind::UnexpectedToken {
                            expected: FIRST_SET_Q.terminals.to_vec(),
                        }
                    });
                }
            }
            // StartQ : Layout start:Q . Layout
            SlotId(86) => {
                if let Some(right_child) = self.parse_layout_ll1(input_index) {
                    if let Some((j, new_node)) =
                        self.create_intermediate_node(result, right_child, SlotId(87), env)
                    {
                        // StartQ : Layout start:Q Layout.
                        self.execute(j, SlotId(87), Some(new_node), gss_node_id, env);
                    }
                }
            }
            // StartQ : Layout start:Q Layout.
            SlotId(87) => {
                let nonterminal_node_id = self.create_nonterminal_node(
                    result,
                    NonterminalId(16),
                    SlotId(87),
                    gss_node_id,
                );
                self.pop(gss_node_id, SlotId(87), nonterminal_node_id, None);
            }
            // StartBeforeP : . Layout start:BeforeP Layout
            SlotId(88) => {
                if let Some(right_child) = self.parse_layout_ll1(input_index) {
                    let j = self.sppf_node(right_child).right_extent();
                    // StartBeforeP : Layout . start:BeforeP Layout
                    self.execute(j, SlotId(89), Some(right_child), gss_node_id, env);
                }
            }
            // StartBeforeP : Layout . start:BeforeP Layout
            SlotId(89) => {
                self.create(NonterminalId(4), result, gss_node_id, SlotId(90), env);
            }
            // StartBeforeP : Layout start:BeforeP . Layout
            SlotId(90) => {
                if let Some(right_child) = self.parse_layout_ll1(input_index) {
                    if let Some((j, new_node)) =
                        self.create_intermediate_node(result, right_child, SlotId(91), env)
                    {
                        // StartBeforeP : Layout start:BeforeP Layout.
                        self.execute(j, SlotId(91), Some(new_node), gss_node_id, env);
                    }
                }
            }
            // StartBeforeP : Layout start:BeforeP Layout.
            SlotId(91) => {
                let nonterminal_node_id = self.create_nonterminal_node(
                    result,
                    NonterminalId(17),
                    SlotId(91),
                    gss_node_id,
                );
                self.pop(gss_node_id, SlotId(91), nonterminal_node_id, None);
            }
            // StartBeforeQ : . Layout start:BeforeQ Layout
            SlotId(92) => {
                if let Some(right_child) = self.parse_layout_ll1(input_index) {
                    let j = self.sppf_node(right_child).right_extent();
                    // StartBeforeQ : Layout . start:BeforeQ Layout
                    self.execute(j, SlotId(93), Some(right_child), gss_node_id, env);
                }
            }
            // StartBeforeQ : Layout . start:BeforeQ Layout
            SlotId(93) => {
                self.create(NonterminalId(5), result, gss_node_id, SlotId(94), env);
            }
            // StartBeforeQ : Layout start:BeforeQ . Layout
            SlotId(94) => {
                if let Some(right_child) = self.parse_layout_ll1(input_index) {
                    if let Some((j, new_node)) =
                        self.create_intermediate_node(result, right_child, SlotId(95), env)
                    {
                        // StartBeforeQ : Layout start:BeforeQ Layout.
                        self.execute(j, SlotId(95), Some(new_node), gss_node_id, env);
                    }
                }
            }
            // StartBeforeQ : Layout start:BeforeQ Layout.
            SlotId(95) => {
                let nonterminal_node_id = self.create_nonterminal_node(
                    result,
                    NonterminalId(18),
                    SlotId(95),
                    gss_node_id,
                );
                self.pop(gss_node_id, SlotId(95), nonterminal_node_id, None);
            }
            // StartUnexpectedFirst : . Layout start:UnexpectedFirst Layout
            SlotId(96) => {
                if let Some(right_child) = self.parse_layout_ll1(input_index) {
                    let j = self.sppf_node(right_child).right_extent();
                    // StartUnexpectedFirst : Layout . start:UnexpectedFirst Layout
                    self.execute(j, SlotId(97), Some(right_child), gss_node_id, env);
                }
            }
            // StartUnexpectedFirst : Layout . start:UnexpectedFirst Layout
            SlotId(97) => {
                self.create(NonterminalId(6), result, gss_node_id, SlotId(98), env);
            }
            // StartUnexpectedFirst : Layout start:UnexpectedFirst . Layout
            SlotId(98) => {
                if let Some(right_child) = self.parse_layout_ll1(input_index) {
                    if let Some((j, new_node)) =
                        self.create_intermediate_node(result, right_child, SlotId(99), env)
                    {
                        // StartUnexpectedFirst : Layout start:UnexpectedFirst Layout.
                        self.execute(j, SlotId(99), Some(new_node), gss_node_id, env);
                    }
                }
            }
            // StartUnexpectedFirst : Layout start:UnexpectedFirst Layout.
            SlotId(99) => {
                let nonterminal_node_id = self.create_nonterminal_node(
                    result,
                    NonterminalId(19),
                    SlotId(99),
                    gss_node_id,
                );
                self.pop(gss_node_id, SlotId(99), nonterminal_node_id, None);
            }
            // StartExcludedFirst : . Layout start:ExcludedFirst Layout
            SlotId(100) => {
                if let Some(right_child) = self.parse_layout_ll1(input_index) {
                    let j = self.sppf_node(right_child).right_extent();
                    // StartExcludedFirst : Layout . start:ExcludedFirst Layout
                    self.execute(j, SlotId(101), Some(right_child), gss_node_id, env);
                }
            }
            // StartExcludedFirst : Layout . start:ExcludedFirst Layout
            SlotId(101) => {
                self.create(NonterminalId(7), result, gss_node_id, SlotId(102), env);
            }
            // StartExcludedFirst : Layout start:ExcludedFirst . Layout
            SlotId(102) => {
                if let Some(right_child) = self.parse_layout_ll1(input_index) {
                    if let Some((j, new_node)) =
                        self.create_intermediate_node(result, right_child, SlotId(103), env)
                    {
                        // StartExcludedFirst : Layout start:ExcludedFirst Layout.
                        self.execute(j, SlotId(103), Some(new_node), gss_node_id, env);
                    }
                }
            }
            // StartExcludedFirst : Layout start:ExcludedFirst Layout.
            SlotId(103) => {
                let nonterminal_node_id = self.create_nonterminal_node(
                    result,
                    NonterminalId(20),
                    SlotId(103),
                    gss_node_id,
                );
                self.pop(gss_node_id, SlotId(103), nonterminal_node_id, None);
            }
            _ => {
                panic!("Unknown grammar slot id: {slot_id}");
            }
        }
    }
    fn add_first_descriptors(
        &mut self,
        nonterminal_id: NonterminalId,
        input_index: u32,
        gss_node_id: GssNodeId,
        env: Option<EnvId>,
    ) {
        match nonterminal_id {
            // T
            NonterminalId(0) => {
                let mut matched = false;
                // T : . "[" BeforeP Layout P Layout "]"
                if self.scanner.match_any(&FIRST_SET_T_ALT0, input_index) {
                    matched = true;
                    self.add_first_descriptor(SlotId(0), input_index, gss_node_id, env);
                }
                // T : . "[" BeforeQ Layout Q Layout "]"
                if self.scanner.match_any(&FIRST_SET_T_ALT1, input_index) {
                    matched = true;
                    self.add_first_descriptor(SlotId(7), input_index, gss_node_id, env);
                }
                if !matched {
                    self.add_failure(input_index, SlotId(0), Some(gss_node_id), || {
                        GLLFailureKind::UnexpectedToken {
                            expected: FIRST_SET_T.terminals.to_vec(),
                        }
                    });
                }
            }
            // U
            NonterminalId(1) => {
                let mut matched = false;
                // U : . "[" BeforeQ Layout Q Layout "]"
                if self.scanner.match_any(&FIRST_SET_U_ALT0, input_index) {
                    matched = true;
                    self.add_first_descriptor(SlotId(14), input_index, gss_node_id, env);
                }
                // U : . "[" BeforeP Layout P Layout "]"
                if self.scanner.match_any(&FIRST_SET_U_ALT1, input_index) {
                    matched = true;
                    self.add_first_descriptor(SlotId(21), input_index, gss_node_id, env);
                }
                if !matched {
                    self.add_failure(input_index, SlotId(14), Some(gss_node_id), || {
                        GLLFailureKind::UnexpectedToken {
                            expected: FIRST_SET_U.terminals.to_vec(),
                        }
                    });
                }
            }
            // P
            NonterminalId(2) => {
                let mut matched = false;
                // P : . "p"
                if self.scanner.match_any(&FIRST_SET_P_ALT0, input_index) {
                    matched = true;
                    self.add_first_descriptor(SlotId(28), input_index, gss_node_id, env);
                }
                // P : . Word
                if self.scanner.match_any(&FIRST_SET_P_ALT1, input_index) {
                    matched = true;
                    self.add_first_descriptor(SlotId(30), input_index, gss_node_id, env);
                }
                if !matched {
                    self.add_failure(input_index, SlotId(28), Some(gss_node_id), || {
                        GLLFailureKind::UnexpectedToken {
                            expected: FIRST_SET_P.terminals.to_vec(),
                        }
                    });
                }
            }
            // Q : . "q"
            NonterminalId(3) => {
                self.add_first_descriptor(SlotId(32), input_index, gss_node_id, env);
            }
            // BeforeP
            NonterminalId(4) => {
                let mut matched = false;
                // BeforeP : . "x"
                if self
                    .scanner
                    .match_any(&FIRST_SET_BEFORE_P_ALT0, input_index)
                {
                    matched = true;
                    self.add_first_descriptor(SlotId(34), input_index, gss_node_id, env);
                }
                // BeforeP : . "x" Layout "y"
                if self
                    .scanner
                    .match_any(&FIRST_SET_BEFORE_P_ALT1, input_index)
                {
                    matched = true;
                    self.add_first_descriptor(SlotId(36), input_index, gss_node_id, env);
                }
                if !matched {
                    self.add_failure(input_index, SlotId(34), Some(gss_node_id), || {
                        GLLFailureKind::UnexpectedToken {
                            expected: FIRST_SET_BEFORE_P.terminals.to_vec(),
                        }
                    });
                }
            }
            // BeforeQ
            NonterminalId(5) => {
                let mut matched = false;
                // BeforeQ : . "x"
                if self
                    .scanner
                    .match_any(&FIRST_SET_BEFORE_Q_ALT0, input_index)
                {
                    matched = true;
                    self.add_first_descriptor(SlotId(40), input_index, gss_node_id, env);
                }
                // BeforeQ : . "x" Layout "z"
                if self
                    .scanner
                    .match_any(&FIRST_SET_BEFORE_Q_ALT1, input_index)
                {
                    matched = true;
                    self.add_first_descriptor(SlotId(42), input_index, gss_node_id, env);
                }
                if !matched {
                    self.add_failure(input_index, SlotId(40), Some(gss_node_id), || {
                        GLLFailureKind::UnexpectedToken {
                            expected: FIRST_SET_BEFORE_Q.terminals.to_vec(),
                        }
                    });
                }
            }
            // UnexpectedFirst
            NonterminalId(6) => {
                let mut matched = false;
                // UnexpectedFirst : . Identifier \ Keyword
                if self
                    .scanner
                    .match_any(&FIRST_SET_UNEXPECTED_FIRST_ALT0, input_index)
                {
                    matched = true;
                    self.add_first_descriptor(SlotId(46), input_index, gss_node_id, env);
                }
                // UnexpectedFirst : . Identifier "!"
                if self
                    .scanner
                    .match_any(&FIRST_SET_UNEXPECTED_FIRST_ALT1, input_index)
                {
                    matched = true;
                    self.add_first_descriptor(SlotId(48), input_index, gss_node_id, env);
                }
                if !matched {
                    self.add_failure(input_index, SlotId(46), Some(gss_node_id), || {
                        GLLFailureKind::UnexpectedToken {
                            expected: FIRST_SET_UNEXPECTED_FIRST.terminals.to_vec(),
                        }
                    });
                }
            }
            // ExcludedFirst
            NonterminalId(7) => {
                let mut matched = false;
                // ExcludedFirst : . Identifier "!"
                if self
                    .scanner
                    .match_any(&FIRST_SET_EXCLUDED_FIRST_ALT0, input_index)
                {
                    matched = true;
                    self.add_first_descriptor(SlotId(51), input_index, gss_node_id, env);
                }
                // ExcludedFirst : . Identifier \ Keyword
                if self
                    .scanner
                    .match_any(&FIRST_SET_EXCLUDED_FIRST_ALT1, input_index)
                {
                    matched = true;
                    self.add_first_descriptor(SlotId(54), input_index, gss_node_id, env);
                }
                if !matched {
                    self.add_failure(input_index, SlotId(51), Some(gss_node_id), || {
                        GLLFailureKind::UnexpectedToken {
                            expected: FIRST_SET_EXCLUDED_FIRST.terminals.to_vec(),
                        }
                    });
                }
            }
            // Layout : . Star_0
            NonterminalId(8) => {
                self.add_first_descriptor(SlotId(56), input_index, gss_node_id, env);
            }
            // Alt_0
            NonterminalId(9) => {
                let mut matched = false;
                // Alt_0 : . WS
                if self.scanner.match_any(&FIRST_SET_ALT_0_ALT0, input_index) {
                    matched = true;
                    self.add_first_descriptor(SlotId(58), input_index, gss_node_id, env);
                }
                // Alt_0 : . Newline
                if self.scanner.match_any(&FIRST_SET_ALT_0_ALT1, input_index) {
                    matched = true;
                    self.add_first_descriptor(SlotId(60), input_index, gss_node_id, env);
                }
                if !matched {
                    self.add_failure(input_index, SlotId(58), Some(gss_node_id), || {
                        GLLFailureKind::UnexpectedToken {
                            expected: FIRST_SET_ALT_0.terminals.to_vec(),
                        }
                    });
                }
            }
            // Plus_0
            NonterminalId(10) => {
                let mut matched = false;
                // Plus_0 : . Plus_0 Alt_0
                if self.scanner.match_any(&FIRST_SET_PLUS_0_ALT0, input_index) {
                    matched = true;
                    self.add_first_descriptor(SlotId(62), input_index, gss_node_id, env);
                }
                // Plus_0 : . Alt_0
                if self.scanner.match_any(&FIRST_SET_PLUS_0_ALT1, input_index) {
                    matched = true;
                    self.add_first_descriptor(SlotId(65), input_index, gss_node_id, env);
                }
                if !matched {
                    self.add_failure(input_index, SlotId(62), Some(gss_node_id), || {
                        GLLFailureKind::UnexpectedToken {
                            expected: FIRST_SET_PLUS_0.terminals.to_vec(),
                        }
                    });
                }
            }
            // Opt_0
            NonterminalId(11) => {
                let mut matched = false;
                // Opt_0 : . Plus_0
                if self.scanner.match_any(&FIRST_SET_OPT_0_ALT0, input_index) {
                    matched = true;
                    self.add_first_descriptor(SlotId(67), input_index, gss_node_id, env);
                }
                // Opt_0 : .
                if self.scanner.match_any(&FIRST_SET_OPT_0_ALT1, input_index)
                    || self.scanner.match_any(&FOLLOW_SET_OPT_0, input_index)
                {
                    matched = true;
                    self.add_first_descriptor(SlotId(69), input_index, gss_node_id, env);
                }
                if !matched {
                    self.add_failure(input_index, SlotId(67), Some(gss_node_id), || {
                        GLLFailureKind::UnexpectedToken {
                            expected: {
                                let mut expected = FIRST_SET_OPT_0.terminals.to_vec();
                                expected.extend_from_slice(FOLLOW_SET_OPT_0.terminals);
                                expected
                            },
                        }
                    });
                }
            }
            // Star_0 : . Opt_0
            NonterminalId(12) => {
                self.add_first_descriptor(SlotId(70), input_index, gss_node_id, env);
            }
            // StartT : . Layout start:T Layout
            NonterminalId(13) => {
                self.add_first_descriptor(SlotId(72), input_index, gss_node_id, env);
            }
            // StartU : . Layout start:U Layout
            NonterminalId(14) => {
                self.add_first_descriptor(SlotId(76), input_index, gss_node_id, env);
            }
            // StartP : . Layout start:P Layout
            NonterminalId(15) => {
                self.add_first_descriptor(SlotId(80), input_index, gss_node_id, env);
            }
            // StartQ : . Layout start:Q Layout
            NonterminalId(16) => {
                self.add_first_descriptor(SlotId(84), input_index, gss_node_id, env);
            }
            // StartBeforeP : . Layout start:BeforeP Layout
            NonterminalId(17) => {
                self.add_first_descriptor(SlotId(88), input_index, gss_node_id, env);
            }
            // StartBeforeQ : . Layout start:BeforeQ Layout
            NonterminalId(18) => {
                self.add_first_descriptor(SlotId(92), input_index, gss_node_id, env);
            }
            // StartUnexpectedFirst : . Layout start:UnexpectedFirst Layout
            NonterminalId(19) => {
                self.add_first_descriptor(SlotId(96), input_index, gss_node_id, env);
            }
            // StartExcludedFirst : . Layout start:ExcludedFirst Layout
            NonterminalId(20) => {
                self.add_first_descriptor(SlotId(100), input_index, gss_node_id, env);
            }
            _ => {
                panic!("Unknown nonterminal id: {nonterminal_id}");
            }
        }
    }
    fn get_gss_node(&self, nonterminal_id: NonterminalId, input_index: u32) -> Option<GssNodeId> {
        self.gss_nodes_index[nonterminal_id.index()]
            .get(&input_index)
            .copied()
    }
    fn add_gss_node(
        &mut self,
        nonterminal_id: NonterminalId,
        input_index: u32,
        gss_node_id: GssNodeId,
    ) {
        let arena = self.vec_arena;
        self.gss_nodes_index[nonterminal_id.index()].insert(input_index, gss_node_id, arena);
    }
    fn new_gss_node(&mut self, nonterminal_id: NonterminalId, input_index: u32) -> GssNodeId {
        let gss_node_id = GssNodeId(self.gss_nodes.len() as u32);
        let gss_node = GSSNode::new(gss_node_id, nonterminal_id, input_index);
        record!(self, GSSNodeCreated, nonterminal_id, input_index);
        self.gss_nodes.push(gss_node);
        gss_node_id
    }
    fn gss_node(&self, id: GssNodeId) -> &GSSNode<'arena> {
        &self.gss_nodes[id.index()]
    }
    fn gss_node_mut(&mut self, id: GssNodeId) -> &mut GSSNode<'arena> {
        self.gss_nodes
            .get_mut(id.index())
            .expect("GSS node id should be valid")
    }
    fn sppf_node(&self, id: SPPFNodeId) -> &SPPFNode {
        &self.sppf_nodes[id.index()]
    }
    fn sppf_node_mut(&mut self, id: SPPFNodeId) -> &mut SPPFNode {
        &mut self.sppf_nodes[id.index()]
    }
    fn add_descriptor(&mut self, descriptor: Descriptor) {
        record!(
            self,
            DescriptorAdded,
            descriptor.input_index,
            descriptor.slot_id,
            descriptor.sppf_node_id(),
            descriptor.gss_node_id
        );
        #[cfg(feature = "instrument")]
        self.increment_descriptor_count();
        self.descriptors.push(descriptor);
        #[cfg(feature = "instrument")]
        {
            if self.descriptors.len() > self.descriptors_peak {
                self.descriptors_peak = self.descriptors.len();
            }
        }
    }
    fn next_descriptor(&mut self) -> Option<Descriptor> {
        self.descriptors.pop()
    }
    fn clear_descriptors(&mut self) {
        self.descriptors.clear();
    }
    fn add_terminal_node(&mut self, terminal_node: TerminalNode) -> SPPFNodeId {
        let terminal_node_id = SPPFNodeId(self.sppf_nodes.len() as u32);
        let arena = self.vec_arena;
        self.terminal_nodes_index[terminal_node.terminal_id.index()].insert(
            terminal_node.span,
            terminal_node_id,
            arena,
        );
        record!(
            self,
            TerminalNodeCreated,
            terminal_node.terminal_id,
            terminal_node.span
        );
        self.sppf_nodes.push(SPPFNode::Terminal(terminal_node));
        terminal_node_id
    }
    fn add_nonterminal_node(&mut self, nonterminal_node: NonterminalNode) -> SPPFNodeId {
        let nonterminal_node_id = SPPFNodeId(self.sppf_nodes.len() as u32);
        record!(
            self,
            NonterminalNodeCreated,
            nonterminal_node.nonterminal_id,
            nonterminal_node.span,
            nonterminal_node.child
        );
        self.sppf_nodes
            .push(SPPFNode::Nonterminal(nonterminal_node));
        nonterminal_node_id
    }
    fn add_intermediate_node(
        &mut self,
        intermediate_node: IntermediateNode,
        env: Option<EnvId>,
        add_to_index: bool,
    ) -> SPPFNodeId {
        let intermediate_node_id = SPPFNodeId(self.sppf_nodes.len() as u32);
        if add_to_index {
            let arena = self.vec_arena;
            let slot_idx = intermediate_node.slot_id.index();
            if slot_idx < 104 {
                self.intermediate_nodes_index[slot_idx].insert(
                    intermediate_node.span,
                    intermediate_node_id,
                    arena,
                );
            } else {
                let idx = slot_idx - 104;
                self.dd_intermediate_nodes_index[idx].insert(
                    (intermediate_node.span, env),
                    intermediate_node_id,
                    arena,
                );
            }
        }
        record!(
            self,
            IntermediateNodeCreated,
            intermediate_node.slot_id,
            intermediate_node.span,
            intermediate_node.child.0,
            intermediate_node.child.1
        );
        self.sppf_nodes
            .push(SPPFNode::Intermediate(intermediate_node));
        intermediate_node_id
    }
    fn input(&self) -> &'i Input {
        self.scanner.input
    }
    fn sppf_nodes(&self) -> &[SPPFNode] {
        &self.sppf_nodes
    }
    #[cfg(feature = "instrument")]
    fn increment_descriptor_count(&mut self) {
        self.descriptors_count += 1;
    }
    #[cfg(feature = "instrument")]
    fn count_descriptors(&self) -> usize {
        self.descriptors_count
    }
    #[cfg(feature = "instrument")]
    fn count_gss_nodes(&self) -> usize {
        self.gss_nodes.len()
    }
    #[cfg(feature = "instrument")]
    fn count_gss_edges(&self) -> usize {
        self.gss_nodes.iter().map(|n| n.edges().len()).sum()
    }
    #[cfg(feature = "instrument")]
    fn count_nonterminal_nodes(&self) -> usize {
        self.sppf_nodes
            .iter()
            .filter(|n| matches!(n, SPPFNode::Nonterminal(_)))
            .count()
    }
    #[cfg(feature = "instrument")]
    fn count_intermediate_nodes(&self) -> usize {
        self.sppf_nodes
            .iter()
            .filter(|n| matches!(n, SPPFNode::Intermediate(_)))
            .count()
    }
    #[cfg(feature = "instrument")]
    fn count_terminal_nodes(&self) -> usize {
        self.sppf_nodes
            .iter()
            .filter(|n| matches!(n, SPPFNode::Terminal(_)))
            .count()
    }
    #[cfg(feature = "instrument")]
    fn count_ambiguous_nodes(&self) -> usize {
        self.sppf_nodes
            .iter()
            .filter(|n| match n {
                SPPFNode::Nonterminal(nn) => nn.ambiguous,
                SPPFNode::Intermediate(in_) => in_.ambiguous,
                SPPFNode::Terminal(_) => false,
            })
            .count()
    }
    fn lookup_intermediate_node(
        &self,
        slot_id: SlotId,
        left_extent: u32,
        right_extent: u32,
        env: Option<EnvId>,
    ) -> Option<SPPFNodeId> {
        let slot_idx = slot_id.index();
        let span = Span::new(left_extent, right_extent);
        if slot_idx < 104 {
            self.intermediate_nodes_index[slot_idx].get(&span).copied()
        } else {
            let idx = slot_idx - 104;
            self.dd_intermediate_nodes_index[idx]
                .get(&(span, env))
                .copied()
        }
    }
    fn lookup_terminal_node(
        &self,
        terminal_id: TerminalId,
        left_extent: u32,
        right_extent: u32,
    ) -> Option<SPPFNodeId> {
        let map = &self.terminal_nodes_index[terminal_id.index()];
        map.get(&Span::new(left_extent, right_extent)).copied()
    }
    fn gss_nodes<'p>(&'p self) -> impl Iterator<Item = &'p GSSNode<'arena>>
    where
        'arena: 'p,
    {
        self.gss_nodes.iter()
    }
    fn add_intermediate_node_child(
        &mut self,
        node: SPPFNodeId,
        child1: SPPFNodeId,
        child2: SPPFNodeId,
    ) {
        self.intermediate_nodes_children
            .push((node, (child1, child2)));
    }
    fn add_nonterminal_node_child(
        &mut self,
        node: SPPFNodeId,
        child: SPPFNodeId,
        return_slot: SlotId,
    ) {
        self.nonterminal_nodes_children
            .push((node, (child, return_slot)));
    }
    fn intermediate_nodes_children_map(
        &self,
    ) -> &FxHashMap<SPPFNodeId, Vec<(SPPFNodeId, SPPFNodeId)>> {
        self.intermediate_nodes_children_map.get_or_init(|| {
            let mut map: FxHashMap<SPPFNodeId, Vec<(SPPFNodeId, SPPFNodeId)>> =
                FxHashMap::default();
            for (k, v) in &self.intermediate_nodes_children {
                map.entry(*k).or_default().push(*v);
            }
            map
        })
    }
    fn nonterminal_nodes_children_map(&self) -> &FxHashMap<SPPFNodeId, Vec<(SPPFNodeId, SlotId)>> {
        self.nonterminal_nodes_children_map.get_or_init(|| {
            let mut map: FxHashMap<SPPFNodeId, Vec<(SPPFNodeId, SlotId)>> = FxHashMap::default();
            for (k, v) in &self.nonterminal_nodes_children {
                map.entry(*k).or_default().push(*v);
            }
            map
        })
    }
    #[cfg(feature = "debug-trace")]
    fn add_trace_event(&mut self, event: TraceEvent) {
        if let Some(trace_events) = &mut self.trace_events {
            trace_events.push(event);
        }
    }
    fn start_nonterminal(&self) -> NonterminalId {
        self.start_nonterminal
    }
    fn start_env(&mut self) -> Option<EnvId> {
        None
    }
    fn add_start_gss_node(
        &mut self,
        nonterminal_id: NonterminalId,
        input_index: u32,
        gss_node_id: GssNodeId,
    ) {
        self.add_gss_node(nonterminal_id, input_index, gss_node_id);
    }
    fn new_env(&mut self) -> (EnvId, &mut Env<'arena>) {
        let id = EnvId(self.envs.len() as u32);
        self.envs.push(Env::default());
        (id, &mut self.envs[id.index()])
    }
    fn lookup(&self, name: BindingId, env_id: EnvId) -> i32 {
        let env = &self.envs[env_id.index()];
        env.get(name)
    }
    fn clone_env(&mut self, source: EnvId) -> (EnvId, &mut Env<'arena>) {
        let bindings = self.envs[source.0 as usize].bindings.clone();
        let (new_id, new_env) = self.new_env();
        new_env.bindings = bindings;
        (new_id, new_env)
    }
    fn envs(&self) -> &[Env<'arena>] {
        &self.envs
    }
    #[cfg(feature = "instrument")]
    fn record_stats(&self) -> iguana_runtime::instrument::Stats {
        let mut stats = iguana_runtime::instrument::Stats::new();
        stats.descriptors_count = self.count_descriptors();
        stats.descriptors_peak = self.descriptors_peak;
        stats.envs_count = self.envs.len();
        stats.gss_nodes_count = self.count_gss_nodes();
        stats.gss_edges_count = self.count_gss_edges();
        stats.nonterminal_nodes_count = self.count_nonterminal_nodes();
        stats.intermediate_nodes_count = self.count_intermediate_nodes();
        stats.terminal_nodes_count = self.count_terminal_nodes();
        stats.ambiguous_nodes_count = self.count_ambiguous_nodes();
        for node in self.gss_nodes() {
            stats.record("GssNode::edges: InlineVec", node.edges().len());
            stats.record(
                "GssNode::popped_elements: InlineMap",
                node.popped_elements().len(),
            );
        }
        for env in self.envs() {
            stats.record("Env::bindings: InlineVec", env.bindings.len());
        }
        for m in self.intermediate_nodes_index.iter() {
            stats.record("Parser::intermediate_nodes_index: InlineMap", m.len());
        }
        for m in self.dd_intermediate_nodes_index.iter() {
            stats.record("Parser::dd_intermediate_nodes_index: InlineMap", m.len());
        }
        for m in self.terminal_nodes_index.iter() {
            stats.record("Parser::terminal_nodes_index: InlineMap", m.len());
        }
        for m in self.gss_nodes_index.iter() {
            stats.record("Parser::gss_nodes_index: InlineMap", m.len());
        }
        for (nt_id, pos) in &self.ll1_call_log {
            let name = NONTERMINALS[nt_id.index()].display;
            stats.record_ll1_call(name, *pos);
        }
        stats
    }
    fn post_conditions(
        &mut self,
        slot: SlotId,
        left_extent: u32,
        right_extent: u32,
    ) -> Option<GLLFailureKind> {
        match slot {
            SlotId(47) => {
                if self
                    .scanner
                    .match_exact(TerminalId(2), left_extent, right_extent)
                {
                    Some(GLLFailureKind::ExcludedMatch {
                        excluded_by: vec![TerminalId(2)],
                    })
                } else {
                    None
                }
            }
            SlotId(55) => {
                if self
                    .scanner
                    .match_exact(TerminalId(2), left_extent, right_extent)
                {
                    Some(GLLFailureKind::ExcludedMatch {
                        excluded_by: vec![TerminalId(2)],
                    })
                } else {
                    None
                }
            }
            _ => None,
        }
    }
    fn follow_set_check(&mut self, nonterminal_id: NonterminalId, input_index: u32) -> bool {
        match nonterminal_id {
            NonterminalId(0) => self.scanner.match_any(&FOLLOW_SET_T, input_index),
            NonterminalId(1) => self.scanner.match_any(&FOLLOW_SET_U, input_index),
            NonterminalId(2) => self.scanner.match_any(&FOLLOW_SET_P, input_index),
            NonterminalId(3) => self.scanner.match_any(&FOLLOW_SET_Q, input_index),
            NonterminalId(4) => self.scanner.match_any(&FOLLOW_SET_BEFORE_P, input_index),
            NonterminalId(5) => self.scanner.match_any(&FOLLOW_SET_BEFORE_Q, input_index),
            NonterminalId(6) => self
                .scanner
                .match_any(&FOLLOW_SET_UNEXPECTED_FIRST, input_index),
            NonterminalId(7) => self
                .scanner
                .match_any(&FOLLOW_SET_EXCLUDED_FIRST, input_index),
            NonterminalId(8) => self.scanner.match_any(&FOLLOW_SET_LAYOUT, input_index),
            NonterminalId(9) => self.scanner.match_any(&FOLLOW_SET_ALT_0, input_index),
            NonterminalId(10) => self.scanner.match_any(&FOLLOW_SET_PLUS_0, input_index),
            NonterminalId(11) => self.scanner.match_any(&FOLLOW_SET_OPT_0, input_index),
            NonterminalId(12) => self.scanner.match_any(&FOLLOW_SET_STAR_0, input_index),
            NonterminalId(13) => self.scanner.match_any(&FOLLOW_SET_START_T, input_index),
            NonterminalId(14) => self.scanner.match_any(&FOLLOW_SET_START_U, input_index),
            NonterminalId(15) => self.scanner.match_any(&FOLLOW_SET_START_P, input_index),
            NonterminalId(16) => self.scanner.match_any(&FOLLOW_SET_START_Q, input_index),
            NonterminalId(17) => self
                .scanner
                .match_any(&FOLLOW_SET_START_BEFORE_P, input_index),
            NonterminalId(18) => self
                .scanner
                .match_any(&FOLLOW_SET_START_BEFORE_Q, input_index),
            NonterminalId(19) => self
                .scanner
                .match_any(&FOLLOW_SET_START_UNEXPECTED_FIRST, input_index),
            NonterminalId(20) => self
                .scanner
                .match_any(&FOLLOW_SET_START_EXCLUDED_FIRST, input_index),
            _ => true,
        }
    }
    fn follow_set_terminals(&self, nonterminal_id: NonterminalId) -> Vec<TerminalId> {
        match nonterminal_id {
            NonterminalId(0) => FOLLOW_SET_T.terminals.to_vec(),
            NonterminalId(1) => FOLLOW_SET_U.terminals.to_vec(),
            NonterminalId(2) => FOLLOW_SET_P.terminals.to_vec(),
            NonterminalId(3) => FOLLOW_SET_Q.terminals.to_vec(),
            NonterminalId(4) => FOLLOW_SET_BEFORE_P.terminals.to_vec(),
            NonterminalId(5) => FOLLOW_SET_BEFORE_Q.terminals.to_vec(),
            NonterminalId(6) => FOLLOW_SET_UNEXPECTED_FIRST.terminals.to_vec(),
            NonterminalId(7) => FOLLOW_SET_EXCLUDED_FIRST.terminals.to_vec(),
            NonterminalId(8) => FOLLOW_SET_LAYOUT.terminals.to_vec(),
            NonterminalId(9) => FOLLOW_SET_ALT_0.terminals.to_vec(),
            NonterminalId(10) => FOLLOW_SET_PLUS_0.terminals.to_vec(),
            NonterminalId(11) => FOLLOW_SET_OPT_0.terminals.to_vec(),
            NonterminalId(12) => FOLLOW_SET_STAR_0.terminals.to_vec(),
            NonterminalId(13) => FOLLOW_SET_START_T.terminals.to_vec(),
            NonterminalId(14) => FOLLOW_SET_START_U.terminals.to_vec(),
            NonterminalId(15) => FOLLOW_SET_START_P.terminals.to_vec(),
            NonterminalId(16) => FOLLOW_SET_START_Q.terminals.to_vec(),
            NonterminalId(17) => FOLLOW_SET_START_BEFORE_P.terminals.to_vec(),
            NonterminalId(18) => FOLLOW_SET_START_BEFORE_Q.terminals.to_vec(),
            NonterminalId(19) => FOLLOW_SET_START_UNEXPECTED_FIRST.terminals.to_vec(),
            NonterminalId(20) => FOLLOW_SET_START_EXCLUDED_FIRST.terminals.to_vec(),
            _ => vec![],
        }
    }
    fn layout_terminals() -> &'static [TerminalId] {
        &[TerminalId(3), TerminalId(4)]
    }
    fn failures(&self) -> impl Iterator<Item = &GLLFailure> {
        self.failures.iter()
    }
    fn add_failure(
        &mut self,
        input_index: u32,
        slot_id: SlotId,
        gss_node_id: Option<GssNodeId>,
        kind: impl FnOnce() -> GLLFailureKind,
    ) {
        if self.suppress_failures {
            return;
        }
        let level = self.failures.first().map_or(0, |e| e.input_index);
        if input_index < level {
            record!(self, GLLFailure, input_index, slot_id, gss_node_id, kind());
            return;
        }
        let kind = kind();
        record!(
            self,
            GLLFailure,
            input_index,
            slot_id,
            gss_node_id,
            kind.clone()
        );
        if input_index > level {
            self.failures.clear();
        }
        self.failures.push(
            GLLFailure {
                input_index,
                slot_id,
                gss_node_id,
                kind,
            },
            self.vec_arena,
        );
    }
    fn match_token(&mut self, terminal_id: TerminalId, input_index: u32) -> Option<u32> {
        self.scanner.match_token(terminal_id, input_index)
    }
    fn vec_arena(&self) -> &'arena Arena {
        self.vec_arena
    }
}
pub struct ErrorUnionParser<'i, 'arena> {
    start_nonterminal: NonterminalId,
    // Arena backing the parser's internal collections; reset in bulk after the parse.
    vec_arena: &'arena Arena,
    scanner: ErrorUnionScanner<'i, 'arena>,
    descriptors: ArenaVec<'arena, Descriptor>,
    gss_nodes: ArenaVec<'arena, GSSNode<'arena>>,
    // Per-nonterminal GSS-node index keyed by input position.
    gss_nodes_index: [InlineMap<'arena, u32, GssNodeId>; 21],
    sppf_nodes: ArenaVec<'arena, SPPFNode>,
    #[cfg(feature = "instrument")]
    descriptors_count: usize,
    #[cfg(feature = "instrument")]
    descriptors_peak: usize,
    #[cfg(feature = "instrument")]
    ll1_call_log: Vec<(NonterminalId, u32)>,
    // Per-slot Span-keyed intermediate-node index, for slots in non-parameterized nonterminals.
    intermediate_nodes_index: [InlineMap<'arena, Span, SPPFNodeId>; 104],
    // Per-slot (Span, env)-keyed intermediate-node index, for slots in parameterized
    // nonterminals; env separates calls made with different parameter values.
    dd_intermediate_nodes_index: [InlineMap<'arena, (Span, Option<EnvId>), SPPFNodeId>; 0],
    terminal_nodes_index: [InlineMap<'arena, Span, SPPFNodeId>; 15],
    // Epsilon nodes keyed by input position; SPPFNodeId::NONE marks an empty slot.
    epsilon_nodes: ArenaVec<'arena, SPPFNodeId>,
    // An intermediate node keeps its first child inline. Children of intermediate nodes are
    // pairs: (left_child, right_child). Extra children, when there is ambiguity, are stored here
    // as (parent node, (left child, right child)).
    intermediate_nodes_children: ArenaVec<'arena, (SPPFNodeId, (SPPFNodeId, SPPFNodeId))>,
    // intermediate_nodes_children grouped by parent node, built lazily for tree construction.
    intermediate_nodes_children_map: OnceCell<FxHashMap<SPPFNodeId, Vec<(SPPFNodeId, SPPFNodeId)>>>,
    // Extra children of ambiguous nonterminal nodes, the counterpart to
    // intermediate_nodes_children: each entry is (parent node, (child, return slot)), a single
    // child plus its return slot rather than a pair.
    nonterminal_nodes_children: ArenaVec<'arena, (SPPFNodeId, (SPPFNodeId, SlotId))>,
    // nonterminal_nodes_children grouped by parent node, built lazily like
    // intermediate_nodes_children_map.
    nonterminal_nodes_children_map: OnceCell<FxHashMap<SPPFNodeId, Vec<(SPPFNodeId, SlotId)>>>,
    envs: ArenaVec<'arena, Env<'arena>>,
    failures: InlineVec<'arena, GLLFailure, 8>,
    // When true, `add_failure` is a no-op. The one user is the layout match of a `!>>>`
    // restriction: that parse is speculative, so its failure must not become the reported error.
    suppress_failures: bool,
    layout_memo: ArenaVec<'arena, Option<SPPFNodeId>>,
    #[cfg(feature = "debug-trace")]
    pub trace_events: Option<Vec<TraceEvent>>,
}
impl<'i, 'arena> ErrorUnionParser<'i, 'arena> {
    pub fn new(
        input: &'i Input,
        start_nonterminal: NonterminalId,
        vec_arena: &'arena Arena,
    ) -> Self {
        init_logger();
        Self {
            start_nonterminal,
            vec_arena,
            scanner: ErrorUnionScanner::new(input, vec_arena),
            gss_nodes_index: [const { InlineMap::Empty }; 21],
            descriptors: vec_arena.vec_with_capacity(
                input.len() as usize / DESCRIPTORS_CAPACITY_DIVISOR + DESCRIPTORS_CAPACITY_FLOOR,
            ),
            gss_nodes: vec_arena.vec_with_capacity(input.len() as usize * GSS_CAPACITY_MULTIPLIER),
            sppf_nodes: vec_arena
                .vec_with_capacity(input.len() as usize * SPPF_CAPACITY_MULTIPLIER),
            intermediate_nodes_index: [const { InlineMap::Empty }; 104],
            dd_intermediate_nodes_index: [],
            terminal_nodes_index: [const { InlineMap::Empty }; 15],
            epsilon_nodes: {
                let mut v = vec_arena.vec_with_capacity(input.len() as usize + 1);
                v.resize(input.len() as usize + 1, SPPFNodeId::NONE);
                v
            },
            #[cfg(feature = "instrument")]
            descriptors_count: 0,
            #[cfg(feature = "instrument")]
            descriptors_peak: 0,
            #[cfg(feature = "instrument")]
            ll1_call_log: vec![],
            intermediate_nodes_children: vec_arena.vec(),
            intermediate_nodes_children_map: OnceCell::new(),
            nonterminal_nodes_children: vec_arena.vec(),
            nonterminal_nodes_children_map: OnceCell::new(),
            envs: vec_arena.vec(),
            failures: InlineVec::Empty,
            suppress_failures: false,
            layout_memo: {
                let mut v = vec_arena.vec_with_capacity(input.len() as usize + 1);
                v.resize(input.len() as usize + 1, None);
                v
            },
            #[cfg(feature = "debug-trace")]
            trace_events: None,
        }
    }
    fn parse_q_ll1(&mut self, i: u32) -> Option<SPPFNodeId> {
        #[cfg(feature = "instrument")]
        self.ll1_call_log.push((NonterminalId(3), i));
        let matched = self.scanner.longest_match(&FIRST_SET_Q, i)?;
        match matched {
            TerminalId(8) => {
                let mut j = i;
                let right_child = {
                    let start = j;
                    let (end, node) =
                        self.match_terminal(TerminalId(8), start, SlotId(33), None)?;
                    j = end;
                    node
                };
                let left_extent = self.sppf_node(right_child).left_extent();
                let current = right_child;
                Some(self.add_nonterminal_node(NonterminalNode {
                    nonterminal_id: NonterminalId(3),
                    return_slot: SlotId(33),
                    span: Span {
                        left_extent,
                        right_extent: j,
                    },
                    child: current,
                    ambiguous: false,
                }))
            }
            _ => unreachable!("LL(1) dispatch covers every terminal in FIRST_SET"),
        }
    }
    fn parse_layout_ll1(&mut self, i: u32) -> Option<SPPFNodeId> {
        #[cfg(feature = "instrument")]
        self.ll1_call_log.push((NonterminalId(8), i));
        if let Some(memo) = self.layout_memo[i as usize] {
            return Some(memo);
        }
        let result: Option<SPPFNodeId> = (|| -> Option<SPPFNodeId> {
            let mut j = i;
            let right_child = {
                let start = j;
                let node = self.parse_star_0_ll1(start)?;
                let end = self.sppf_node(node).right_extent();
                j = end;
                node
            };
            let left_extent = self.sppf_node(right_child).left_extent();
            let current = right_child;
            Some(self.add_nonterminal_node(NonterminalNode {
                nonterminal_id: NonterminalId(8),
                return_slot: SlotId(57),
                span: Span {
                    left_extent,
                    right_extent: j,
                },
                child: current,
                ambiguous: false,
            }))
        })();
        if let Some(node) = result {
            self.layout_memo[i as usize] = Some(node);
        }
        result
    }
    fn parse_alt_0_ll1(&mut self, i: u32) -> Option<SPPFNodeId> {
        #[cfg(feature = "instrument")]
        self.ll1_call_log.push((NonterminalId(9), i));
        let matched = self.scanner.longest_match(&FIRST_SET_ALT_0, i)?;
        match matched {
            TerminalId(3) => {
                let mut j = i;
                let right_child = {
                    let start = j;
                    let (end, node) =
                        self.match_terminal(TerminalId(3), start, SlotId(59), None)?;
                    j = end;
                    node
                };
                let left_extent = self.sppf_node(right_child).left_extent();
                let current = right_child;
                Some(self.add_nonterminal_node(NonterminalNode {
                    nonterminal_id: NonterminalId(9),
                    return_slot: SlotId(59),
                    span: Span {
                        left_extent,
                        right_extent: j,
                    },
                    child: current,
                    ambiguous: false,
                }))
            }
            TerminalId(4) => {
                let mut j = i;
                let right_child = {
                    let start = j;
                    let (end, node) =
                        self.match_terminal(TerminalId(4), start, SlotId(61), None)?;
                    j = end;
                    node
                };
                let left_extent = self.sppf_node(right_child).left_extent();
                let current = right_child;
                Some(self.add_nonterminal_node(NonterminalNode {
                    nonterminal_id: NonterminalId(9),
                    return_slot: SlotId(61),
                    span: Span {
                        left_extent,
                        right_extent: j,
                    },
                    child: current,
                    ambiguous: false,
                }))
            }
            _ => unreachable!("LL(1) dispatch covers every terminal in FIRST_SET"),
        }
    }
    fn parse_plus_0_ll1(&mut self, i: u32) -> Option<SPPFNodeId> {
        #[cfg(feature = "instrument")]
        self.ll1_call_log.push((NonterminalId(10), i));
        let mut j = i;
        let (body_node, body_end) = (self.parse_alt_0_ll1(j).map(|node| {
            let end = self.sppf_node(node).right_extent();
            (node, end)
        }))?;
        j = body_end;
        let left_extent = i;
        let mut current = self.add_nonterminal_node(NonterminalNode {
            nonterminal_id: NonterminalId(10),
            return_slot: SlotId(66),
            span: Span {
                left_extent,
                right_extent: j,
            },
            child: body_node,
            ambiguous: false,
        });
        #[allow(clippy::while_let_loop)]
        loop {
            let Some((node_0, pos_0)) = self.parse_alt_0_ll1(j).map(|node| {
                let end = self.sppf_node(node).right_extent();
                (node, end)
            }) else {
                break;
            };
            j = pos_0;
            current =
                self.create_intermediate_node_ll1(SlotId(64), left_extent, pos_0, current, node_0);
            current = self.add_nonterminal_node(NonterminalNode {
                nonterminal_id: NonterminalId(10),
                return_slot: SlotId(64),
                span: Span {
                    left_extent,
                    right_extent: j,
                },
                child: current,
                ambiguous: false,
            });
        }
        Some(current)
    }
    fn parse_opt_0_ll1(&mut self, i: u32) -> Option<SPPFNodeId> {
        #[cfg(feature = "instrument")]
        self.ll1_call_log.push((NonterminalId(11), i));
        let Some(matched) = self.scanner.longest_match(&FIRST_SET_OPT_0, i) else {
            return {
                let epsilon_node_id = self.get_or_create_epsilon_node(i);
                Some(self.add_nonterminal_node(NonterminalNode {
                    nonterminal_id: NonterminalId(11),
                    return_slot: SlotId(69),
                    span: Span {
                        left_extent: i,
                        right_extent: i,
                    },
                    child: epsilon_node_id,
                    ambiguous: false,
                }))
            };
        };
        match matched {
            TerminalId(4) | TerminalId(3) => {
                let mut j = i;
                let right_child = {
                    let start = j;
                    let Some(node) = self.parse_plus_0_ll1(start) else {
                        self.add_failure(start, SlotId(68), None, || {
                            GLLFailureKind::UnexpectedToken {
                                expected: FIRST_SET_PLUS_0.terminals.to_vec(),
                            }
                        });
                        return None;
                    };
                    let end = self.sppf_node(node).right_extent();
                    j = end;
                    node
                };
                let left_extent = self.sppf_node(right_child).left_extent();
                let current = right_child;
                Some(self.add_nonterminal_node(NonterminalNode {
                    nonterminal_id: NonterminalId(11),
                    return_slot: SlotId(68),
                    span: Span {
                        left_extent,
                        right_extent: j,
                    },
                    child: current,
                    ambiguous: false,
                }))
            }
            _ => unreachable!("LL(1) dispatch covers every terminal in FIRST_SET"),
        }
    }
    fn parse_star_0_ll1(&mut self, i: u32) -> Option<SPPFNodeId> {
        #[cfg(feature = "instrument")]
        self.ll1_call_log.push((NonterminalId(12), i));
        let mut j = i;
        let right_child = {
            let start = j;
            let node = self.parse_opt_0_ll1(start)?;
            let end = self.sppf_node(node).right_extent();
            j = end;
            node
        };
        let left_extent = self.sppf_node(right_child).left_extent();
        let current = right_child;
        Some(self.add_nonterminal_node(NonterminalNode {
            nonterminal_id: NonterminalId(12),
            return_slot: SlotId(71),
            span: Span {
                left_extent,
                right_extent: j,
            },
            child: current,
            ambiguous: false,
        }))
    }
    fn get_or_create_epsilon_node(&mut self, i: u32) -> SPPFNodeId {
        let existing = self.epsilon_nodes[i as usize];
        if existing != SPPFNodeId::NONE {
            record!(self, TerminalNodeFound, existing);
            return existing;
        }
        let span = Span::new(i, i);
        let terminal_id = TerminalId(13);
        let node_id = SPPFNodeId(self.sppf_nodes.len() as u32);
        record!(self, TerminalNodeCreated, terminal_id, span);
        self.sppf_nodes
            .push(SPPFNode::Terminal(TerminalNode { terminal_id, span }));
        self.epsilon_nodes[i as usize] = node_id;
        node_id
    }
    // True if a local ambiguity node was added during parsing. This does not guarantee the
    // ambiguity is reachable from the root, so a tree walk is still needed to confirm it.
    pub fn ambiguity_node_added(&self) -> bool {
        !self.intermediate_nodes_children.is_empty() || !self.nonterminal_nodes_children.is_empty()
    }
}
