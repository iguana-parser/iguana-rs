// @generated by iguana. Do not edit manually.
// grammar Group
// 
// A
//   = A_Group_0
// 
// B
//   = "b"
// 
// C
//   = "c"
// 
// D
//   = "d"
// 
// A_Group_0
//   = B Layout C Layout D
// 
// StartA
//   = Layout start:A Layout
// 
// StartB
//   = Layout start:B Layout
// 
// StartC
//   = Layout start:C Layout
// 
// StartD
//   = Layout start:D Layout
// 
// "b" = b
// "c" = c
// "d" = d
// Layout = ε
use std::cell::OnceCell;
use crate::{scanner::GroupScanner, types::{EbnfKind, Nonterminal, Slot, Terminal}};
use iguana_runtime::{
    descriptor::Descriptor, env::{Env, EnvId},
    gss::{GSSNode, PoppedElement},
    ids::{GssNodeId, NonterminalId, SlotId, TerminalId},
    input::Input, parser::{Parser, Stats, init_logger},
    record, scanner::Scanner,
    sppf::{IntermediateNode, NonterminalNode, SPPFNode, SPPFNodeId, Span, TerminalNode},
    utils::{inline_map::InlineMap, inline_vec::InlineVec},
};
#[cfg(feature = "debug-trace")]
use iguana_runtime::trace::TraceEvent;
use rustc_hash::FxHashMap;
use phf::phf_map;
pub const NONTERMINALS: [Nonterminal; 9] = [
    Nonterminal {
        name: "A",
        display: "A",
        kind: None,
    },
    Nonterminal {
        name: "B",
        display: "B",
        kind: None,
    },
    Nonterminal {
        name: "C",
        display: "C",
        kind: None,
    },
    Nonterminal {
        name: "D",
        display: "D",
        kind: None,
    },
    Nonterminal {
        name: "A_Group_0",
        display: "(B C D)",
        kind: Some(EbnfKind::Group),
    },
    Nonterminal {
        name: "StartA",
        display: "StartA",
        kind: None,
    },
    Nonterminal {
        name: "StartB",
        display: "StartB",
        kind: None,
    },
    Nonterminal {
        name: "StartC",
        display: "StartC",
        kind: None,
    },
    Nonterminal {
        name: "StartD",
        display: "StartD",
        kind: None,
    },
];
static NONTERMINAL_IDS: phf::Map<&'static str, NonterminalId> = phf_map! {
    "A" => NonterminalId(0), "B" => NonterminalId(1), "C" => NonterminalId(2), "D" =>
    NonterminalId(3), "A_Group_0" => NonterminalId(4), "StartA" => NonterminalId(5),
    "StartB" => NonterminalId(6), "StartC" => NonterminalId(7), "StartD" =>
    NonterminalId(8)
};
pub const TERMINALS: [Terminal; 5] = [
    Terminal { name: "\"b\"" },
    Terminal { name: "\"c\"" },
    Terminal { name: "\"d\"" },
    Terminal { name: "Layout" },
    Terminal { name: "Epsilon" },
];
pub const SLOTS: [Slot; 30] = [
    Slot {
        display_name: "A : . (B C D)",
    },
    Slot {
        display_name: "A : (B C D).",
    },
    Slot {
        display_name: "B : . \"b\"",
    },
    Slot { display_name: "B : \"b\"." },
    Slot {
        display_name: "C : . \"c\"",
    },
    Slot { display_name: "C : \"c\"." },
    Slot {
        display_name: "D : . \"d\"",
    },
    Slot { display_name: "D : \"d\"." },
    Slot {
        display_name: "(B C D) : . B Layout C Layout D",
    },
    Slot {
        display_name: "(B C D) : B . Layout C Layout D",
    },
    Slot {
        display_name: "(B C D) : B Layout . C Layout D",
    },
    Slot {
        display_name: "(B C D) : B Layout C . Layout D",
    },
    Slot {
        display_name: "(B C D) : B Layout C Layout . D",
    },
    Slot {
        display_name: "(B C D) : B Layout C Layout D.",
    },
    Slot {
        display_name: "StartA : . Layout start:A Layout",
    },
    Slot {
        display_name: "StartA : Layout . start:A Layout",
    },
    Slot {
        display_name: "StartA : Layout start:A . Layout",
    },
    Slot {
        display_name: "StartA : Layout start:A Layout.",
    },
    Slot {
        display_name: "StartB : . Layout start:B Layout",
    },
    Slot {
        display_name: "StartB : Layout . start:B Layout",
    },
    Slot {
        display_name: "StartB : Layout start:B . Layout",
    },
    Slot {
        display_name: "StartB : Layout start:B Layout.",
    },
    Slot {
        display_name: "StartC : . Layout start:C Layout",
    },
    Slot {
        display_name: "StartC : Layout . start:C Layout",
    },
    Slot {
        display_name: "StartC : Layout start:C . Layout",
    },
    Slot {
        display_name: "StartC : Layout start:C Layout.",
    },
    Slot {
        display_name: "StartD : . Layout start:D Layout",
    },
    Slot {
        display_name: "StartD : Layout . start:D Layout",
    },
    Slot {
        display_name: "StartD : Layout start:D . Layout",
    },
    Slot {
        display_name: "StartD : Layout start:D Layout.",
    },
];
impl<'i> Parser<'i> for GroupParser<'i> {
    fn nonterminal_display_name(nonterminal_id: NonterminalId) -> &'static str {
        NONTERMINALS[nonterminal_id.index()].display
    }
    fn nonterminal_id(name: &str) -> Option<NonterminalId> {
        NONTERMINAL_IDS.get(name).copied()
    }
    fn terminal_name(terminal_id: TerminalId) -> &'static str {
        TERMINALS[terminal_id.index()].name
    }
    fn slot_name(slot_id: SlotId) -> &'static str {
        SLOTS[slot_id.index()].display_name
    }
    fn epsilon() -> TerminalId {
        TerminalId((TERMINALS.len() - 1) as u16)
    }
    fn execute(
        &mut self,
        input_index: u32,
        slot_id: SlotId,
        result: Option<SPPFNodeId>,
        gss_node_id: GssNodeId,
        env: Option<EnvId>,
    ) {
        record!(self, ProcessingDescriptor, input_index, slot_id, result, gss_node_id);
        match slot_id {
            //A : . A_Group_0
            SlotId(0) => {
                let i = input_index;
                if let Some(right_child_id) = self.parse_a_group_0_ll1(i) {
                    let j = self.sppf_node(right_child_id).right_extent();
                    //A : A_Group_0.
                    let next_slot_id = SlotId(1);
                    let new_node = right_child_id;
                    self.execute(j, next_slot_id, Some(new_node), gss_node_id, env);
                }
            }
            //A : A_Group_0.
            SlotId(1) => {
                let Some(result) = result else {
                    unreachable!("result cannot be None here.")
                };
                let node = self.sppf_node(result);
                let left_extent = node.left_extent();
                let right_extent = node.right_extent();
                let nonterminal_id = NonterminalId(0);
                let end_slot_id = SlotId(1);
                if let Some(nonterminal_node_id) = self
                    .create_nonterminal_node_or_attach_children(
                        nonterminal_id,
                        end_slot_id,
                        left_extent,
                        right_extent,
                        result,
                    )
                {
                    let popped_element = PoppedElement {
                        nonterminal_node_id,
                        return_value: None,
                    };
                    self.pop(gss_node_id, end_slot_id, popped_element);
                }
            }
            //B : . "b"
            SlotId(2) => {
                let i = input_index;
                record!(self, MatchingTerminal, "\"b\"", i);
                match self.scanner.match_token(TerminalId(0), i) {
                    Some(j) => {
                        record!(self, MatchSuccess, "\"b\"", i, j);
                        let right_child_id = self
                            .get_or_create_terminal_node(TerminalId(0), i, j);
                        //B : "b".
                        let next_slot_id = SlotId(3);
                        let new_node = right_child_id;
                        self.execute(j, next_slot_id, Some(new_node), gss_node_id, env);
                    }
                    None => {
                        record!(
                            self, MatchFailed, "\"b\"", i, SlotId(2), gss_node_id, result
                        );
                    }
                }
            }
            //B : "b".
            SlotId(3) => {
                let Some(result) = result else {
                    unreachable!("result cannot be None here.")
                };
                let node = self.sppf_node(result);
                let left_extent = node.left_extent();
                let right_extent = node.right_extent();
                let nonterminal_id = NonterminalId(1);
                let end_slot_id = SlotId(3);
                if let Some(nonterminal_node_id) = self
                    .create_nonterminal_node_or_attach_children(
                        nonterminal_id,
                        end_slot_id,
                        left_extent,
                        right_extent,
                        result,
                    )
                {
                    let popped_element = PoppedElement {
                        nonterminal_node_id,
                        return_value: None,
                    };
                    self.pop(gss_node_id, end_slot_id, popped_element);
                }
            }
            //C : . "c"
            SlotId(4) => {
                let i = input_index;
                record!(self, MatchingTerminal, "\"c\"", i);
                match self.scanner.match_token(TerminalId(1), i) {
                    Some(j) => {
                        record!(self, MatchSuccess, "\"c\"", i, j);
                        let right_child_id = self
                            .get_or_create_terminal_node(TerminalId(1), i, j);
                        //C : "c".
                        let next_slot_id = SlotId(5);
                        let new_node = right_child_id;
                        self.execute(j, next_slot_id, Some(new_node), gss_node_id, env);
                    }
                    None => {
                        record!(
                            self, MatchFailed, "\"c\"", i, SlotId(4), gss_node_id, result
                        );
                    }
                }
            }
            //C : "c".
            SlotId(5) => {
                let Some(result) = result else {
                    unreachable!("result cannot be None here.")
                };
                let node = self.sppf_node(result);
                let left_extent = node.left_extent();
                let right_extent = node.right_extent();
                let nonterminal_id = NonterminalId(2);
                let end_slot_id = SlotId(5);
                if let Some(nonterminal_node_id) = self
                    .create_nonterminal_node_or_attach_children(
                        nonterminal_id,
                        end_slot_id,
                        left_extent,
                        right_extent,
                        result,
                    )
                {
                    let popped_element = PoppedElement {
                        nonterminal_node_id,
                        return_value: None,
                    };
                    self.pop(gss_node_id, end_slot_id, popped_element);
                }
            }
            //D : . "d"
            SlotId(6) => {
                let i = input_index;
                record!(self, MatchingTerminal, "\"d\"", i);
                match self.scanner.match_token(TerminalId(2), i) {
                    Some(j) => {
                        record!(self, MatchSuccess, "\"d\"", i, j);
                        let right_child_id = self
                            .get_or_create_terminal_node(TerminalId(2), i, j);
                        //D : "d".
                        let next_slot_id = SlotId(7);
                        let new_node = right_child_id;
                        self.execute(j, next_slot_id, Some(new_node), gss_node_id, env);
                    }
                    None => {
                        record!(
                            self, MatchFailed, "\"d\"", i, SlotId(6), gss_node_id, result
                        );
                    }
                }
            }
            //D : "d".
            SlotId(7) => {
                let Some(result) = result else {
                    unreachable!("result cannot be None here.")
                };
                let node = self.sppf_node(result);
                let left_extent = node.left_extent();
                let right_extent = node.right_extent();
                let nonterminal_id = NonterminalId(3);
                let end_slot_id = SlotId(7);
                if let Some(nonterminal_node_id) = self
                    .create_nonterminal_node_or_attach_children(
                        nonterminal_id,
                        end_slot_id,
                        left_extent,
                        right_extent,
                        result,
                    )
                {
                    let popped_element = PoppedElement {
                        nonterminal_node_id,
                        return_value: None,
                    };
                    self.pop(gss_node_id, end_slot_id, popped_element);
                }
            }
            //A_Group_0 : . B Layout C Layout D
            SlotId(8) => {
                let i = input_index;
                if let Some(right_child_id) = self.parse_b_ll1(i) {
                    let j = self.sppf_node(right_child_id).right_extent();
                    //A_Group_0 : B . Layout C Layout D
                    let next_slot_id = SlotId(9);
                    let new_node = right_child_id;
                    self.execute(j, next_slot_id, Some(new_node), gss_node_id, env);
                }
            }
            //A_Group_0 : B . Layout C Layout D
            SlotId(9) => {
                let i = input_index;
                record!(self, MatchingTerminal, "Layout", i);
                match self.scanner.match_token(TerminalId(3), i) {
                    Some(j) => {
                        record!(self, MatchSuccess, "Layout", i, j);
                        let right_child_id = self
                            .get_or_create_terminal_node(TerminalId(3), i, j);
                        //A_Group_0 : B Layout . C Layout D
                        let next_slot_id = SlotId(10);
                        let left_child_id = result.expect("Result should not be None.");
                        let left_child = self.sppf_node(left_child_id);
                        let left_extent = left_child.left_extent();
                        if let Some(new_node) = self
                            .create_intermediate_node_or_attach_children(
                                next_slot_id,
                                left_extent,
                                j,
                                left_child_id,
                                right_child_id,
                            )
                        {
                            self.execute(
                                j,
                                next_slot_id,
                                Some(new_node),
                                gss_node_id,
                                env,
                            );
                        }
                    }
                    None => {
                        record!(
                            self, MatchFailed, "Layout", i, SlotId(9), gss_node_id,
                            result
                        );
                    }
                }
            }
            //A_Group_0 : B Layout . C Layout D
            SlotId(10) => {
                let i = input_index;
                if let Some(right_child_id) = self.parse_c_ll1(i) {
                    let j = self.sppf_node(right_child_id).right_extent();
                    //A_Group_0 : B Layout C . Layout D
                    let next_slot_id = SlotId(11);
                    let left_child_id = result.expect("Result should not be None.");
                    let left_child = self.sppf_node(left_child_id);
                    let left_extent = left_child.left_extent();
                    if let Some(new_node) = self
                        .create_intermediate_node_or_attach_children(
                            next_slot_id,
                            left_extent,
                            j,
                            left_child_id,
                            right_child_id,
                        )
                    {
                        self.execute(j, next_slot_id, Some(new_node), gss_node_id, env);
                    }
                }
            }
            //A_Group_0 : B Layout C . Layout D
            SlotId(11) => {
                let i = input_index;
                record!(self, MatchingTerminal, "Layout", i);
                match self.scanner.match_token(TerminalId(3), i) {
                    Some(j) => {
                        record!(self, MatchSuccess, "Layout", i, j);
                        let right_child_id = self
                            .get_or_create_terminal_node(TerminalId(3), i, j);
                        //A_Group_0 : B Layout C Layout . D
                        let next_slot_id = SlotId(12);
                        let left_child_id = result.expect("Result should not be None.");
                        let left_child = self.sppf_node(left_child_id);
                        let left_extent = left_child.left_extent();
                        if let Some(new_node) = self
                            .create_intermediate_node_or_attach_children(
                                next_slot_id,
                                left_extent,
                                j,
                                left_child_id,
                                right_child_id,
                            )
                        {
                            self.execute(
                                j,
                                next_slot_id,
                                Some(new_node),
                                gss_node_id,
                                env,
                            );
                        }
                    }
                    None => {
                        record!(
                            self, MatchFailed, "Layout", i, SlotId(11), gss_node_id,
                            result
                        );
                    }
                }
            }
            //A_Group_0 : B Layout C Layout . D
            SlotId(12) => {
                let i = input_index;
                if let Some(right_child_id) = self.parse_d_ll1(i) {
                    let j = self.sppf_node(right_child_id).right_extent();
                    //A_Group_0 : B Layout C Layout D.
                    let next_slot_id = SlotId(13);
                    let left_child_id = result.expect("Result should not be None.");
                    let left_child = self.sppf_node(left_child_id);
                    let left_extent = left_child.left_extent();
                    if let Some(new_node) = self
                        .create_intermediate_node_or_attach_children(
                            next_slot_id,
                            left_extent,
                            j,
                            left_child_id,
                            right_child_id,
                        )
                    {
                        self.execute(j, next_slot_id, Some(new_node), gss_node_id, env);
                    }
                }
            }
            //A_Group_0 : B Layout C Layout D.
            SlotId(13) => {
                let Some(result) = result else {
                    unreachable!("result cannot be None here.")
                };
                let node = self.sppf_node(result);
                let left_extent = node.left_extent();
                let right_extent = node.right_extent();
                let nonterminal_id = NonterminalId(4);
                let end_slot_id = SlotId(13);
                if let Some(nonterminal_node_id) = self
                    .create_nonterminal_node_or_attach_children(
                        nonterminal_id,
                        end_slot_id,
                        left_extent,
                        right_extent,
                        result,
                    )
                {
                    let popped_element = PoppedElement {
                        nonterminal_node_id,
                        return_value: None,
                    };
                    self.pop(gss_node_id, end_slot_id, popped_element);
                }
            }
            //StartA : . Layout start:A Layout
            SlotId(14) => {
                let i = input_index;
                record!(self, MatchingTerminal, "Layout", i);
                match self.scanner.match_token(TerminalId(3), i) {
                    Some(j) => {
                        record!(self, MatchSuccess, "Layout", i, j);
                        let right_child_id = self
                            .get_or_create_terminal_node(TerminalId(3), i, j);
                        //StartA : Layout . start:A Layout
                        let next_slot_id = SlotId(15);
                        let new_node = right_child_id;
                        self.execute(j, next_slot_id, Some(new_node), gss_node_id, env);
                    }
                    None => {
                        record!(
                            self, MatchFailed, "Layout", i, SlotId(14), gss_node_id,
                            result
                        );
                    }
                }
            }
            //StartA : Layout . start:A Layout
            SlotId(15) => {
                let i = input_index;
                if let Some(right_child_id) = self.parse_a_ll1(i) {
                    let j = self.sppf_node(right_child_id).right_extent();
                    //StartA : Layout start:A . Layout
                    let next_slot_id = SlotId(16);
                    let left_child_id = result.expect("Result should not be None.");
                    let left_child = self.sppf_node(left_child_id);
                    let left_extent = left_child.left_extent();
                    if let Some(new_node) = self
                        .create_intermediate_node_or_attach_children(
                            next_slot_id,
                            left_extent,
                            j,
                            left_child_id,
                            right_child_id,
                        )
                    {
                        self.execute(j, next_slot_id, Some(new_node), gss_node_id, env);
                    }
                }
            }
            //StartA : Layout start:A . Layout
            SlotId(16) => {
                let i = input_index;
                record!(self, MatchingTerminal, "Layout", i);
                match self.scanner.match_token(TerminalId(3), i) {
                    Some(j) => {
                        record!(self, MatchSuccess, "Layout", i, j);
                        let right_child_id = self
                            .get_or_create_terminal_node(TerminalId(3), i, j);
                        //StartA : Layout start:A Layout.
                        let next_slot_id = SlotId(17);
                        let left_child_id = result.expect("Result should not be None.");
                        let left_child = self.sppf_node(left_child_id);
                        let left_extent = left_child.left_extent();
                        if let Some(new_node) = self
                            .create_intermediate_node_or_attach_children(
                                next_slot_id,
                                left_extent,
                                j,
                                left_child_id,
                                right_child_id,
                            )
                        {
                            self.execute(
                                j,
                                next_slot_id,
                                Some(new_node),
                                gss_node_id,
                                env,
                            );
                        }
                    }
                    None => {
                        record!(
                            self, MatchFailed, "Layout", i, SlotId(16), gss_node_id,
                            result
                        );
                    }
                }
            }
            //StartA : Layout start:A Layout.
            SlotId(17) => {
                let Some(result) = result else {
                    unreachable!("result cannot be None here.")
                };
                let node = self.sppf_node(result);
                let left_extent = node.left_extent();
                let right_extent = node.right_extent();
                let nonterminal_id = NonterminalId(5);
                let end_slot_id = SlotId(17);
                if let Some(nonterminal_node_id) = self
                    .create_nonterminal_node_or_attach_children(
                        nonterminal_id,
                        end_slot_id,
                        left_extent,
                        right_extent,
                        result,
                    )
                {
                    let popped_element = PoppedElement {
                        nonterminal_node_id,
                        return_value: None,
                    };
                    self.pop(gss_node_id, end_slot_id, popped_element);
                }
            }
            //StartB : . Layout start:B Layout
            SlotId(18) => {
                let i = input_index;
                record!(self, MatchingTerminal, "Layout", i);
                match self.scanner.match_token(TerminalId(3), i) {
                    Some(j) => {
                        record!(self, MatchSuccess, "Layout", i, j);
                        let right_child_id = self
                            .get_or_create_terminal_node(TerminalId(3), i, j);
                        //StartB : Layout . start:B Layout
                        let next_slot_id = SlotId(19);
                        let new_node = right_child_id;
                        self.execute(j, next_slot_id, Some(new_node), gss_node_id, env);
                    }
                    None => {
                        record!(
                            self, MatchFailed, "Layout", i, SlotId(18), gss_node_id,
                            result
                        );
                    }
                }
            }
            //StartB : Layout . start:B Layout
            SlotId(19) => {
                let i = input_index;
                if let Some(right_child_id) = self.parse_b_ll1(i) {
                    let j = self.sppf_node(right_child_id).right_extent();
                    //StartB : Layout start:B . Layout
                    let next_slot_id = SlotId(20);
                    let left_child_id = result.expect("Result should not be None.");
                    let left_child = self.sppf_node(left_child_id);
                    let left_extent = left_child.left_extent();
                    if let Some(new_node) = self
                        .create_intermediate_node_or_attach_children(
                            next_slot_id,
                            left_extent,
                            j,
                            left_child_id,
                            right_child_id,
                        )
                    {
                        self.execute(j, next_slot_id, Some(new_node), gss_node_id, env);
                    }
                }
            }
            //StartB : Layout start:B . Layout
            SlotId(20) => {
                let i = input_index;
                record!(self, MatchingTerminal, "Layout", i);
                match self.scanner.match_token(TerminalId(3), i) {
                    Some(j) => {
                        record!(self, MatchSuccess, "Layout", i, j);
                        let right_child_id = self
                            .get_or_create_terminal_node(TerminalId(3), i, j);
                        //StartB : Layout start:B Layout.
                        let next_slot_id = SlotId(21);
                        let left_child_id = result.expect("Result should not be None.");
                        let left_child = self.sppf_node(left_child_id);
                        let left_extent = left_child.left_extent();
                        if let Some(new_node) = self
                            .create_intermediate_node_or_attach_children(
                                next_slot_id,
                                left_extent,
                                j,
                                left_child_id,
                                right_child_id,
                            )
                        {
                            self.execute(
                                j,
                                next_slot_id,
                                Some(new_node),
                                gss_node_id,
                                env,
                            );
                        }
                    }
                    None => {
                        record!(
                            self, MatchFailed, "Layout", i, SlotId(20), gss_node_id,
                            result
                        );
                    }
                }
            }
            //StartB : Layout start:B Layout.
            SlotId(21) => {
                let Some(result) = result else {
                    unreachable!("result cannot be None here.")
                };
                let node = self.sppf_node(result);
                let left_extent = node.left_extent();
                let right_extent = node.right_extent();
                let nonterminal_id = NonterminalId(6);
                let end_slot_id = SlotId(21);
                if let Some(nonterminal_node_id) = self
                    .create_nonterminal_node_or_attach_children(
                        nonterminal_id,
                        end_slot_id,
                        left_extent,
                        right_extent,
                        result,
                    )
                {
                    let popped_element = PoppedElement {
                        nonterminal_node_id,
                        return_value: None,
                    };
                    self.pop(gss_node_id, end_slot_id, popped_element);
                }
            }
            //StartC : . Layout start:C Layout
            SlotId(22) => {
                let i = input_index;
                record!(self, MatchingTerminal, "Layout", i);
                match self.scanner.match_token(TerminalId(3), i) {
                    Some(j) => {
                        record!(self, MatchSuccess, "Layout", i, j);
                        let right_child_id = self
                            .get_or_create_terminal_node(TerminalId(3), i, j);
                        //StartC : Layout . start:C Layout
                        let next_slot_id = SlotId(23);
                        let new_node = right_child_id;
                        self.execute(j, next_slot_id, Some(new_node), gss_node_id, env);
                    }
                    None => {
                        record!(
                            self, MatchFailed, "Layout", i, SlotId(22), gss_node_id,
                            result
                        );
                    }
                }
            }
            //StartC : Layout . start:C Layout
            SlotId(23) => {
                let i = input_index;
                if let Some(right_child_id) = self.parse_c_ll1(i) {
                    let j = self.sppf_node(right_child_id).right_extent();
                    //StartC : Layout start:C . Layout
                    let next_slot_id = SlotId(24);
                    let left_child_id = result.expect("Result should not be None.");
                    let left_child = self.sppf_node(left_child_id);
                    let left_extent = left_child.left_extent();
                    if let Some(new_node) = self
                        .create_intermediate_node_or_attach_children(
                            next_slot_id,
                            left_extent,
                            j,
                            left_child_id,
                            right_child_id,
                        )
                    {
                        self.execute(j, next_slot_id, Some(new_node), gss_node_id, env);
                    }
                }
            }
            //StartC : Layout start:C . Layout
            SlotId(24) => {
                let i = input_index;
                record!(self, MatchingTerminal, "Layout", i);
                match self.scanner.match_token(TerminalId(3), i) {
                    Some(j) => {
                        record!(self, MatchSuccess, "Layout", i, j);
                        let right_child_id = self
                            .get_or_create_terminal_node(TerminalId(3), i, j);
                        //StartC : Layout start:C Layout.
                        let next_slot_id = SlotId(25);
                        let left_child_id = result.expect("Result should not be None.");
                        let left_child = self.sppf_node(left_child_id);
                        let left_extent = left_child.left_extent();
                        if let Some(new_node) = self
                            .create_intermediate_node_or_attach_children(
                                next_slot_id,
                                left_extent,
                                j,
                                left_child_id,
                                right_child_id,
                            )
                        {
                            self.execute(
                                j,
                                next_slot_id,
                                Some(new_node),
                                gss_node_id,
                                env,
                            );
                        }
                    }
                    None => {
                        record!(
                            self, MatchFailed, "Layout", i, SlotId(24), gss_node_id,
                            result
                        );
                    }
                }
            }
            //StartC : Layout start:C Layout.
            SlotId(25) => {
                let Some(result) = result else {
                    unreachable!("result cannot be None here.")
                };
                let node = self.sppf_node(result);
                let left_extent = node.left_extent();
                let right_extent = node.right_extent();
                let nonterminal_id = NonterminalId(7);
                let end_slot_id = SlotId(25);
                if let Some(nonterminal_node_id) = self
                    .create_nonterminal_node_or_attach_children(
                        nonterminal_id,
                        end_slot_id,
                        left_extent,
                        right_extent,
                        result,
                    )
                {
                    let popped_element = PoppedElement {
                        nonterminal_node_id,
                        return_value: None,
                    };
                    self.pop(gss_node_id, end_slot_id, popped_element);
                }
            }
            //StartD : . Layout start:D Layout
            SlotId(26) => {
                let i = input_index;
                record!(self, MatchingTerminal, "Layout", i);
                match self.scanner.match_token(TerminalId(3), i) {
                    Some(j) => {
                        record!(self, MatchSuccess, "Layout", i, j);
                        let right_child_id = self
                            .get_or_create_terminal_node(TerminalId(3), i, j);
                        //StartD : Layout . start:D Layout
                        let next_slot_id = SlotId(27);
                        let new_node = right_child_id;
                        self.execute(j, next_slot_id, Some(new_node), gss_node_id, env);
                    }
                    None => {
                        record!(
                            self, MatchFailed, "Layout", i, SlotId(26), gss_node_id,
                            result
                        );
                    }
                }
            }
            //StartD : Layout . start:D Layout
            SlotId(27) => {
                let i = input_index;
                if let Some(right_child_id) = self.parse_d_ll1(i) {
                    let j = self.sppf_node(right_child_id).right_extent();
                    //StartD : Layout start:D . Layout
                    let next_slot_id = SlotId(28);
                    let left_child_id = result.expect("Result should not be None.");
                    let left_child = self.sppf_node(left_child_id);
                    let left_extent = left_child.left_extent();
                    if let Some(new_node) = self
                        .create_intermediate_node_or_attach_children(
                            next_slot_id,
                            left_extent,
                            j,
                            left_child_id,
                            right_child_id,
                        )
                    {
                        self.execute(j, next_slot_id, Some(new_node), gss_node_id, env);
                    }
                }
            }
            //StartD : Layout start:D . Layout
            SlotId(28) => {
                let i = input_index;
                record!(self, MatchingTerminal, "Layout", i);
                match self.scanner.match_token(TerminalId(3), i) {
                    Some(j) => {
                        record!(self, MatchSuccess, "Layout", i, j);
                        let right_child_id = self
                            .get_or_create_terminal_node(TerminalId(3), i, j);
                        //StartD : Layout start:D Layout.
                        let next_slot_id = SlotId(29);
                        let left_child_id = result.expect("Result should not be None.");
                        let left_child = self.sppf_node(left_child_id);
                        let left_extent = left_child.left_extent();
                        if let Some(new_node) = self
                            .create_intermediate_node_or_attach_children(
                                next_slot_id,
                                left_extent,
                                j,
                                left_child_id,
                                right_child_id,
                            )
                        {
                            self.execute(
                                j,
                                next_slot_id,
                                Some(new_node),
                                gss_node_id,
                                env,
                            );
                        }
                    }
                    None => {
                        record!(
                            self, MatchFailed, "Layout", i, SlotId(28), gss_node_id,
                            result
                        );
                    }
                }
            }
            //StartD : Layout start:D Layout.
            SlotId(29) => {
                let Some(result) = result else {
                    unreachable!("result cannot be None here.")
                };
                let node = self.sppf_node(result);
                let left_extent = node.left_extent();
                let right_extent = node.right_extent();
                let nonterminal_id = NonterminalId(8);
                let end_slot_id = SlotId(29);
                if let Some(nonterminal_node_id) = self
                    .create_nonterminal_node_or_attach_children(
                        nonterminal_id,
                        end_slot_id,
                        left_extent,
                        right_extent,
                        result,
                    )
                {
                    let popped_element = PoppedElement {
                        nonterminal_node_id,
                        return_value: None,
                    };
                    self.pop(gss_node_id, end_slot_id, popped_element);
                }
            }
            _ => {
                panic!("Unknown grammar slot id: {slot_id}");
            }
        }
    }
    fn add_first_descriptors(
        &mut self,
        nonterminal_id: NonterminalId,
        input_index: u32,
        gss_node_id: GssNodeId,
        env: Option<EnvId>,
    ) {
        match nonterminal_id {
            //A
            NonterminalId(0) => {
                //A : . A_Group_0
                self.add_descriptor(Descriptor {
                    input_index,
                    slot_id: SlotId(0),
                    sppf_node_id: None,
                    gss_node_id,
                    env,
                });
            }
            //B
            NonterminalId(1) => {
                //B : . "b"
                self.add_descriptor(Descriptor {
                    input_index,
                    slot_id: SlotId(2),
                    sppf_node_id: None,
                    gss_node_id,
                    env,
                });
            }
            //C
            NonterminalId(2) => {
                //C : . "c"
                self.add_descriptor(Descriptor {
                    input_index,
                    slot_id: SlotId(4),
                    sppf_node_id: None,
                    gss_node_id,
                    env,
                });
            }
            //D
            NonterminalId(3) => {
                //D : . "d"
                self.add_descriptor(Descriptor {
                    input_index,
                    slot_id: SlotId(6),
                    sppf_node_id: None,
                    gss_node_id,
                    env,
                });
            }
            //A_Group_0
            NonterminalId(4) => {
                //A_Group_0 : . B Layout C Layout D
                self.add_descriptor(Descriptor {
                    input_index,
                    slot_id: SlotId(8),
                    sppf_node_id: None,
                    gss_node_id,
                    env,
                });
            }
            //StartA
            NonterminalId(5) => {
                //StartA : . Layout start:A Layout
                self.add_descriptor(Descriptor {
                    input_index,
                    slot_id: SlotId(14),
                    sppf_node_id: None,
                    gss_node_id,
                    env,
                });
            }
            //StartB
            NonterminalId(6) => {
                //StartB : . Layout start:B Layout
                self.add_descriptor(Descriptor {
                    input_index,
                    slot_id: SlotId(18),
                    sppf_node_id: None,
                    gss_node_id,
                    env,
                });
            }
            //StartC
            NonterminalId(7) => {
                //StartC : . Layout start:C Layout
                self.add_descriptor(Descriptor {
                    input_index,
                    slot_id: SlotId(22),
                    sppf_node_id: None,
                    gss_node_id,
                    env,
                });
            }
            //StartD
            NonterminalId(8) => {
                //StartD : . Layout start:D Layout
                self.add_descriptor(Descriptor {
                    input_index,
                    slot_id: SlotId(26),
                    sppf_node_id: None,
                    gss_node_id,
                    env,
                });
            }
            _ => {
                panic!("Unknown nonterminal id: {nonterminal_id}");
            }
        }
    }
    fn get_gss_node(
        &self,
        nonterminal_id: NonterminalId,
        input_index: u32,
    ) -> Option<GssNodeId> {
        let gss_nodes = &self.gss_nodes_index[nonterminal_id.index()];
        gss_nodes.iter().find(|(k, _)| *k == input_index).map(|x| x.1)
    }
    fn add_gss_node(
        &mut self,
        nonterminal_id: NonterminalId,
        input_index: u32,
        gss_node_id: GssNodeId,
    ) {
        let gss_nodes = &mut self.gss_nodes_index[nonterminal_id.index()];
        gss_nodes.push((input_index, gss_node_id));
    }
    fn new_gss_node(
        &mut self,
        nonterminal_id: NonterminalId,
        input_index: u32,
    ) -> GssNodeId {
        let gss_node_id = GssNodeId(self.gss_nodes.len() as u32);
        let gss_node = GSSNode::new(gss_node_id, nonterminal_id, input_index);
        record!(self, GSSNodeCreated, nonterminal_id, input_index);
        self.gss_nodes.push(gss_node);
        self.stats.gss_nodes_count += 1;
        gss_node_id
    }
    fn gss_node(&self, id: GssNodeId) -> &GSSNode {
        &self.gss_nodes[id.index()]
    }
    fn gss_node_mut(&mut self, id: GssNodeId) -> &mut GSSNode {
        self.gss_nodes.get_mut(id.index()).expect("GSS node id should be valid")
    }
    fn sppf_node(&self, id: SPPFNodeId) -> &SPPFNode {
        &self.sppf_nodes[id.index()]
    }
    fn sppf_node_mut(&mut self, id: SPPFNodeId) -> &mut SPPFNode {
        &mut self.sppf_nodes[id.index()]
    }
    fn add_descriptor(&mut self, descriptor: Descriptor) {
        record!(
            self, DescriptorAdded, descriptor.input_index, descriptor.slot_id, descriptor
            .sppf_node_id, descriptor.gss_node_id
        );
        self.stats_mut().descriptors_count += 1;
        self.descriptors.push(descriptor);
    }
    fn next_descriptor(&mut self) -> Option<Descriptor> {
        self.descriptors.pop()
    }
    fn add_terminal_node(&mut self, terminal_node: TerminalNode) -> SPPFNodeId {
        let terminal_node_id = SPPFNodeId(self.sppf_nodes.len() as u32);
        self.stats.terminal_nodes_count += 1;
        self.terminal_nodes_index[terminal_node.terminal_id.index()]
            .insert(terminal_node.span, terminal_node_id);
        record!(
            self, TerminalNodeCreated, terminal_node.terminal_id, terminal_node.span
        );
        self.sppf_nodes.push(SPPFNode::Terminal(terminal_node));
        terminal_node_id
    }
    fn add_nonterminal_node(&mut self, nonterminal_node: NonterminalNode) -> SPPFNodeId {
        let nonterminal_node_id = SPPFNodeId(self.sppf_nodes.len() as u32);
        self.stats.nonterminal_nodes_count += 1;
        self.nonterminal_nodes_index[nonterminal_node.nonterminal_id.index()]
            .insert(nonterminal_node.span, nonterminal_node_id);
        record!(
            self, NonterminalNodeCreated, nonterminal_node.nonterminal_id,
            nonterminal_node.span, nonterminal_node.child
        );
        self.sppf_nodes.push(SPPFNode::Nonterminal(nonterminal_node));
        nonterminal_node_id
    }
    fn add_intermediate_node(
        &mut self,
        intermediate_node: IntermediateNode,
    ) -> SPPFNodeId {
        let intermediate_node_id = SPPFNodeId(self.sppf_nodes.len() as u32);
        self.stats.intermediate_nodes_count += 1;
        self.intermediate_nodes_index[intermediate_node.slot_id.index()]
            .insert(intermediate_node.span, intermediate_node_id);
        record!(
            self, IntermediateNodeCreated, intermediate_node.slot_id, intermediate_node
            .span, intermediate_node.child.0, intermediate_node.child.1
        );
        self.sppf_nodes.push(SPPFNode::Intermediate(intermediate_node));
        intermediate_node_id
    }
    fn input(&self) -> &'i Input {
        self.scanner.input
    }
    fn stats(&self) -> &Stats {
        &self.stats
    }
    fn stats_mut(&mut self) -> &mut Stats {
        &mut self.stats
    }
    fn lookup_nonterminal_node(
        &self,
        nonterminal_id: NonterminalId,
        left_extent: u32,
        right_extent: u32,
    ) -> Option<SPPFNodeId> {
        let map = &self.nonterminal_nodes_index[nonterminal_id.index()];
        map.get(&Span::new(left_extent, right_extent)).copied()
    }
    fn lookup_intermediate_node(
        &self,
        slot_id: SlotId,
        left_extent: u32,
        right_extent: u32,
    ) -> Option<SPPFNodeId> {
        let map = &self.intermediate_nodes_index[slot_id.index()];
        map.get(&Span::new(left_extent, right_extent)).copied()
    }
    fn lookup_terminal_node(
        &self,
        terminal_id: TerminalId,
        left_extent: u32,
        right_extent: u32,
    ) -> Option<SPPFNodeId> {
        let map = &self.terminal_nodes_index[terminal_id.index()];
        map.get(&Span::new(left_extent, right_extent)).copied()
    }
    fn gss_nodes(&self) -> impl Iterator<Item = &GSSNode> {
        self.gss_nodes.iter()
    }
    fn add_intermediate_node_child(
        &mut self,
        node: SPPFNodeId,
        child1: SPPFNodeId,
        child2: SPPFNodeId,
    ) {
        self.intermediate_nodes_children.push((node, (child1, child2)));
    }
    fn add_nonterminal_node_child(&mut self, node: SPPFNodeId, child: SPPFNodeId) {
        self.nonterminal_nodes_children.push((node, child));
    }
    fn intermediate_nodes_children_map(
        &self,
    ) -> &FxHashMap<SPPFNodeId, Vec<(SPPFNodeId, SPPFNodeId)>> {
        self.intermediate_nodes_children_map
            .get_or_init(|| {
                let mut map: FxHashMap<SPPFNodeId, Vec<(SPPFNodeId, SPPFNodeId)>> = FxHashMap::default();
                for (k, v) in &self.intermediate_nodes_children {
                    map.entry(*k).or_default().push(*v);
                }
                map
            })
    }
    fn nonterminal_nodes_children_map(&self) -> &FxHashMap<SPPFNodeId, Vec<SPPFNodeId>> {
        self.nonterminal_nodes_children_map
            .get_or_init(|| {
                let mut map: FxHashMap<SPPFNodeId, Vec<SPPFNodeId>> = FxHashMap::default();
                for (k, v) in &self.nonterminal_nodes_children {
                    map.entry(*k).or_default().push(*v);
                }
                map
            })
    }
    #[cfg(feature = "debug-trace")]
    fn add_trace_event(&mut self, event: TraceEvent) {
        if let Some(trace_events) = &mut self.trace_events {
            trace_events.push(event);
        }
    }
    fn start_nonterminal(&self) -> NonterminalId {
        self.start_nonterminal
    }
    fn new_env(&mut self) -> (EnvId, &mut Env) {
        let id = EnvId(self.envs.len() as u32);
        self.envs.push(Env::default());
        (id, &mut self.envs[id.index()])
    }
    fn lookup(&self, name: &str, env_id: EnvId) -> i32 {
        let env = &self.envs[env_id.index()];
        env.get(name)
    }
    fn clone_env(&mut self, source: EnvId) -> (EnvId, &mut Env) {
        let bindings = self.envs[source.0 as usize].bindings.clone();
        let (new_id, new_env) = self.new_env();
        new_env.bindings = bindings;
        (new_id, new_env)
    }
    fn post_conditions(
        &self,
        slot: SlotId,
        left_extent: u32,
        right_extent: u32,
    ) -> bool {
        match slot {
            _ => true,
        }
    }
}
pub struct GroupParser<'i> {
    start_nonterminal: NonterminalId,
    scanner: GroupScanner<'i>,
    descriptors: Vec<Descriptor>,
    gss_nodes: Vec<GSSNode>,
    //A vector from nonterminal_ids to a tuple (input_index, gss_node_id)
    gss_nodes_index: [Vec<(u32, GssNodeId)>; 9],
    sppf_nodes: Vec<SPPFNode>,
    stats: Stats,
    nonterminal_nodes_index: [InlineMap<Span, SPPFNodeId>; 9],
    intermediate_nodes_index: [InlineMap<Span, SPPFNodeId>; 30],
    terminal_nodes_index: [InlineMap<Span, SPPFNodeId>; 5],
    intermediate_nodes_children: Vec<(SPPFNodeId, (SPPFNodeId, SPPFNodeId))>,
    intermediate_nodes_children_map: OnceCell<
        FxHashMap<SPPFNodeId, Vec<(SPPFNodeId, SPPFNodeId)>>,
    >,
    nonterminal_nodes_children: Vec<(SPPFNodeId, SPPFNodeId)>,
    nonterminal_nodes_children_map: OnceCell<FxHashMap<SPPFNodeId, Vec<SPPFNodeId>>>,
    envs: Vec<Env>,
    #[cfg(feature = "debug-trace")]
    pub trace_events: Option<Vec<TraceEvent>>,
}
impl<'i> GroupParser<'i> {
    pub fn new(input: &'i Input, start_nonterminal: NonterminalId) -> Self {
        init_logger();
        Self {
            start_nonterminal,
            scanner: GroupScanner::new(input),
            gss_nodes_index: [const { vec![] }; 9],
            descriptors: vec![],
            gss_nodes: vec![],
            sppf_nodes: vec![],
            nonterminal_nodes_index: [const { InlineMap::Empty }; 9],
            intermediate_nodes_index: [const { InlineMap::Empty }; 30],
            terminal_nodes_index: [const { InlineMap::Empty }; 5],
            stats: Stats::default(),
            intermediate_nodes_children: vec![],
            intermediate_nodes_children_map: OnceCell::new(),
            nonterminal_nodes_children: vec![],
            nonterminal_nodes_children_map: OnceCell::new(),
            envs: vec![],
            #[cfg(feature = "debug-trace")]
            trace_events: None,
        }
    }
    fn create_a(
        &mut self,
        sppf_node_id: Option<SPPFNodeId>,
        gss_node_id: GssNodeId,
        return_slot: SlotId,
    ) {
        self.create(NonterminalId(0), sppf_node_id, gss_node_id, return_slot);
    }
    fn create_b(
        &mut self,
        sppf_node_id: Option<SPPFNodeId>,
        gss_node_id: GssNodeId,
        return_slot: SlotId,
    ) {
        self.create(NonterminalId(1), sppf_node_id, gss_node_id, return_slot);
    }
    fn create_c(
        &mut self,
        sppf_node_id: Option<SPPFNodeId>,
        gss_node_id: GssNodeId,
        return_slot: SlotId,
    ) {
        self.create(NonterminalId(2), sppf_node_id, gss_node_id, return_slot);
    }
    fn create_d(
        &mut self,
        sppf_node_id: Option<SPPFNodeId>,
        gss_node_id: GssNodeId,
        return_slot: SlotId,
    ) {
        self.create(NonterminalId(3), sppf_node_id, gss_node_id, return_slot);
    }
    fn create_a_group_0(
        &mut self,
        sppf_node_id: Option<SPPFNodeId>,
        gss_node_id: GssNodeId,
        return_slot: SlotId,
    ) {
        self.create(NonterminalId(4), sppf_node_id, gss_node_id, return_slot);
    }
    fn create_start_a(
        &mut self,
        sppf_node_id: Option<SPPFNodeId>,
        gss_node_id: GssNodeId,
        return_slot: SlotId,
    ) {
        self.create(NonterminalId(5), sppf_node_id, gss_node_id, return_slot);
    }
    fn create_start_b(
        &mut self,
        sppf_node_id: Option<SPPFNodeId>,
        gss_node_id: GssNodeId,
        return_slot: SlotId,
    ) {
        self.create(NonterminalId(6), sppf_node_id, gss_node_id, return_slot);
    }
    fn create_start_c(
        &mut self,
        sppf_node_id: Option<SPPFNodeId>,
        gss_node_id: GssNodeId,
        return_slot: SlotId,
    ) {
        self.create(NonterminalId(7), sppf_node_id, gss_node_id, return_slot);
    }
    fn create_start_d(
        &mut self,
        sppf_node_id: Option<SPPFNodeId>,
        gss_node_id: GssNodeId,
        return_slot: SlotId,
    ) {
        self.create(NonterminalId(8), sppf_node_id, gss_node_id, return_slot);
    }
    fn parse_a_ll1(&mut self, i: u32) -> Option<SPPFNodeId> {
        if self.scanner.match_token(TerminalId(0), i).is_some() {
            let mut j = i;
            let right_child_id = {
                let start = j;
                let node = self.parse_a_group_0_ll1(start)?;
                let end = self.sppf_node(node).right_extent();
                j = end;
                node
            };
            let left_extent = self.sppf_node(right_child_id).left_extent();
            let mut current = right_child_id;
            return self
                .create_nonterminal_node_or_attach_children(
                    NonterminalId(0),
                    SlotId(1),
                    left_extent,
                    j,
                    current,
                );
        }
        None
    }
    fn parse_b_ll1(&mut self, i: u32) -> Option<SPPFNodeId> {
        if self.scanner.match_token(TerminalId(0), i).is_some() {
            let mut j = i;
            let right_child_id = {
                let start = j;
                let end = self.scanner.match_token(TerminalId(0), start)?;
                let node = self.get_or_create_terminal_node(TerminalId(0), start, end);
                j = end;
                node
            };
            let left_extent = self.sppf_node(right_child_id).left_extent();
            let mut current = right_child_id;
            return self
                .create_nonterminal_node_or_attach_children(
                    NonterminalId(1),
                    SlotId(3),
                    left_extent,
                    j,
                    current,
                );
        }
        None
    }
    fn parse_c_ll1(&mut self, i: u32) -> Option<SPPFNodeId> {
        if self.scanner.match_token(TerminalId(1), i).is_some() {
            let mut j = i;
            let right_child_id = {
                let start = j;
                let end = self.scanner.match_token(TerminalId(1), start)?;
                let node = self.get_or_create_terminal_node(TerminalId(1), start, end);
                j = end;
                node
            };
            let left_extent = self.sppf_node(right_child_id).left_extent();
            let mut current = right_child_id;
            return self
                .create_nonterminal_node_or_attach_children(
                    NonterminalId(2),
                    SlotId(5),
                    left_extent,
                    j,
                    current,
                );
        }
        None
    }
    fn parse_d_ll1(&mut self, i: u32) -> Option<SPPFNodeId> {
        if self.scanner.match_token(TerminalId(2), i).is_some() {
            let mut j = i;
            let right_child_id = {
                let start = j;
                let end = self.scanner.match_token(TerminalId(2), start)?;
                let node = self.get_or_create_terminal_node(TerminalId(2), start, end);
                j = end;
                node
            };
            let left_extent = self.sppf_node(right_child_id).left_extent();
            let mut current = right_child_id;
            return self
                .create_nonterminal_node_or_attach_children(
                    NonterminalId(3),
                    SlotId(7),
                    left_extent,
                    j,
                    current,
                );
        }
        None
    }
    fn parse_a_group_0_ll1(&mut self, i: u32) -> Option<SPPFNodeId> {
        if self.scanner.match_token(TerminalId(0), i).is_some() {
            let mut j = i;
            let right_child_id = {
                let start = j;
                let node = self.parse_b_ll1(start)?;
                let end = self.sppf_node(node).right_extent();
                j = end;
                node
            };
            let left_extent = self.sppf_node(right_child_id).left_extent();
            let mut current = right_child_id;
            let right_child_id = {
                let start = j;
                let end = self.scanner.match_token(TerminalId(3), start)?;
                let node = self.get_or_create_terminal_node(TerminalId(3), start, end);
                j = end;
                node
            };
            current = self
                .create_intermediate_node_or_attach_children(
                    SlotId(10),
                    left_extent,
                    j,
                    current,
                    right_child_id,
                )?;
            let right_child_id = {
                let start = j;
                let node = self.parse_c_ll1(start)?;
                let end = self.sppf_node(node).right_extent();
                j = end;
                node
            };
            current = self
                .create_intermediate_node_or_attach_children(
                    SlotId(11),
                    left_extent,
                    j,
                    current,
                    right_child_id,
                )?;
            let right_child_id = {
                let start = j;
                let end = self.scanner.match_token(TerminalId(3), start)?;
                let node = self.get_or_create_terminal_node(TerminalId(3), start, end);
                j = end;
                node
            };
            current = self
                .create_intermediate_node_or_attach_children(
                    SlotId(12),
                    left_extent,
                    j,
                    current,
                    right_child_id,
                )?;
            let right_child_id = {
                let start = j;
                let node = self.parse_d_ll1(start)?;
                let end = self.sppf_node(node).right_extent();
                j = end;
                node
            };
            current = self
                .create_intermediate_node_or_attach_children(
                    SlotId(13),
                    left_extent,
                    j,
                    current,
                    right_child_id,
                )?;
            return self
                .create_nonterminal_node_or_attach_children(
                    NonterminalId(4),
                    SlotId(13),
                    left_extent,
                    j,
                    current,
                );
        }
        None
    }
    fn parse_start_a_ll1(&mut self, i: u32) -> Option<SPPFNodeId> {
        if self.scanner.match_token(TerminalId(3), i).is_some()
            || self.scanner.match_token(TerminalId(0), i).is_some()
        {
            let mut j = i;
            let right_child_id = {
                let start = j;
                let end = self.scanner.match_token(TerminalId(3), start)?;
                let node = self.get_or_create_terminal_node(TerminalId(3), start, end);
                j = end;
                node
            };
            let left_extent = self.sppf_node(right_child_id).left_extent();
            let mut current = right_child_id;
            let right_child_id = {
                let start = j;
                let node = self.parse_a_ll1(start)?;
                let end = self.sppf_node(node).right_extent();
                j = end;
                node
            };
            current = self
                .create_intermediate_node_or_attach_children(
                    SlotId(16),
                    left_extent,
                    j,
                    current,
                    right_child_id,
                )?;
            let right_child_id = {
                let start = j;
                let end = self.scanner.match_token(TerminalId(3), start)?;
                let node = self.get_or_create_terminal_node(TerminalId(3), start, end);
                j = end;
                node
            };
            current = self
                .create_intermediate_node_or_attach_children(
                    SlotId(17),
                    left_extent,
                    j,
                    current,
                    right_child_id,
                )?;
            return self
                .create_nonterminal_node_or_attach_children(
                    NonterminalId(5),
                    SlotId(17),
                    left_extent,
                    j,
                    current,
                );
        }
        None
    }
    fn parse_start_b_ll1(&mut self, i: u32) -> Option<SPPFNodeId> {
        if self.scanner.match_token(TerminalId(3), i).is_some()
            || self.scanner.match_token(TerminalId(0), i).is_some()
        {
            let mut j = i;
            let right_child_id = {
                let start = j;
                let end = self.scanner.match_token(TerminalId(3), start)?;
                let node = self.get_or_create_terminal_node(TerminalId(3), start, end);
                j = end;
                node
            };
            let left_extent = self.sppf_node(right_child_id).left_extent();
            let mut current = right_child_id;
            let right_child_id = {
                let start = j;
                let node = self.parse_b_ll1(start)?;
                let end = self.sppf_node(node).right_extent();
                j = end;
                node
            };
            current = self
                .create_intermediate_node_or_attach_children(
                    SlotId(20),
                    left_extent,
                    j,
                    current,
                    right_child_id,
                )?;
            let right_child_id = {
                let start = j;
                let end = self.scanner.match_token(TerminalId(3), start)?;
                let node = self.get_or_create_terminal_node(TerminalId(3), start, end);
                j = end;
                node
            };
            current = self
                .create_intermediate_node_or_attach_children(
                    SlotId(21),
                    left_extent,
                    j,
                    current,
                    right_child_id,
                )?;
            return self
                .create_nonterminal_node_or_attach_children(
                    NonterminalId(6),
                    SlotId(21),
                    left_extent,
                    j,
                    current,
                );
        }
        None
    }
    fn parse_start_c_ll1(&mut self, i: u32) -> Option<SPPFNodeId> {
        if self.scanner.match_token(TerminalId(1), i).is_some()
            || self.scanner.match_token(TerminalId(3), i).is_some()
        {
            let mut j = i;
            let right_child_id = {
                let start = j;
                let end = self.scanner.match_token(TerminalId(3), start)?;
                let node = self.get_or_create_terminal_node(TerminalId(3), start, end);
                j = end;
                node
            };
            let left_extent = self.sppf_node(right_child_id).left_extent();
            let mut current = right_child_id;
            let right_child_id = {
                let start = j;
                let node = self.parse_c_ll1(start)?;
                let end = self.sppf_node(node).right_extent();
                j = end;
                node
            };
            current = self
                .create_intermediate_node_or_attach_children(
                    SlotId(24),
                    left_extent,
                    j,
                    current,
                    right_child_id,
                )?;
            let right_child_id = {
                let start = j;
                let end = self.scanner.match_token(TerminalId(3), start)?;
                let node = self.get_or_create_terminal_node(TerminalId(3), start, end);
                j = end;
                node
            };
            current = self
                .create_intermediate_node_or_attach_children(
                    SlotId(25),
                    left_extent,
                    j,
                    current,
                    right_child_id,
                )?;
            return self
                .create_nonterminal_node_or_attach_children(
                    NonterminalId(7),
                    SlotId(25),
                    left_extent,
                    j,
                    current,
                );
        }
        None
    }
    fn parse_start_d_ll1(&mut self, i: u32) -> Option<SPPFNodeId> {
        if self.scanner.match_token(TerminalId(3), i).is_some()
            || self.scanner.match_token(TerminalId(2), i).is_some()
        {
            let mut j = i;
            let right_child_id = {
                let start = j;
                let end = self.scanner.match_token(TerminalId(3), start)?;
                let node = self.get_or_create_terminal_node(TerminalId(3), start, end);
                j = end;
                node
            };
            let left_extent = self.sppf_node(right_child_id).left_extent();
            let mut current = right_child_id;
            let right_child_id = {
                let start = j;
                let node = self.parse_d_ll1(start)?;
                let end = self.sppf_node(node).right_extent();
                j = end;
                node
            };
            current = self
                .create_intermediate_node_or_attach_children(
                    SlotId(28),
                    left_extent,
                    j,
                    current,
                    right_child_id,
                )?;
            let right_child_id = {
                let start = j;
                let end = self.scanner.match_token(TerminalId(3), start)?;
                let node = self.get_or_create_terminal_node(TerminalId(3), start, end);
                j = end;
                node
            };
            current = self
                .create_intermediate_node_or_attach_children(
                    SlotId(29),
                    left_extent,
                    j,
                    current,
                    right_child_id,
                )?;
            return self
                .create_nonterminal_node_or_attach_children(
                    NonterminalId(8),
                    SlotId(29),
                    left_extent,
                    j,
                    current,
                );
        }
        None
    }
}

