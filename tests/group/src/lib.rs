// @generated by iguana. Do not edit manually.
pub mod parser;
pub mod parse_tree;
pub mod scanner;
pub mod types;
use std::time::Duration;
use iguana_runtime::{input::Input, parser::{ParseResult, Parser}};
use parse_tree::{ParseTree, GroupParseTreeBuilder, create_parse_tree};
use parser::GroupParser;
pub struct ParseSuccess {
    pub tree: ParseTree,
    pub parse_duration: Duration,
    pub tree_construction_duration: Duration,
}
pub fn parse(input: &Input, start_nonterminal: &str) -> Option<ParseSuccess> {
    let start_id = GroupParser::nonterminal_id(start_nonterminal)?;
    let mut parser = GroupParser::new(input, start_id);
    match parser.run() {
        ParseResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = std::time::Instant::now();
            let tree = create_parse_tree(
                success.sppf_node_id,
                start_nonterminal,
                &parser,
                &GroupParseTreeBuilder,
            );
            let tree_construction_duration = tree_start.elapsed();
            Some(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
            })
        }
        ParseResult::Failure(_) => None,
    }
}

