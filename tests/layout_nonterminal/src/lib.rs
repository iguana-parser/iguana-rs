// @generated by iguana. Do not edit manually.

pub mod grammar_data;
pub mod parse_tree;
pub mod parser;
pub mod scanner;
pub mod types;
use iguana_runtime::{
    input::Input,
    parse_tree::ParseContext,
    parser::{ParseResult, Parser},
};
use parse_tree::*;
use parser::LayoutNonterminalParser;
use std::error::Error;
use std::fmt::{self, Display, Formatter};
use std::time::Duration;
#[derive(Debug)]
pub struct ParseError {
    pub line: u32,
    pub column: u32,
    pub message: String,
}
impl Display for ParseError {
    fn fmt(&self, f: &mut Formatter<'_>) -> fmt::Result {
        write!(
            f,
            "Parse error at line {}, column {}: {}",
            self.line, self.column, self.message
        )
    }
}
impl Error for ParseError {}
pub struct ParseSuccess<T> {
    pub tree: T,
    pub parse_duration: Duration,
    pub tree_construction_duration: Duration,
}
pub fn parse_s<'a>(
    input: &Input,
    ctx: &'a ParseContext,
) -> std::result::Result<ParseSuccess<&'a Start<&'a S, &'a Layout<'a>>>, ParseError> {
    let mut parser = LayoutNonterminalParser::new(input, grammar_data::START_S);
    match parser.run() {
        ParseResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = std::time::Instant::now();
            let parse_tree_builder = LayoutNonterminalParseTreeBuilder::new(ctx);
            let tree = parse_tree::create_parse_tree_start_s(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
            })
        }
        ParseResult::Failure(error) => {
            let (line, column, message) = parser.format_error(&error);
            Err(ParseError {
                line,
                column,
                message,
            })
        }
    }
}
pub fn parse_layout<'a>(
    input: &Input,
    ctx: &'a ParseContext,
) -> std::result::Result<ParseSuccess<&'a Layout<'a>>, ParseError> {
    let mut parser = LayoutNonterminalParser::new(input, grammar_data::LAYOUT);
    match parser.run() {
        ParseResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = std::time::Instant::now();
            let parse_tree_builder = LayoutNonterminalParseTreeBuilder::new(ctx);
            let tree = parse_tree::create_parse_tree_layout(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
            })
        }
        ParseResult::Failure(error) => {
            let (line, column, message) = parser.format_error(&error);
            Err(ParseError {
                line,
                column,
                message,
            })
        }
    }
}
