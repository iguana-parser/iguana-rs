// @generated by iguana. Do not edit manually.

pub mod grammar_data;
pub mod parse_tree;
pub mod parser;
pub mod scanner;
pub mod types;
pub use iguana_runtime::result::{ParseError, ParseSuccess};
use iguana_runtime::{
    arena::Arena,
    input::Input,
    parser::{GLLResult, Parser},
};
use parse_tree::*;
use parser::IndirectBinaryParser;
/// Parses `input` starting from `S`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_s<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a S<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser = IndirectBinaryParser::new(input, grammar_data::START_S, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_s(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `E`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_e<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a E<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser = IndirectBinaryParser::new(input, grammar_data::START_E, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_e(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `L`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_l<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a L<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser = IndirectBinaryParser::new(input, grammar_data::START_L, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_l(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `Only`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_only<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a Only<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser = IndirectBinaryParser::new(input, grammar_data::START_ONLY, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_only(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `OnlyOperand`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_only_operand<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a OnlyOperand<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser = IndirectBinaryParser::new(input, grammar_data::START_ONLY_OPERAND, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_only_operand(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `Cross`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_cross<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a Cross<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser = IndirectBinaryParser::new(input, grammar_data::START_CROSS, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_cross(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `Foreign`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_foreign<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a Foreign<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser = IndirectBinaryParser::new(input, grammar_data::START_FOREIGN, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_foreign(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `Cascade`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_cascade<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a Cascade<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser = IndirectBinaryParser::new(input, grammar_data::START_CASCADE, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_cascade(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `Ternary`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_ternary<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a Ternary<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser = IndirectBinaryParser::new(input, grammar_data::START_TERNARY, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_ternary(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `Nullable`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_nullable<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a Nullable<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser = IndirectBinaryParser::new(input, grammar_data::START_NULLABLE, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_nullable(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `NullableLeft`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_nullable_left<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a NullableLeft<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_NULLABLE_LEFT, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_nullable_left(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `NullableRight`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_nullable_right<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a NullableRight<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_NULLABLE_RIGHT, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_nullable_right(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `RightExpr`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_right_expr<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a RightExpr<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser = IndirectBinaryParser::new(input, grammar_data::START_RIGHT_EXPR, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_right_expr(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `RightOperand`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_right_operand<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a RightOperand<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_RIGHT_OPERAND, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_right_operand(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `NoneExpr`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_none_expr<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a NoneExpr<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser = IndirectBinaryParser::new(input, grammar_data::START_NONE_EXPR, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_none_expr(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `NoneOperand`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_none_operand<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a NoneOperand<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser = IndirectBinaryParser::new(input, grammar_data::START_NONE_OPERAND, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_none_operand(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `Excluded`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_excluded<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a Excluded<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser = IndirectBinaryParser::new(input, grammar_data::START_EXCLUDED, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_excluded(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `ExcludedOperand`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_excluded_operand<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a ExcludedOperand<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_EXCLUDED_OPERAND, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_excluded_operand(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `Filtered`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_filtered<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a Filtered<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser = IndirectBinaryParser::new(input, grammar_data::START_FILTERED, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_filtered(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `LexNullable`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_lex_nullable<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a LexNullable<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser = IndirectBinaryParser::new(input, grammar_data::START_LEX_NULLABLE, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_lex_nullable(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `LexNullableLeft`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_lex_nullable_left<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a LexNullableLeft<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_LEX_NULLABLE_LEFT, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_lex_nullable_left(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `LexNullableRight`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_lex_nullable_right<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a LexNullableRight<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_LEX_NULLABLE_RIGHT, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_lex_nullable_right(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `MixedScale`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_mixed_scale<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a MixedScale<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser = IndirectBinaryParser::new(input, grammar_data::START_MIXED_SCALE, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_mixed_scale(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `MixedOperand`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_mixed_operand<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a MixedOperand<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_MIXED_OPERAND, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_mixed_operand(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `MixedRight`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_mixed_right<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a MixedRight<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser = IndirectBinaryParser::new(input, grammar_data::START_MIXED_RIGHT, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_mixed_right(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `MixedForeign`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_mixed_foreign<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a MixedForeign<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_MIXED_FOREIGN, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_mixed_foreign(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `Packed`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_packed<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a Packed<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser = IndirectBinaryParser::new(input, grammar_data::START_PACKED, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_packed(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `PackedOperand`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_packed_operand<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a PackedOperand<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_PACKED_OPERAND, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_packed_operand(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `PackedMiddle`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_packed_middle<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a PackedMiddle<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_PACKED_MIDDLE, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_packed_middle(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `PackedFiltered`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_packed_filtered<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a PackedFiltered<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_PACKED_FILTERED, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_packed_filtered(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `NullableRecursive`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_nullable_recursive<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a NullableRecursive<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_NULLABLE_RECURSIVE, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_nullable_recursive(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `NullableOperand`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_nullable_operand<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a NullableOperand<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_NULLABLE_OPERAND, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_nullable_operand(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `NullableMaybe`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_nullable_maybe<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a NullableMaybe<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_NULLABLE_MAYBE, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_nullable_maybe(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `NullableLexRecursive`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_nullable_lex_recursive<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a NullableLexRecursive<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser = IndirectBinaryParser::new(
        input,
        grammar_data::START_NULLABLE_LEX_RECURSIVE,
        &vec_arena,
    );
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_nullable_lex_recursive(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `NullableLexOperand`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_nullable_lex_operand<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a NullableLexOperand<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_NULLABLE_LEX_OPERAND, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_nullable_lex_operand(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `NullableLexMaybe`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_nullable_lex_maybe<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a NullableLexMaybe<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_NULLABLE_LEX_MAYBE, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_nullable_lex_maybe(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `NullableSuffix`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_nullable_suffix<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a NullableSuffix<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_NULLABLE_SUFFIX, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_nullable_suffix(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `NullableSuffixOperand`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_nullable_suffix_operand<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a NullableSuffixOperand<'a>, Token>>, ParseError>
{
    let vec_arena = Arena::new();
    let mut parser = IndirectBinaryParser::new(
        input,
        grammar_data::START_NULLABLE_SUFFIX_OPERAND,
        &vec_arena,
    );
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_nullable_suffix_operand(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `NullableSuffixMaybe`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_nullable_suffix_maybe<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a NullableSuffixMaybe<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_NULLABLE_SUFFIX_MAYBE, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_nullable_suffix_maybe(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `NullableShared`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_nullable_shared<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a NullableShared<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_NULLABLE_SHARED, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_nullable_shared(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `NullableSharedOperand`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_nullable_shared_operand<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a NullableSharedOperand<'a>, Token>>, ParseError>
{
    let vec_arena = Arena::new();
    let mut parser = IndirectBinaryParser::new(
        input,
        grammar_data::START_NULLABLE_SHARED_OPERAND,
        &vec_arena,
    );
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_nullable_shared_operand(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `NullableSharedMaybe`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_nullable_shared_maybe<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a NullableSharedMaybe<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_NULLABLE_SHARED_MAYBE, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_nullable_shared_maybe(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `OwnLabel`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_own_label<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a OwnLabel<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser = IndirectBinaryParser::new(input, grammar_data::START_OWN_LABEL, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_own_label(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `OwnOperand`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_own_operand<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a OwnOperand<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser = IndirectBinaryParser::new(input, grammar_data::START_OWN_OPERAND, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_own_operand(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `OwnHeadFiltered`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_own_head_filtered<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a OwnHeadFiltered<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_OWN_HEAD_FILTERED, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_own_head_filtered(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `ExcludeOnly`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_exclude_only<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a ExcludeOnly<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser = IndirectBinaryParser::new(input, grammar_data::START_EXCLUDE_ONLY, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_exclude_only(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `ExcludeOnlyFiltered`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_exclude_only_filtered<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a ExcludeOnlyFiltered<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_EXCLUDE_ONLY_FILTERED, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_exclude_only_filtered(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `Absent`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_absent<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a Absent<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser = IndirectBinaryParser::new(input, grammar_data::START_ABSENT, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_absent(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `AbsentOperand`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_absent_operand<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a AbsentOperand<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_ABSENT_OPERAND, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_absent_operand(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `ClosedPrefix`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_closed_prefix<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a ClosedPrefix<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_CLOSED_PREFIX, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_closed_prefix(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `ClosedPrefixBody`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_closed_prefix_body<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a ClosedPrefixBody<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_CLOSED_PREFIX_BODY, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_closed_prefix_body(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `ClosedPostfix`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_closed_postfix<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a ClosedPostfix<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_CLOSED_POSTFIX, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_closed_postfix(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `ClosedPostfixBody`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_closed_postfix_body<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a ClosedPostfixBody<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_CLOSED_POSTFIX_BODY, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_closed_postfix_body(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `HeadChoice`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_head_choice<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a HeadChoice<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser = IndirectBinaryParser::new(input, grammar_data::START_HEAD_CHOICE, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_head_choice(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `HeadOperand`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_head_operand<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a HeadOperand<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser = IndirectBinaryParser::new(input, grammar_data::START_HEAD_OPERAND, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_head_operand(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `Inner`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_inner<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a Inner<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser = IndirectBinaryParser::new(input, grammar_data::START_INNER, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_inner(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `UnaryMixedLeft`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_unary_mixed_left<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a UnaryMixedLeft<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_UNARY_MIXED_LEFT, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_unary_mixed_left(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `UnaryMixedRight`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_unary_mixed_right<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a UnaryMixedRight<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_UNARY_MIXED_RIGHT, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_unary_mixed_right(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `UnaryMixedNone`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_unary_mixed_none<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a UnaryMixedNone<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_UNARY_MIXED_NONE, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_unary_mixed_none(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `Dynamic`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_dynamic<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a Dynamic<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser = IndirectBinaryParser::new(input, grammar_data::START_DYNAMIC, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_dynamic(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `DynamicOperand`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_dynamic_operand<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a DynamicOperand<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_DYNAMIC_OPERAND, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_dynamic_operand(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `DynamicFiltered`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_dynamic_filtered<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a DynamicFiltered<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_DYNAMIC_FILTERED, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_dynamic_filtered(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `DynamicHeadFiltered`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_dynamic_head_filtered<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a DynamicHeadFiltered<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_DYNAMIC_HEAD_FILTERED, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_dynamic_head_filtered(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `DynamicMulti`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_dynamic_multi<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a DynamicMulti<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_DYNAMIC_MULTI, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_dynamic_multi(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `DynamicMultiOperand`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_dynamic_multi_operand<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a DynamicMultiOperand<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_DYNAMIC_MULTI_OPERAND, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_dynamic_multi_operand(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `NullableBoundaryPrefix`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_nullable_boundary_prefix<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a NullableBoundaryPrefix<'a>, Token>>, ParseError>
{
    let vec_arena = Arena::new();
    let mut parser = IndirectBinaryParser::new(
        input,
        grammar_data::START_NULLABLE_BOUNDARY_PREFIX,
        &vec_arena,
    );
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_nullable_boundary_prefix(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `NullableBoundaryRight`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_nullable_boundary_right<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a NullableBoundaryRight<'a>, Token>>, ParseError>
{
    let vec_arena = Arena::new();
    let mut parser = IndirectBinaryParser::new(
        input,
        grammar_data::START_NULLABLE_BOUNDARY_RIGHT,
        &vec_arena,
    );
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_nullable_boundary_right(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `NullableBoundaryOpt`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_nullable_boundary_opt<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a NullableBoundaryOpt<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_NULLABLE_BOUNDARY_OPT, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_nullable_boundary_opt(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `NullableBoundaryPostfix`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_nullable_boundary_postfix<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a NullableBoundaryPostfix<'a>, Token>>, ParseError>
{
    let vec_arena = Arena::new();
    let mut parser = IndirectBinaryParser::new(
        input,
        grammar_data::START_NULLABLE_BOUNDARY_POSTFIX,
        &vec_arena,
    );
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_nullable_boundary_postfix(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `NullableBoundaryLeft`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_nullable_boundary_left<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a NullableBoundaryLeft<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser = IndirectBinaryParser::new(
        input,
        grammar_data::START_NULLABLE_BOUNDARY_LEFT,
        &vec_arena,
    );
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_nullable_boundary_left(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `NullableBoundaryBinary`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_nullable_boundary_binary<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a NullableBoundaryBinary<'a>, Token>>, ParseError>
{
    let vec_arena = Arena::new();
    let mut parser = IndirectBinaryParser::new(
        input,
        grammar_data::START_NULLABLE_BOUNDARY_BINARY,
        &vec_arena,
    );
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_nullable_boundary_binary(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `NullableBoundaryOperand`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_nullable_boundary_operand<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a NullableBoundaryOperand<'a>, Token>>, ParseError>
{
    let vec_arena = Arena::new();
    let mut parser = IndirectBinaryParser::new(
        input,
        grammar_data::START_NULLABLE_BOUNDARY_OPERAND,
        &vec_arena,
    );
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_nullable_boundary_operand(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `ClosedLeftAssoc`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_closed_left_assoc<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a ClosedLeftAssoc<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_CLOSED_LEFT_ASSOC, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_closed_left_assoc(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `ClosedLeftOperand`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_closed_left_operand<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a ClosedLeftOperand<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_CLOSED_LEFT_OPERAND, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_closed_left_operand(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `ClosedRightAssoc`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_closed_right_assoc<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a ClosedRightAssoc<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_CLOSED_RIGHT_ASSOC, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_closed_right_assoc(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `ClosedRightOperand`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_closed_right_operand<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a ClosedRightOperand<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_CLOSED_RIGHT_OPERAND, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_closed_right_operand(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `IndirectExclude`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_indirect_exclude<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a IndirectExclude<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_INDIRECT_EXCLUDE, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_indirect_exclude(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `IndirectExcludeOperand`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_indirect_exclude_operand<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a IndirectExcludeOperand<'a>, Token>>, ParseError>
{
    let vec_arena = Arena::new();
    let mut parser = IndirectBinaryParser::new(
        input,
        grammar_data::START_INDIRECT_EXCLUDE_OPERAND,
        &vec_arena,
    );
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_indirect_exclude_operand(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `IndirectExcludeFiltered`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_indirect_exclude_filtered<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a IndirectExcludeFiltered<'a>, Token>>, ParseError>
{
    let vec_arena = Arena::new();
    let mut parser = IndirectBinaryParser::new(
        input,
        grammar_data::START_INDIRECT_EXCLUDE_FILTERED,
        &vec_arena,
    );
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_indirect_exclude_filtered(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `OperandLiteral`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_operand_literal<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a OperandLiteral<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_OPERAND_LITERAL, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_operand_literal(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `OperandLiteralEnd`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_operand_literal_end<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a OperandLiteralEnd<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_OPERAND_LITERAL_END, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_operand_literal_end(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `HeadLiteral`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_head_literal<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a HeadLiteral<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser = IndirectBinaryParser::new(input, grammar_data::START_HEAD_LITERAL, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_head_literal(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
/// Parses `input` starting from `HeadLiteralEnd`.
///
/// `tree_arena` holds the constructed parse tree: the returned tree borrows it
/// and lives until the arena is reset or dropped. Once the tree goes out of
/// scope, the arena can be reset with `tree_arena.reset()` and reused for the
/// next parse; that is the pattern for repeated parsing, as in an editor or a
/// benchmark loop.
pub fn parse_head_literal_end<'a>(
    input: &Input,
    tree_arena: &'a Arena,
) -> std::result::Result<ParseSuccess<&'a Start<&'a HeadLiteralEnd<'a>, Token>>, ParseError> {
    let vec_arena = Arena::new();
    let mut parser =
        IndirectBinaryParser::new(input, grammar_data::START_HEAD_LITERAL_END, &vec_arena);
    match parser.run() {
        GLLResult::Success(success) => {
            let parse_duration = success.duration;
            let tree_start = iguana_runtime::Instant::now();
            let parse_tree_builder = IndirectBinaryParseTreeBuilder::new(tree_arena);
            let tree = parse_tree::create_parse_tree_start_head_literal_end(
                success.sppf_node_id,
                &parser,
                &parse_tree_builder,
            );
            let tree_construction_duration = tree_start.elapsed();
            let ambiguity_node_added = parser.ambiguity_node_added();
            Ok(ParseSuccess {
                tree,
                parse_duration,
                tree_construction_duration,
                ambiguity_node_added,
            })
        }
        GLLResult::Failure(error) => Err(parser.to_parse_error(&error)),
    }
}
