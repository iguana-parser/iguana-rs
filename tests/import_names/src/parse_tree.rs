// @generated by iguana. Do not edit manually.

use crate::{grammar, parser::ImportNamesParser};
use iguana_runtime::{
    arena::Arena,
    ids::{NonterminalId, SlotId, TerminalId},
    input::Span,
    parse_tree::{NodeKind, OneOrMany, Origin, ParseTreeBuilder, ParseTreeNode, visit_sppf},
    sppf::{NonterminalNode, SPPFNodeId, TerminalNode},
};
use std::vec::IntoIter;
#[derive(Debug, Clone, Copy)]
pub enum TokenKind {
    // "a"
    T0,
    // "b"
    T1,
    // "c"
    T2,
    // "d"
    T3,
    // "e"
    T4,
    // "f"
    T5,
    // "g"
    T6,
    // "h"
    T7,
    // "i"
    T8,
    // "j"
    T9,
    // "k"
    T10,
}
impl TokenKind {
    pub fn name(&self) -> &'static str {
        match self {
            TokenKind::T0 => "\"a\"",
            TokenKind::T1 => "\"b\"",
            TokenKind::T2 => "\"c\"",
            TokenKind::T3 => "\"d\"",
            TokenKind::T4 => "\"e\"",
            TokenKind::T5 => "\"f\"",
            TokenKind::T6 => "\"g\"",
            TokenKind::T7 => "\"h\"",
            TokenKind::T8 => "\"i\"",
            TokenKind::T9 => "\"j\"",
            TokenKind::T10 => "\"k\"",
        }
    }
}
#[derive(Debug)]
pub struct Start<T, L> {
    pub before: L,
    pub node: T,
    pub after: L,
    pub span: Span,
}
pub type StartS<'a> = Start<&'a S<'a>, ()>;
pub type StartParse<'a> = Start<&'a Parse<'a>, ()>;
pub type StartParser<'a> = Start<&'a Parser<'a>, ()>;
pub type StartSPPFNode<'a> = Start<&'a SPPFNode<'a>, ()>;
pub type StartInput<'a> = Start<&'a Input<'a>, ()>;
pub type StartParseError<'a> = Start<&'a ParseError<'a>, ()>;
pub type StartParseSuccess<'a> = Start<&'a ParseSuccess<'a>, ()>;
pub type StartGrammar<'a> = Start<&'a Grammar<'a>, ()>;
pub type StartNonterminal<'a> = Start<&'a Nonterminal<'a>, ()>;
pub type StartTerminal<'a> = Start<&'a Terminal<'a>, ()>;
pub type StartSlot<'a> = Start<&'a Slot<'a>, ()>;
pub type StartTerminalSet<'a> = Start<&'a TerminalSet<'a>, ()>;
#[derive(Debug, Clone, Copy)]
pub enum ParseTree<'a> {
    S(&'a S<'a>),
    Parse(&'a Parse<'a>),
    Parser(&'a Parser<'a>),
    SPPFNode(&'a SPPFNode<'a>),
    Input(&'a Input<'a>),
    ParseError(&'a ParseError<'a>),
    ParseSuccess(&'a ParseSuccess<'a>),
    Grammar(&'a Grammar<'a>),
    Nonterminal(&'a Nonterminal<'a>),
    Terminal(&'a Terminal<'a>),
    Slot(&'a Slot<'a>),
    TerminalSet(&'a TerminalSet<'a>),
    // S
    StartS(&'a Start<&'a S<'a>, ()>),
    // Parse
    StartParse(&'a Start<&'a Parse<'a>, ()>),
    // Parser
    StartParser(&'a Start<&'a Parser<'a>, ()>),
    // SPPFNode
    StartSPPFNode(&'a Start<&'a SPPFNode<'a>, ()>),
    // Input
    StartInput(&'a Start<&'a Input<'a>, ()>),
    // ParseError
    StartParseError(&'a Start<&'a ParseError<'a>, ()>),
    // ParseSuccess
    StartParseSuccess(&'a Start<&'a ParseSuccess<'a>, ()>),
    // Grammar
    StartGrammar(&'a Start<&'a Grammar<'a>, ()>),
    // Nonterminal
    StartNonterminal(&'a Start<&'a Nonterminal<'a>, ()>),
    // Terminal
    StartTerminal(&'a Start<&'a Terminal<'a>, ()>),
    // Slot
    StartSlot(&'a Start<&'a Slot<'a>, ()>),
    // TerminalSet
    StartTerminalSet(&'a Start<&'a TerminalSet<'a>, ()>),
    Token(Token),
}
impl<'a> ParseTree<'a> {
    pub fn children(&self) -> Vec<ParseTree<'a>> {
        match self {
            ParseTree::S(s) => (0..s.child_count()).filter_map(|i| s.child(i)).collect(),
            ParseTree::Parse(parse) => (0..parse.child_count())
                .filter_map(|i| parse.child(i))
                .collect(),
            ParseTree::Parser(parser) => (0..parser.child_count())
                .filter_map(|i| parser.child(i))
                .collect(),
            ParseTree::SPPFNode(sppf_node) => (0..sppf_node.child_count())
                .filter_map(|i| sppf_node.child(i))
                .collect(),
            ParseTree::Input(input) => (0..input.child_count())
                .filter_map(|i| input.child(i))
                .collect(),
            ParseTree::ParseError(parse_error) => (0..parse_error.child_count())
                .filter_map(|i| parse_error.child(i))
                .collect(),
            ParseTree::ParseSuccess(parse_success) => (0..parse_success.child_count())
                .filter_map(|i| parse_success.child(i))
                .collect(),
            ParseTree::Grammar(grammar) => (0..grammar.child_count())
                .filter_map(|i| grammar.child(i))
                .collect(),
            ParseTree::Nonterminal(nonterminal) => (0..nonterminal.child_count())
                .filter_map(|i| nonterminal.child(i))
                .collect(),
            ParseTree::Terminal(terminal) => (0..terminal.child_count())
                .filter_map(|i| terminal.child(i))
                .collect(),
            ParseTree::Slot(slot) => (0..slot.child_count())
                .filter_map(|i| slot.child(i))
                .collect(),
            ParseTree::TerminalSet(terminal_set) => (0..terminal_set.child_count())
                .filter_map(|i| terminal_set.child(i))
                .collect(),
            ParseTree::StartS(start_s) => (0..start_s.child_count())
                .filter_map(|i| start_s.child(i))
                .collect(),
            ParseTree::StartParse(start_parse) => (0..start_parse.child_count())
                .filter_map(|i| start_parse.child(i))
                .collect(),
            ParseTree::StartParser(start_parser) => (0..start_parser.child_count())
                .filter_map(|i| start_parser.child(i))
                .collect(),
            ParseTree::StartSPPFNode(start_sppf_node) => (0..start_sppf_node.child_count())
                .filter_map(|i| start_sppf_node.child(i))
                .collect(),
            ParseTree::StartInput(start_input) => (0..start_input.child_count())
                .filter_map(|i| start_input.child(i))
                .collect(),
            ParseTree::StartParseError(start_parse_error) => (0..start_parse_error.child_count())
                .filter_map(|i| start_parse_error.child(i))
                .collect(),
            ParseTree::StartParseSuccess(start_parse_success) => (0..start_parse_success
                .child_count())
                .filter_map(|i| start_parse_success.child(i))
                .collect(),
            ParseTree::StartGrammar(start_grammar) => (0..start_grammar.child_count())
                .filter_map(|i| start_grammar.child(i))
                .collect(),
            ParseTree::StartNonterminal(start_nonterminal) => (0..start_nonterminal.child_count())
                .filter_map(|i| start_nonterminal.child(i))
                .collect(),
            ParseTree::StartTerminal(start_terminal) => (0..start_terminal.child_count())
                .filter_map(|i| start_terminal.child(i))
                .collect(),
            ParseTree::StartSlot(start_slot) => (0..start_slot.child_count())
                .filter_map(|i| start_slot.child(i))
                .collect(),
            ParseTree::StartTerminalSet(start_terminal_set) => (0..start_terminal_set
                .child_count())
                .filter_map(|i| start_terminal_set.child(i))
                .collect(),
            ParseTree::Token(_) => vec![],
        }
    }
    pub fn display_name(&self) -> &'static str {
        match self {
            ParseTree::S(s) => s.display_name(),
            ParseTree::Parse(parse) => parse.display_name(),
            ParseTree::Parser(parser) => parser.display_name(),
            ParseTree::SPPFNode(sppf_node) => sppf_node.display_name(),
            ParseTree::Input(input) => input.display_name(),
            ParseTree::ParseError(parse_error) => parse_error.display_name(),
            ParseTree::ParseSuccess(parse_success) => parse_success.display_name(),
            ParseTree::Grammar(grammar) => grammar.display_name(),
            ParseTree::Nonterminal(nonterminal) => nonterminal.display_name(),
            ParseTree::Terminal(terminal) => terminal.display_name(),
            ParseTree::Slot(slot) => slot.display_name(),
            ParseTree::TerminalSet(terminal_set) => terminal_set.display_name(),
            ParseTree::StartS(start_s) => start_s.display_name(),
            ParseTree::StartParse(start_parse) => start_parse.display_name(),
            ParseTree::StartParser(start_parser) => start_parser.display_name(),
            ParseTree::StartSPPFNode(start_sppf_node) => start_sppf_node.display_name(),
            ParseTree::StartInput(start_input) => start_input.display_name(),
            ParseTree::StartParseError(start_parse_error) => start_parse_error.display_name(),
            ParseTree::StartParseSuccess(start_parse_success) => start_parse_success.display_name(),
            ParseTree::StartGrammar(start_grammar) => start_grammar.display_name(),
            ParseTree::StartNonterminal(start_nonterminal) => start_nonterminal.display_name(),
            ParseTree::StartTerminal(start_terminal) => start_terminal.display_name(),
            ParseTree::StartSlot(start_slot) => start_slot.display_name(),
            ParseTree::StartTerminalSet(start_terminal_set) => start_terminal_set.display_name(),
            ParseTree::Token(token) => token.kind.name(),
        }
    }
    pub fn child_count(&self) -> usize {
        match self {
            ParseTree::S(s) => s.child_count(),
            ParseTree::Parse(parse) => parse.child_count(),
            ParseTree::Parser(parser) => parser.child_count(),
            ParseTree::SPPFNode(sppf_node) => sppf_node.child_count(),
            ParseTree::Input(input) => input.child_count(),
            ParseTree::ParseError(parse_error) => parse_error.child_count(),
            ParseTree::ParseSuccess(parse_success) => parse_success.child_count(),
            ParseTree::Grammar(grammar) => grammar.child_count(),
            ParseTree::Nonterminal(nonterminal) => nonterminal.child_count(),
            ParseTree::Terminal(terminal) => terminal.child_count(),
            ParseTree::Slot(slot) => slot.child_count(),
            ParseTree::TerminalSet(terminal_set) => terminal_set.child_count(),
            ParseTree::StartS(start_s) => start_s.child_count(),
            ParseTree::StartParse(start_parse) => start_parse.child_count(),
            ParseTree::StartParser(start_parser) => start_parser.child_count(),
            ParseTree::StartSPPFNode(start_sppf_node) => start_sppf_node.child_count(),
            ParseTree::StartInput(start_input) => start_input.child_count(),
            ParseTree::StartParseError(start_parse_error) => start_parse_error.child_count(),
            ParseTree::StartParseSuccess(start_parse_success) => start_parse_success.child_count(),
            ParseTree::StartGrammar(start_grammar) => start_grammar.child_count(),
            ParseTree::StartNonterminal(start_nonterminal) => start_nonterminal.child_count(),
            ParseTree::StartTerminal(start_terminal) => start_terminal.child_count(),
            ParseTree::StartSlot(start_slot) => start_slot.child_count(),
            ParseTree::StartTerminalSet(start_terminal_set) => start_terminal_set.child_count(),
            ParseTree::Token(_) => 0,
        }
    }
    pub fn span(&self) -> Span {
        match self {
            ParseTree::S(s) => s.span(),
            ParseTree::Parse(parse) => parse.span(),
            ParseTree::Parser(parser) => parser.span(),
            ParseTree::SPPFNode(sppf_node) => sppf_node.span(),
            ParseTree::Input(input) => input.span(),
            ParseTree::ParseError(parse_error) => parse_error.span(),
            ParseTree::ParseSuccess(parse_success) => parse_success.span(),
            ParseTree::Grammar(grammar) => grammar.span(),
            ParseTree::Nonterminal(nonterminal) => nonterminal.span(),
            ParseTree::Terminal(terminal) => terminal.span(),
            ParseTree::Slot(slot) => slot.span(),
            ParseTree::TerminalSet(terminal_set) => terminal_set.span(),
            ParseTree::StartS(start_s) => start_s.span(),
            ParseTree::StartParse(start_parse) => start_parse.span(),
            ParseTree::StartParser(start_parser) => start_parser.span(),
            ParseTree::StartSPPFNode(start_sppf_node) => start_sppf_node.span(),
            ParseTree::StartInput(start_input) => start_input.span(),
            ParseTree::StartParseError(start_parse_error) => start_parse_error.span(),
            ParseTree::StartParseSuccess(start_parse_success) => start_parse_success.span(),
            ParseTree::StartGrammar(start_grammar) => start_grammar.span(),
            ParseTree::StartNonterminal(start_nonterminal) => start_nonterminal.span(),
            ParseTree::StartTerminal(start_terminal) => start_terminal.span(),
            ParseTree::StartSlot(start_slot) => start_slot.span(),
            ParseTree::StartTerminalSet(start_terminal_set) => start_terminal_set.span(),
            ParseTree::Token(token) => token.span(),
        }
    }
    ///True when this node is an ambiguity cluster (any `*::Amb` variant). The
    ///uniform way to detect ambiguity without matching each nonterminal's enum.
    pub fn is_amb(&self) -> bool {
        match self {
            ParseTree::S(s) => matches!(s, S::Amb(_)),
            ParseTree::Parse(parse) => matches!(parse, Parse::Amb(_)),
            ParseTree::Parser(parser) => matches!(parser, Parser::Amb(_)),
            ParseTree::SPPFNode(sppf_node) => matches!(sppf_node, SPPFNode::Amb(_)),
            ParseTree::Input(input) => matches!(input, Input::Amb(_)),
            ParseTree::ParseError(parse_error) => matches!(parse_error, ParseError::Amb(_)),
            ParseTree::ParseSuccess(parse_success) => matches!(parse_success, ParseSuccess::Amb(_)),
            ParseTree::Grammar(grammar) => matches!(grammar, Grammar::Amb(_)),
            ParseTree::Nonterminal(nonterminal) => matches!(nonterminal, Nonterminal::Amb(_)),
            ParseTree::Terminal(terminal) => matches!(terminal, Terminal::Amb(_)),
            ParseTree::Slot(slot) => matches!(slot, Slot::Amb(_)),
            ParseTree::TerminalSet(terminal_set) => matches!(terminal_set, TerminalSet::Amb(_)),
            ParseTree::StartS(_) => false,
            ParseTree::StartParse(_) => false,
            ParseTree::StartParser(_) => false,
            ParseTree::StartSPPFNode(_) => false,
            ParseTree::StartInput(_) => false,
            ParseTree::StartParseError(_) => false,
            ParseTree::StartParseSuccess(_) => false,
            ParseTree::StartGrammar(_) => false,
            ParseTree::StartNonterminal(_) => false,
            ParseTree::StartTerminal(_) => false,
            ParseTree::StartSlot(_) => false,
            ParseTree::StartTerminalSet(_) => false,
            ParseTree::Token(_) => false,
        }
    }
    ///Pointer identity of the underlying node, or `None` for tokens (by-value
    ///leaves that are never shared). Two parse trees with the same `node_id` are
    ///the same allocation, i.e. a node shared between parents in the ambiguity DAG.
    pub fn node_id(&self) -> Option<usize> {
        match self {
            ParseTree::S(s) => Some(*s as *const _ as usize),
            ParseTree::Parse(parse) => Some(*parse as *const _ as usize),
            ParseTree::Parser(parser) => Some(*parser as *const _ as usize),
            ParseTree::SPPFNode(sppf_node) => Some(*sppf_node as *const _ as usize),
            ParseTree::Input(input) => Some(*input as *const _ as usize),
            ParseTree::ParseError(parse_error) => Some(*parse_error as *const _ as usize),
            ParseTree::ParseSuccess(parse_success) => Some(*parse_success as *const _ as usize),
            ParseTree::Grammar(grammar) => Some(*grammar as *const _ as usize),
            ParseTree::Nonterminal(nonterminal) => Some(*nonterminal as *const _ as usize),
            ParseTree::Terminal(terminal) => Some(*terminal as *const _ as usize),
            ParseTree::Slot(slot) => Some(*slot as *const _ as usize),
            ParseTree::TerminalSet(terminal_set) => Some(*terminal_set as *const _ as usize),
            ParseTree::StartS(start_s) => Some(*start_s as *const _ as usize),
            ParseTree::StartParse(start_parse) => Some(*start_parse as *const _ as usize),
            ParseTree::StartParser(start_parser) => Some(*start_parser as *const _ as usize),
            ParseTree::StartSPPFNode(start_sppf_node) => {
                Some(*start_sppf_node as *const _ as usize)
            }
            ParseTree::StartInput(start_input) => Some(*start_input as *const _ as usize),
            ParseTree::StartParseError(start_parse_error) => {
                Some(*start_parse_error as *const _ as usize)
            }
            ParseTree::StartParseSuccess(start_parse_success) => {
                Some(*start_parse_success as *const _ as usize)
            }
            ParseTree::StartGrammar(start_grammar) => Some(*start_grammar as *const _ as usize),
            ParseTree::StartNonterminal(start_nonterminal) => {
                Some(*start_nonterminal as *const _ as usize)
            }
            ParseTree::StartTerminal(start_terminal) => Some(*start_terminal as *const _ as usize),
            ParseTree::StartSlot(start_slot) => Some(*start_slot as *const _ as usize),
            ParseTree::StartTerminalSet(start_terminal_set) => {
                Some(*start_terminal_set as *const _ as usize)
            }
            ParseTree::Token(_) => None,
        }
    }
    pub fn origin(&self) -> Option<Origin> {
        match self {
            ParseTree::S(s) => s.origin(),
            ParseTree::Parse(parse) => parse.origin(),
            ParseTree::Parser(parser) => parser.origin(),
            ParseTree::SPPFNode(sppf_node) => sppf_node.origin(),
            ParseTree::Input(input) => input.origin(),
            ParseTree::ParseError(parse_error) => parse_error.origin(),
            ParseTree::ParseSuccess(parse_success) => parse_success.origin(),
            ParseTree::Grammar(grammar) => grammar.origin(),
            ParseTree::Nonterminal(nonterminal) => nonterminal.origin(),
            ParseTree::Terminal(terminal) => terminal.origin(),
            ParseTree::Slot(slot) => slot.origin(),
            ParseTree::TerminalSet(terminal_set) => terminal_set.origin(),
            ParseTree::StartS(start_s) => start_s.origin(),
            ParseTree::StartParse(start_parse) => start_parse.origin(),
            ParseTree::StartParser(start_parser) => start_parser.origin(),
            ParseTree::StartSPPFNode(start_sppf_node) => start_sppf_node.origin(),
            ParseTree::StartInput(start_input) => start_input.origin(),
            ParseTree::StartParseError(start_parse_error) => start_parse_error.origin(),
            ParseTree::StartParseSuccess(start_parse_success) => start_parse_success.origin(),
            ParseTree::StartGrammar(start_grammar) => start_grammar.origin(),
            ParseTree::StartNonterminal(start_nonterminal) => start_nonterminal.origin(),
            ParseTree::StartTerminal(start_terminal) => start_terminal.origin(),
            ParseTree::StartSlot(start_slot) => start_slot.origin(),
            ParseTree::StartTerminalSet(start_terminal_set) => start_terminal_set.origin(),
            ParseTree::Token(_) => None,
        }
    }
    pub(crate) fn unwrap_s(self) -> &'a S<'a> {
        match self {
            ParseTree::S(s) => s,
            _ => panic!(),
        }
    }
    pub(crate) fn unwrap_parse(self) -> &'a Parse<'a> {
        match self {
            ParseTree::Parse(parse) => parse,
            _ => panic!(),
        }
    }
    pub(crate) fn unwrap_parser(self) -> &'a Parser<'a> {
        match self {
            ParseTree::Parser(parser) => parser,
            _ => panic!(),
        }
    }
    pub(crate) fn unwrap_sppf_node(self) -> &'a SPPFNode<'a> {
        match self {
            ParseTree::SPPFNode(sppf_node) => sppf_node,
            _ => panic!(),
        }
    }
    pub(crate) fn unwrap_input(self) -> &'a Input<'a> {
        match self {
            ParseTree::Input(input) => input,
            _ => panic!(),
        }
    }
    pub(crate) fn unwrap_parse_error(self) -> &'a ParseError<'a> {
        match self {
            ParseTree::ParseError(parse_error) => parse_error,
            _ => panic!(),
        }
    }
    pub(crate) fn unwrap_parse_success(self) -> &'a ParseSuccess<'a> {
        match self {
            ParseTree::ParseSuccess(parse_success) => parse_success,
            _ => panic!(),
        }
    }
    pub(crate) fn unwrap_grammar(self) -> &'a Grammar<'a> {
        match self {
            ParseTree::Grammar(grammar) => grammar,
            _ => panic!(),
        }
    }
    pub(crate) fn unwrap_nonterminal(self) -> &'a Nonterminal<'a> {
        match self {
            ParseTree::Nonterminal(nonterminal) => nonterminal,
            _ => panic!(),
        }
    }
    pub(crate) fn unwrap_terminal(self) -> &'a Terminal<'a> {
        match self {
            ParseTree::Terminal(terminal) => terminal,
            _ => panic!(),
        }
    }
    pub(crate) fn unwrap_slot(self) -> &'a Slot<'a> {
        match self {
            ParseTree::Slot(slot) => slot,
            _ => panic!(),
        }
    }
    pub(crate) fn unwrap_terminal_set(self) -> &'a TerminalSet<'a> {
        match self {
            ParseTree::TerminalSet(terminal_set) => terminal_set,
            _ => panic!(),
        }
    }
    pub(crate) fn unwrap_start_s(self) -> &'a Start<&'a S<'a>, ()> {
        match self {
            ParseTree::StartS(start_s) => start_s,
            _ => panic!(),
        }
    }
    pub(crate) fn unwrap_start_parse(self) -> &'a Start<&'a Parse<'a>, ()> {
        match self {
            ParseTree::StartParse(start_parse) => start_parse,
            _ => panic!(),
        }
    }
    pub(crate) fn unwrap_start_parser(self) -> &'a Start<&'a Parser<'a>, ()> {
        match self {
            ParseTree::StartParser(start_parser) => start_parser,
            _ => panic!(),
        }
    }
    pub(crate) fn unwrap_start_sppf_node(self) -> &'a Start<&'a SPPFNode<'a>, ()> {
        match self {
            ParseTree::StartSPPFNode(start_sppf_node) => start_sppf_node,
            _ => panic!(),
        }
    }
    pub(crate) fn unwrap_start_input(self) -> &'a Start<&'a Input<'a>, ()> {
        match self {
            ParseTree::StartInput(start_input) => start_input,
            _ => panic!(),
        }
    }
    pub(crate) fn unwrap_start_parse_error(self) -> &'a Start<&'a ParseError<'a>, ()> {
        match self {
            ParseTree::StartParseError(start_parse_error) => start_parse_error,
            _ => panic!(),
        }
    }
    pub(crate) fn unwrap_start_parse_success(self) -> &'a Start<&'a ParseSuccess<'a>, ()> {
        match self {
            ParseTree::StartParseSuccess(start_parse_success) => start_parse_success,
            _ => panic!(),
        }
    }
    pub(crate) fn unwrap_start_grammar(self) -> &'a Start<&'a Grammar<'a>, ()> {
        match self {
            ParseTree::StartGrammar(start_grammar) => start_grammar,
            _ => panic!(),
        }
    }
    pub(crate) fn unwrap_start_nonterminal(self) -> &'a Start<&'a Nonterminal<'a>, ()> {
        match self {
            ParseTree::StartNonterminal(start_nonterminal) => start_nonterminal,
            _ => panic!(),
        }
    }
    pub(crate) fn unwrap_start_terminal(self) -> &'a Start<&'a Terminal<'a>, ()> {
        match self {
            ParseTree::StartTerminal(start_terminal) => start_terminal,
            _ => panic!(),
        }
    }
    pub(crate) fn unwrap_start_slot(self) -> &'a Start<&'a Slot<'a>, ()> {
        match self {
            ParseTree::StartSlot(start_slot) => start_slot,
            _ => panic!(),
        }
    }
    pub(crate) fn unwrap_start_terminal_set(self) -> &'a Start<&'a TerminalSet<'a>, ()> {
        match self {
            ParseTree::StartTerminalSet(start_terminal_set) => start_terminal_set,
            _ => panic!(),
        }
    }
    fn unwrap_token(self) -> Token {
        match self {
            ParseTree::Token(t) => t,
            _ => panic!(),
        }
    }
}
pub trait ListNode<'a> {
    fn iter(&'a self) -> IntoIter<ParseTree<'a>>;
}
pub trait OptNode {
    type Inner;
    fn value(&self) -> Option<&Self::Inner>;
}
// S = Parse Parser SPPFNode Input ParseError ParseSuccess Grammar Nonterminal Terminal Slot
// TerminalSet
#[derive(Debug)]
pub enum S<'a> {
    Alt0 {
        parse: &'a Parse<'a>,
        parser: &'a Parser<'a>,
        sppf_node: &'a SPPFNode<'a>,
        input: &'a Input<'a>,
        parse_error: &'a ParseError<'a>,
        parse_success: &'a ParseSuccess<'a>,
        grammar: &'a Grammar<'a>,
        nonterminal: &'a Nonterminal<'a>,
        terminal: &'a Terminal<'a>,
        slot: &'a Slot<'a>,
        terminal_set: &'a TerminalSet<'a>,
        span: Span,
    },
    Amb(&'a [&'a S<'a>]),
}
// Parse = "a"
#[derive(Debug)]
pub enum Parse<'a> {
    Alt0 { lit_0: Token, span: Span },
    Amb(&'a [&'a Parse<'a>]),
}
// Parser = "b"
#[derive(Debug)]
pub enum Parser<'a> {
    Alt0 { lit_0: Token, span: Span },
    Amb(&'a [&'a Parser<'a>]),
}
// SPPFNode = "c"
#[derive(Debug)]
pub enum SPPFNode<'a> {
    Alt0 { lit_0: Token, span: Span },
    Amb(&'a [&'a SPPFNode<'a>]),
}
// Input = "d"
#[derive(Debug)]
pub enum Input<'a> {
    Alt0 { lit_0: Token, span: Span },
    Amb(&'a [&'a Input<'a>]),
}
// ParseError = "e"
#[derive(Debug)]
pub enum ParseError<'a> {
    Alt0 { lit_0: Token, span: Span },
    Amb(&'a [&'a ParseError<'a>]),
}
// ParseSuccess = "f"
#[derive(Debug)]
pub enum ParseSuccess<'a> {
    Alt0 { lit_0: Token, span: Span },
    Amb(&'a [&'a ParseSuccess<'a>]),
}
// Grammar = "g"
#[derive(Debug)]
pub enum Grammar<'a> {
    Alt0 { lit_0: Token, span: Span },
    Amb(&'a [&'a Grammar<'a>]),
}
// Nonterminal = "h"
#[derive(Debug)]
pub enum Nonterminal<'a> {
    Alt0 { lit_0: Token, span: Span },
    Amb(&'a [&'a Nonterminal<'a>]),
}
// Terminal = "i"
#[derive(Debug)]
pub enum Terminal<'a> {
    Alt0 { lit_0: Token, span: Span },
    Amb(&'a [&'a Terminal<'a>]),
}
// Slot = "j"
#[derive(Debug)]
pub enum Slot<'a> {
    Alt0 { lit_0: Token, span: Span },
    Amb(&'a [&'a Slot<'a>]),
}
// TerminalSet = "k"
#[derive(Debug)]
pub enum TerminalSet<'a> {
    Alt0 { lit_0: Token, span: Span },
    Amb(&'a [&'a TerminalSet<'a>]),
}
impl<'a> S<'a> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::S(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match self {
            S::Alt0 {
                parse,
                parser,
                sppf_node,
                input,
                parse_error,
                parse_success,
                grammar,
                nonterminal,
                terminal,
                slot,
                terminal_set,
                ..
            } => match index {
                0 => Some(ParseTree::Parse(parse)),
                1 => Some(ParseTree::Parser(parser)),
                2 => Some(ParseTree::SPPFNode(sppf_node)),
                3 => Some(ParseTree::Input(input)),
                4 => Some(ParseTree::ParseError(parse_error)),
                5 => Some(ParseTree::ParseSuccess(parse_success)),
                6 => Some(ParseTree::Grammar(grammar)),
                7 => Some(ParseTree::Nonterminal(nonterminal)),
                8 => Some(ParseTree::Terminal(terminal)),
                9 => Some(ParseTree::Slot(slot)),
                10 => Some(ParseTree::TerminalSet(terminal_set)),
                _ => None,
            },
            S::Amb(alts) => alts.get(index).copied().map(ParseTree::S),
        }
    }
    pub fn child_count(&self) -> usize {
        match self {
            S::Alt0 { .. } => 11usize,
            S::Amb(alts) => alts.len(),
        }
    }
    pub fn span(&self) -> Span {
        match self {
            S::Alt0 { span, .. } => *span,
            S::Amb(alts) => alts[0].span(),
        }
    }
    pub fn display_name(&self) -> &'static str {
        match self {
            S::Amb(_) => "Amb",
            _ => "S",
        }
    }
    pub fn origin(&self) -> Option<Origin> {
        None
    }
    pub fn parse(&self) -> &'a Parse<'a> {
        match self {
            S::Alt0 { parse, .. } => parse,
            S::Amb(_) => panic!("S is ambiguous"),
        }
    }
    pub fn parser(&self) -> &'a Parser<'a> {
        match self {
            S::Alt0 { parser, .. } => parser,
            S::Amb(_) => panic!("S is ambiguous"),
        }
    }
    pub fn sppf_node(&self) -> &'a SPPFNode<'a> {
        match self {
            S::Alt0 { sppf_node, .. } => sppf_node,
            S::Amb(_) => panic!("S is ambiguous"),
        }
    }
    pub fn input(&self) -> &'a Input<'a> {
        match self {
            S::Alt0 { input, .. } => input,
            S::Amb(_) => panic!("S is ambiguous"),
        }
    }
    pub fn parse_error(&self) -> &'a ParseError<'a> {
        match self {
            S::Alt0 { parse_error, .. } => parse_error,
            S::Amb(_) => panic!("S is ambiguous"),
        }
    }
    pub fn parse_success(&self) -> &'a ParseSuccess<'a> {
        match self {
            S::Alt0 { parse_success, .. } => parse_success,
            S::Amb(_) => panic!("S is ambiguous"),
        }
    }
    pub fn grammar(&self) -> &'a Grammar<'a> {
        match self {
            S::Alt0 { grammar, .. } => grammar,
            S::Amb(_) => panic!("S is ambiguous"),
        }
    }
    pub fn nonterminal(&self) -> &'a Nonterminal<'a> {
        match self {
            S::Alt0 { nonterminal, .. } => nonterminal,
            S::Amb(_) => panic!("S is ambiguous"),
        }
    }
    pub fn terminal(&self) -> &'a Terminal<'a> {
        match self {
            S::Alt0 { terminal, .. } => terminal,
            S::Amb(_) => panic!("S is ambiguous"),
        }
    }
    pub fn slot(&self) -> &'a Slot<'a> {
        match self {
            S::Alt0 { slot, .. } => slot,
            S::Amb(_) => panic!("S is ambiguous"),
        }
    }
    pub fn terminal_set(&self) -> &'a TerminalSet<'a> {
        match self {
            S::Alt0 { terminal_set, .. } => terminal_set,
            S::Amb(_) => panic!("S is ambiguous"),
        }
    }
}
impl<'a> Parse<'a> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::Parse(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match self {
            Parse::Alt0 { lit_0, .. } => match index {
                0 => Some(ParseTree::Token(*lit_0)),
                _ => None,
            },
            Parse::Amb(alts) => alts.get(index).copied().map(ParseTree::Parse),
        }
    }
    pub fn child_count(&self) -> usize {
        match self {
            Parse::Alt0 { .. } => 1usize,
            Parse::Amb(alts) => alts.len(),
        }
    }
    pub fn span(&self) -> Span {
        match self {
            Parse::Alt0 { span, .. } => *span,
            Parse::Amb(alts) => alts[0].span(),
        }
    }
    pub fn display_name(&self) -> &'static str {
        match self {
            Parse::Amb(_) => "Amb",
            _ => "Parse",
        }
    }
    pub fn origin(&self) -> Option<Origin> {
        None
    }
    pub fn lit_0(&self) -> Token {
        match self {
            Parse::Alt0 { lit_0, .. } => *lit_0,
            Parse::Amb(_) => panic!("Parse is ambiguous"),
        }
    }
}
impl<'a> Parser<'a> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::Parser(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match self {
            Parser::Alt0 { lit_0, .. } => match index {
                0 => Some(ParseTree::Token(*lit_0)),
                _ => None,
            },
            Parser::Amb(alts) => alts.get(index).copied().map(ParseTree::Parser),
        }
    }
    pub fn child_count(&self) -> usize {
        match self {
            Parser::Alt0 { .. } => 1usize,
            Parser::Amb(alts) => alts.len(),
        }
    }
    pub fn span(&self) -> Span {
        match self {
            Parser::Alt0 { span, .. } => *span,
            Parser::Amb(alts) => alts[0].span(),
        }
    }
    pub fn display_name(&self) -> &'static str {
        match self {
            Parser::Amb(_) => "Amb",
            _ => "Parser",
        }
    }
    pub fn origin(&self) -> Option<Origin> {
        None
    }
    pub fn lit_0(&self) -> Token {
        match self {
            Parser::Alt0 { lit_0, .. } => *lit_0,
            Parser::Amb(_) => panic!("Parser is ambiguous"),
        }
    }
}
impl<'a> SPPFNode<'a> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::SPPFNode(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match self {
            SPPFNode::Alt0 { lit_0, .. } => match index {
                0 => Some(ParseTree::Token(*lit_0)),
                _ => None,
            },
            SPPFNode::Amb(alts) => alts.get(index).copied().map(ParseTree::SPPFNode),
        }
    }
    pub fn child_count(&self) -> usize {
        match self {
            SPPFNode::Alt0 { .. } => 1usize,
            SPPFNode::Amb(alts) => alts.len(),
        }
    }
    pub fn span(&self) -> Span {
        match self {
            SPPFNode::Alt0 { span, .. } => *span,
            SPPFNode::Amb(alts) => alts[0].span(),
        }
    }
    pub fn display_name(&self) -> &'static str {
        match self {
            SPPFNode::Amb(_) => "Amb",
            _ => "SPPFNode",
        }
    }
    pub fn origin(&self) -> Option<Origin> {
        None
    }
    pub fn lit_0(&self) -> Token {
        match self {
            SPPFNode::Alt0 { lit_0, .. } => *lit_0,
            SPPFNode::Amb(_) => panic!("SPPFNode is ambiguous"),
        }
    }
}
impl<'a> Input<'a> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::Input(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match self {
            Input::Alt0 { lit_0, .. } => match index {
                0 => Some(ParseTree::Token(*lit_0)),
                _ => None,
            },
            Input::Amb(alts) => alts.get(index).copied().map(ParseTree::Input),
        }
    }
    pub fn child_count(&self) -> usize {
        match self {
            Input::Alt0 { .. } => 1usize,
            Input::Amb(alts) => alts.len(),
        }
    }
    pub fn span(&self) -> Span {
        match self {
            Input::Alt0 { span, .. } => *span,
            Input::Amb(alts) => alts[0].span(),
        }
    }
    pub fn display_name(&self) -> &'static str {
        match self {
            Input::Amb(_) => "Amb",
            _ => "Input",
        }
    }
    pub fn origin(&self) -> Option<Origin> {
        None
    }
    pub fn lit_0(&self) -> Token {
        match self {
            Input::Alt0 { lit_0, .. } => *lit_0,
            Input::Amb(_) => panic!("Input is ambiguous"),
        }
    }
}
impl<'a> ParseError<'a> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::ParseError(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match self {
            ParseError::Alt0 { lit_0, .. } => match index {
                0 => Some(ParseTree::Token(*lit_0)),
                _ => None,
            },
            ParseError::Amb(alts) => alts.get(index).copied().map(ParseTree::ParseError),
        }
    }
    pub fn child_count(&self) -> usize {
        match self {
            ParseError::Alt0 { .. } => 1usize,
            ParseError::Amb(alts) => alts.len(),
        }
    }
    pub fn span(&self) -> Span {
        match self {
            ParseError::Alt0 { span, .. } => *span,
            ParseError::Amb(alts) => alts[0].span(),
        }
    }
    pub fn display_name(&self) -> &'static str {
        match self {
            ParseError::Amb(_) => "Amb",
            _ => "ParseError",
        }
    }
    pub fn origin(&self) -> Option<Origin> {
        None
    }
    pub fn lit_0(&self) -> Token {
        match self {
            ParseError::Alt0 { lit_0, .. } => *lit_0,
            ParseError::Amb(_) => panic!("ParseError is ambiguous"),
        }
    }
}
impl<'a> ParseSuccess<'a> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::ParseSuccess(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match self {
            ParseSuccess::Alt0 { lit_0, .. } => match index {
                0 => Some(ParseTree::Token(*lit_0)),
                _ => None,
            },
            ParseSuccess::Amb(alts) => alts.get(index).copied().map(ParseTree::ParseSuccess),
        }
    }
    pub fn child_count(&self) -> usize {
        match self {
            ParseSuccess::Alt0 { .. } => 1usize,
            ParseSuccess::Amb(alts) => alts.len(),
        }
    }
    pub fn span(&self) -> Span {
        match self {
            ParseSuccess::Alt0 { span, .. } => *span,
            ParseSuccess::Amb(alts) => alts[0].span(),
        }
    }
    pub fn display_name(&self) -> &'static str {
        match self {
            ParseSuccess::Amb(_) => "Amb",
            _ => "ParseSuccess",
        }
    }
    pub fn origin(&self) -> Option<Origin> {
        None
    }
    pub fn lit_0(&self) -> Token {
        match self {
            ParseSuccess::Alt0 { lit_0, .. } => *lit_0,
            ParseSuccess::Amb(_) => panic!("ParseSuccess is ambiguous"),
        }
    }
}
impl<'a> Grammar<'a> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::Grammar(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match self {
            Grammar::Alt0 { lit_0, .. } => match index {
                0 => Some(ParseTree::Token(*lit_0)),
                _ => None,
            },
            Grammar::Amb(alts) => alts.get(index).copied().map(ParseTree::Grammar),
        }
    }
    pub fn child_count(&self) -> usize {
        match self {
            Grammar::Alt0 { .. } => 1usize,
            Grammar::Amb(alts) => alts.len(),
        }
    }
    pub fn span(&self) -> Span {
        match self {
            Grammar::Alt0 { span, .. } => *span,
            Grammar::Amb(alts) => alts[0].span(),
        }
    }
    pub fn display_name(&self) -> &'static str {
        match self {
            Grammar::Amb(_) => "Amb",
            _ => "Grammar",
        }
    }
    pub fn origin(&self) -> Option<Origin> {
        None
    }
    pub fn lit_0(&self) -> Token {
        match self {
            Grammar::Alt0 { lit_0, .. } => *lit_0,
            Grammar::Amb(_) => panic!("Grammar is ambiguous"),
        }
    }
}
impl<'a> Nonterminal<'a> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::Nonterminal(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match self {
            Nonterminal::Alt0 { lit_0, .. } => match index {
                0 => Some(ParseTree::Token(*lit_0)),
                _ => None,
            },
            Nonterminal::Amb(alts) => alts.get(index).copied().map(ParseTree::Nonterminal),
        }
    }
    pub fn child_count(&self) -> usize {
        match self {
            Nonterminal::Alt0 { .. } => 1usize,
            Nonterminal::Amb(alts) => alts.len(),
        }
    }
    pub fn span(&self) -> Span {
        match self {
            Nonterminal::Alt0 { span, .. } => *span,
            Nonterminal::Amb(alts) => alts[0].span(),
        }
    }
    pub fn display_name(&self) -> &'static str {
        match self {
            Nonterminal::Amb(_) => "Amb",
            _ => "Nonterminal",
        }
    }
    pub fn origin(&self) -> Option<Origin> {
        None
    }
    pub fn lit_0(&self) -> Token {
        match self {
            Nonterminal::Alt0 { lit_0, .. } => *lit_0,
            Nonterminal::Amb(_) => panic!("Nonterminal is ambiguous"),
        }
    }
}
impl<'a> Terminal<'a> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::Terminal(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match self {
            Terminal::Alt0 { lit_0, .. } => match index {
                0 => Some(ParseTree::Token(*lit_0)),
                _ => None,
            },
            Terminal::Amb(alts) => alts.get(index).copied().map(ParseTree::Terminal),
        }
    }
    pub fn child_count(&self) -> usize {
        match self {
            Terminal::Alt0 { .. } => 1usize,
            Terminal::Amb(alts) => alts.len(),
        }
    }
    pub fn span(&self) -> Span {
        match self {
            Terminal::Alt0 { span, .. } => *span,
            Terminal::Amb(alts) => alts[0].span(),
        }
    }
    pub fn display_name(&self) -> &'static str {
        match self {
            Terminal::Amb(_) => "Amb",
            _ => "Terminal",
        }
    }
    pub fn origin(&self) -> Option<Origin> {
        None
    }
    pub fn lit_0(&self) -> Token {
        match self {
            Terminal::Alt0 { lit_0, .. } => *lit_0,
            Terminal::Amb(_) => panic!("Terminal is ambiguous"),
        }
    }
}
impl<'a> Slot<'a> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::Slot(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match self {
            Slot::Alt0 { lit_0, .. } => match index {
                0 => Some(ParseTree::Token(*lit_0)),
                _ => None,
            },
            Slot::Amb(alts) => alts.get(index).copied().map(ParseTree::Slot),
        }
    }
    pub fn child_count(&self) -> usize {
        match self {
            Slot::Alt0 { .. } => 1usize,
            Slot::Amb(alts) => alts.len(),
        }
    }
    pub fn span(&self) -> Span {
        match self {
            Slot::Alt0 { span, .. } => *span,
            Slot::Amb(alts) => alts[0].span(),
        }
    }
    pub fn display_name(&self) -> &'static str {
        match self {
            Slot::Amb(_) => "Amb",
            _ => "Slot",
        }
    }
    pub fn origin(&self) -> Option<Origin> {
        None
    }
    pub fn lit_0(&self) -> Token {
        match self {
            Slot::Alt0 { lit_0, .. } => *lit_0,
            Slot::Amb(_) => panic!("Slot is ambiguous"),
        }
    }
}
impl<'a> TerminalSet<'a> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::TerminalSet(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match self {
            TerminalSet::Alt0 { lit_0, .. } => match index {
                0 => Some(ParseTree::Token(*lit_0)),
                _ => None,
            },
            TerminalSet::Amb(alts) => alts.get(index).copied().map(ParseTree::TerminalSet),
        }
    }
    pub fn child_count(&self) -> usize {
        match self {
            TerminalSet::Alt0 { .. } => 1usize,
            TerminalSet::Amb(alts) => alts.len(),
        }
    }
    pub fn span(&self) -> Span {
        match self {
            TerminalSet::Alt0 { span, .. } => *span,
            TerminalSet::Amb(alts) => alts[0].span(),
        }
    }
    pub fn display_name(&self) -> &'static str {
        match self {
            TerminalSet::Amb(_) => "Amb",
            _ => "TerminalSet",
        }
    }
    pub fn origin(&self) -> Option<Origin> {
        None
    }
    pub fn lit_0(&self) -> Token {
        match self {
            TerminalSet::Alt0 { lit_0, .. } => *lit_0,
            TerminalSet::Amb(_) => panic!("TerminalSet is ambiguous"),
        }
    }
}
impl<'a> Start<&'a S<'a>, ()> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::StartS(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match index {
            0 => Some(ParseTree::S(self.node)),
            _ => None,
        }
    }
    pub fn child_count(&self) -> usize {
        1usize
    }
    pub fn span(&self) -> Span {
        self.span
    }
    pub fn display_name(&self) -> &'static str {
        "Start"
    }
    pub fn origin(&self) -> Option<Origin> {
        Some(Origin::Start)
    }
}
impl<'a> Start<&'a Parse<'a>, ()> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::StartParse(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match index {
            0 => Some(ParseTree::Parse(self.node)),
            _ => None,
        }
    }
    pub fn child_count(&self) -> usize {
        1usize
    }
    pub fn span(&self) -> Span {
        self.span
    }
    pub fn display_name(&self) -> &'static str {
        "Start"
    }
    pub fn origin(&self) -> Option<Origin> {
        Some(Origin::Start)
    }
}
impl<'a> Start<&'a Parser<'a>, ()> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::StartParser(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match index {
            0 => Some(ParseTree::Parser(self.node)),
            _ => None,
        }
    }
    pub fn child_count(&self) -> usize {
        1usize
    }
    pub fn span(&self) -> Span {
        self.span
    }
    pub fn display_name(&self) -> &'static str {
        "Start"
    }
    pub fn origin(&self) -> Option<Origin> {
        Some(Origin::Start)
    }
}
impl<'a> Start<&'a SPPFNode<'a>, ()> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::StartSPPFNode(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match index {
            0 => Some(ParseTree::SPPFNode(self.node)),
            _ => None,
        }
    }
    pub fn child_count(&self) -> usize {
        1usize
    }
    pub fn span(&self) -> Span {
        self.span
    }
    pub fn display_name(&self) -> &'static str {
        "Start"
    }
    pub fn origin(&self) -> Option<Origin> {
        Some(Origin::Start)
    }
}
impl<'a> Start<&'a Input<'a>, ()> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::StartInput(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match index {
            0 => Some(ParseTree::Input(self.node)),
            _ => None,
        }
    }
    pub fn child_count(&self) -> usize {
        1usize
    }
    pub fn span(&self) -> Span {
        self.span
    }
    pub fn display_name(&self) -> &'static str {
        "Start"
    }
    pub fn origin(&self) -> Option<Origin> {
        Some(Origin::Start)
    }
}
impl<'a> Start<&'a ParseError<'a>, ()> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::StartParseError(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match index {
            0 => Some(ParseTree::ParseError(self.node)),
            _ => None,
        }
    }
    pub fn child_count(&self) -> usize {
        1usize
    }
    pub fn span(&self) -> Span {
        self.span
    }
    pub fn display_name(&self) -> &'static str {
        "Start"
    }
    pub fn origin(&self) -> Option<Origin> {
        Some(Origin::Start)
    }
}
impl<'a> Start<&'a ParseSuccess<'a>, ()> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::StartParseSuccess(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match index {
            0 => Some(ParseTree::ParseSuccess(self.node)),
            _ => None,
        }
    }
    pub fn child_count(&self) -> usize {
        1usize
    }
    pub fn span(&self) -> Span {
        self.span
    }
    pub fn display_name(&self) -> &'static str {
        "Start"
    }
    pub fn origin(&self) -> Option<Origin> {
        Some(Origin::Start)
    }
}
impl<'a> Start<&'a Grammar<'a>, ()> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::StartGrammar(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match index {
            0 => Some(ParseTree::Grammar(self.node)),
            _ => None,
        }
    }
    pub fn child_count(&self) -> usize {
        1usize
    }
    pub fn span(&self) -> Span {
        self.span
    }
    pub fn display_name(&self) -> &'static str {
        "Start"
    }
    pub fn origin(&self) -> Option<Origin> {
        Some(Origin::Start)
    }
}
impl<'a> Start<&'a Nonterminal<'a>, ()> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::StartNonterminal(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match index {
            0 => Some(ParseTree::Nonterminal(self.node)),
            _ => None,
        }
    }
    pub fn child_count(&self) -> usize {
        1usize
    }
    pub fn span(&self) -> Span {
        self.span
    }
    pub fn display_name(&self) -> &'static str {
        "Start"
    }
    pub fn origin(&self) -> Option<Origin> {
        Some(Origin::Start)
    }
}
impl<'a> Start<&'a Terminal<'a>, ()> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::StartTerminal(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match index {
            0 => Some(ParseTree::Terminal(self.node)),
            _ => None,
        }
    }
    pub fn child_count(&self) -> usize {
        1usize
    }
    pub fn span(&self) -> Span {
        self.span
    }
    pub fn display_name(&self) -> &'static str {
        "Start"
    }
    pub fn origin(&self) -> Option<Origin> {
        Some(Origin::Start)
    }
}
impl<'a> Start<&'a Slot<'a>, ()> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::StartSlot(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match index {
            0 => Some(ParseTree::Slot(self.node)),
            _ => None,
        }
    }
    pub fn child_count(&self) -> usize {
        1usize
    }
    pub fn span(&self) -> Span {
        self.span
    }
    pub fn display_name(&self) -> &'static str {
        "Start"
    }
    pub fn origin(&self) -> Option<Origin> {
        Some(Origin::Start)
    }
}
impl<'a> Start<&'a TerminalSet<'a>, ()> {
    pub fn as_parse_tree(&'a self) -> ParseTree<'a> {
        ParseTree::StartTerminalSet(self)
    }
    pub fn child(&self, index: usize) -> Option<ParseTree<'a>> {
        match index {
            0 => Some(ParseTree::TerminalSet(self.node)),
            _ => None,
        }
    }
    pub fn child_count(&self) -> usize {
        1usize
    }
    pub fn span(&self) -> Span {
        self.span
    }
    pub fn display_name(&self) -> &'static str {
        "Start"
    }
    pub fn origin(&self) -> Option<Origin> {
        Some(Origin::Start)
    }
}
#[derive(Debug, Clone, Copy)]
pub struct Token {
    pub kind: TokenKind,
    span: Span,
}
impl Token {
    pub fn as_parse_tree<'a>(&self) -> ParseTree<'a> {
        ParseTree::Token(*self)
    }
    pub fn span(&self) -> Span {
        self.span
    }
}
fn token_kind(terminal_id: TerminalId) -> TokenKind {
    match terminal_id {
        // "a"
        TerminalId(0) => TokenKind::T0,
        // "b"
        TerminalId(1) => TokenKind::T1,
        // "c"
        TerminalId(2) => TokenKind::T2,
        // "d"
        TerminalId(3) => TokenKind::T3,
        // "e"
        TerminalId(4) => TokenKind::T4,
        // "f"
        TerminalId(5) => TokenKind::T5,
        // "g"
        TerminalId(6) => TokenKind::T6,
        // "h"
        TerminalId(7) => TokenKind::T7,
        // "i"
        TerminalId(8) => TokenKind::T8,
        // "j"
        TerminalId(9) => TokenKind::T9,
        // "k"
        TerminalId(10) => TokenKind::T10,
        _ => unreachable!("Unknown TerminalId: {:?}", terminal_id),
    }
}
pub struct ImportNamesParseTreeBuilder<'a> {
    pub arena: &'a Arena,
}
impl<'a> ImportNamesParseTreeBuilder<'a> {
    pub fn new(tree_arena: &'a Arena) -> Self {
        Self { arena: tree_arena }
    }
}
impl<'a> ParseTreeBuilder<ParseTree<'a>> for ImportNamesParseTreeBuilder<'a> {
    fn new_nonterminal_node(
        &self,
        nonterminal_node: &NonterminalNode,
        children: OneOrMany<ParseTree<'a>>,
    ) -> ParseTree<'a> {
        match nonterminal_node.nonterminal_id {
            // S
            NonterminalId(0) => match nonterminal_node.return_slot {
                // S = Parse Parser SPPFNode Input ParseError ParseSuccess Grammar Nonterminal Terminal Slot
                // TerminalSet
                SlotId(11) => {
                    let [
                        parse,
                        parser,
                        sppf_node,
                        input,
                        parse_error,
                        parse_success,
                        grammar,
                        nonterminal,
                        terminal,
                        slot,
                        terminal_set,
                    ] = children.into_array::<11usize>();
                    ParseTree::S(self.arena.alloc(S::Alt0 {
                        parse: parse.unwrap_parse(),
                        parser: parser.unwrap_parser(),
                        sppf_node: sppf_node.unwrap_sppf_node(),
                        input: input.unwrap_input(),
                        parse_error: parse_error.unwrap_parse_error(),
                        parse_success: parse_success.unwrap_parse_success(),
                        grammar: grammar.unwrap_grammar(),
                        nonterminal: nonterminal.unwrap_nonterminal(),
                        terminal: terminal.unwrap_terminal(),
                        slot: slot.unwrap_slot(),
                        terminal_set: terminal_set.unwrap_terminal_set(),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // Parse
            NonterminalId(1) => match nonterminal_node.return_slot {
                // Parse = "a"
                SlotId(13) => {
                    let [lit_0] = children.into_array::<1usize>();
                    ParseTree::Parse(self.arena.alloc(Parse::Alt0 {
                        lit_0: lit_0.unwrap_token(),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // Parser
            NonterminalId(2) => match nonterminal_node.return_slot {
                // Parser = "b"
                SlotId(15) => {
                    let [lit_0] = children.into_array::<1usize>();
                    ParseTree::Parser(self.arena.alloc(Parser::Alt0 {
                        lit_0: lit_0.unwrap_token(),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // SPPFNode
            NonterminalId(3) => match nonterminal_node.return_slot {
                // SPPFNode = "c"
                SlotId(17) => {
                    let [lit_0] = children.into_array::<1usize>();
                    ParseTree::SPPFNode(self.arena.alloc(SPPFNode::Alt0 {
                        lit_0: lit_0.unwrap_token(),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // Input
            NonterminalId(4) => match nonterminal_node.return_slot {
                // Input = "d"
                SlotId(19) => {
                    let [lit_0] = children.into_array::<1usize>();
                    ParseTree::Input(self.arena.alloc(Input::Alt0 {
                        lit_0: lit_0.unwrap_token(),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // ParseError
            NonterminalId(5) => match nonterminal_node.return_slot {
                // ParseError = "e"
                SlotId(21) => {
                    let [lit_0] = children.into_array::<1usize>();
                    ParseTree::ParseError(self.arena.alloc(ParseError::Alt0 {
                        lit_0: lit_0.unwrap_token(),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // ParseSuccess
            NonterminalId(6) => match nonterminal_node.return_slot {
                // ParseSuccess = "f"
                SlotId(23) => {
                    let [lit_0] = children.into_array::<1usize>();
                    ParseTree::ParseSuccess(self.arena.alloc(ParseSuccess::Alt0 {
                        lit_0: lit_0.unwrap_token(),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // Grammar
            NonterminalId(7) => match nonterminal_node.return_slot {
                // Grammar = "g"
                SlotId(25) => {
                    let [lit_0] = children.into_array::<1usize>();
                    ParseTree::Grammar(self.arena.alloc(Grammar::Alt0 {
                        lit_0: lit_0.unwrap_token(),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // Nonterminal
            NonterminalId(8) => match nonterminal_node.return_slot {
                // Nonterminal = "h"
                SlotId(27) => {
                    let [lit_0] = children.into_array::<1usize>();
                    ParseTree::Nonterminal(self.arena.alloc(Nonterminal::Alt0 {
                        lit_0: lit_0.unwrap_token(),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // Terminal
            NonterminalId(9) => match nonterminal_node.return_slot {
                // Terminal = "i"
                SlotId(29) => {
                    let [lit_0] = children.into_array::<1usize>();
                    ParseTree::Terminal(self.arena.alloc(Terminal::Alt0 {
                        lit_0: lit_0.unwrap_token(),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // Slot
            NonterminalId(10) => match nonterminal_node.return_slot {
                // Slot = "j"
                SlotId(31) => {
                    let [lit_0] = children.into_array::<1usize>();
                    ParseTree::Slot(self.arena.alloc(Slot::Alt0 {
                        lit_0: lit_0.unwrap_token(),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // TerminalSet
            NonterminalId(11) => match nonterminal_node.return_slot {
                // TerminalSet = "k"
                SlotId(33) => {
                    let [lit_0] = children.into_array::<1usize>();
                    ParseTree::TerminalSet(self.arena.alloc(TerminalSet::Alt0 {
                        lit_0: lit_0.unwrap_token(),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // StartS
            NonterminalId(12) => match nonterminal_node.return_slot {
                // StartS = start:S
                SlotId(35) => {
                    let [start] = children.into_array::<1usize>();
                    ParseTree::StartS(self.arena.alloc(Start {
                        before: (),
                        node: start.unwrap_s(),
                        after: (),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // StartParse
            NonterminalId(13) => match nonterminal_node.return_slot {
                // StartParse = start:Parse
                SlotId(37) => {
                    let [start] = children.into_array::<1usize>();
                    ParseTree::StartParse(self.arena.alloc(Start {
                        before: (),
                        node: start.unwrap_parse(),
                        after: (),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // StartParser
            NonterminalId(14) => match nonterminal_node.return_slot {
                // StartParser = start:Parser
                SlotId(39) => {
                    let [start] = children.into_array::<1usize>();
                    ParseTree::StartParser(self.arena.alloc(Start {
                        before: (),
                        node: start.unwrap_parser(),
                        after: (),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // StartSPPFNode
            NonterminalId(15) => match nonterminal_node.return_slot {
                // StartSPPFNode = start:SPPFNode
                SlotId(41) => {
                    let [start] = children.into_array::<1usize>();
                    ParseTree::StartSPPFNode(self.arena.alloc(Start {
                        before: (),
                        node: start.unwrap_sppf_node(),
                        after: (),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // StartInput
            NonterminalId(16) => match nonterminal_node.return_slot {
                // StartInput = start:Input
                SlotId(43) => {
                    let [start] = children.into_array::<1usize>();
                    ParseTree::StartInput(self.arena.alloc(Start {
                        before: (),
                        node: start.unwrap_input(),
                        after: (),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // StartParseError
            NonterminalId(17) => match nonterminal_node.return_slot {
                // StartParseError = start:ParseError
                SlotId(45) => {
                    let [start] = children.into_array::<1usize>();
                    ParseTree::StartParseError(self.arena.alloc(Start {
                        before: (),
                        node: start.unwrap_parse_error(),
                        after: (),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // StartParseSuccess
            NonterminalId(18) => match nonterminal_node.return_slot {
                // StartParseSuccess = start:ParseSuccess
                SlotId(47) => {
                    let [start] = children.into_array::<1usize>();
                    ParseTree::StartParseSuccess(self.arena.alloc(Start {
                        before: (),
                        node: start.unwrap_parse_success(),
                        after: (),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // StartGrammar
            NonterminalId(19) => match nonterminal_node.return_slot {
                // StartGrammar = start:Grammar
                SlotId(49) => {
                    let [start] = children.into_array::<1usize>();
                    ParseTree::StartGrammar(self.arena.alloc(Start {
                        before: (),
                        node: start.unwrap_grammar(),
                        after: (),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // StartNonterminal
            NonterminalId(20) => match nonterminal_node.return_slot {
                // StartNonterminal = start:Nonterminal
                SlotId(51) => {
                    let [start] = children.into_array::<1usize>();
                    ParseTree::StartNonterminal(self.arena.alloc(Start {
                        before: (),
                        node: start.unwrap_nonterminal(),
                        after: (),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // StartTerminal
            NonterminalId(21) => match nonterminal_node.return_slot {
                // StartTerminal = start:Terminal
                SlotId(53) => {
                    let [start] = children.into_array::<1usize>();
                    ParseTree::StartTerminal(self.arena.alloc(Start {
                        before: (),
                        node: start.unwrap_terminal(),
                        after: (),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // StartSlot
            NonterminalId(22) => match nonterminal_node.return_slot {
                // StartSlot = start:Slot
                SlotId(55) => {
                    let [start] = children.into_array::<1usize>();
                    ParseTree::StartSlot(self.arena.alloc(Start {
                        before: (),
                        node: start.unwrap_slot(),
                        after: (),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            // StartTerminalSet
            NonterminalId(23) => match nonterminal_node.return_slot {
                // StartTerminalSet = start:TerminalSet
                SlotId(57) => {
                    let [start] = children.into_array::<1usize>();
                    ParseTree::StartTerminalSet(self.arena.alloc(Start {
                        before: (),
                        node: start.unwrap_terminal_set(),
                        after: (),
                        span: nonterminal_node.span,
                    }))
                }
                _ => unreachable!(),
            },
            _ => unreachable!(),
        }
    }
    fn new_token(&self, terminal_node: &TerminalNode) -> ParseTree<'a> {
        ParseTree::Token(Token {
            kind: token_kind(terminal_node.terminal_id),
            span: terminal_node.span,
        })
    }
    fn new_ambiguity_node(
        &self,
        parent: NonterminalId,
        alternatives: Vec<ParseTree<'a>>,
    ) -> ParseTree<'a> {
        match parent {
            grammar::S => {
                let slice = self
                    .arena
                    .alloc_slice(alternatives.into_iter().map(|a| a.unwrap_s()));
                ParseTree::S(self.arena.alloc(S::Amb(slice)))
            }
            grammar::PARSE => {
                let slice = self
                    .arena
                    .alloc_slice(alternatives.into_iter().map(|a| a.unwrap_parse()));
                ParseTree::Parse(self.arena.alloc(Parse::Amb(slice)))
            }
            grammar::PARSER => {
                let slice = self
                    .arena
                    .alloc_slice(alternatives.into_iter().map(|a| a.unwrap_parser()));
                ParseTree::Parser(self.arena.alloc(Parser::Amb(slice)))
            }
            grammar::SPPF_NODE => {
                let slice = self
                    .arena
                    .alloc_slice(alternatives.into_iter().map(|a| a.unwrap_sppf_node()));
                ParseTree::SPPFNode(self.arena.alloc(SPPFNode::Amb(slice)))
            }
            grammar::INPUT => {
                let slice = self
                    .arena
                    .alloc_slice(alternatives.into_iter().map(|a| a.unwrap_input()));
                ParseTree::Input(self.arena.alloc(Input::Amb(slice)))
            }
            grammar::PARSE_ERROR => {
                let slice = self
                    .arena
                    .alloc_slice(alternatives.into_iter().map(|a| a.unwrap_parse_error()));
                ParseTree::ParseError(self.arena.alloc(ParseError::Amb(slice)))
            }
            grammar::PARSE_SUCCESS => {
                let slice = self
                    .arena
                    .alloc_slice(alternatives.into_iter().map(|a| a.unwrap_parse_success()));
                ParseTree::ParseSuccess(self.arena.alloc(ParseSuccess::Amb(slice)))
            }
            grammar::GRAMMAR => {
                let slice = self
                    .arena
                    .alloc_slice(alternatives.into_iter().map(|a| a.unwrap_grammar()));
                ParseTree::Grammar(self.arena.alloc(Grammar::Amb(slice)))
            }
            grammar::NONTERMINAL => {
                let slice = self
                    .arena
                    .alloc_slice(alternatives.into_iter().map(|a| a.unwrap_nonterminal()));
                ParseTree::Nonterminal(self.arena.alloc(Nonterminal::Amb(slice)))
            }
            grammar::TERMINAL => {
                let slice = self
                    .arena
                    .alloc_slice(alternatives.into_iter().map(|a| a.unwrap_terminal()));
                ParseTree::Terminal(self.arena.alloc(Terminal::Amb(slice)))
            }
            grammar::SLOT => {
                let slice = self
                    .arena
                    .alloc_slice(alternatives.into_iter().map(|a| a.unwrap_slot()));
                ParseTree::Slot(self.arena.alloc(Slot::Amb(slice)))
            }
            grammar::TERMINAL_SET => {
                let slice = self
                    .arena
                    .alloc_slice(alternatives.into_iter().map(|a| a.unwrap_terminal_set()));
                ParseTree::TerminalSet(self.arena.alloc(TerminalSet::Amb(slice)))
            }
            grammar::START_S => {
                let first = alternatives[0].unwrap_start_s();
                let inner = self
                    .arena
                    .alloc_slice(alternatives.into_iter().map(|a| a.unwrap_start_s().node));
                let node = &*self.arena.alloc(S::Amb(inner));
                ParseTree::StartS(self.arena.alloc(Start {
                    before: first.before,
                    node,
                    after: first.after,
                    span: first.span,
                }))
            }
            grammar::START_PARSE => {
                let first = alternatives[0].unwrap_start_parse();
                let inner = self.arena.alloc_slice(
                    alternatives
                        .into_iter()
                        .map(|a| a.unwrap_start_parse().node),
                );
                let node = &*self.arena.alloc(Parse::Amb(inner));
                ParseTree::StartParse(self.arena.alloc(Start {
                    before: first.before,
                    node,
                    after: first.after,
                    span: first.span,
                }))
            }
            grammar::START_PARSER => {
                let first = alternatives[0].unwrap_start_parser();
                let inner = self.arena.alloc_slice(
                    alternatives
                        .into_iter()
                        .map(|a| a.unwrap_start_parser().node),
                );
                let node = &*self.arena.alloc(Parser::Amb(inner));
                ParseTree::StartParser(self.arena.alloc(Start {
                    before: first.before,
                    node,
                    after: first.after,
                    span: first.span,
                }))
            }
            grammar::START_SPPF_NODE => {
                let first = alternatives[0].unwrap_start_sppf_node();
                let inner = self.arena.alloc_slice(
                    alternatives
                        .into_iter()
                        .map(|a| a.unwrap_start_sppf_node().node),
                );
                let node = &*self.arena.alloc(SPPFNode::Amb(inner));
                ParseTree::StartSPPFNode(self.arena.alloc(Start {
                    before: first.before,
                    node,
                    after: first.after,
                    span: first.span,
                }))
            }
            grammar::START_INPUT => {
                let first = alternatives[0].unwrap_start_input();
                let inner = self.arena.alloc_slice(
                    alternatives
                        .into_iter()
                        .map(|a| a.unwrap_start_input().node),
                );
                let node = &*self.arena.alloc(Input::Amb(inner));
                ParseTree::StartInput(self.arena.alloc(Start {
                    before: first.before,
                    node,
                    after: first.after,
                    span: first.span,
                }))
            }
            grammar::START_PARSE_ERROR => {
                let first = alternatives[0].unwrap_start_parse_error();
                let inner = self.arena.alloc_slice(
                    alternatives
                        .into_iter()
                        .map(|a| a.unwrap_start_parse_error().node),
                );
                let node = &*self.arena.alloc(ParseError::Amb(inner));
                ParseTree::StartParseError(self.arena.alloc(Start {
                    before: first.before,
                    node,
                    after: first.after,
                    span: first.span,
                }))
            }
            grammar::START_PARSE_SUCCESS => {
                let first = alternatives[0].unwrap_start_parse_success();
                let inner = self.arena.alloc_slice(
                    alternatives
                        .into_iter()
                        .map(|a| a.unwrap_start_parse_success().node),
                );
                let node = &*self.arena.alloc(ParseSuccess::Amb(inner));
                ParseTree::StartParseSuccess(self.arena.alloc(Start {
                    before: first.before,
                    node,
                    after: first.after,
                    span: first.span,
                }))
            }
            grammar::START_GRAMMAR => {
                let first = alternatives[0].unwrap_start_grammar();
                let inner = self.arena.alloc_slice(
                    alternatives
                        .into_iter()
                        .map(|a| a.unwrap_start_grammar().node),
                );
                let node = &*self.arena.alloc(Grammar::Amb(inner));
                ParseTree::StartGrammar(self.arena.alloc(Start {
                    before: first.before,
                    node,
                    after: first.after,
                    span: first.span,
                }))
            }
            grammar::START_NONTERMINAL => {
                let first = alternatives[0].unwrap_start_nonterminal();
                let inner = self.arena.alloc_slice(
                    alternatives
                        .into_iter()
                        .map(|a| a.unwrap_start_nonterminal().node),
                );
                let node = &*self.arena.alloc(Nonterminal::Amb(inner));
                ParseTree::StartNonterminal(self.arena.alloc(Start {
                    before: first.before,
                    node,
                    after: first.after,
                    span: first.span,
                }))
            }
            grammar::START_TERMINAL => {
                let first = alternatives[0].unwrap_start_terminal();
                let inner = self.arena.alloc_slice(
                    alternatives
                        .into_iter()
                        .map(|a| a.unwrap_start_terminal().node),
                );
                let node = &*self.arena.alloc(Terminal::Amb(inner));
                ParseTree::StartTerminal(self.arena.alloc(Start {
                    before: first.before,
                    node,
                    after: first.after,
                    span: first.span,
                }))
            }
            grammar::START_SLOT => {
                let first = alternatives[0].unwrap_start_slot();
                let inner = self
                    .arena
                    .alloc_slice(alternatives.into_iter().map(|a| a.unwrap_start_slot().node));
                let node = &*self.arena.alloc(Slot::Amb(inner));
                ParseTree::StartSlot(self.arena.alloc(Start {
                    before: first.before,
                    node,
                    after: first.after,
                    span: first.span,
                }))
            }
            grammar::START_TERMINAL_SET => {
                let first = alternatives[0].unwrap_start_terminal_set();
                let inner = self.arena.alloc_slice(
                    alternatives
                        .into_iter()
                        .map(|a| a.unwrap_start_terminal_set().node),
                );
                let node = &*self.arena.alloc(TerminalSet::Amb(inner));
                ParseTree::StartTerminalSet(self.arena.alloc(Start {
                    before: first.before,
                    node,
                    after: first.after,
                    span: first.span,
                }))
            }
            _ => unreachable!("nonterminal cannot be ambiguous"),
        }
    }
}
// The parse tree of a finished parse, from the nonterminal node that spans the input.
pub fn create_parse_tree<'a>(
    root_id: SPPFNodeId,
    parser: &ImportNamesParser,
    builder: &ImportNamesParseTreeBuilder<'a>,
) -> ParseTree<'a> {
    let iguana_runtime::sppf::SPPFNode::Nonterminal(root) =
        iguana_runtime::parser::Parser::sppf_node(parser, root_id)
    else {
        panic!("the SPPF root is not a nonterminal node")
    };
    match root.nonterminal_id {
        grammar::S => ParseTree::S(visit_sppf(root_id, parser, builder).unwrap_one().unwrap_s()),
        grammar::PARSE => ParseTree::Parse(
            visit_sppf(root_id, parser, builder)
                .unwrap_one()
                .unwrap_parse(),
        ),
        grammar::PARSER => ParseTree::Parser(
            visit_sppf(root_id, parser, builder)
                .unwrap_one()
                .unwrap_parser(),
        ),
        grammar::SPPF_NODE => ParseTree::SPPFNode(
            visit_sppf(root_id, parser, builder)
                .unwrap_one()
                .unwrap_sppf_node(),
        ),
        grammar::INPUT => ParseTree::Input(
            visit_sppf(root_id, parser, builder)
                .unwrap_one()
                .unwrap_input(),
        ),
        grammar::PARSE_ERROR => ParseTree::ParseError(
            visit_sppf(root_id, parser, builder)
                .unwrap_one()
                .unwrap_parse_error(),
        ),
        grammar::PARSE_SUCCESS => ParseTree::ParseSuccess(
            visit_sppf(root_id, parser, builder)
                .unwrap_one()
                .unwrap_parse_success(),
        ),
        grammar::GRAMMAR => ParseTree::Grammar(
            visit_sppf(root_id, parser, builder)
                .unwrap_one()
                .unwrap_grammar(),
        ),
        grammar::NONTERMINAL => ParseTree::Nonterminal(
            visit_sppf(root_id, parser, builder)
                .unwrap_one()
                .unwrap_nonterminal(),
        ),
        grammar::TERMINAL => ParseTree::Terminal(
            visit_sppf(root_id, parser, builder)
                .unwrap_one()
                .unwrap_terminal(),
        ),
        grammar::SLOT => ParseTree::Slot(
            visit_sppf(root_id, parser, builder)
                .unwrap_one()
                .unwrap_slot(),
        ),
        grammar::TERMINAL_SET => ParseTree::TerminalSet(
            visit_sppf(root_id, parser, builder)
                .unwrap_one()
                .unwrap_terminal_set(),
        ),
        grammar::START_S => ParseTree::StartS(
            visit_sppf(root_id, parser, builder)
                .unwrap_one()
                .unwrap_start_s(),
        ),
        grammar::START_PARSE => ParseTree::StartParse(
            visit_sppf(root_id, parser, builder)
                .unwrap_one()
                .unwrap_start_parse(),
        ),
        grammar::START_PARSER => ParseTree::StartParser(
            visit_sppf(root_id, parser, builder)
                .unwrap_one()
                .unwrap_start_parser(),
        ),
        grammar::START_SPPF_NODE => ParseTree::StartSPPFNode(
            visit_sppf(root_id, parser, builder)
                .unwrap_one()
                .unwrap_start_sppf_node(),
        ),
        grammar::START_INPUT => ParseTree::StartInput(
            visit_sppf(root_id, parser, builder)
                .unwrap_one()
                .unwrap_start_input(),
        ),
        grammar::START_PARSE_ERROR => ParseTree::StartParseError(
            visit_sppf(root_id, parser, builder)
                .unwrap_one()
                .unwrap_start_parse_error(),
        ),
        grammar::START_PARSE_SUCCESS => ParseTree::StartParseSuccess(
            visit_sppf(root_id, parser, builder)
                .unwrap_one()
                .unwrap_start_parse_success(),
        ),
        grammar::START_GRAMMAR => ParseTree::StartGrammar(
            visit_sppf(root_id, parser, builder)
                .unwrap_one()
                .unwrap_start_grammar(),
        ),
        grammar::START_NONTERMINAL => ParseTree::StartNonterminal(
            visit_sppf(root_id, parser, builder)
                .unwrap_one()
                .unwrap_start_nonterminal(),
        ),
        grammar::START_TERMINAL => ParseTree::StartTerminal(
            visit_sppf(root_id, parser, builder)
                .unwrap_one()
                .unwrap_start_terminal(),
        ),
        grammar::START_SLOT => ParseTree::StartSlot(
            visit_sppf(root_id, parser, builder)
                .unwrap_one()
                .unwrap_start_slot(),
        ),
        grammar::START_TERMINAL_SET => ParseTree::StartTerminalSet(
            visit_sppf(root_id, parser, builder)
                .unwrap_one()
                .unwrap_start_terminal_set(),
        ),
        _ => panic!(),
    }
}
impl<'a> ParseTreeNode for ParseTree<'a> {
    fn children(&self) -> Vec<Self> {
        ParseTree::children(self)
    }
    fn display_name(&self) -> &'static str {
        ParseTree::display_name(self)
    }
    fn span(&self) -> Span {
        ParseTree::span(self)
    }
    fn kind(&self) -> NodeKind {
        if self.is_amb() {
            NodeKind::Amb
        } else if matches!(self, ParseTree::Token(_)) {
            NodeKind::Token
        } else {
            NodeKind::Nonterminal
        }
    }
    fn node_id(&self) -> Option<usize> {
        ParseTree::node_id(self)
    }
    fn origin(&self) -> Option<Origin> {
        ParseTree::origin(self)
    }
}
