// @generated by iguana. Do not edit manually.

use crate::{
    grammar::*,
    parse_tree::{self, ImportNamesParseTreeBuilder, ParseTree, create_parse_tree},
    scanner::ImportNamesScanner,
};
#[cfg(feature = "instrument")]
use iguana_runtime::grammar::Grammar;
#[allow(unused_imports)]
use iguana_runtime::input::Span;
#[cfg(feature = "debug-trace")]
use iguana_runtime::trace::TraceEvent;
use iguana_runtime::{
    arena::{Arena, ArenaVec},
    descriptor::Descriptor,
    env::{Env, EnvId},
    gss::GSSNode,
    ids::{BindingId, GssNodeId, NonterminalId, SlotId, TerminalId},
    input::Input,
    parser::{
        DESCRIPTORS_CAPACITY_DIVISOR, DESCRIPTORS_CAPACITY_FLOOR, GLLFailure, GLLFailureKind,
        GSS_CAPACITY_MULTIPLIER, Parser, SPPF_CAPACITY_MULTIPLIER, init_logger,
    },
    record,
    result::{ParseError, ParseSuccess},
    scanner::Scanner,
    sppf::{IntermediateNode, NonterminalNode, SPPFNode, SPPFNodeId, TerminalNode},
    utils::{inline_map::InlineMap, inline_vec::InlineVec},
};
use rustc_hash::FxHashMap;
use std::cell::OnceCell;
impl<'i, 'arena> Parser<'i, 'arena> for ImportNamesParser<'i, 'arena> {
    type Grammar = ImportNamesGrammar;
    type Tree<'a> = ParseTree<'a>;
    fn build_tree<'a>(&self, root: SPPFNodeId, tree_arena: &'a Arena) -> ParseTree<'a> {
        let builder = ImportNamesParseTreeBuilder::new(tree_arena);
        create_parse_tree(root, self, &builder)
    }
    // env is threaded only through recursive execute calls in grammars without data-dependent
    // constructs, so clippy sees it as recursion-only there.
    #[allow(clippy::only_used_in_recursion)]
    fn execute(
        &mut self,
        input_index: u32,
        slot_id: SlotId,
        result: Option<SPPFNodeId>,
        gss_node_id: GssNodeId,
        env: Option<EnvId>,
    ) {
        record!(
            self,
            ProcessingDescriptor,
            input_index,
            slot_id,
            result,
            gss_node_id
        );
        match slot_id {
            // S : . Parse Parser SPPFNode Input ParseError ParseSuccess Grammar Nonterminal Terminal
            // Slot TerminalSet
            SlotId(0) => {
                if let Some(right_child) = self.parse_parse_ll1(input_index) {
                    let j = self.sppf_node(right_child).right_extent();
                    // S : Parse . Parser SPPFNode Input ParseError ParseSuccess Grammar Nonterminal Terminal
                    // Slot TerminalSet
                    self.execute(j, SlotId(1), Some(right_child), gss_node_id, env);
                } else {
                    self.add_failure(input_index, SlotId(0), Some(gss_node_id), || {
                        GLLFailureKind::UnexpectedToken {
                            expected: FIRST_SET_PARSE.terminals.to_vec(),
                        }
                    });
                }
            }
            // S : Parse . Parser SPPFNode Input ParseError ParseSuccess Grammar Nonterminal Terminal
            // Slot TerminalSet
            SlotId(1) => {
                if let Some(right_child) = self.parse_parser_ll1(input_index) {
                    if let Some((j, new_node)) =
                        self.create_intermediate_node(result, right_child, SlotId(2), env)
                    {
                        // S : Parse Parser . SPPFNode Input ParseError ParseSuccess Grammar Nonterminal Terminal
                        // Slot TerminalSet
                        self.execute(j, SlotId(2), Some(new_node), gss_node_id, env);
                    }
                } else {
                    self.add_failure(input_index, SlotId(1), Some(gss_node_id), || {
                        GLLFailureKind::UnexpectedToken {
                            expected: FIRST_SET_PARSER.terminals.to_vec(),
                        }
                    });
                }
            }
            // S : Parse Parser . SPPFNode Input ParseError ParseSuccess Grammar Nonterminal Terminal
            // Slot TerminalSet
            SlotId(2) => {
                if let Some(right_child) = self.parse_sppf_node_ll1(input_index) {
                    if let Some((j, new_node)) =
                        self.create_intermediate_node(result, right_child, SlotId(3), env)
                    {
                        // S : Parse Parser SPPFNode . Input ParseError ParseSuccess Grammar Nonterminal Terminal
                        // Slot TerminalSet
                        self.execute(j, SlotId(3), Some(new_node), gss_node_id, env);
                    }
                } else {
                    self.add_failure(input_index, SlotId(2), Some(gss_node_id), || {
                        GLLFailureKind::UnexpectedToken {
                            expected: FIRST_SET_SPPF_NODE.terminals.to_vec(),
                        }
                    });
                }
            }
            // S : Parse Parser SPPFNode . Input ParseError ParseSuccess Grammar Nonterminal Terminal
            // Slot TerminalSet
            SlotId(3) => {
                if let Some(right_child) = self.parse_input_ll1(input_index) {
                    if let Some((j, new_node)) =
                        self.create_intermediate_node(result, right_child, SlotId(4), env)
                    {
                        // S : Parse Parser SPPFNode Input . ParseError ParseSuccess Grammar Nonterminal Terminal
                        // Slot TerminalSet
                        self.execute(j, SlotId(4), Some(new_node), gss_node_id, env);
                    }
                } else {
                    self.add_failure(input_index, SlotId(3), Some(gss_node_id), || {
                        GLLFailureKind::UnexpectedToken {
                            expected: FIRST_SET_INPUT.terminals.to_vec(),
                        }
                    });
                }
            }
            // S : Parse Parser SPPFNode Input . ParseError ParseSuccess Grammar Nonterminal Terminal
            // Slot TerminalSet
            SlotId(4) => {
                if let Some(right_child) = self.parse_parse_error_ll1(input_index) {
                    if let Some((j, new_node)) =
                        self.create_intermediate_node(result, right_child, SlotId(5), env)
                    {
                        // S : Parse Parser SPPFNode Input ParseError . ParseSuccess Grammar Nonterminal Terminal
                        // Slot TerminalSet
                        self.execute(j, SlotId(5), Some(new_node), gss_node_id, env);
                    }
                } else {
                    self.add_failure(input_index, SlotId(4), Some(gss_node_id), || {
                        GLLFailureKind::UnexpectedToken {
                            expected: FIRST_SET_PARSE_ERROR.terminals.to_vec(),
                        }
                    });
                }
            }
            // S : Parse Parser SPPFNode Input ParseError . ParseSuccess Grammar Nonterminal Terminal
            // Slot TerminalSet
            SlotId(5) => {
                if let Some(right_child) = self.parse_parse_success_ll1(input_index) {
                    if let Some((j, new_node)) =
                        self.create_intermediate_node(result, right_child, SlotId(6), env)
                    {
                        // S : Parse Parser SPPFNode Input ParseError ParseSuccess . Grammar Nonterminal Terminal
                        // Slot TerminalSet
                        self.execute(j, SlotId(6), Some(new_node), gss_node_id, env);
                    }
                } else {
                    self.add_failure(input_index, SlotId(5), Some(gss_node_id), || {
                        GLLFailureKind::UnexpectedToken {
                            expected: FIRST_SET_PARSE_SUCCESS.terminals.to_vec(),
                        }
                    });
                }
            }
            // S : Parse Parser SPPFNode Input ParseError ParseSuccess . Grammar Nonterminal Terminal
            // Slot TerminalSet
            SlotId(6) => {
                if let Some(right_child) = self.parse_grammar_ll1(input_index) {
                    if let Some((j, new_node)) =
                        self.create_intermediate_node(result, right_child, SlotId(7), env)
                    {
                        // S : Parse Parser SPPFNode Input ParseError ParseSuccess Grammar . Nonterminal Terminal
                        // Slot TerminalSet
                        self.execute(j, SlotId(7), Some(new_node), gss_node_id, env);
                    }
                } else {
                    self.add_failure(input_index, SlotId(6), Some(gss_node_id), || {
                        GLLFailureKind::UnexpectedToken {
                            expected: FIRST_SET_GRAMMAR.terminals.to_vec(),
                        }
                    });
                }
            }
            // S : Parse Parser SPPFNode Input ParseError ParseSuccess Grammar . Nonterminal Terminal
            // Slot TerminalSet
            SlotId(7) => {
                if let Some(right_child) = self.parse_nonterminal_ll1(input_index) {
                    if let Some((j, new_node)) =
                        self.create_intermediate_node(result, right_child, SlotId(8), env)
                    {
                        // S : Parse Parser SPPFNode Input ParseError ParseSuccess Grammar Nonterminal . Terminal
                        // Slot TerminalSet
                        self.execute(j, SlotId(8), Some(new_node), gss_node_id, env);
                    }
                } else {
                    self.add_failure(input_index, SlotId(7), Some(gss_node_id), || {
                        GLLFailureKind::UnexpectedToken {
                            expected: FIRST_SET_NONTERMINAL.terminals.to_vec(),
                        }
                    });
                }
            }
            // S : Parse Parser SPPFNode Input ParseError ParseSuccess Grammar Nonterminal . Terminal
            // Slot TerminalSet
            SlotId(8) => {
                if let Some(right_child) = self.parse_terminal_ll1(input_index) {
                    if let Some((j, new_node)) =
                        self.create_intermediate_node(result, right_child, SlotId(9), env)
                    {
                        // S : Parse Parser SPPFNode Input ParseError ParseSuccess Grammar Nonterminal Terminal .
                        // Slot TerminalSet
                        self.execute(j, SlotId(9), Some(new_node), gss_node_id, env);
                    }
                } else {
                    self.add_failure(input_index, SlotId(8), Some(gss_node_id), || {
                        GLLFailureKind::UnexpectedToken {
                            expected: FIRST_SET_TERMINAL.terminals.to_vec(),
                        }
                    });
                }
            }
            // S : Parse Parser SPPFNode Input ParseError ParseSuccess Grammar Nonterminal Terminal .
            // Slot TerminalSet
            SlotId(9) => {
                if let Some(right_child) = self.parse_slot_ll1(input_index) {
                    if let Some((j, new_node)) =
                        self.create_intermediate_node(result, right_child, SlotId(10), env)
                    {
                        // S : Parse Parser SPPFNode Input ParseError ParseSuccess Grammar Nonterminal Terminal Slot
                        // . TerminalSet
                        self.execute(j, SlotId(10), Some(new_node), gss_node_id, env);
                    }
                } else {
                    self.add_failure(input_index, SlotId(9), Some(gss_node_id), || {
                        GLLFailureKind::UnexpectedToken {
                            expected: FIRST_SET_SLOT.terminals.to_vec(),
                        }
                    });
                }
            }
            // S : Parse Parser SPPFNode Input ParseError ParseSuccess Grammar Nonterminal Terminal Slot
            // . TerminalSet
            SlotId(10) => {
                if let Some(right_child) = self.parse_terminal_set_ll1(input_index) {
                    if let Some((j, new_node)) =
                        self.create_intermediate_node(result, right_child, SlotId(11), env)
                    {
                        // S : Parse Parser SPPFNode Input ParseError ParseSuccess Grammar Nonterminal Terminal Slot
                        // TerminalSet.
                        self.execute(j, SlotId(11), Some(new_node), gss_node_id, env);
                    }
                } else {
                    self.add_failure(input_index, SlotId(10), Some(gss_node_id), || {
                        GLLFailureKind::UnexpectedToken {
                            expected: FIRST_SET_TERMINAL_SET.terminals.to_vec(),
                        }
                    });
                }
            }
            // S : Parse Parser SPPFNode Input ParseError ParseSuccess Grammar Nonterminal Terminal Slot
            // TerminalSet.
            SlotId(11) => {
                let nonterminal_node_id =
                    self.create_nonterminal_node(result, NonterminalId(0), SlotId(11), gss_node_id);
                self.pop(gss_node_id, SlotId(11), nonterminal_node_id, None);
            }
            // Parse : . "a"
            SlotId(12) => {
                if let Some((j, right_child)) =
                    self.match_terminal(TerminalId(0), input_index, SlotId(12), Some(gss_node_id))
                {
                    // Parse : "a".
                    self.execute(j, SlotId(13), Some(right_child), gss_node_id, env);
                }
            }
            // Parse : "a".
            SlotId(13) => {
                let nonterminal_node_id =
                    self.create_nonterminal_node(result, NonterminalId(1), SlotId(13), gss_node_id);
                self.pop(gss_node_id, SlotId(13), nonterminal_node_id, None);
            }
            // Parser : . "b"
            SlotId(14) => {
                if let Some((j, right_child)) =
                    self.match_terminal(TerminalId(1), input_index, SlotId(14), Some(gss_node_id))
                {
                    // Parser : "b".
                    self.execute(j, SlotId(15), Some(right_child), gss_node_id, env);
                }
            }
            // Parser : "b".
            SlotId(15) => {
                let nonterminal_node_id =
                    self.create_nonterminal_node(result, NonterminalId(2), SlotId(15), gss_node_id);
                self.pop(gss_node_id, SlotId(15), nonterminal_node_id, None);
            }
            // SPPFNode : . "c"
            SlotId(16) => {
                if let Some((j, right_child)) =
                    self.match_terminal(TerminalId(2), input_index, SlotId(16), Some(gss_node_id))
                {
                    // SPPFNode : "c".
                    self.execute(j, SlotId(17), Some(right_child), gss_node_id, env);
                }
            }
            // SPPFNode : "c".
            SlotId(17) => {
                let nonterminal_node_id =
                    self.create_nonterminal_node(result, NonterminalId(3), SlotId(17), gss_node_id);
                self.pop(gss_node_id, SlotId(17), nonterminal_node_id, None);
            }
            // Input : . "d"
            SlotId(18) => {
                if let Some((j, right_child)) =
                    self.match_terminal(TerminalId(3), input_index, SlotId(18), Some(gss_node_id))
                {
                    // Input : "d".
                    self.execute(j, SlotId(19), Some(right_child), gss_node_id, env);
                }
            }
            // Input : "d".
            SlotId(19) => {
                let nonterminal_node_id =
                    self.create_nonterminal_node(result, NonterminalId(4), SlotId(19), gss_node_id);
                self.pop(gss_node_id, SlotId(19), nonterminal_node_id, None);
            }
            // ParseError : . "e"
            SlotId(20) => {
                if let Some((j, right_child)) =
                    self.match_terminal(TerminalId(4), input_index, SlotId(20), Some(gss_node_id))
                {
                    // ParseError : "e".
                    self.execute(j, SlotId(21), Some(right_child), gss_node_id, env);
                }
            }
            // ParseError : "e".
            SlotId(21) => {
                let nonterminal_node_id =
                    self.create_nonterminal_node(result, NonterminalId(5), SlotId(21), gss_node_id);
                self.pop(gss_node_id, SlotId(21), nonterminal_node_id, None);
            }
            // ParseSuccess : . "f"
            SlotId(22) => {
                if let Some((j, right_child)) =
                    self.match_terminal(TerminalId(5), input_index, SlotId(22), Some(gss_node_id))
                {
                    // ParseSuccess : "f".
                    self.execute(j, SlotId(23), Some(right_child), gss_node_id, env);
                }
            }
            // ParseSuccess : "f".
            SlotId(23) => {
                let nonterminal_node_id =
                    self.create_nonterminal_node(result, NonterminalId(6), SlotId(23), gss_node_id);
                self.pop(gss_node_id, SlotId(23), nonterminal_node_id, None);
            }
            // Grammar : . "g"
            SlotId(24) => {
                if let Some((j, right_child)) =
                    self.match_terminal(TerminalId(6), input_index, SlotId(24), Some(gss_node_id))
                {
                    // Grammar : "g".
                    self.execute(j, SlotId(25), Some(right_child), gss_node_id, env);
                }
            }
            // Grammar : "g".
            SlotId(25) => {
                let nonterminal_node_id =
                    self.create_nonterminal_node(result, NonterminalId(7), SlotId(25), gss_node_id);
                self.pop(gss_node_id, SlotId(25), nonterminal_node_id, None);
            }
            // Nonterminal : . "h"
            SlotId(26) => {
                if let Some((j, right_child)) =
                    self.match_terminal(TerminalId(7), input_index, SlotId(26), Some(gss_node_id))
                {
                    // Nonterminal : "h".
                    self.execute(j, SlotId(27), Some(right_child), gss_node_id, env);
                }
            }
            // Nonterminal : "h".
            SlotId(27) => {
                let nonterminal_node_id =
                    self.create_nonterminal_node(result, NonterminalId(8), SlotId(27), gss_node_id);
                self.pop(gss_node_id, SlotId(27), nonterminal_node_id, None);
            }
            // Terminal : . "i"
            SlotId(28) => {
                if let Some((j, right_child)) =
                    self.match_terminal(TerminalId(8), input_index, SlotId(28), Some(gss_node_id))
                {
                    // Terminal : "i".
                    self.execute(j, SlotId(29), Some(right_child), gss_node_id, env);
                }
            }
            // Terminal : "i".
            SlotId(29) => {
                let nonterminal_node_id =
                    self.create_nonterminal_node(result, NonterminalId(9), SlotId(29), gss_node_id);
                self.pop(gss_node_id, SlotId(29), nonterminal_node_id, None);
            }
            // Slot : . "j"
            SlotId(30) => {
                if let Some((j, right_child)) =
                    self.match_terminal(TerminalId(9), input_index, SlotId(30), Some(gss_node_id))
                {
                    // Slot : "j".
                    self.execute(j, SlotId(31), Some(right_child), gss_node_id, env);
                }
            }
            // Slot : "j".
            SlotId(31) => {
                let nonterminal_node_id = self.create_nonterminal_node(
                    result,
                    NonterminalId(10),
                    SlotId(31),
                    gss_node_id,
                );
                self.pop(gss_node_id, SlotId(31), nonterminal_node_id, None);
            }
            // TerminalSet : . "k"
            SlotId(32) => {
                if let Some((j, right_child)) =
                    self.match_terminal(TerminalId(10), input_index, SlotId(32), Some(gss_node_id))
                {
                    // TerminalSet : "k".
                    self.execute(j, SlotId(33), Some(right_child), gss_node_id, env);
                }
            }
            // TerminalSet : "k".
            SlotId(33) => {
                let nonterminal_node_id = self.create_nonterminal_node(
                    result,
                    NonterminalId(11),
                    SlotId(33),
                    gss_node_id,
                );
                self.pop(gss_node_id, SlotId(33), nonterminal_node_id, None);
            }
            // StartS : . start:S
            SlotId(34) => {
                if let Some(right_child) = self.parse_s_ll1(input_index) {
                    let j = self.sppf_node(right_child).right_extent();
                    // StartS : start:S.
                    self.execute(j, SlotId(35), Some(right_child), gss_node_id, env);
                } else {
                    self.add_failure(input_index, SlotId(34), Some(gss_node_id), || {
                        GLLFailureKind::UnexpectedToken {
                            expected: FIRST_SET_S.terminals.to_vec(),
                        }
                    });
                }
            }
            // StartS : start:S.
            SlotId(35) => {
                let nonterminal_node_id = self.create_nonterminal_node(
                    result,
                    NonterminalId(12),
                    SlotId(35),
                    gss_node_id,
                );
                self.pop(gss_node_id, SlotId(35), nonterminal_node_id, None);
            }
            // StartParse : . start:Parse
            SlotId(36) => {
                if let Some(right_child) = self.parse_parse_ll1(input_index) {
                    let j = self.sppf_node(right_child).right_extent();
                    // StartParse : start:Parse.
                    self.execute(j, SlotId(37), Some(right_child), gss_node_id, env);
                } else {
                    self.add_failure(input_index, SlotId(36), Some(gss_node_id), || {
                        GLLFailureKind::UnexpectedToken {
                            expected: FIRST_SET_PARSE.terminals.to_vec(),
                        }
                    });
                }
            }
            // StartParse : start:Parse.
            SlotId(37) => {
                let nonterminal_node_id = self.create_nonterminal_node(
                    result,
                    NonterminalId(13),
                    SlotId(37),
                    gss_node_id,
                );
                self.pop(gss_node_id, SlotId(37), nonterminal_node_id, None);
            }
            // StartParser : . start:Parser
            SlotId(38) => {
                if let Some(right_child) = self.parse_parser_ll1(input_index) {
                    let j = self.sppf_node(right_child).right_extent();
                    // StartParser : start:Parser.
                    self.execute(j, SlotId(39), Some(right_child), gss_node_id, env);
                } else {
                    self.add_failure(input_index, SlotId(38), Some(gss_node_id), || {
                        GLLFailureKind::UnexpectedToken {
                            expected: FIRST_SET_PARSER.terminals.to_vec(),
                        }
                    });
                }
            }
            // StartParser : start:Parser.
            SlotId(39) => {
                let nonterminal_node_id = self.create_nonterminal_node(
                    result,
                    NonterminalId(14),
                    SlotId(39),
                    gss_node_id,
                );
                self.pop(gss_node_id, SlotId(39), nonterminal_node_id, None);
            }
            // StartSPPFNode : . start:SPPFNode
            SlotId(40) => {
                if let Some(right_child) = self.parse_sppf_node_ll1(input_index) {
                    let j = self.sppf_node(right_child).right_extent();
                    // StartSPPFNode : start:SPPFNode.
                    self.execute(j, SlotId(41), Some(right_child), gss_node_id, env);
                } else {
                    self.add_failure(input_index, SlotId(40), Some(gss_node_id), || {
                        GLLFailureKind::UnexpectedToken {
                            expected: FIRST_SET_SPPF_NODE.terminals.to_vec(),
                        }
                    });
                }
            }
            // StartSPPFNode : start:SPPFNode.
            SlotId(41) => {
                let nonterminal_node_id = self.create_nonterminal_node(
                    result,
                    NonterminalId(15),
                    SlotId(41),
                    gss_node_id,
                );
                self.pop(gss_node_id, SlotId(41), nonterminal_node_id, None);
            }
            // StartInput : . start:Input
            SlotId(42) => {
                if let Some(right_child) = self.parse_input_ll1(input_index) {
                    let j = self.sppf_node(right_child).right_extent();
                    // StartInput : start:Input.
                    self.execute(j, SlotId(43), Some(right_child), gss_node_id, env);
                } else {
                    self.add_failure(input_index, SlotId(42), Some(gss_node_id), || {
                        GLLFailureKind::UnexpectedToken {
                            expected: FIRST_SET_INPUT.terminals.to_vec(),
                        }
                    });
                }
            }
            // StartInput : start:Input.
            SlotId(43) => {
                let nonterminal_node_id = self.create_nonterminal_node(
                    result,
                    NonterminalId(16),
                    SlotId(43),
                    gss_node_id,
                );
                self.pop(gss_node_id, SlotId(43), nonterminal_node_id, None);
            }
            // StartParseError : . start:ParseError
            SlotId(44) => {
                if let Some(right_child) = self.parse_parse_error_ll1(input_index) {
                    let j = self.sppf_node(right_child).right_extent();
                    // StartParseError : start:ParseError.
                    self.execute(j, SlotId(45), Some(right_child), gss_node_id, env);
                } else {
                    self.add_failure(input_index, SlotId(44), Some(gss_node_id), || {
                        GLLFailureKind::UnexpectedToken {
                            expected: FIRST_SET_PARSE_ERROR.terminals.to_vec(),
                        }
                    });
                }
            }
            // StartParseError : start:ParseError.
            SlotId(45) => {
                let nonterminal_node_id = self.create_nonterminal_node(
                    result,
                    NonterminalId(17),
                    SlotId(45),
                    gss_node_id,
                );
                self.pop(gss_node_id, SlotId(45), nonterminal_node_id, None);
            }
            // StartParseSuccess : . start:ParseSuccess
            SlotId(46) => {
                if let Some(right_child) = self.parse_parse_success_ll1(input_index) {
                    let j = self.sppf_node(right_child).right_extent();
                    // StartParseSuccess : start:ParseSuccess.
                    self.execute(j, SlotId(47), Some(right_child), gss_node_id, env);
                } else {
                    self.add_failure(input_index, SlotId(46), Some(gss_node_id), || {
                        GLLFailureKind::UnexpectedToken {
                            expected: FIRST_SET_PARSE_SUCCESS.terminals.to_vec(),
                        }
                    });
                }
            }
            // StartParseSuccess : start:ParseSuccess.
            SlotId(47) => {
                let nonterminal_node_id = self.create_nonterminal_node(
                    result,
                    NonterminalId(18),
                    SlotId(47),
                    gss_node_id,
                );
                self.pop(gss_node_id, SlotId(47), nonterminal_node_id, None);
            }
            // StartGrammar : . start:Grammar
            SlotId(48) => {
                if let Some(right_child) = self.parse_grammar_ll1(input_index) {
                    let j = self.sppf_node(right_child).right_extent();
                    // StartGrammar : start:Grammar.
                    self.execute(j, SlotId(49), Some(right_child), gss_node_id, env);
                } else {
                    self.add_failure(input_index, SlotId(48), Some(gss_node_id), || {
                        GLLFailureKind::UnexpectedToken {
                            expected: FIRST_SET_GRAMMAR.terminals.to_vec(),
                        }
                    });
                }
            }
            // StartGrammar : start:Grammar.
            SlotId(49) => {
                let nonterminal_node_id = self.create_nonterminal_node(
                    result,
                    NonterminalId(19),
                    SlotId(49),
                    gss_node_id,
                );
                self.pop(gss_node_id, SlotId(49), nonterminal_node_id, None);
            }
            // StartNonterminal : . start:Nonterminal
            SlotId(50) => {
                if let Some(right_child) = self.parse_nonterminal_ll1(input_index) {
                    let j = self.sppf_node(right_child).right_extent();
                    // StartNonterminal : start:Nonterminal.
                    self.execute(j, SlotId(51), Some(right_child), gss_node_id, env);
                } else {
                    self.add_failure(input_index, SlotId(50), Some(gss_node_id), || {
                        GLLFailureKind::UnexpectedToken {
                            expected: FIRST_SET_NONTERMINAL.terminals.to_vec(),
                        }
                    });
                }
            }
            // StartNonterminal : start:Nonterminal.
            SlotId(51) => {
                let nonterminal_node_id = self.create_nonterminal_node(
                    result,
                    NonterminalId(20),
                    SlotId(51),
                    gss_node_id,
                );
                self.pop(gss_node_id, SlotId(51), nonterminal_node_id, None);
            }
            // StartTerminal : . start:Terminal
            SlotId(52) => {
                if let Some(right_child) = self.parse_terminal_ll1(input_index) {
                    let j = self.sppf_node(right_child).right_extent();
                    // StartTerminal : start:Terminal.
                    self.execute(j, SlotId(53), Some(right_child), gss_node_id, env);
                } else {
                    self.add_failure(input_index, SlotId(52), Some(gss_node_id), || {
                        GLLFailureKind::UnexpectedToken {
                            expected: FIRST_SET_TERMINAL.terminals.to_vec(),
                        }
                    });
                }
            }
            // StartTerminal : start:Terminal.
            SlotId(53) => {
                let nonterminal_node_id = self.create_nonterminal_node(
                    result,
                    NonterminalId(21),
                    SlotId(53),
                    gss_node_id,
                );
                self.pop(gss_node_id, SlotId(53), nonterminal_node_id, None);
            }
            // StartSlot : . start:Slot
            SlotId(54) => {
                if let Some(right_child) = self.parse_slot_ll1(input_index) {
                    let j = self.sppf_node(right_child).right_extent();
                    // StartSlot : start:Slot.
                    self.execute(j, SlotId(55), Some(right_child), gss_node_id, env);
                } else {
                    self.add_failure(input_index, SlotId(54), Some(gss_node_id), || {
                        GLLFailureKind::UnexpectedToken {
                            expected: FIRST_SET_SLOT.terminals.to_vec(),
                        }
                    });
                }
            }
            // StartSlot : start:Slot.
            SlotId(55) => {
                let nonterminal_node_id = self.create_nonterminal_node(
                    result,
                    NonterminalId(22),
                    SlotId(55),
                    gss_node_id,
                );
                self.pop(gss_node_id, SlotId(55), nonterminal_node_id, None);
            }
            // StartTerminalSet : . start:TerminalSet
            SlotId(56) => {
                if let Some(right_child) = self.parse_terminal_set_ll1(input_index) {
                    let j = self.sppf_node(right_child).right_extent();
                    // StartTerminalSet : start:TerminalSet.
                    self.execute(j, SlotId(57), Some(right_child), gss_node_id, env);
                } else {
                    self.add_failure(input_index, SlotId(56), Some(gss_node_id), || {
                        GLLFailureKind::UnexpectedToken {
                            expected: FIRST_SET_TERMINAL_SET.terminals.to_vec(),
                        }
                    });
                }
            }
            // StartTerminalSet : start:TerminalSet.
            SlotId(57) => {
                let nonterminal_node_id = self.create_nonterminal_node(
                    result,
                    NonterminalId(23),
                    SlotId(57),
                    gss_node_id,
                );
                self.pop(gss_node_id, SlotId(57), nonterminal_node_id, None);
            }
            _ => {
                panic!("Unknown grammar slot id: {slot_id}");
            }
        }
    }
    fn add_first_descriptors(
        &mut self,
        nonterminal_id: NonterminalId,
        input_index: u32,
        gss_node_id: GssNodeId,
        env: Option<EnvId>,
    ) {
        match nonterminal_id {
            // S : . Parse Parser SPPFNode Input ParseError ParseSuccess Grammar Nonterminal Terminal
            // Slot TerminalSet
            NonterminalId(0) => {
                self.add_first_descriptor(SlotId(0), input_index, gss_node_id, env);
            }
            // Parse : . "a"
            NonterminalId(1) => {
                self.add_first_descriptor(SlotId(12), input_index, gss_node_id, env);
            }
            // Parser : . "b"
            NonterminalId(2) => {
                self.add_first_descriptor(SlotId(14), input_index, gss_node_id, env);
            }
            // SPPFNode : . "c"
            NonterminalId(3) => {
                self.add_first_descriptor(SlotId(16), input_index, gss_node_id, env);
            }
            // Input : . "d"
            NonterminalId(4) => {
                self.add_first_descriptor(SlotId(18), input_index, gss_node_id, env);
            }
            // ParseError : . "e"
            NonterminalId(5) => {
                self.add_first_descriptor(SlotId(20), input_index, gss_node_id, env);
            }
            // ParseSuccess : . "f"
            NonterminalId(6) => {
                self.add_first_descriptor(SlotId(22), input_index, gss_node_id, env);
            }
            // Grammar : . "g"
            NonterminalId(7) => {
                self.add_first_descriptor(SlotId(24), input_index, gss_node_id, env);
            }
            // Nonterminal : . "h"
            NonterminalId(8) => {
                self.add_first_descriptor(SlotId(26), input_index, gss_node_id, env);
            }
            // Terminal : . "i"
            NonterminalId(9) => {
                self.add_first_descriptor(SlotId(28), input_index, gss_node_id, env);
            }
            // Slot : . "j"
            NonterminalId(10) => {
                self.add_first_descriptor(SlotId(30), input_index, gss_node_id, env);
            }
            // TerminalSet : . "k"
            NonterminalId(11) => {
                self.add_first_descriptor(SlotId(32), input_index, gss_node_id, env);
            }
            // StartS : . start:S
            NonterminalId(12) => {
                self.add_first_descriptor(SlotId(34), input_index, gss_node_id, env);
            }
            // StartParse : . start:Parse
            NonterminalId(13) => {
                self.add_first_descriptor(SlotId(36), input_index, gss_node_id, env);
            }
            // StartParser : . start:Parser
            NonterminalId(14) => {
                self.add_first_descriptor(SlotId(38), input_index, gss_node_id, env);
            }
            // StartSPPFNode : . start:SPPFNode
            NonterminalId(15) => {
                self.add_first_descriptor(SlotId(40), input_index, gss_node_id, env);
            }
            // StartInput : . start:Input
            NonterminalId(16) => {
                self.add_first_descriptor(SlotId(42), input_index, gss_node_id, env);
            }
            // StartParseError : . start:ParseError
            NonterminalId(17) => {
                self.add_first_descriptor(SlotId(44), input_index, gss_node_id, env);
            }
            // StartParseSuccess : . start:ParseSuccess
            NonterminalId(18) => {
                self.add_first_descriptor(SlotId(46), input_index, gss_node_id, env);
            }
            // StartGrammar : . start:Grammar
            NonterminalId(19) => {
                self.add_first_descriptor(SlotId(48), input_index, gss_node_id, env);
            }
            // StartNonterminal : . start:Nonterminal
            NonterminalId(20) => {
                self.add_first_descriptor(SlotId(50), input_index, gss_node_id, env);
            }
            // StartTerminal : . start:Terminal
            NonterminalId(21) => {
                self.add_first_descriptor(SlotId(52), input_index, gss_node_id, env);
            }
            // StartSlot : . start:Slot
            NonterminalId(22) => {
                self.add_first_descriptor(SlotId(54), input_index, gss_node_id, env);
            }
            // StartTerminalSet : . start:TerminalSet
            NonterminalId(23) => {
                self.add_first_descriptor(SlotId(56), input_index, gss_node_id, env);
            }
            _ => {
                panic!("Unknown nonterminal id: {nonterminal_id}");
            }
        }
    }
    fn get_gss_node(&self, nonterminal_id: NonterminalId, input_index: u32) -> Option<GssNodeId> {
        self.gss_nodes_index[nonterminal_id.index()]
            .get(&input_index)
            .copied()
    }
    fn add_gss_node(
        &mut self,
        nonterminal_id: NonterminalId,
        input_index: u32,
        gss_node_id: GssNodeId,
    ) {
        let arena = self.vec_arena;
        self.gss_nodes_index[nonterminal_id.index()].insert(input_index, gss_node_id, arena);
    }
    fn new_gss_node(&mut self, nonterminal_id: NonterminalId, input_index: u32) -> GssNodeId {
        let gss_node_id = GssNodeId(self.gss_nodes.len() as u32);
        let gss_node = GSSNode::new(gss_node_id, nonterminal_id, input_index);
        record!(self, GSSNodeCreated, nonterminal_id, input_index);
        self.gss_nodes.push(gss_node);
        gss_node_id
    }
    fn gss_node(&self, id: GssNodeId) -> &GSSNode<'arena> {
        &self.gss_nodes[id.index()]
    }
    fn gss_node_mut(&mut self, id: GssNodeId) -> &mut GSSNode<'arena> {
        self.gss_nodes
            .get_mut(id.index())
            .expect("GSS node id should be valid")
    }
    fn sppf_node(&self, id: SPPFNodeId) -> &SPPFNode {
        &self.sppf_nodes[id.index()]
    }
    fn sppf_node_mut(&mut self, id: SPPFNodeId) -> &mut SPPFNode {
        &mut self.sppf_nodes[id.index()]
    }
    fn add_descriptor(&mut self, descriptor: Descriptor) {
        record!(
            self,
            DescriptorAdded,
            descriptor.input_index,
            descriptor.slot_id,
            descriptor.sppf_node_id(),
            descriptor.gss_node_id
        );
        #[cfg(feature = "instrument")]
        self.increment_descriptor_count();
        self.descriptors.push(descriptor);
        #[cfg(feature = "instrument")]
        {
            if self.descriptors.len() > self.descriptors_peak {
                self.descriptors_peak = self.descriptors.len();
            }
        }
    }
    fn next_descriptor(&mut self) -> Option<Descriptor> {
        self.descriptors.pop()
    }
    fn clear_descriptors(&mut self) {
        self.descriptors.clear();
    }
    fn add_terminal_node(&mut self, terminal_node: TerminalNode) -> SPPFNodeId {
        let terminal_node_id = SPPFNodeId(self.sppf_nodes.len() as u32);
        let arena = self.vec_arena;
        self.terminal_nodes_index[terminal_node.terminal_id.index()].insert(
            terminal_node.span,
            terminal_node_id,
            arena,
        );
        record!(
            self,
            TerminalNodeCreated,
            terminal_node.terminal_id,
            terminal_node.span
        );
        self.sppf_nodes.push(SPPFNode::Terminal(terminal_node));
        terminal_node_id
    }
    fn add_nonterminal_node(&mut self, nonterminal_node: NonterminalNode) -> SPPFNodeId {
        let nonterminal_node_id = SPPFNodeId(self.sppf_nodes.len() as u32);
        record!(
            self,
            NonterminalNodeCreated,
            nonterminal_node.nonterminal_id,
            nonterminal_node.span,
            nonterminal_node.child
        );
        self.sppf_nodes
            .push(SPPFNode::Nonterminal(nonterminal_node));
        nonterminal_node_id
    }
    fn add_intermediate_node(
        &mut self,
        intermediate_node: IntermediateNode,
        env: Option<EnvId>,
        add_to_index: bool,
    ) -> SPPFNodeId {
        let intermediate_node_id = SPPFNodeId(self.sppf_nodes.len() as u32);
        if add_to_index {
            let arena = self.vec_arena;
            let slot_idx = intermediate_node.slot_id.index();
            if slot_idx < 58 {
                self.intermediate_nodes_index[slot_idx].insert(
                    intermediate_node.span,
                    intermediate_node_id,
                    arena,
                );
            } else {
                let idx = slot_idx - 58;
                self.dd_intermediate_nodes_index[idx].insert(
                    (intermediate_node.span, env),
                    intermediate_node_id,
                    arena,
                );
            }
        }
        record!(
            self,
            IntermediateNodeCreated,
            intermediate_node.slot_id,
            intermediate_node.span,
            intermediate_node.child.0,
            intermediate_node.child.1
        );
        self.sppf_nodes
            .push(SPPFNode::Intermediate(intermediate_node));
        intermediate_node_id
    }
    fn input(&self) -> &'i Input {
        self.scanner.input
    }
    fn sppf_nodes(&self) -> &[SPPFNode] {
        &self.sppf_nodes
    }
    #[cfg(feature = "instrument")]
    fn increment_descriptor_count(&mut self) {
        self.descriptors_count += 1;
    }
    #[cfg(feature = "instrument")]
    fn count_descriptors(&self) -> usize {
        self.descriptors_count
    }
    #[cfg(feature = "instrument")]
    fn count_gss_nodes(&self) -> usize {
        self.gss_nodes.len()
    }
    #[cfg(feature = "instrument")]
    fn count_gss_edges(&self) -> usize {
        self.gss_nodes.iter().map(|n| n.edges().len()).sum()
    }
    #[cfg(feature = "instrument")]
    fn count_nonterminal_nodes(&self) -> usize {
        self.sppf_nodes
            .iter()
            .filter(|n| matches!(n, SPPFNode::Nonterminal(_)))
            .count()
    }
    #[cfg(feature = "instrument")]
    fn count_intermediate_nodes(&self) -> usize {
        self.sppf_nodes
            .iter()
            .filter(|n| matches!(n, SPPFNode::Intermediate(_)))
            .count()
    }
    #[cfg(feature = "instrument")]
    fn count_terminal_nodes(&self) -> usize {
        self.sppf_nodes
            .iter()
            .filter(|n| matches!(n, SPPFNode::Terminal(_)))
            .count()
    }
    #[cfg(feature = "instrument")]
    fn count_ambiguous_nodes(&self) -> usize {
        self.sppf_nodes
            .iter()
            .filter(|n| match n {
                SPPFNode::Nonterminal(nn) => nn.ambiguous,
                SPPFNode::Intermediate(in_) => in_.ambiguous,
                SPPFNode::Terminal(_) => false,
            })
            .count()
    }
    fn lookup_intermediate_node(
        &self,
        slot_id: SlotId,
        left_extent: u32,
        right_extent: u32,
        env: Option<EnvId>,
    ) -> Option<SPPFNodeId> {
        let slot_idx = slot_id.index();
        let span = Span::new(left_extent, right_extent);
        if slot_idx < 58 {
            self.intermediate_nodes_index[slot_idx].get(&span).copied()
        } else {
            let idx = slot_idx - 58;
            self.dd_intermediate_nodes_index[idx]
                .get(&(span, env))
                .copied()
        }
    }
    fn lookup_terminal_node(
        &self,
        terminal_id: TerminalId,
        left_extent: u32,
        right_extent: u32,
    ) -> Option<SPPFNodeId> {
        let map = &self.terminal_nodes_index[terminal_id.index()];
        map.get(&Span::new(left_extent, right_extent)).copied()
    }
    fn gss_nodes<'p>(&'p self) -> impl Iterator<Item = &'p GSSNode<'arena>>
    where
        'arena: 'p,
    {
        self.gss_nodes.iter()
    }
    fn add_intermediate_node_child(
        &mut self,
        node: SPPFNodeId,
        child1: SPPFNodeId,
        child2: SPPFNodeId,
    ) {
        self.intermediate_nodes_children
            .push((node, (child1, child2)));
    }
    fn add_nonterminal_node_child(
        &mut self,
        node: SPPFNodeId,
        child: SPPFNodeId,
        return_slot: SlotId,
    ) {
        self.nonterminal_nodes_children
            .push((node, (child, return_slot)));
    }
    fn intermediate_nodes_children_map(
        &self,
    ) -> &FxHashMap<SPPFNodeId, Vec<(SPPFNodeId, SPPFNodeId)>> {
        self.intermediate_nodes_children_map.get_or_init(|| {
            let mut map: FxHashMap<SPPFNodeId, Vec<(SPPFNodeId, SPPFNodeId)>> =
                FxHashMap::default();
            for (k, v) in &self.intermediate_nodes_children {
                map.entry(*k).or_default().push(*v);
            }
            map
        })
    }
    fn nonterminal_nodes_children_map(&self) -> &FxHashMap<SPPFNodeId, Vec<(SPPFNodeId, SlotId)>> {
        self.nonterminal_nodes_children_map.get_or_init(|| {
            let mut map: FxHashMap<SPPFNodeId, Vec<(SPPFNodeId, SlotId)>> = FxHashMap::default();
            for (k, v) in &self.nonterminal_nodes_children {
                map.entry(*k).or_default().push(*v);
            }
            map
        })
    }
    #[cfg(feature = "debug-trace")]
    fn add_trace_event(&mut self, event: TraceEvent) {
        if let Some(trace_events) = &mut self.trace_events {
            trace_events.push(event);
        }
    }
    fn start_env(&mut self, _start: NonterminalId) -> Option<EnvId> {
        None
    }
    // True if a local ambiguity node was added during parsing. This does not guarantee the
    // ambiguity is reachable from the root, so a tree walk is still needed to confirm it.
    fn ambiguity_node_added(&self) -> bool {
        !self.intermediate_nodes_children.is_empty() || !self.nonterminal_nodes_children.is_empty()
    }
    fn add_start_gss_node(
        &mut self,
        nonterminal_id: NonterminalId,
        input_index: u32,
        gss_node_id: GssNodeId,
    ) {
        self.add_gss_node(nonterminal_id, input_index, gss_node_id);
    }
    fn new_env(&mut self) -> (EnvId, &mut Env<'arena>) {
        let id = EnvId(self.envs.len() as u32);
        self.envs.push(Env::default());
        (id, &mut self.envs[id.index()])
    }
    fn lookup(&self, name: BindingId, env_id: EnvId) -> i32 {
        let env = &self.envs[env_id.index()];
        env.get(name)
    }
    fn clone_env(&mut self, source: EnvId) -> (EnvId, &mut Env<'arena>) {
        let bindings = self.envs[source.0 as usize].bindings.clone();
        let (new_id, new_env) = self.new_env();
        new_env.bindings = bindings;
        (new_id, new_env)
    }
    fn envs(&self) -> &[Env<'arena>] {
        &self.envs
    }
    #[cfg(feature = "instrument")]
    fn record_stats(&self) -> iguana_runtime::instrument::Stats {
        let mut stats = iguana_runtime::instrument::Stats::new();
        stats.descriptors_count = self.count_descriptors();
        stats.descriptors_peak = self.descriptors_peak;
        stats.envs_count = self.envs.len();
        stats.gss_nodes_count = self.count_gss_nodes();
        stats.gss_edges_count = self.count_gss_edges();
        stats.nonterminal_nodes_count = self.count_nonterminal_nodes();
        stats.intermediate_nodes_count = self.count_intermediate_nodes();
        stats.terminal_nodes_count = self.count_terminal_nodes();
        stats.ambiguous_nodes_count = self.count_ambiguous_nodes();
        for node in self.gss_nodes() {
            stats.record("GssNode::edges: InlineVec", node.edges().len());
            stats.record(
                "GssNode::popped_elements: InlineMap",
                node.popped_elements().len(),
            );
        }
        for env in self.envs() {
            stats.record("Env::bindings: Bindings", env.bindings.len());
        }
        for m in self.intermediate_nodes_index.iter() {
            stats.record("Parser::intermediate_nodes_index: InlineMap", m.len());
        }
        for m in self.dd_intermediate_nodes_index.iter() {
            stats.record("Parser::dd_intermediate_nodes_index: InlineMap", m.len());
        }
        for m in self.terminal_nodes_index.iter() {
            stats.record("Parser::terminal_nodes_index: InlineMap", m.len());
        }
        for m in self.gss_nodes_index.iter() {
            stats.record("Parser::gss_nodes_index: InlineMap", m.len());
        }
        for (nt_id, pos) in &self.ll1_call_log {
            let name = Self::Grammar::nonterminal_display_name(*nt_id);
            stats.record_ll1_call(name, *pos);
        }
        stats
    }
    fn post_conditions(
        &mut self,
        _slot: SlotId,
        _left_extent: u32,
        _right_extent: u32,
    ) -> Option<GLLFailureKind> {
        None
    }
    fn follow_set_check(&mut self, nonterminal_id: NonterminalId, input_index: u32) -> bool {
        match nonterminal_id {
            NonterminalId(0) => self.scanner.match_any(&FOLLOW_SET_S, input_index),
            NonterminalId(1) => self.scanner.match_any(&FOLLOW_SET_PARSE, input_index),
            NonterminalId(2) => self.scanner.match_any(&FOLLOW_SET_PARSER, input_index),
            NonterminalId(3) => self.scanner.match_any(&FOLLOW_SET_SPPF_NODE, input_index),
            NonterminalId(4) => self.scanner.match_any(&FOLLOW_SET_INPUT, input_index),
            NonterminalId(5) => self.scanner.match_any(&FOLLOW_SET_PARSE_ERROR, input_index),
            NonterminalId(6) => self
                .scanner
                .match_any(&FOLLOW_SET_PARSE_SUCCESS, input_index),
            NonterminalId(7) => self.scanner.match_any(&FOLLOW_SET_GRAMMAR, input_index),
            NonterminalId(8) => self.scanner.match_any(&FOLLOW_SET_NONTERMINAL, input_index),
            NonterminalId(9) => self.scanner.match_any(&FOLLOW_SET_TERMINAL, input_index),
            NonterminalId(10) => self.scanner.match_any(&FOLLOW_SET_SLOT, input_index),
            NonterminalId(11) => self
                .scanner
                .match_any(&FOLLOW_SET_TERMINAL_SET, input_index),
            NonterminalId(12) => self.scanner.match_any(&FOLLOW_SET_START_S, input_index),
            NonterminalId(13) => self.scanner.match_any(&FOLLOW_SET_START_PARSE, input_index),
            NonterminalId(14) => self
                .scanner
                .match_any(&FOLLOW_SET_START_PARSER, input_index),
            NonterminalId(15) => self
                .scanner
                .match_any(&FOLLOW_SET_START_SPPF_NODE, input_index),
            NonterminalId(16) => self.scanner.match_any(&FOLLOW_SET_START_INPUT, input_index),
            NonterminalId(17) => self
                .scanner
                .match_any(&FOLLOW_SET_START_PARSE_ERROR, input_index),
            NonterminalId(18) => self
                .scanner
                .match_any(&FOLLOW_SET_START_PARSE_SUCCESS, input_index),
            NonterminalId(19) => self
                .scanner
                .match_any(&FOLLOW_SET_START_GRAMMAR, input_index),
            NonterminalId(20) => self
                .scanner
                .match_any(&FOLLOW_SET_START_NONTERMINAL, input_index),
            NonterminalId(21) => self
                .scanner
                .match_any(&FOLLOW_SET_START_TERMINAL, input_index),
            NonterminalId(22) => self.scanner.match_any(&FOLLOW_SET_START_SLOT, input_index),
            NonterminalId(23) => self
                .scanner
                .match_any(&FOLLOW_SET_START_TERMINAL_SET, input_index),
            _ => true,
        }
    }
    fn follow_set_terminals(&self, nonterminal_id: NonterminalId) -> Vec<TerminalId> {
        match nonterminal_id {
            NonterminalId(0) => FOLLOW_SET_S.terminals.to_vec(),
            NonterminalId(1) => FOLLOW_SET_PARSE.terminals.to_vec(),
            NonterminalId(2) => FOLLOW_SET_PARSER.terminals.to_vec(),
            NonterminalId(3) => FOLLOW_SET_SPPF_NODE.terminals.to_vec(),
            NonterminalId(4) => FOLLOW_SET_INPUT.terminals.to_vec(),
            NonterminalId(5) => FOLLOW_SET_PARSE_ERROR.terminals.to_vec(),
            NonterminalId(6) => FOLLOW_SET_PARSE_SUCCESS.terminals.to_vec(),
            NonterminalId(7) => FOLLOW_SET_GRAMMAR.terminals.to_vec(),
            NonterminalId(8) => FOLLOW_SET_NONTERMINAL.terminals.to_vec(),
            NonterminalId(9) => FOLLOW_SET_TERMINAL.terminals.to_vec(),
            NonterminalId(10) => FOLLOW_SET_SLOT.terminals.to_vec(),
            NonterminalId(11) => FOLLOW_SET_TERMINAL_SET.terminals.to_vec(),
            NonterminalId(12) => FOLLOW_SET_START_S.terminals.to_vec(),
            NonterminalId(13) => FOLLOW_SET_START_PARSE.terminals.to_vec(),
            NonterminalId(14) => FOLLOW_SET_START_PARSER.terminals.to_vec(),
            NonterminalId(15) => FOLLOW_SET_START_SPPF_NODE.terminals.to_vec(),
            NonterminalId(16) => FOLLOW_SET_START_INPUT.terminals.to_vec(),
            NonterminalId(17) => FOLLOW_SET_START_PARSE_ERROR.terminals.to_vec(),
            NonterminalId(18) => FOLLOW_SET_START_PARSE_SUCCESS.terminals.to_vec(),
            NonterminalId(19) => FOLLOW_SET_START_GRAMMAR.terminals.to_vec(),
            NonterminalId(20) => FOLLOW_SET_START_NONTERMINAL.terminals.to_vec(),
            NonterminalId(21) => FOLLOW_SET_START_TERMINAL.terminals.to_vec(),
            NonterminalId(22) => FOLLOW_SET_START_SLOT.terminals.to_vec(),
            NonterminalId(23) => FOLLOW_SET_START_TERMINAL_SET.terminals.to_vec(),
            _ => vec![],
        }
    }
    fn failures(&self) -> impl Iterator<Item = &GLLFailure> {
        self.failures.iter()
    }
    fn add_failure(
        &mut self,
        input_index: u32,
        slot_id: SlotId,
        gss_node_id: Option<GssNodeId>,
        kind: impl FnOnce() -> GLLFailureKind,
    ) {
        if self.suppress_failures {
            return;
        }
        let level = self.failures.first().map_or(0, |e| e.input_index);
        if input_index < level {
            record!(self, GLLFailure, input_index, slot_id, gss_node_id, kind());
            return;
        }
        let kind = kind();
        record!(
            self,
            GLLFailure,
            input_index,
            slot_id,
            gss_node_id,
            kind.clone()
        );
        if input_index > level {
            self.failures.clear();
        }
        self.failures.push(
            GLLFailure {
                input_index,
                slot_id,
                gss_node_id,
                kind,
            },
            self.vec_arena,
        );
    }
    fn match_token(&mut self, terminal_id: TerminalId, input_index: u32) -> Option<u32> {
        self.scanner.match_token(terminal_id, input_index)
    }
    fn vec_arena(&self) -> &'arena Arena {
        self.vec_arena
    }
}
pub struct ImportNamesParser<'i, 'arena> {
    // Arena backing the parser's internal collections; reset in bulk after the parse.
    vec_arena: &'arena Arena,
    scanner: ImportNamesScanner<'i, 'arena>,
    descriptors: ArenaVec<'arena, Descriptor>,
    gss_nodes: ArenaVec<'arena, GSSNode<'arena>>,
    // Per-nonterminal GSS-node index keyed by input position.
    gss_nodes_index: [InlineMap<'arena, u32, GssNodeId>; 24],
    sppf_nodes: ArenaVec<'arena, SPPFNode>,
    #[cfg(feature = "instrument")]
    descriptors_count: usize,
    #[cfg(feature = "instrument")]
    descriptors_peak: usize,
    #[cfg(feature = "instrument")]
    ll1_call_log: Vec<(NonterminalId, u32)>,
    // Per-slot Span-keyed intermediate-node index, for slots in non-parameterized nonterminals.
    intermediate_nodes_index: [InlineMap<'arena, Span, SPPFNodeId>; 58],
    // Per-slot (Span, env)-keyed intermediate-node index, for slots in parameterized
    // nonterminals; env separates calls made with different parameter values.
    dd_intermediate_nodes_index: [InlineMap<'arena, (Span, Option<EnvId>), SPPFNodeId>; 0],
    terminal_nodes_index: [InlineMap<'arena, Span, SPPFNodeId>; 13],
    // An intermediate node keeps its first child inline. Children of intermediate nodes are
    // pairs: (left_child, right_child). Extra children, when there is ambiguity, are stored here
    // as (parent node, (left child, right child)).
    intermediate_nodes_children: ArenaVec<'arena, (SPPFNodeId, (SPPFNodeId, SPPFNodeId))>,
    // intermediate_nodes_children grouped by parent node, built lazily for tree construction.
    intermediate_nodes_children_map: OnceCell<FxHashMap<SPPFNodeId, Vec<(SPPFNodeId, SPPFNodeId)>>>,
    // Extra children of ambiguous nonterminal nodes, the counterpart to
    // intermediate_nodes_children: each entry is (parent node, (child, return slot)), a single
    // child plus its return slot rather than a pair.
    nonterminal_nodes_children: ArenaVec<'arena, (SPPFNodeId, (SPPFNodeId, SlotId))>,
    // nonterminal_nodes_children grouped by parent node, built lazily like
    // intermediate_nodes_children_map.
    nonterminal_nodes_children_map: OnceCell<FxHashMap<SPPFNodeId, Vec<(SPPFNodeId, SlotId)>>>,
    envs: ArenaVec<'arena, Env<'arena>>,
    failures: InlineVec<'arena, GLLFailure, 8>,
    // When true, `add_failure` is a no-op. The one user is the layout match of a `!>>>`
    // restriction: that parse is speculative, so its failure must not become the reported error.
    suppress_failures: bool,
    #[cfg(feature = "debug-trace")]
    pub trace_events: Option<Vec<TraceEvent>>,
}
impl<'i, 'arena> ImportNamesParser<'i, 'arena> {
    /// Creates a new parser.
    /// A reference to `input` is stored in the parser for scanning.
    /// `parser_arena` is where the parser's internal objects (GSS, SPPF nodes,
    /// etc.) are allocated.
    pub fn new(input: &'i Input, parser_arena: &'arena Arena) -> Self {
        init_logger();
        let vec_arena = parser_arena;
        Self {
            vec_arena,
            scanner: ImportNamesScanner::new(input, vec_arena),
            gss_nodes_index: [const { InlineMap::Empty }; 24],
            descriptors: vec_arena.vec_with_capacity(
                input.len() as usize / DESCRIPTORS_CAPACITY_DIVISOR + DESCRIPTORS_CAPACITY_FLOOR,
            ),
            gss_nodes: vec_arena.vec_with_capacity(input.len() as usize * GSS_CAPACITY_MULTIPLIER),
            sppf_nodes: vec_arena
                .vec_with_capacity(input.len() as usize * SPPF_CAPACITY_MULTIPLIER),
            intermediate_nodes_index: [const { InlineMap::Empty }; 58],
            dd_intermediate_nodes_index: [],
            terminal_nodes_index: [const { InlineMap::Empty }; 13],
            #[cfg(feature = "instrument")]
            descriptors_count: 0,
            #[cfg(feature = "instrument")]
            descriptors_peak: 0,
            #[cfg(feature = "instrument")]
            ll1_call_log: vec![],
            intermediate_nodes_children: vec_arena.vec(),
            intermediate_nodes_children_map: OnceCell::new(),
            nonterminal_nodes_children: vec_arena.vec(),
            nonterminal_nodes_children_map: OnceCell::new(),
            envs: vec_arena.vec(),
            failures: InlineVec::Empty,
            suppress_failures: false,
            #[cfg(feature = "debug-trace")]
            trace_events: None,
        }
    }
    /// Parses the input from `start` and builds the parse tree in the given
    /// `tree_arena`. The parser is consumed. The tree lives as long as the
    /// arena lives. `start` is normally the start wrapper of a declared
    /// nonterminal, `grammar::START_<NAME>`, which allows layout around the
    /// input; a bare nonterminal id parses without it.
    pub fn parse<'a>(
        self,
        start: NonterminalId,
        tree_arena: &'a Arena,
    ) -> Result<ParseSuccess<ParseTree<'a>>, ParseError> {
        Parser::parse(self, start, tree_arena)
    }
    pub fn parse_s(
        self,
        tree_arena: &Arena,
    ) -> Result<ParseSuccess<&parse_tree::StartS<'_>>, ParseError> {
        Ok(self
            .parse(START_S, tree_arena)?
            .map(ParseTree::unwrap_start_s))
    }
    pub fn parse_parse(
        self,
        tree_arena: &Arena,
    ) -> Result<ParseSuccess<&parse_tree::StartParse<'_>>, ParseError> {
        Ok(self
            .parse(START_PARSE, tree_arena)?
            .map(ParseTree::unwrap_start_parse))
    }
    pub fn parse_parser(
        self,
        tree_arena: &Arena,
    ) -> Result<ParseSuccess<&parse_tree::StartParser<'_>>, ParseError> {
        Ok(self
            .parse(START_PARSER, tree_arena)?
            .map(ParseTree::unwrap_start_parser))
    }
    pub fn parse_sppf_node(
        self,
        tree_arena: &Arena,
    ) -> Result<ParseSuccess<&parse_tree::StartSPPFNode<'_>>, ParseError> {
        Ok(self
            .parse(START_SPPF_NODE, tree_arena)?
            .map(ParseTree::unwrap_start_sppf_node))
    }
    pub fn parse_input(
        self,
        tree_arena: &Arena,
    ) -> Result<ParseSuccess<&parse_tree::StartInput<'_>>, ParseError> {
        Ok(self
            .parse(START_INPUT, tree_arena)?
            .map(ParseTree::unwrap_start_input))
    }
    pub fn parse_parse_error(
        self,
        tree_arena: &Arena,
    ) -> Result<ParseSuccess<&parse_tree::StartParseError<'_>>, ParseError> {
        Ok(self
            .parse(START_PARSE_ERROR, tree_arena)?
            .map(ParseTree::unwrap_start_parse_error))
    }
    pub fn parse_parse_success(
        self,
        tree_arena: &Arena,
    ) -> Result<ParseSuccess<&parse_tree::StartParseSuccess<'_>>, ParseError> {
        Ok(self
            .parse(START_PARSE_SUCCESS, tree_arena)?
            .map(ParseTree::unwrap_start_parse_success))
    }
    pub fn parse_grammar(
        self,
        tree_arena: &Arena,
    ) -> Result<ParseSuccess<&parse_tree::StartGrammar<'_>>, ParseError> {
        Ok(self
            .parse(START_GRAMMAR, tree_arena)?
            .map(ParseTree::unwrap_start_grammar))
    }
    pub fn parse_nonterminal(
        self,
        tree_arena: &Arena,
    ) -> Result<ParseSuccess<&parse_tree::StartNonterminal<'_>>, ParseError> {
        Ok(self
            .parse(START_NONTERMINAL, tree_arena)?
            .map(ParseTree::unwrap_start_nonterminal))
    }
    pub fn parse_terminal(
        self,
        tree_arena: &Arena,
    ) -> Result<ParseSuccess<&parse_tree::StartTerminal<'_>>, ParseError> {
        Ok(self
            .parse(START_TERMINAL, tree_arena)?
            .map(ParseTree::unwrap_start_terminal))
    }
    pub fn parse_slot(
        self,
        tree_arena: &Arena,
    ) -> Result<ParseSuccess<&parse_tree::StartSlot<'_>>, ParseError> {
        Ok(self
            .parse(START_SLOT, tree_arena)?
            .map(ParseTree::unwrap_start_slot))
    }
    pub fn parse_terminal_set(
        self,
        tree_arena: &Arena,
    ) -> Result<ParseSuccess<&parse_tree::StartTerminalSet<'_>>, ParseError> {
        Ok(self
            .parse(START_TERMINAL_SET, tree_arena)?
            .map(ParseTree::unwrap_start_terminal_set))
    }
    fn parse_s_ll1(&mut self, i: u32) -> Option<SPPFNodeId> {
        #[cfg(feature = "instrument")]
        self.ll1_call_log.push((NonterminalId(0), i));
        let matched = self.scanner.longest_match(&FIRST_SET_S, i)?;
        match matched {
            TerminalId(0) => {
                let mut j = i;
                let right_child = {
                    let start = j;
                    let Some(node) = self.parse_parse_ll1(start) else {
                        self.add_failure(start, SlotId(1), None, || {
                            GLLFailureKind::UnexpectedToken {
                                expected: FIRST_SET_PARSE.terminals.to_vec(),
                            }
                        });
                        return None;
                    };
                    let end = self.sppf_node(node).right_extent();
                    j = end;
                    node
                };
                let left_extent = self.sppf_node(right_child).left_extent();
                let mut current = right_child;
                let right_child = {
                    let start = j;
                    let Some(node) = self.parse_parser_ll1(start) else {
                        self.add_failure(start, SlotId(2), None, || {
                            GLLFailureKind::UnexpectedToken {
                                expected: FIRST_SET_PARSER.terminals.to_vec(),
                            }
                        });
                        return None;
                    };
                    let end = self.sppf_node(node).right_extent();
                    j = end;
                    node
                };
                current = self.create_intermediate_node_ll1(
                    SlotId(2),
                    left_extent,
                    j,
                    current,
                    right_child,
                );
                let right_child = {
                    let start = j;
                    let Some(node) = self.parse_sppf_node_ll1(start) else {
                        self.add_failure(start, SlotId(3), None, || {
                            GLLFailureKind::UnexpectedToken {
                                expected: FIRST_SET_SPPF_NODE.terminals.to_vec(),
                            }
                        });
                        return None;
                    };
                    let end = self.sppf_node(node).right_extent();
                    j = end;
                    node
                };
                current = self.create_intermediate_node_ll1(
                    SlotId(3),
                    left_extent,
                    j,
                    current,
                    right_child,
                );
                let right_child = {
                    let start = j;
                    let Some(node) = self.parse_input_ll1(start) else {
                        self.add_failure(start, SlotId(4), None, || {
                            GLLFailureKind::UnexpectedToken {
                                expected: FIRST_SET_INPUT.terminals.to_vec(),
                            }
                        });
                        return None;
                    };
                    let end = self.sppf_node(node).right_extent();
                    j = end;
                    node
                };
                current = self.create_intermediate_node_ll1(
                    SlotId(4),
                    left_extent,
                    j,
                    current,
                    right_child,
                );
                let right_child = {
                    let start = j;
                    let Some(node) = self.parse_parse_error_ll1(start) else {
                        self.add_failure(start, SlotId(5), None, || {
                            GLLFailureKind::UnexpectedToken {
                                expected: FIRST_SET_PARSE_ERROR.terminals.to_vec(),
                            }
                        });
                        return None;
                    };
                    let end = self.sppf_node(node).right_extent();
                    j = end;
                    node
                };
                current = self.create_intermediate_node_ll1(
                    SlotId(5),
                    left_extent,
                    j,
                    current,
                    right_child,
                );
                let right_child = {
                    let start = j;
                    let Some(node) = self.parse_parse_success_ll1(start) else {
                        self.add_failure(start, SlotId(6), None, || {
                            GLLFailureKind::UnexpectedToken {
                                expected: FIRST_SET_PARSE_SUCCESS.terminals.to_vec(),
                            }
                        });
                        return None;
                    };
                    let end = self.sppf_node(node).right_extent();
                    j = end;
                    node
                };
                current = self.create_intermediate_node_ll1(
                    SlotId(6),
                    left_extent,
                    j,
                    current,
                    right_child,
                );
                let right_child = {
                    let start = j;
                    let Some(node) = self.parse_grammar_ll1(start) else {
                        self.add_failure(start, SlotId(7), None, || {
                            GLLFailureKind::UnexpectedToken {
                                expected: FIRST_SET_GRAMMAR.terminals.to_vec(),
                            }
                        });
                        return None;
                    };
                    let end = self.sppf_node(node).right_extent();
                    j = end;
                    node
                };
                current = self.create_intermediate_node_ll1(
                    SlotId(7),
                    left_extent,
                    j,
                    current,
                    right_child,
                );
                let right_child = {
                    let start = j;
                    let Some(node) = self.parse_nonterminal_ll1(start) else {
                        self.add_failure(start, SlotId(8), None, || {
                            GLLFailureKind::UnexpectedToken {
                                expected: FIRST_SET_NONTERMINAL.terminals.to_vec(),
                            }
                        });
                        return None;
                    };
                    let end = self.sppf_node(node).right_extent();
                    j = end;
                    node
                };
                current = self.create_intermediate_node_ll1(
                    SlotId(8),
                    left_extent,
                    j,
                    current,
                    right_child,
                );
                let right_child = {
                    let start = j;
                    let Some(node) = self.parse_terminal_ll1(start) else {
                        self.add_failure(start, SlotId(9), None, || {
                            GLLFailureKind::UnexpectedToken {
                                expected: FIRST_SET_TERMINAL.terminals.to_vec(),
                            }
                        });
                        return None;
                    };
                    let end = self.sppf_node(node).right_extent();
                    j = end;
                    node
                };
                current = self.create_intermediate_node_ll1(
                    SlotId(9),
                    left_extent,
                    j,
                    current,
                    right_child,
                );
                let right_child = {
                    let start = j;
                    let Some(node) = self.parse_slot_ll1(start) else {
                        self.add_failure(start, SlotId(10), None, || {
                            GLLFailureKind::UnexpectedToken {
                                expected: FIRST_SET_SLOT.terminals.to_vec(),
                            }
                        });
                        return None;
                    };
                    let end = self.sppf_node(node).right_extent();
                    j = end;
                    node
                };
                current = self.create_intermediate_node_ll1(
                    SlotId(10),
                    left_extent,
                    j,
                    current,
                    right_child,
                );
                let right_child = {
                    let start = j;
                    let Some(node) = self.parse_terminal_set_ll1(start) else {
                        self.add_failure(start, SlotId(11), None, || {
                            GLLFailureKind::UnexpectedToken {
                                expected: FIRST_SET_TERMINAL_SET.terminals.to_vec(),
                            }
                        });
                        return None;
                    };
                    let end = self.sppf_node(node).right_extent();
                    j = end;
                    node
                };
                current = self.create_intermediate_node_ll1(
                    SlotId(11),
                    left_extent,
                    j,
                    current,
                    right_child,
                );
                Some(self.add_nonterminal_node(NonterminalNode {
                    nonterminal_id: NonterminalId(0),
                    return_slot: SlotId(11),
                    span: Span {
                        left_extent,
                        right_extent: j,
                    },
                    child: current,
                    ambiguous: false,
                }))
            }
            _ => unreachable!("LL(1) dispatch covers every terminal in FIRST_SET"),
        }
    }
    fn parse_parse_ll1(&mut self, i: u32) -> Option<SPPFNodeId> {
        #[cfg(feature = "instrument")]
        self.ll1_call_log.push((NonterminalId(1), i));
        let matched = self.scanner.longest_match(&FIRST_SET_PARSE, i)?;
        match matched {
            TerminalId(0) => {
                let mut j = i;
                let right_child = {
                    let start = j;
                    let (end, node) =
                        self.match_terminal(TerminalId(0), start, SlotId(13), None)?;
                    j = end;
                    node
                };
                let left_extent = self.sppf_node(right_child).left_extent();
                let current = right_child;
                Some(self.add_nonterminal_node(NonterminalNode {
                    nonterminal_id: NonterminalId(1),
                    return_slot: SlotId(13),
                    span: Span {
                        left_extent,
                        right_extent: j,
                    },
                    child: current,
                    ambiguous: false,
                }))
            }
            _ => unreachable!("LL(1) dispatch covers every terminal in FIRST_SET"),
        }
    }
    fn parse_parser_ll1(&mut self, i: u32) -> Option<SPPFNodeId> {
        #[cfg(feature = "instrument")]
        self.ll1_call_log.push((NonterminalId(2), i));
        let matched = self.scanner.longest_match(&FIRST_SET_PARSER, i)?;
        match matched {
            TerminalId(1) => {
                let mut j = i;
                let right_child = {
                    let start = j;
                    let (end, node) =
                        self.match_terminal(TerminalId(1), start, SlotId(15), None)?;
                    j = end;
                    node
                };
                let left_extent = self.sppf_node(right_child).left_extent();
                let current = right_child;
                Some(self.add_nonterminal_node(NonterminalNode {
                    nonterminal_id: NonterminalId(2),
                    return_slot: SlotId(15),
                    span: Span {
                        left_extent,
                        right_extent: j,
                    },
                    child: current,
                    ambiguous: false,
                }))
            }
            _ => unreachable!("LL(1) dispatch covers every terminal in FIRST_SET"),
        }
    }
    fn parse_sppf_node_ll1(&mut self, i: u32) -> Option<SPPFNodeId> {
        #[cfg(feature = "instrument")]
        self.ll1_call_log.push((NonterminalId(3), i));
        let matched = self.scanner.longest_match(&FIRST_SET_SPPF_NODE, i)?;
        match matched {
            TerminalId(2) => {
                let mut j = i;
                let right_child = {
                    let start = j;
                    let (end, node) =
                        self.match_terminal(TerminalId(2), start, SlotId(17), None)?;
                    j = end;
                    node
                };
                let left_extent = self.sppf_node(right_child).left_extent();
                let current = right_child;
                Some(self.add_nonterminal_node(NonterminalNode {
                    nonterminal_id: NonterminalId(3),
                    return_slot: SlotId(17),
                    span: Span {
                        left_extent,
                        right_extent: j,
                    },
                    child: current,
                    ambiguous: false,
                }))
            }
            _ => unreachable!("LL(1) dispatch covers every terminal in FIRST_SET"),
        }
    }
    fn parse_input_ll1(&mut self, i: u32) -> Option<SPPFNodeId> {
        #[cfg(feature = "instrument")]
        self.ll1_call_log.push((NonterminalId(4), i));
        let matched = self.scanner.longest_match(&FIRST_SET_INPUT, i)?;
        match matched {
            TerminalId(3) => {
                let mut j = i;
                let right_child = {
                    let start = j;
                    let (end, node) =
                        self.match_terminal(TerminalId(3), start, SlotId(19), None)?;
                    j = end;
                    node
                };
                let left_extent = self.sppf_node(right_child).left_extent();
                let current = right_child;
                Some(self.add_nonterminal_node(NonterminalNode {
                    nonterminal_id: NonterminalId(4),
                    return_slot: SlotId(19),
                    span: Span {
                        left_extent,
                        right_extent: j,
                    },
                    child: current,
                    ambiguous: false,
                }))
            }
            _ => unreachable!("LL(1) dispatch covers every terminal in FIRST_SET"),
        }
    }
    fn parse_parse_error_ll1(&mut self, i: u32) -> Option<SPPFNodeId> {
        #[cfg(feature = "instrument")]
        self.ll1_call_log.push((NonterminalId(5), i));
        let matched = self.scanner.longest_match(&FIRST_SET_PARSE_ERROR, i)?;
        match matched {
            TerminalId(4) => {
                let mut j = i;
                let right_child = {
                    let start = j;
                    let (end, node) =
                        self.match_terminal(TerminalId(4), start, SlotId(21), None)?;
                    j = end;
                    node
                };
                let left_extent = self.sppf_node(right_child).left_extent();
                let current = right_child;
                Some(self.add_nonterminal_node(NonterminalNode {
                    nonterminal_id: NonterminalId(5),
                    return_slot: SlotId(21),
                    span: Span {
                        left_extent,
                        right_extent: j,
                    },
                    child: current,
                    ambiguous: false,
                }))
            }
            _ => unreachable!("LL(1) dispatch covers every terminal in FIRST_SET"),
        }
    }
    fn parse_parse_success_ll1(&mut self, i: u32) -> Option<SPPFNodeId> {
        #[cfg(feature = "instrument")]
        self.ll1_call_log.push((NonterminalId(6), i));
        let matched = self.scanner.longest_match(&FIRST_SET_PARSE_SUCCESS, i)?;
        match matched {
            TerminalId(5) => {
                let mut j = i;
                let right_child = {
                    let start = j;
                    let (end, node) =
                        self.match_terminal(TerminalId(5), start, SlotId(23), None)?;
                    j = end;
                    node
                };
                let left_extent = self.sppf_node(right_child).left_extent();
                let current = right_child;
                Some(self.add_nonterminal_node(NonterminalNode {
                    nonterminal_id: NonterminalId(6),
                    return_slot: SlotId(23),
                    span: Span {
                        left_extent,
                        right_extent: j,
                    },
                    child: current,
                    ambiguous: false,
                }))
            }
            _ => unreachable!("LL(1) dispatch covers every terminal in FIRST_SET"),
        }
    }
    fn parse_grammar_ll1(&mut self, i: u32) -> Option<SPPFNodeId> {
        #[cfg(feature = "instrument")]
        self.ll1_call_log.push((NonterminalId(7), i));
        let matched = self.scanner.longest_match(&FIRST_SET_GRAMMAR, i)?;
        match matched {
            TerminalId(6) => {
                let mut j = i;
                let right_child = {
                    let start = j;
                    let (end, node) =
                        self.match_terminal(TerminalId(6), start, SlotId(25), None)?;
                    j = end;
                    node
                };
                let left_extent = self.sppf_node(right_child).left_extent();
                let current = right_child;
                Some(self.add_nonterminal_node(NonterminalNode {
                    nonterminal_id: NonterminalId(7),
                    return_slot: SlotId(25),
                    span: Span {
                        left_extent,
                        right_extent: j,
                    },
                    child: current,
                    ambiguous: false,
                }))
            }
            _ => unreachable!("LL(1) dispatch covers every terminal in FIRST_SET"),
        }
    }
    fn parse_nonterminal_ll1(&mut self, i: u32) -> Option<SPPFNodeId> {
        #[cfg(feature = "instrument")]
        self.ll1_call_log.push((NonterminalId(8), i));
        let matched = self.scanner.longest_match(&FIRST_SET_NONTERMINAL, i)?;
        match matched {
            TerminalId(7) => {
                let mut j = i;
                let right_child = {
                    let start = j;
                    let (end, node) =
                        self.match_terminal(TerminalId(7), start, SlotId(27), None)?;
                    j = end;
                    node
                };
                let left_extent = self.sppf_node(right_child).left_extent();
                let current = right_child;
                Some(self.add_nonterminal_node(NonterminalNode {
                    nonterminal_id: NonterminalId(8),
                    return_slot: SlotId(27),
                    span: Span {
                        left_extent,
                        right_extent: j,
                    },
                    child: current,
                    ambiguous: false,
                }))
            }
            _ => unreachable!("LL(1) dispatch covers every terminal in FIRST_SET"),
        }
    }
    fn parse_terminal_ll1(&mut self, i: u32) -> Option<SPPFNodeId> {
        #[cfg(feature = "instrument")]
        self.ll1_call_log.push((NonterminalId(9), i));
        let matched = self.scanner.longest_match(&FIRST_SET_TERMINAL, i)?;
        match matched {
            TerminalId(8) => {
                let mut j = i;
                let right_child = {
                    let start = j;
                    let (end, node) =
                        self.match_terminal(TerminalId(8), start, SlotId(29), None)?;
                    j = end;
                    node
                };
                let left_extent = self.sppf_node(right_child).left_extent();
                let current = right_child;
                Some(self.add_nonterminal_node(NonterminalNode {
                    nonterminal_id: NonterminalId(9),
                    return_slot: SlotId(29),
                    span: Span {
                        left_extent,
                        right_extent: j,
                    },
                    child: current,
                    ambiguous: false,
                }))
            }
            _ => unreachable!("LL(1) dispatch covers every terminal in FIRST_SET"),
        }
    }
    fn parse_slot_ll1(&mut self, i: u32) -> Option<SPPFNodeId> {
        #[cfg(feature = "instrument")]
        self.ll1_call_log.push((NonterminalId(10), i));
        let matched = self.scanner.longest_match(&FIRST_SET_SLOT, i)?;
        match matched {
            TerminalId(9) => {
                let mut j = i;
                let right_child = {
                    let start = j;
                    let (end, node) =
                        self.match_terminal(TerminalId(9), start, SlotId(31), None)?;
                    j = end;
                    node
                };
                let left_extent = self.sppf_node(right_child).left_extent();
                let current = right_child;
                Some(self.add_nonterminal_node(NonterminalNode {
                    nonterminal_id: NonterminalId(10),
                    return_slot: SlotId(31),
                    span: Span {
                        left_extent,
                        right_extent: j,
                    },
                    child: current,
                    ambiguous: false,
                }))
            }
            _ => unreachable!("LL(1) dispatch covers every terminal in FIRST_SET"),
        }
    }
    fn parse_terminal_set_ll1(&mut self, i: u32) -> Option<SPPFNodeId> {
        #[cfg(feature = "instrument")]
        self.ll1_call_log.push((NonterminalId(11), i));
        let matched = self.scanner.longest_match(&FIRST_SET_TERMINAL_SET, i)?;
        match matched {
            TerminalId(10) => {
                let mut j = i;
                let right_child = {
                    let start = j;
                    let (end, node) =
                        self.match_terminal(TerminalId(10), start, SlotId(33), None)?;
                    j = end;
                    node
                };
                let left_extent = self.sppf_node(right_child).left_extent();
                let current = right_child;
                Some(self.add_nonterminal_node(NonterminalNode {
                    nonterminal_id: NonterminalId(11),
                    return_slot: SlotId(33),
                    span: Span {
                        left_extent,
                        right_extent: j,
                    },
                    child: current,
                    ambiguous: false,
                }))
            }
            _ => unreachable!("LL(1) dispatch covers every terminal in FIRST_SET"),
        }
    }
}
