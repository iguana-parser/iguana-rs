{
  "name": "@iguana-parser/design-viewer",
  "version": "0.1.0",
  "description": "Static web app that mounts the shared read-only design view over the iguana-lsp wasm module.",
  "license": "MIT OR Apache-2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json --fail-on-warnings"
  },
  "dependencies": {
    "@iguana-parser/web-ui": "*",
    "monaco-editor": "^0.56.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.0.0",
    "svelte": "^5.0.0",
    "svelte-check": "^4.0.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.8"
  }
}
